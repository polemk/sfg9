Olá <%= @user.name %>,

Use o código abaixo para entrar no <%= ENV.fetch('APP_NAME', 'Safegold') %>:

    <%= @code %>

Ele expira em 5 minutos.

Importante: não compartilhe este código com ninguém. Ninguém do Safegold vai
pedir esse código por telefone ou mensagem.

Se você não pediu este código, ignore este e-mail.
