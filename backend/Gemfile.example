# frozen_string_literal: true

source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '3.2.3'

gem 'active_storage_validations'
gem 'bootsnap', '>= 1.4.4', require: false
gem 'image_processing', '~> 1.2'
gem 'pg', '~> 1.1'
gem 'puma', '~> 6.4'
gem 'rails', '~> 8.0.0'

# API
gem 'grape'
gem 'grape-entity'
gem 'grape-swagger'
gem 'grape-swagger-rails'
gem 'grape-swagger-entity'

# Autenticação
gem 'devise'
gem 'devise-jwt'
gem 'omniauth'
gem 'omniauth-facebook'
gem 'omniauth-google-oauth2'
gem 'omniauth-rails_csrf_protection'

# Segurança & CORS
gem 'rack-attack'
gem 'rack-cors'

# Jobs & Realtime
gem 'redis', '>= 5.0'        # Atualizado para maior compatibilidade com Rails 8
gem 'connection_pool', '< 3.0' # Travado para evitar o crash de argumentos no initialize
gem 'sidekiq'

# Integrações HTTP
gem 'faraday'
gem 'faraday-multipart'

# Utilitários
gem 'aasm'
gem 'dotenv-rails'
gem 'paper_trail'

# Documentação
gem 'yard'

gem 'httparty'

# I18n para traduções padrão em pt-BR
gem 'rails-i18n'

group :development, :test do
  gem 'byebug', platforms: %i[mri mingw x64_mingw]
  gem 'database_cleaner-active_record'
  gem 'factory_bot_rails'
  gem 'faker'
  gem 'rspec-rails'
  gem 'vcr'
  gem 'webmock'
end

group :development do
  gem 'brakeman', require: false
  gem 'bundler-audit', require: false
  gem 'listen', '~> 3.3'
  gem 'rubocop', require: false
  gem 'rubocop-rails', require: false
  gem 'rubocop-rspec', require: false
  gem 'solargraph', require: false
  gem 'spring'
end

group :test do
  gem 'simplecov', require: false
end

gem "shoulda-matchers", "~> 7.0", group: :test
