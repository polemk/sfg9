# Improvements log — sfg -> ai9

> Toda melhoria intencional sobre o comportamento legado. QA lê isto para nao
> confundir melhoria com regressao.

| ID | O que mudou | Por que | Impacto observavel |
| -- | ----------- | ------- | ------------------ |
| S2-28-01 | **[correção achada RODANDO]** Verbo não suportado numa rota de detalhe respondia **500** — agora é **405**, e o backtrace sai do corpo | Conferindo a tarefa 2.8 rota por rota: `GET /api/v1/project_carrier_connections/<uuid>` (a conexão só expõe `DELETE :id`) devolvia HTTP 500 com **9,5 KB de backtrace** no corpo, embora o cabeçalho `Allow: OPTIONS, DELETE` saísse correto ao lado. O `rescue_from :all` herdado da base ai9 engolia o `Grape::Exceptions::MethodNotAllowed`. É literalmente o "500 de verdade" que a 2.8 manda eliminar, e o vazamento é EXCEÇÃO-2 do DEC-30 | Qualquer verbo errado em qualquer rota da API v1 passa a devolver o status certo. O corpo do 500 genérico traz `request_id` para casar com o log; o backtrace fica só no log. **QA: não procurar equivalente no legado.** Registrado como UF-S2-01 |
| S2-29-01 | **[correção achada CONFERINDO]** O atalho "Conectar portadores" do borderô apontava para rota inexistente | O estado vazio de `ReceivableFormPage` (sem portador conectado ao projeto) chamava `navigate('/project-carriers')`; a rota registrada em `consoleNavigation.tsx` é `/project-carrier-connections`. O botão caía na rota curinga e levava à tela de "não encontrado" — o estado vazio explicava o problema e o atalho não resolvia, que é meio caminho de volta ao defeito do legado (onde a URL era montada com variável indefinida) | O botão leva à tela de conexões de portador do projeto. Varredura dos ~33 destinos de `navigate`/`to`/`href` do app: era o único link morto |
| S2-47-01 | **[GANHO, não paridade]** O formulário longo avisa antes de perder o que foi digitado, e "Descartar" não recarrega a página | FE-400. O legado tinha a barra de ações pendentes e ainda assim **descartava a edição em silêncio** ao navegar (o `go()` do console fechava a gaveta sem perguntar); e o `cancel()` dela montava `{ reload: defaultReload() }` **com os parênteses**, executando a recarga da página na hora de montar o objeto | No borderô: a barra anuncia "Alterações não salvas"; **Descartar** restaura o ponto inicial em memória, com confirmação e sem nenhum `reload`; sair com alteração pendente abre "Sair sem salvar?". A guarda pega o clique em link interno (a barra lateral é `NavLink`) e o `beforeunload`. **Não pega o botão Voltar do navegador** — está escrito no cabeçalho de `useUnsavedChanges`. **QA: no legado nada disso existia** |
| S2-47-02 | **[correção achada RENDERIZANDO em 390×844]** A barra de ações ficava **atrás** da `MobileBottomBar`, e a lista de pendências comia um oitavo da tela | Captura do borderô no telefone: `FormActionBar` é `fixed bottom-0 z-20` e a barra de abas é `z-appbar` (30) — o botão "Cadastrar borderô", único jeito de terminar a tela, aparecia como uma tira de ~6 px sob a aba "Recebíveis". E com o formulário em branco são **nove** pendências, cuja frase ocupava quatro linhas | A barra passa a ficar acima das abas (`bottom-[calc(4.25rem+env(safe-area-inset-bottom))] md:bottom-0`), o texto empilha sobre os botões no telefone, e a lista nasce recolhida com a contagem em primeiro plano ("Faltam 9 campos: …", tocável). No desktop nada muda. **`tsc` e a suíte passavam com o botão inalcançável** |
| S3-57-01 | **[correção de SEGURANÇA DE DADO]** Excluir portador com limite de risco: **422 e o limite permanece** | D-24 / BE-070. No legado `Carrier` tinha `has_many :risk_controls, dependent: :destroy` — excluir um portador **apagava os limites de risco** dele, o teto que autoriza toda operação de crédito, sem 422, sem aviso e sem trilha. A regra já estava declarada por nome em `blocking_dependents`; com `risk_controls` entregue pela S5, virou associação real | `DELETE /api/v1/carriers/:id` com limite responde **422** e a mensagem NOMEIA o vínculo ("1 limite(s) de risco"). Três camadas conferidas rodando: registro por nome, `restrict_with_error` e FK `NO ACTION` do Postgres (`delete_all` por fora do model levanta `InvalidForeignKey`). **O outro lado também**: removido o limite, a exclusão volta a responder 200 |
| S9-ETL-01 | **Na migração do binário, o tipo do anexo passa a vir dos magic bytes, não da coluna `file_content_type`** | Aquela coluna guarda o que o **navegador declarou** no upload de 2022, e o legado ainda desligava a conferência (`do_not_validate_attachment_file_type` + spoof detector monkey-patchado para `false` — **D-82**). Reanexar confiando nela porta a vulnerabilidade junto com o arquivo | Medido no acervo real: 44 arquivos lidos, **0 fora da allowlist** do catálogo (41 PDF, 1 xlsx, 1 docx, 1 zip) e **1 divergência declarada** — `#45`, cujo tipo no banco é `inode/x-empty`. QA: um `.svg` renomeado para `.pdf` passa a ser **recusado na carga**, não reanexado |
| S9-ETL-02 | **Anexo de 0 byte não é reanexado: fica sem binário e NOMEADO no relatório** | `renegotiation_attachments#45` (`ANEXO_INSTRUMENTO_DE_GARANTIA.pdf`) tem 0 byte no acervo **e** no banco (**D-133**). Carimbá-lo pela extensão daria um download de 0 byte com cara de PDF válido — que é literalmente o "pior que ausência declarada" da DEC-84 | 43 de 44 documentos financeiros religados; o 44º aparece no relatório com nome e motivo, e deixa o relatório **vermelho** (`:abort`). QA: **não é regressão** — é a recusa de fabricar um documento que nunca existiu |
| S9-ETL-03 | **`fix_renegotiations_without_company` deixa de reescrever a empresa de quem já tem a empresa certa** | O legado (`renegotiation.rb:303`) terminava com `p.renegotiations.update_all(company_id: c.id)` **sem condição**: numa rotina chamada "corrigir as *sem* empresa", isso sobrescreve a empresa correta de todas as outras. Exceção (1) do **DEC-30** — não é preservar número que o cliente vê, é produzir dado errado num banco novo | Nenhuma mudança visível em produção (medido: as 169 renegociações do dump têm `company_id` preenchido, **0 nulo**). A rotina passa a tocar só o que está errado, e a etapa ganha a troca de empresa **de outro projeto** pela do próprio (C1 aplicado ao dado) |
| S9-ETL-04 | **As três rotinas de `rails c` do legado viram um fixup em lotes, com ensaio e retomada** | `Renegotiation.all.each { update_values! }` e irmãs materializavam a tabela inteira antes da primeira gravação, e cada `save` reentrava na cascata de `after_save` — uma passada recalculava a mesma renegociação uma vez **por parcela** (BE-220) | `find_each` em lotes, **uma** transação e **um** recálculo de agregado por renegociação, sem broadcast. Ensaio por padrão que **grava e reverte** (reconciliar exige calcular sobre o estado final), retomada por pulo e `AFTER=<uuid>`. QA: os números finais são os mesmos; o que muda é o custo e a possibilidade de rodar de novo |
| — | Google Maps API key sai do código-fonte para ENV/credentials | Estava hardcoded e versionada em `app/definitions/SFG/metadata.rb`; chave exposta em repositório é vazamento de credencial | Nenhuma mudança funcional. Ação externa recomendada ao usuário: **rotacionar a chave** no Google Cloud e restringi-la por referrer/IP, já que a antiga está no histórico do git |
| — | Constantes de negócio de `SFG::Metadata` viram configuração do ai9 | `MAX_FILES_PER_RENEGOTIATION`, `MAX_FILE_SIZE`, `PUBLIC_CREATE_USER`, viés de geolocalização estavam como constantes Ruby; no ai9 viram config/ENV | Mesmos valores padrão (4 arquivos, 5 MB, auto-cadastro ligado) — comportamento idêntico, mas ajustável sem deploy |
| D-93 | **Melhoria DECLINADA pelo usuario (DEC-01)**: a convencao de sinal invertida da exposicao ao risco sera replicada como esta | Decisao do usuario em 24/08/2026: prioridade e o painel mostrar os mesmos numeros de hoje | Nenhuma mudanca visivel. QA **nao** deve tratar a inversao como regressao — ela e intencional. Testes de caracterizacao travam os numeros atuais |
| D-104 | **Melhoria DECLINADA pelo usuario (DEC-02)**: a aritmetica em float sera replicada para os totais baterem com o legado | Decisao do usuario em 24/08/2026: paridade numerica exata acima de precisao correta | Nenhuma divergencia de total no Phase 4. O defeito de precisao vai junto para a base nova, de forma consciente e registrada |
| D-120/Q-09 | `vendor/doughnut` e `vendor/dialog` (proprietarios, sem equivalente npm) substituidos pelas libs do ai9 (DEC-10) | Codigo proprietario sem manutencao e sem testes; a base ai9 ja tem equivalentes padronizados | O grafico e o dialogo mudam de aparencia. O `frontend-engineer` mostra o resultado visual antes de fechar o slice |
| PWA-01 | O console vira **instalável**: `manifest.webmanifest` + ícones da marca + metas `apple-mobile-web-app-*` | O legado (Rails/Webpacker 2019) e a base ai9 não tinham nada disso. `NEW-003`, DEC-21.3 | Aparece "Instalar aplicativo" no menu do navegador; instalado, abre em janela própria, sem barra de endereço, com o nome e o ícone Safegold, na tela de login (`start_url: "/"`). **Sem service worker: não há offline, cache nem push** — escolha de produto, não esquecimento. Num sistema financeiro, número em cache de ontem exibido como de hoje é pior que não ter ícone na tela inicial |
| PWA-02 | O ícone **maskable** deixa de perder metade da marca | Ele tinha fundo `#2D2D2A` — o mesmo grafite das duas barras superiores do símbolo. Medido: barras em `(41,44,40)` contra fundo `(45,45,42)`, razão de contraste ~1,03:1. No recorte circular do Android apareciam **só as duas barras douradas** | O ícone do Android passa a mostrar o símbolo inteiro. Fundo agora é `#FAFAF9` (o token `--background` do claro), com zona segura conferida: raio máximo do conteúdo 194 px contra o limite de 204,8 px em 512. **Continua sendo arquivo próprio com fundo cheio** — maskable transparente o Android recorta errado (DEC-93) |
| PWA-03 | O `apple-touch-icon` ganha **arquivo próprio com fundo**, `safegold-icon-apple-touch-180.png` | O iOS compõe ícone transparente sobre **preto**, e metade do símbolo Safegold é grafite `#2D2D2A`: na tela de início do iPhone sumiriam as duas barras superiores — o mesmo defeito do maskable, por outra plataforma | Na tela de início do iOS o ícone aparece inteiro. **O conjunto transparente `safegold-icon-{32,180,192,512}` continua transparente**, como o usuário pediu: a exceção é o arquivo da plataforma que compõe mal, seguindo o precedente já aceito do maskable |
| PWA-04 | `theme_color` e `background_color` do manifest passam de `#2D2D2A` para `#FAFAF9` | Divergiam do `<meta name="theme-color">` do HTML (`#FAFAF9` claro / `#181817` escuro). Divergência produz barra de status de uma cor e tela de abertura de outra; e o tema **padrão** do app é o claro (`themeStore`, que corresponde ao legado), então uma abertura em grafite piscava escuro antes de pintar claro | Splash e barra de título do app instalado batem com o app. O escuro continua atendido pelo `<meta>` com `prefers-color-scheme`, que o navegador honra em tempo de execução |
| MOB-01 | A barra de abas do rodapé passa a **rotular** cada aba, marcar a rota ativa com `aria-current` e respeitar `env(safe-area-inset-bottom)` | Era só ícone, com o inativo em `opacity-40`. Ícone sozinho é adivinhação — "Operações" e "Operações estruturadas" viram o mesmo desenho, e este produto tem os dois. E instalado como PWA o indicador de início do iPhone dividia espaço com a última aba | As cinco abas mostram nome (truncado quando não cabe) e a ativa é anunciada por leitor de tela, não só pela cor. **Não é regressão visual**: é a barra que existia, legível |
| MOB-02 | O seletor de modo da `MobileTopBar` renderiza em **portal** | O cabeçalho usa `glass-panel`, que aplica `backdrop-filter` e **cria contexto de empilhamento**: o painel ficava preso no `z-sticky` do cabeçalho por mais alto que fosse seu `z`. É o mesmo defeito do `SidebarModeToggle` descrito na §5.4.4 | O painel abre por cima do conteúdo, nos dois modos (conferido em 390×844). Fecha por `pointerdown` e por `Escape` |
| MOB-03 | A biblioteca `components/mobile/` ganha **carregando, vazio e erro** (`MobileListState`) | Tinha card, KPI, gráfico e paginação — e nenhum dos três estados. S5..S11 iam consumi-la assim, e cada fatia inventaria o seu: uma com spinner, outra com `<p>` cru, uma terceira com tela em branco, que no telefone é indistinguível de app travado | Lista mobile passa a ter esqueleto com a forma do card, vazio que distingue "não há" de "seu filtro não achou", e erro em `role="alert"` com "tentar de novo". **Erro nunca mais se parece com vazio** — decidir sobre carteira lendo "nada aqui" quando a verdade é "não consegui perguntar" é decidir errado. `NEW-004`, DEC-100 |
| MOB-04 | APIs da biblioteca mobile deixam de falar em paleta e em booleano ambíguo | `MobileCard` tinha `statusColor="emerald\|rose\|amber"` (nome de paleta numa base cujo §5.4.2 proíbe cor literal — e é o nome que a próxima pessoa copia); `MobileKPI` tinha `isCurrency` (padrão `true`) mais `isPercentage`, e com os dois ligados o resultado dependia da ordem dos `if` — uma contagem de parcelas saía como `R$ 12` | `statusTone` fala token (`success/warning/destructive/info/neutral`) e `format` fala `currency\|percent\|plain`. **Nenhuma tela consumia os componentes ainda**, então a troca não quebra ninguém. Travado em teste |
| MOB-05 | `MobileCard` clicável vira alcançável por teclado; `MobileChartCard` formata em pt-BR e diz quando não há série | O card era `<div onClick>` — inalcançável por Tab, invisível ao leitor de tela. O gráfico tipava a série como `any[]` e rotulava `R$ 1234567.89`, sem separador de milhar, num app financeiro; e a bolinha pulsando no canto sugeria "ao vivo" sobre dado que não é | Linha de lista abre por Enter/Espaço e recebe foco visível. Valor sai `R$ 1.234.567,89`, eixo em notação compacta, e gráfico sem dado diz "Sem dados no período" em vez de mostrar retângulo vazio |
| THEME-01 | Tema Safegold definido por inteiro nos **dois modos** sobre os tokens do ai9 (`frontend/src/styles/globals.css`) | O default do ai9 era roxo #7C3AED sobre cinza-azulado, sem nenhuma relação com a marca. Agora `primary` = ouro #FFC107 nos dois modos e todas as superfícies derivam do grafite #2D2D2A | Todo o app muda de cor. **Não é regressão.** O claro é o modo padrão e corresponde ao legado; o escuro é desenho novo (o legado não tem dark — `app_theme.rb:109` tem a opção comentada) |
| THEME-02 | Divergência de `primary` do legado resolvida a favor de `SFG::Theme` (#2D2D2A) | Havia três valores (`theme.rb` #2D2D2A, `colors.scss` #050517, factory do banco #373435). A amostragem de pixel do `app_logo_full_original.png` deu **#292C28**, que confirma o `theme.rb`. #050517 (navy) fica registrado como variante herdada do SCSS e não é usado | O grafite do app é morno, não azulado. Bate com o logo lado a lado |
| THEME-03 | Botão único com **cinco** variantes canônicas (`primary/secondary/ghost/destructive/link`) | O ai9 tinha nove, incluindo `gradient` (azul→roxo), `primary` (também gradiente) e `uiverse` (borda em gradiente animado com glow — o visual "feito por IA"). Botão da mesma função renderizava diferente em cada tela | Todo botão do app vira sólido e limpo. `default→primary`, `outline→secondary`, `gradient→primary`, `uiverse→primary` nos call sites. Sobra zero |
| THEME-04 | Fontes da marca aplicadas: **Work Sans** (texto/título) e **Fira Mono** (`font-numeric`) | São as fontes do legado (`app/frontend/css/pub/font.scss`). O ai9 usava Poppins/Space Grotesk/Poly, que não são da marca | Além da marca: `font-numeric` + `tabular-nums` alinha coluna de valor em borderô/recebível/indicador. O legado já usava fonte monoespaçada para número (`$numeric_font`) e isso é requisito, não enfeite |
| THEME-05 | `p { font-size: 1.2rem }` global removido do `globals.css` | Regra do ai9 que inflava **todo** parágrafo do app. Num app de crédito, denso de tabela e formulário, isso desperdiça a tela | Texto de corpo volta a 1rem. Telas cabem mais |
| THEME-06 | Efeitos decorativos do ai9 removidos da camada compartilhada | `.btn` (borda em gradiente animado), `.card-highlight` (mesma borda + hue-rotate no ícone), `.mouse-glow` (halo verde neon seguindo o mouse), `.bg-goat-gradient-*`, `.text-goat-gradient-*`, tokens `--pika-*` | Menos animação rodando o tempo todo. `.card-highlight` virou contorno sólido de marca; `.glass-panel` foi tokenizado (era `rgba(255,255,255,.95)` cravado, que dava painel branco sobre fundo branco no claro) |
| THEME-07 | Carousel de login do ai9 substituído | Eram 5 imagens geradas por IA + copy sobre "Inteligência Artificial Nativa", "Conectividade Global" e "painéis futuristas" — nada disso é o Safegold | Cinco slides novos em pt-BR sobre risco, recebível/borderô, limite por portador, renegociação e indicadores. **Pendência aberta:** sem arte fotográfica (ver relatório ao orquestrador) |
| THEME-08 | Metadados e ícones de marca refeitos (`index.html`, `manifest.webmanifest`, favicon, ícones de app, OG) | O `index.html` anunciava "GOAT — CRM brasileiro com WhatsApp, agentes de IA", com `og:url`/`canonical` apontando para `goat.polemk.com` e dado estruturado da polemk | Title/description/OG do Safegold, favicon e app icon gerados do símbolo do logo, `theme-color` por modo. URLs de OG ficaram **relativas** de propósito: o host público vem do deploy |
| THEME-09 | Template de e-mail rebrandeado (`layouts/mailer.html.erb`, `auth_mailer/*`) | O e-mail de código de acesso — a primeira coisa que o usuário do Safegold vê — trazia "GOAT BY POLEMK" em verde neon com `text-shadow` triplo, sobre fundo #0B0F19 | E-mail claro, wordmark SAFE/GOLD, código em Fira Mono, botão de ação em ouro. Fallback de `APP_NAME` passou de `'ai9'` para `'Safegold'` em assunto, corpo e mensagem de WhatsApp |
| THEME-10 | Bundle i18n do ai9 substituído por stub do Safegold | `locales/{pt-br,en}/translation.json` eram ~350 linhas de copy de venda da landing do GOAT ("batalhão de ferramentas pronto pra guerra", "Builder @ Goat", hashtags de Instagram). Órfãs desde que o site público saiu de escopo (Phase 0) e nenhuma tela chama `useTranslation` | Nenhuma. É reversível pelo git se alguém precisar do conteúdo antigo |
| THEME-11 | Superfície flutuante ganhou contraste real no escuro (`--popover` 13% → 18%) e o `<Toaster>` do sonner passou a seguir o tema do app | Com `--card` em 12% e `--popover` em 13%, todo menu suspenso, select e popover abria "transparente" no escuro. O sonner tem tema próprio com default claro, então o toast saía branco sobre o app escuro | Menu, select, popover, dialog, sheet e toast legíveis nos dois modos. Regra escrita em `ai9-conventions.md` §5.4.4 |
| THEME-12 | Empilhamento da sidebar corrigido na camada compartilhada | O bloco que hospeda `VENDO COMO`/`MODO` e o `<nav>` logo abaixo estavam ambos em `z-10`; empatados, o `<nav>` (posterior no DOM) ganhava e o menu aberto ia parar **atrás** dos itens de navegação. `z-modal` no painel não resolvia — ele estava preso no contexto de empilhamento do ancestral | Bloco passou a `z-sticky`, `<nav>` a `z-base`. Também normalizados `z-50`/`z-40`/`z-30` remanescentes (`SearchableSelect`, FAB do chat, `PageHeader`) para a escala nomeada |
| THEME-13 | Véu de modal padronizado em `bg-brand-ink/70` | Havia três padrões convivendo (`bg-black/80`, `bg-foreground/40`, `bg-background/80`). O `bg-background/80` escurece no escuro e **clareia** no claro — ou seja, no claro não era véu nenhum | Todo modal/gaveta escurece o fundo do mesmo jeito nos dois modos |
| THEME-14 | Avatar de iniciais deixou de vir do `api.dicebear.com` | O dicebear sorteia a cor de fundo pelo hash do nome (aparecia vermelho e roxo aleatórios numa interface grafite e ouro) e era uma requisição a terceiro só para desenhar duas letras, com o nome do usuário na query string | Novo `components/ui/UserAvatar.tsx`, tokenizado e offline. Dado de demo (`UsersPage`) ainda referencia dicebear — é seed, não render |
| THEME-15 | Arte e metadados mortos do ai9 removidos de `frontend/public` | 7,1 MB de slides de outro app (Escola Aplicar), 460 KB dos slides de IA do ai9, `og-goat.png`, `og-preview/og-social`, `site_preview.png`, logo da brsw, fontes não referenciadas, e um `sitemap.xml` apontando para `goat.polemk.com` | `public/` caiu de 15 MB para ~5 MB. `robots.txt` passou a `Disallow: /` — o Safegold é console interno, não tem o que indexar |
| PLAT-01 | **O boot falha quando falta segredo obrigatório** (`config/initializers/required_env.rb`, OPS-611) | O legado escolheu o oposto: `secret_key_base` de 128 hex commitado em texto puro (`config/development_credentials.yml:1`) para que "sempre funcione", mais um `credentials.yml.enc` **sem** `master.key` — cifrado, inútil, e invisível porque o Rails cai no caminho não cifrado. O resultado é subir em produção com o segredo de desenvolvimento e nada quebrar | **Muda o comportamento de quem já roda o ambiente.** A lista é por ambiente (test não exige nada, para a suíte de C2 rodar limpa) e a mensagem nomeia **todas** as ausentes de uma vez. Escape para build sem segredos: `SKIP_ENV_CHECK=true` |
| PLAT-02 | **Verificação de certificado TLS ativa por padrão** (OPS-626 / **C-05**) | O legado desligava `VERIFY_PEER` globalmente no Windows (`config/initializers/ssl_for_win.rb:1`) — o que atinge Google Maps, ReceitaWS e SMTP, não só um deles. A **própria base ai9** repetia o defeito com `openssl_verify_mode: 'none'` em `production.rb:83` e `development.rb:58`, sem a desculpa de plataforma | **Muda o comportamento de quem já roda o ambiente.** Quem tem SMTP com certificado interno precisa declarar `SMTP_OPENSSL_VERIFY_MODE=none` de propósito. Uma linha por arquivo; nada mais foi refatorado |
| PLAT-03 | `cpf`, `cnpj` e `cpf_cnpj` deixam de aparecer no log (OPS-627) | Identificador pessoal em texto puro no log de requisição tem retenção diferente da do banco e ninguém o expurga — enquanto o banco ganhou expurgo por DEC-60/DEC-90 | Nenhuma mudança funcional. `[FILTERED]` no lugar do número |
| PLAT-04 | **CSP bloqueante e cabeçalhos de segurança passam a existir** (OPS-628 / **DEC-48**) | Não existiam nem no legado nem na base ai9. O usuário recusou conscientemente o modo `report-only` | **Risco aceito com a demo em 28/08.** A política foi montada a partir do que a aplicação realmente carrega (Google Fonts, `data:`/`blob:`, WebSocket do Action Cable, host da API, unpkg só em `/docs`), e a varredura de console em 10 rotas × light/dark deu **zero violação**. Ligar recurso externo novo passa a exigir alterar `frontend/csp.config.ts` |
| PLAT-05 | **Um ambiente reproduzível**: um `Gemfile` **e um `Gemfile.lock` versionados**, um `database.yml`, um `.env.example` (OPS-613/614) | O legado tinha três Gemfiles com o principal no `.gitignore`, quatro `application.*.yml` e cinco `database.*.yml` por plataforma. A base ai9 herdou metade disso: `backend/Gemfile` e `backend/Gemfile.lock` estavam no `.gitignore` enquanto o `pnpm-lock.yaml` do front era versionado | Lock ignorado significa duas máquinas resolvendo versões diferentes das mesmas gems, e ninguém sabendo qual rodou. O contrato **C2** depende de existir um ambiente de referência |
| PLAT-06 | **Coerção deixou de ser monkey patch** (`Sfg::Coercion`, OPS-619) | O `type_casting.rb` do legado reabria `String`, `Integer`, `NilClass`, `TrueClass`, `Float` e `BigDecimal`. É o initializer com mais regra de negócio do sistema — e é regra que decide centavo | **Quem lia `"false".to_bool` passa a chamar `Sfg::Coercion.to_bool("false")`.** O comportamento numérico foi preservado **com teste**, inclusive onde é estranho: `to_number("1.234,56")` devolve **nil** no legado (o `numeric?` aceita um separador só) e `to_number("1.234")` devolve **1.234**, não 1234. Os 49 casos estão em `golden/coercion.json`, extraídos executando o initializer legado |
| PLAT-07 | Sentinelas de data explícitas (`Sfg::DateBounds`, OPS-618) | `date_overload.rb` reabria `DateTime` com `dinosaurs`/`mars`. Constante nomeada aparece no diff e no grep; `DateTime.mars` não aparece em nenhum dos dois — e o nome não diz que é sentinela | Comportamento idêntico, incluindo o `today_end` de 23h59 (**não** 23h59m59s): registro gravado às 23:59:30 continua ficando de fora do filtro "até hoje", como no legado |
| PLAT-08 | **O agendamento de jobs passa a existir em código** (`config/schedule.yml`, `upstream-flags.md` #13) | Não havia **uma** ocorrência de `Sidekiq::Cron` em `backend/`, e mesmo assim 10 crons rodavam — todos `source: dynamic`, persistidos só em chaves `cron_job:*` do Redis. Redis limpo, deploy novo ou ambiente novo = o cron some **sem erro**. E o trim do Phase 1b deixou 8 crons órfãos re-enfileirando `NameError`: 916 jobs em retry | O `cleanup_login_codes` (o único legítimo) foi **adotado** pelo arquivo e agora sobrevive a Redis limpo. Um portão reprova o boot se alguma classe agendada não existir. Nenhum cron desta migração é cadastrado pela Web UI |
| PLAT-09 | Expurgo de `login_attempts` (90 dias) e `email_logs` (180 dias) (**DEC-60**, **DEC-90**) | `login_attempts` é da base ai9, guarda `ip_address` e `user_agent` de toda tentativa e **não tinha expurgo nenhum**: passivo de LGPD adotado, não herdado. O log de e-mail do legado guardava o **corpo** — inclusive de e-mails de credencial — com retenção infinita | Os dois jobs entram no schedule versionado. O de `email_logs` é tolerante à ausência da tabela (ela é de outra fatia) para poder nascer junto, como a decisão pede |
| PLAT-10 | **Google Analytics entra desligado e corrigido** (**DEC-87**) | No legado o snippet é injetado na primeira linha de quatro entrypoints, **sem consentimento nenhum** — e está quebrado: o ID é GA4 (`G-7E78XXZX5X`) e o snippet é Universal Analytics, então hoje **não coleta absolutamente nada** | O snippet GA4 correto entra em `frontend/public/analytics-ga4.js`, desligado por `VITE_GA_ENABLED` (nunca hardcode), com Consent Mode v2 negando tudo por default e sem carregar o `gtag.js` até `grantConsent()`. **Duas travas**: o CSP não libera o domínio do Google enquanto a flag estiver desligada. Pendência declarada: a interface de consentimento |
| PLAT-11 | A chave do Google Maps chega ao navegador por **endpoint autenticado**, não pelo bundle (**DEC-61**) | O DEC-61 mandou as chaves de terceiro para o model `Credential`, encriptado — o que resolve a ReceitaWS (consumida pelo backend) e **não** resolve o Maps, cujo script é carregado pelo navegador. Sem isso o mapa não carrega e o `Credential` guarda uma chave que ninguém lê. No legado a chave está hardcoded e duplicada em `SFG/metadata.rb:8,9`, indo para o HTML | `GET /api/v1/runtime_config` devolve a chave em runtime, com sessão. Trocar a chave passa a ser editar uma linha em `/admin/credentials`, sem build. **Não é cofre**: chave que vai para o navegador é pública por construção — a proteção real é a restrição por referrer/IP no Google Cloud, que continua sendo ação externa |
| PLAT-12 | Login social do Facebook: de configuração morta para **flag desligada** (OPS-605) | No legado a chave existia e o botão não. Configuração morta é pior que ausência: ela sugere que existe | Com o default (`OAUTH_FACEBOOK_ENABLED=false`) a estratégia nem é registrada no OmniAuth — `/users/auth/facebook` responde 404 em vez de iniciar um fluxo que ninguém termina |
| PLAT-13 | Chaves do Active Record Encryption saem do código (OPS-610, ligado ao DEC-61) | As três vinham **commitadas como default** no initializer da base (`qnpYZCIR…`). Chave de cifra versionada é cifra decorativa — e é justamente ela que protege o `Credential#api_key` onde o DEC-61 põe as chaves de terceiro | Em produção não há default: o boot reprova sem as três. Em desenvolvimento o default sobrevive, porque trocá-lo torna ilegível o que já está gravado no banco local |
| PLAT-14 | Catálogo `pt-BR` que é **realmente lido** (OPS-629) | O legado tem 6 arquivos de locale em 2 idiomas e a UI é 100% hardcoded: os catálogos nunca foram usados. Portá-los reproduziria o adorno | As mensagens de validação de parâmetro do **Grape** passam a sair do catálogo — não é chave de exemplo esperando alguém chamar, é o caminho por onde já passa todo request malformado da API. Verificado: `q é inválido` em vez de `q is invalid` |
| PLAT-15 | `storage.yml` de produção parametrizado, apontando para **fora** da árvore da aplicação (OPS-616 / **Q-07 em aberto**) | `:local` grava em `backend/storage/`, dentro do release: qualquer deploy que troque o diretório faz os anexos sumirem **em silêncio**, porque o registro no banco continua apontando para um blob que não existe | Produção passa a `disk_persistent` (`ACTIVE_STORAGE_DISK_ROOT`), com `amazon` e `s3_compatible` prontos. **Q-07 continua bloqueada por dependência externa** — o provedor é decisão do cliente, e a fatia não a marca como concluída |
| PLAT-16 | `puma.rb` com ambiente e cluster por ENV (OPS-631) | O legado fixava `RAILS_ENV` em `development` no `puma.rb`: em produção isso significa código recarregável a cada requisição, log verboso e o segredo de desenvolvimento | `WEB_CONCURRENCY=0` (default) mantém processo único, que é o que desenvolvimento quer. Acima de zero liga `preload_app!` e reconecta o ActiveRecord no `on_worker_boot` |
| IMP-R3 | A busca da tela de **Limites** passa a filtrar (S5) | `pub/risk_controls_controller.rb:10,17-22` lia `params[:q]`, dava `""` de default e **nunca** o aplicava ao `where`. A caixa de busca não fazia nada | Buscar por portador/empresa filtra a lista, e a mensagem "Nenhum resultado para «x»" passa a ser alcançável. `ILIKE` com bind, `%` e `_` tratados como texto |
| IMP-R4 | Excluir limite bloqueado responde **422 real**, nomeando a dependência (S5) | O legado respondia **202** no ramo de erro (`:168`) e a tela dizia "removido com sucesso" sem ter removido — família D-24/D-98 | O toast diz quantas operações ou posições impedem. Nada é apagado em silêncio |
| IMP-R5 | Ativar limite usa `save!` (S5) | `activate!` fazia `save` e o controller respondia 200 mesmo com a validação recusando: a tela dizia "ativado" e o limite continuava inativo | Falha de validação vira 422 visível |
| IMP-R6 | Empresa, portador e tipo do limite são **imutáveis no servidor** (S5, decisão B-01) | Os três estavam no `permit` e só a tela os desabilitava — quem chamasse o endpoint direto movia a exposição de uma combinação para outra, arrastando as operações | `PUT` que tente trocá-los responde 422 com `details.immutable` |
| IMP-R7 | Os três tipos funcionais de movimento resolvem por **chave**, não por título (S5, B-09) | `RiskMovementType.liberação_de_recurso_id` fazia `where(title: "Liberação do Recurso")`; renomear o tipo pela tela de administração quebrava a criação de movimentos com `NoMethodError` em `nil.id` | Renomear é seguro. Tipo ausente vira erro de negócio nomeando a chave, **antes** de gravar |
| IMP-R8 | Tela de **Movimentações de risco** ganha filtro por estado (S5) | Ela listava `RiskMovementType.all` — desativados inclusive, sem marca — enquanto a tela irmã listava só ativos. As duas telas do mesmo cadastro discordavam sobre o que é "a lista" | Filtro "só ativos" existe, e o estado aparece na linha |
| IMP-R9 | `has_pre_faturamento` do tipo de limite deixa de ser editável (S5) | Estava no `permit` e no formulário. Mudá-la não cria nem remove subtipo, e troca **em silêncio** o bucket em que toda operação daquele tipo passa a somar no painel | O campo é somente leitura na edição, e a tela diz por quê. Nenhum número muda para quem não tentava editá-la |
| IMP-R10 | Console de risco ganha **estado de erro** (S5, FE-239) | O `failure` do proxy AJAX era vazio: consulta falhando deixava o painel em carregamento eterno ou com o dado da consulta anterior | Falha aparece com a mensagem do servidor e um "tentar de novo". **Erro deixou de parecer vazio** |
| IMP-R11 | Modo "portador único" **avisa** quando a combinação tem mais de um limite (S5, FE-236) | A view fazia `r[:rcs].first` e escondia os demais em silêncio — o que acontece com dado legado | Selo "N limites" na linha, com a explicação |
| IMP-R12 | O par estático do limite usa `is_static`, não datas sentinela (S5, B-08) | O legado gravava `DateTime.dinosaurs` (ano −2000) e `DateTime.mars` (ano +2000) em `issue_date`/`due_date` para manter o par dentro de toda janela | Mesmo conjunto de resultados nos agregados, sem data falsa no banco. Check constraint garante os dois casos |
| IMP-R13 | Coluna `order` de `risk_movements` vira `sequence` (S5, DB-236) | `order` é palavra reservada em SQL e só sobrevive no legado porque o Rails a cita | O payload da API expõe `sequence`. Nenhum comportamento muda |
| IMP-R1 | ~~**Renovar passa a encerrar a operação original**, e a exposição deixa de ser contada em dobro (S7, D-94)~~ **CANCELADO pela DEC-35** | — | **A DEC-35 (25/08/2026) mandou REPLICAR o ciclo de vida:** *"renovar NÃO encerra a original. As duas operações ficam vivas e as duas consomem limite ao mesmo tempo."* O orquestrador apresentou o veredito "corrigir" do `legacy-defects.md` e o usuário reafirmou replicar. **Melhoria DECLINADA, não esquecida.** O `tasks.md`/`proposal.md` da S7 são do Phase 2 e pediam o contrário; estão riscados lá. **QA: exposição em dobro na renovação NÃO é regressão** |
| IMP-R14 | Prorrogar **não encurta mais** o vencimento (S7, BE-277) | No legado só o `minDate` do datepicker impedia; por requisição direta o `after_create` aplicava a data menor sem reclamar, jogando movimentos legítimos para fora da janela de `BE-274` | Validação no servidor **e** `CHECK` no banco. É a única metade do D-94 que foi corrigida, e não contraria a DEC-35: não preserva número nenhum — impede gravar registro incoerente do zero |
| IMP-R15 | **[observável]** Excluir operação de risco bloqueada por recibo responde **422 real** (S7, BE-258 / D-98) | A resposta era literalmente `errors.any? ? :ok : :ok` (`risk_operations_controller.rb:139`) e a tela dizia "Operação foi removida com sucesso!" mesmo quando o `restrict_with_error` tinha barrado | O toast passa a dizer o que impede. **Quem hoje acha que excluiu vai passar a ver o erro** — é mudança visível, e intencional |
| IMP-R16 | **[observável]** A paginação da lista de operações passa a funcionar (S7, BE-253 / FE-255) | `@total_count` era calculado **depois** do `limit!/offset!` (`:34,40,44`): o total era o tamanho da página, então a lista nunca passava de uma | `X-Total-Count` vem da relação antes do recorte. Listas grandes passam a mostrar mais de uma página — o dado sempre esteve lá |
| IMP-R17 | Chave de ordenação desconhecida responde **400**, não 500 (S7, BE-253) | `get_ordering_key` devolve `nil` fora do `case` e a linha seguinte faz `nil + " "` → `NoMethodError`. Um `?ordering_keys[]=x` na barra de endereço derrubava a listagem | 400 nomeando a chave e listando as aceitas. A allowlist é a mesma do front |
| IMP-R18 | **A transferência pré↔antecipação passa a funcionar** (S7, BE-275/BE-276 / D-97) | Duas coisas quebradas: `on_duplicate_key_update: [:date, movement_value]` tem `movement_value` **sem os dois-pontos** — variável local inexistente, `NameError` em produção ao editar movimento com par; e o par nascia no `after_create`, então operação sem `pair_operation` gravava **meia transferência** | O par é validado **antes** e as duas pontas nascem na mesma transação: sem contrapartida, nada é gravado. Excluir uma transferência leva o par junto |
| IMP-R19 | Abrir o detalhe de operação **sem movimento** deixa de dar 500 (S7, BE-255) | `geral_update_values` faz `@last_movement.date.strftime(...)` sem checar `nil` — e o par estático do limite nasce exatamente assim | Payload vazio e o cartão diz "ainda não tem movimento" |
| IMP-R20 | **[observável]** As duas IDORs de risco fechadas (S7, BE-253/BE-270 — família D-01/D-16/D-29/D-76/D-100) | `risk_operations#search:23` substituía a relation inteira ao receber `risk_operation_id`, perdendo o escopo de projeto; `risk_movements#search:15` **não tinha escopo nenhum** — qualquer `risk_operation_id` era aceito | Id de outro projeto responde 404/vazio. **Quem hoje alcança operação ou extrato de projeto alheio deixa de alcançar.** EXCEÇÃO-2 do DEC-30 |
| IMP-R21 | `movement_value > 0` passa a valer no **servidor** (S7, decisão B-05) | Só o botão do drawer conferia. Valor negativo **inverte o sinal** do movimento: um crédito de −1.000 vira débito | 422 no servidor. **Não cai no DEC-01**: não é convenção de sinal, é registro corrompido — a mesma família do D-10 |
| IMP-R22 | O extrato de movimentos ganha **paginação e estado de erro** (S7, BE-270 / FE-276) | `l`/`o`/`q` eram lidos, recebiam default e **nunca** eram aplicados; e não há estado de erro em nenhuma das duas listas do detalhe — falha de rede parecia lista vazia | As duas abas passam pelo `AsyncSection`: carregando, vazio, **erro** e conteúdo. Erro deixou de parecer vazio |
| IMP-R23 | Os 13 tooltips do formulário deixam de ler o YAML **a cada render** (S7, OPS-237) | `_body.html.erb:15` faz `YAML.load_file` por render: sumir o arquivo no deploy dava **500 no formulário inteiro** | Servido pelo mecanismo da S12 (`GET /help/fields`), uma vez por sessão. **Conteúdo continua em branco** — os 13 textos do legado são todos o mesmo placeholder (Q-R9), e campo sem texto não ganha ícone |
| IMP-R24 | Rótulos de outro módulo corrigidos na tela de risco (S7) | O modal de exclusão de operação dizia **"Excluir renegociação"**; a confirmação de movimento, **"Excluir previsão"**; os toasts de movimento, **"A previsão foi criada/atualizada com sucesso"**; e os de renovação, **"O tipo de operação foi criado/atualizado com sucesso"** — todos copiados junto com as parciais | Cada texto fala do que a ação faz |
| IMP-A10 | Concessão de permissão com **origem, revogação e trilha** (S0) | No legado a `Ability` era editada em runtime e **não deixava rastro** (Q-A1 nasceu disso). Agora `user_permissions`/`user_type_permissions` são versionados pelo `paper_trail` com autor, motivo e IP | `GET /api/v1/audit_trail` (OG/Admin) mostra quem concedeu o quê, sobre quem e por quê. Fecha **D-34** e **D-35** |
| IMP-A18 | **[observável]** Revogar permissão passa a ter efeito em **quem já existe** (S0) | A `Role` do legado CLONAVA as 17 abilities na atribuição; alterar o papel não alcançava usuário antigo. Agora permissão é **consulta** a cada request (`Authorization::PermissionResolver`), nunca cópia | Tirar `user_is_readonly` de um papel devolve a escrita a todo mundo daquele papel **na requisição seguinte**. Antes não acontecia nada. QA: isto é melhoria, não regressão. Fecha **D-35** |
| IMP-A19 | Auto-participação em projeto é **impossível** (S0) | `memberships_controller.rb` do legado não tinha gate nenhum: qualquer sessão se adicionava a qualquer projeto | `POST /api/v1/memberships` com o próprio `user_id` responde 403 `SELF_MEMBERSHIP`. Fecha **D-28** e a parte de membership do **D-34** |
| IMP-A21 | Unicidade da participação garantida pelo **banco** (S0) | O legado não tinha índice; duplicata dependia de a tela não deixar | Índice único `(user_id, project_id)` em `memberships`. Duplicar responde 422, não cria linha órfã |
| IMP-A22 | **Sem metaprogramação em runtime** (S0) | `include_ability_methods` rodava em todo `after_initialize` e fazia `define_method` **na classe** — mutação global a cada instância carregada | Nenhuma mudança visível; some o custo e o efeito colateral. Permissão é consulta declarada, legível na revisão |
| IMP-A25 | Troca de papel e de permissão **auditada** (S0) | `UsersService.update` aceitava `user_type_id` sem registrar nada | `users`, `user_types`, `permissions`, `user_permissions`, `user_type_permissions`, `projects` e `memberships` são versionados (`paper_trail`, DEC-59). O `whodunnit` grava o **usuário real** — na impersonação, quem impersonou, nunca o impersonado |
| IMP-A12 | Impersonação com **trava de hierarquia** e sem encadeamento (S0) | `ImpersonateService` só checava `can_impersonate?` do ator. Com o Admin do Safegold chegando, um Admin impersonaria um OG e sairia com poder de OG — escalada completa. Era a flag de upstream #14 | Admin personifica só hierarquia inferior; sessão personificada não inicia outra. Verificado nos dois lados: "Admin NÃO personifica OG" **e** "Admin personifica Colaborador" |
| IMP-A31 | (frontend) Marca Safegold em tokens, light **e** dark — ver linhas THEME-* | — | — |
| PAG-01 | **[observável]** Paginação real por Kaminari, envelope em **cabeçalho** (S0, DEC-62) | O legado trazia a lista inteira e a base tinha `limit/offset` manual em `users_service.rb`. Ficar "meio a meio" era o risco explícito da decisão | Toda lista emite `X-Total-Count`, `X-Page`, `X-Per-Page` e `X-Total-Pages`. `X-Total-Pages` é novo — é o que o `PaginationPill` espera como `totalPages` |
| ESC-01 | **[observável]** Escopo por projeto aplicado **no endpoint** (S0, contrato C1) | O legado descartava o filtro de projeto sempre que chegava um id por parâmetro (família D-01/D-16/D-29/D-76/D-100) | Id de outro projeto passa a devolver vazio/404. **Quem hoje alcança registro de projeto alheio deixa de alcançar** — é a mudança de comportamento mais visível de S0, e é intencional |
| ESC-02 | Projeto inexistente e projeto sem participação respondem **o mesmo 404** (S0) | Distinguir 403 de 404 transforma o helper num oráculo de existência de ids de projeto | Nenhum caminho feliz muda. Quem enumerava ids de projeto por diferença de status não consegue mais |
| ROL-01 | **[observável]** `client`, `free` e `visitor` foram **removidos** (S0, DEC-41) | Não são papéis do Safegold e disputavam os níveis 2 e 4 com Admin e Colaborador | Usuário que apontava para um tipo removido virou **Colaborador** e saiu na lista de exceções do log da migration (DEC-18.8). O `Cliente Teste` do seed de dev é agora `colaborador` |
| CAD-01 | **[observável]** As 4 rotas de auto-cadastro **não existem mais** (S0, DEC-49) | `minimal_type_to_sign_up_through_web = "Admin"` deixava qualquer pessoa na internet se cadastrar até a hierarquia 998 (**D-39**) | `POST /auth/v1/{pre_register,complete_registration,visitor_signup,visitor_signup_with_link}` respondem **404**. Entrada é só por convite (DEC-18.7). QA: 404 aqui é o comportamento correto |
| TRILHA-01 | **[observável]** Evento de trilha deixou de poder sumir — **por construção** (S19) | No legado `trackings.resume` é `string` com `length: { maximum: 300 }`, e os 20 emissores do `TrackingFacade` fazem `t.save` **sem olhar o retorno**. Resumo maior que 300 → `save` devolve `false` → **o evento não existe**. E o que sumia era sempre o do caso complicado: o emissor de falha concatena a mensagem da exceção (`t.resume << " falhou com o erro: #{e}"`) | A frase em pt-BR passa a ser **derivada na leitura** (`Sfg::AuditSummary`), montada de `item_type` + `event` com rótulo e gênero do catálogo. **Não existe coluna de texto**, logo não existe o limite. Mais forte que o "truncamento explícito" que a fatia previa: a classe inteira do defeito sai |
| TRILHA-02 | **[observável]** `Tracking` **não é portado** — a trilha é o `paper_trail` (S19, **DEC-59**/**DEC-63**) | Seis evidências, todas no repositório de origem: (1) os 20 emissores gravam só `kind="JOB"`, sobre 2 tipos de registro; (2) `Tracking.new` não é chamado em nenhum outro lugar; (3) `target_id`/`target_group_*` — a dimensão "destinatário" — **nunca são escritos**, e são as colunas de 3 dos filtros do `index`; (4) `GET /api/v1/trackings` **não tem consumidor**: o botão que o abriria (`.app_card_tracking_button`) existe em 5 SCSS e em **zero HTML**; (5) `tracking_color`/`tracking_icon` ignoram o argumento e devolvem constante; (6) o `show` já estava quebrado em 3 frentes (A-5) | **QA do Phase 4: não abrir defeito por não existir tela de "trilha" ligada a um card.** Ela não existia no legado. O que a trilha real cobre é mais: autor verdadeiro (inclusive em impersonação), motivo, IP e a foto completa do registro. As fases de job (`request→start→failure`) são progresso de job — `OPS-127`, S13 |
| TRILHA-03 | **[observável]** O item da trilha passa a **distinguir** os eventos (S19, `FE-443`) | `tracking_color(t)` devolvia `SFG::Theme.COLOR__ACCENT` e `tracking_icon(t)` devolvia `'zmdi zmdi-info'` — **para qualquer evento**, ignorando o argumento. Não havia mapa a portar; havia duas constantes com cara de função | Criação, alteração, remoção e as duas fases de impersonação têm ícone e tom próprios, vindos de um **mapa de dados** (`auditAppearance.ts`) em tokens semânticos. Acrescentar um evento é acrescentar uma linha; nenhum componente muda. QA: é comportamento novo, não divergência |
| TRILHA-04 | Impersonação tem **verbete próprio** na trilha (S19) | `Auth::ImpersonateService` grava `impersonate_start`/`impersonate_stop` na mesma tabela `versions`. Sem verbete, a frase cairia no `update` e diria "a conta de usuário foi alterada" — a informação errada sobre o ato mais sensível do sistema | A trilha diz "a conta de usuário passou a ser personificada", com o autor real e o personificado lado a lado, e os dois eventos são **filtráveis** |
| TRILHA-05 | A lista de models versionados virou **código com portão** (S19, **DEC-78 #1**) | O DEC-78 exige lista "deliberada e curta" porque payload completo em tudo duplica a base a cada save. Lista que mora só num documento não é conferida por nada | `Sfg::AuditTrail::VERSIONED` + `EXCLUDED`, com fatia dona e motivo por linha, e um spec que reprova quatro desvios. **Pegou um caso real na primeira execução:** três models de atendimento (`AdminMessage`, `MessageNote`, `Observer`) tinham ganhado `has_paper_trail` sem declaração e sem leitor |
| FMT-01 | **[observável]** Cor de identificação **determinística** (S19, `FE-435`) | `random_color` usava a gem `color-generator` e sorteava **a cada render**: o mesmo usuário mudava de cor entre a linha da tabela e o cabeçalho da tela, o que anula o propósito de uma cor de identificação | É `avatarTone` (hash djb2 sobre o id), já existente no `UserAvatar`. A gem sai e **não é substituída por outra**. QA: a cor mudar de item para item é esperado; a cor **não** mudar entre renders é a correção |
| FMT-02 | Validar URL deixou de fazer **requisição de rede** (S19, `BE-456`) | `UriValidator` fazia `Net::HTTP.get_response` **dentro do `validate`** do model: salvar virava chamada de rede, dentro da transação, sem timeout definido — e um endereço válido era **recusado** porque o site estava fora do ar naquele segundo. O `rescue` sem classe engolia tudo | Fica só o formato `http(s)`. Se disponibilidade virar requisito, é verificação assíncrona com resultado gravado, nunca condição de salvar. (Detalhe adjacente: a mensagem do legado apontava para `errors.events.invalid_url`, chave que **não existe** em nenhum dos 6 locales — sairia "translation missing") |
| FMT-03 | `IntervalValidator` passa a **validar** (S19, `BE-455`) | Três defeitos somados faziam o original não fazer nada de útil: `value.to_i.to_s == value` compara `String` com `Integer` e **recusava todo inteiro** como "não inteiro"; `record.errors[field] << ...` é a forma do Rails 5 e, do 6.1 em diante, escreve num array descartado — **nenhum erro registrado**; e `value <= record.max_value` com a outra ponta nula levanta dentro do `valid?`, devolvendo 500 no lugar do erro de campo | Faixa coerente é aceita, faixa invertida ou igual é recusada com mensagem, outra ponta vazia não levanta. Nenhum consumidor no legado — nasce correto para S5 e S13 |
| FMT-04 | Ordenação dirigida pelo cliente com **allowlist**, uma vez só (S19, `BE-449`) | O trio `prepare_ordering`/`get_ordering_key`/`get_ordering_style` está copiado, idêntico, em **18 models**. Chave desconhecida fazia `nil + " "` → `NoMethodError` → **500 a partir da barra de endereço**; estilo desconhecido, o mesmo | `Sfg::Sortable`: chave fora da allowlist é ignorada, estilo desconhecido cai em `asc`, e o `ORDER BY` é Hash — nada vindo do cliente é interpolado. Teto de 5 colunas por requisição |
| FMT-05 | Plural e concordância de gênero viram **catálogo** (S19, `FE-438`, `FE-432`) | `pluralize_for` fazia `string + "s"` ("permissãos", "papels", "recebívels") e `gender_prefix` era um `if` sobre `user.info.gender`. Nenhum dos dois tinha consumidor no legado | Formas plurais em `pt-BR.audit_trail.plurais` mais três regras regulares em `Sfg::Inflection`; gênero é o par `rotulo`/`genero` do catálogo. A frase da trilha é montada **uma vez, no servidor** — o cliente recebe `summary` pronto |
| FMT-06 | `sliceIn(lista, 0)` deixou de **descartar a lista** (S19, `FE-439`) | O `slice_in` do legado (três cópias, duas dentro de controllers) faz `for i in 0..size-1` e devolve `[]` quando `size` é zero — a lista inteira some, em silêncio | Uma cópia só, e menos de uma coluna vira uma coluna com tudo. Nenhum item some porque alguém passou zero |
| FMT-07 | Datas trafegam em **ISO-8601** em toda a fronteira (S19, `FE-440`) | `days_js_array` montava `"%m/%d/%Y"` para o datepicker. Formato ambíguo na fronteira é a fonte clássica de dia/mês trocados — num sistema de vencimentos, erro financeiro | `toIsoDate` (dia de calendário) e `toIsoDateTime` (instante) no cliente; os filtros de período são `DateTime` do Grape, que recusa `dd/mm/aaaa`. A formatação pt-BR acontece **só** na camada de apresentação |
| CAD-02 | **[observável]** A flag `public_create_user?` **não é portada** (S19, `FE-444`) | Ela era a trava prevista para o **D-39**. Depois da DEC-49 não há o que travar: as 4 rotas respondem 404 e a quinta porta (`VisitorAuthService`, que criava conta com JWT emitido pelo nó de chat) virou `Auth::ExistingUserSessionService`, que só casa usuário existente. E no legado o nome mentia: `PUBLIC_CREATE_USER = 1` com `unless public_create_user?` nas views significa que `true` **esconde** o botão "Cadastre-se agora" | Flag cujo único trabalho é fechar rota que não existe é convite a reconstruir a rota. A trava passa a ser **estrutural**, que é o que a própria DEC-49 pediu: "rota que não existe não reabre por engano". QA: não procurar por configuração de auto-cadastro — ela não existe, de propósito |
| IMP-A11 | **[observável]** Deep-link e histórico de navegador de verdade no console (S2, `BE-390`, `FE-397`, `OPS-393`) | O legado tinha **uma** rota-mestra (`resource/topic/section`), estado num objeto global JS (`dashHolder`) e a URL espelhada por `replaceState` — **nunca** `pushState`. Resultado observável: o botão Voltar do navegador **saía do console**, e nenhuma tela era compartilhável por link (**D-92**) | Toda área tem URL estável; `/messages?message=<id>` abre a conversa já carregada em aba nova; Voltar navega **dentro** do console. QA: se um roteiro do legado dependia de Voltar sair do sistema, ele muda |
| IMP-A23 | **[observável]** Cor determinística das iniciais no console (S2, `FE-396`) | O `random_color` do legado (`observer.rb:32-36`) sorteava com `ColorGenerator` **a cada render**: a mesma pessoa aparecia de uma cor a cada carregamento. Cor que muda sozinha não identifica ninguém | O avatar de um usuário tem sempre o mesmo tom (`UserAvatar`/`avatarTone`, derivado do identificador). Mesma correção que a `FMT-01` da S19, no outro consumidor |
| IMP-A24 | **[observável]** Endereço desconhecido responde **404 com tela**, nunca tela em branco nem rebaixamento silencioso (S2, `OPS-634`) | `App.tsx` da base **não tinha rota `*`**: área desconhecida virava tela branca sem nada a clicar. E o legado fazia pior — **rebaixava em silêncio para o `dash`**, e o usuário achava que tinha chegado | O usuário lê "esta página não existe", vê o caminho que falhou e tem duas saídas. Vale também para exceção de runtime, que passa a virar a tela de 500 em vez de desmontar o app |
| IMP-A25 | Mensagem administrativa deixa de ser **truncada em silêncio** (S2, `DB-390`) | A coluna era `string(255)` e a validação do model aceitava 500 (`feedback19/.../message.rb:16`): tudo entre 256 e 500 caracteres passava na validação e o banco cortava, sem erro e sem aviso | Corpo acima de 500 é **recusado** com mensagem, e até 500 é gravado inteiro. QA: mensagens longas que "sumiam pela metade" param de sumir |
| DEC-73 | **Melhoria DECLINADA pelo usuário (DEC-73)**: a inversão Concluído/Fechado das mensagens administrativas é **REPLICADA** (S2, `BE-527`) | Os dois estados existem e estão trocados entre si: no `update`, pedir "Concluído" grava **Fechado** (`messages_controller.rb:118-119`); a action `close` grava **Concluído** (`:156-159`). Inversão dupla, não typo de um lado. O usuário decidiu replicar (vale o DEC-30) | Nenhuma mudança visível — e é de propósito. **O QA não deve abrir defeito.** O golden test `admin_messages_spec` trava os DOIS sentidos e reprova quem "consertar" sem passar por uma DEC nova. Contraria o que o `tasks.md` 3.3/6.4.2 da S2 pedia: ele foi escrito **antes** da DEC-73 |
| IMP-A1 | **[observável]** Quem entrava com senha passa a entrar por **código** (S1, DEC-14) | O legado tinha Devise com senha, mais um hash Django herdado (`legacy_password`) adivinhável a partir do primeiro nome + `#6230` (**D-106**/**D-109**) | Não existe campo de senha em lugar nenhum do produto. A tela de login pede e-mail, telefone **ou** usuário e manda um código de 6 dígitos. **Avisar o usuário antes da demo** (DC-01): quem hoje digita senha vai digitar código |
| IMP-A2 | **[observável]** `username` continua servindo para entrar (S1, **DEC-45**) | `devise.rb:14` usava `authentication_keys = [:login]` e resolvia por `lower(username) OR lower(email)`. Sem portar, quem só sabe o próprio usuário perderia acesso no dia 1 do cutover | Terceira chave de identidade, na **mesma** caixa de texto. Coluna nullable, índice único **parcial**. **`username` identifica, não recebe**: o código sai por e-mail ou telefone. Quem tem `username` e nenhum dos dois recebe uma mensagem explícita — e o dry-run do P-049 tem de contar esses casos antes do cutover |
| IMP-A3 | Destino pós-login com **allowlist same-origin** (S1, `BE-007`) | O legado interpolava o `next` direto no JavaScript da página — família **D-69**, open redirect | `safeNextPath` recusa `//evil.com`, `https://…`, `javascript:`, `data:`, `\` e as formas percent-encoded dos mesmos. Só caminho relativo passa. E o aviso do destino, que no legado vivia num container **comentado no HTML**, agora aparece |
| IMP-A6 | **[observável]** O convite carrega **magic link**, nunca senha (S1, `OPS-001`, D-38) | O e-mail de criação de conta do legado mandava credencial no corpo | Link de uso único, 24 h, e o texto diz em letras claras que o Safegold não tem senha. Os dois e-mails de senha ("Perdeu a senha?", "Nova senha configurada") **não foram portados** (DEC-75) |
| IMP-A7 | **[observável]** Remover a própria conta confirma por **código**, não por senha (S1, `BE-014`) | Num produto sem senha a confirmação do legado perdeu o objeto | O mesmo `LoginCode` do login serve de prova de posse. E a auto-remoção **não passa pela matriz**: remover a própria conta é direito do titular, não poder administrativo — sem essa linha o Colaborador, o papel mais numeroso, não conseguiria sair do sistema |
| IMP-A13 | Busca de contas insensível a acento **dos dois lados** (S1, `BE-025`) | O legado transliterava só o dado do banco e comparava contra o termo cru: "João" não achava "Joao", mas "Joao" achava os dois. Metade da busca funcionando é pior que nenhuma — o operador conclui que a pessoa não está cadastrada | `unaccent` nas duas pontas quando a extensão existe, `LOWER` simétrico quando não. A busca também passa a alcançar `username` e o `identifier` |
| IMP-A14 | `validate_cpf` responde **422/409** (S1, `BE-035`) | O legado respondia **405** (Method Not Allowed) para CPF malformado e **406** (Not Acceptable) para CPF já cadastrado. Nenhum dos dois descreve o que aconteceu, e cliente nenhum sabe tratá-los | 422 para formato/dígito verificador inválido, 409 para conflito de unicidade |
| IMP-A15/A16 | **[observável]** Bloquear conta **derruba a sessão na hora** (S1, `BE-036`, `BE-037`, DEC-39) | Os dois campos concorrentes do legado (`is_active`, `deactivated`) **não tinham um único leitor**: nenhum `active_for_authentication?`, nenhum filtro em controller. "Replicar" seria não bloquear ninguém | Colapsam em `users.blocked_at`, lido pelo **gate central**. `User#block!` rotaciona o `jti`, então o access token da aba já aberta deixa de valer — verificado pela tela: bloqueei, e o `GET /auth/v1/me` do token anterior passou de 200 para 403 |
| IMP-A17 | **[observável]** Conta bloqueada **explica o motivo** (S1, `BE-038`, `FE-044`) | Sessão derrubada sem explicação é logout mudo: a pessoa vê o formulário de novo e não sabe se errou o código, se caiu a rede ou se perdeu o acesso | 403 com `code: ACCOUNT_BLOCKED` e o motivo escrito por quem bloqueou; o interceptor encerra a sessão levando o texto para a tela de login. Conferido nos dois modos |
| IMP-A20 | Credencial de máquina por `Authorization: Bearer` (S1, `BE-047`) | `X-LAA-Agent`/`X-LAA-Token` eram cabeçalhos próprios fora de qualquer padrão | O token de máquina é `ClientApplication` e **não herda papel de OG** |
| IMP-A24 | Serialização de usuário **sem N+1** (S1, `BE-003`) | A entity disparava uma consulta de `permissions` por usuário: uma lista de 100 contas fazia 101 idas ao banco, piorando sozinha conforme a base crescesse — sem erro nenhum, só a tela ficando lenta | `includes(:user_type, :rich_text_biography, user_permissions: :permission)`. **A terceira associação foi o teste de contagem que achou**: `has_rich_text :biography` também consultava por linha, e sem ela a contagem continuava crescendo com o resto já resolvido. Há spec que reprova a volta |
| IMP-A27 | **[observável]** A tela de contas **nunca** mostra dado falso (S1, `FE-011`, U11) | `UsersPage.tsx` servia nove pessoas inventadas, com papéis `client` que a DEC-41 removeu, sempre que o usuário logado fosse visitante ou cliente. Num sistema de crédito, base inventada sem sinal nenhum de que é exemplo | O mock saiu. Quem não tem permissão vê **"Seu perfil não tem acesso à lista de contas."**, e lista vazia vê estado vazio explícito. Um teste lê o **arquivo-fonte** e reprova se qualquer um dos nomes inventados voltar — teste de render só pegaria o ramo que servia o mock |
| IMP-A28 | Impersonação: **403 antes de 404** (S1, `BE-031`) | A ordem era 404 → 422 → 403, o que transforma o endpoint num oráculo de existência de ids: quem não tinha permissão nenhuma distinguia "id existe" de "id não existe" e enumerava a base de usuários | Quem não pode personificar recebe o **mesmo** 403 nos dois casos. Spec compara as duas respostas literalmente |
| AUTH-01 | **[observável]** Login social **não cria conta** (S1, **DEC-44**) | Google e Facebook ficam ligados e anunciados. Se o callback criasse usuário, o **D-39** voltaria por uma porta que a DEC-49 não fechou — e por uma pior, porque não passa pela allowlist de `api/root.rb` | Casa por `(provider, uid)` e depois por **e-mail**; **nunca por nome** (o legado casava por `formal`, e dois homônimos viravam a mesma conta). Sem casamento: 403 `INVITE_ONLY`, com a **mesma** mensagem de "conta existe sem este provedor" — senão o botão do Google vira verificador de quem é cliente do Safegold. Vale nas **duas** superfícies: `POST /auth/v1/oauth/callback` e o callback do Devise |
| AUTH-02 | **[observável]** A **quinta** porta de auto-cadastro foi fechada (S1) | `Auth::VisitorAuthService` fazia `find_or_create` a partir do e-mail que estivesse no contexto de uma conversa, e **emitia JWT**. A DEC-49 fechou 4 rotas em `api/root.rb`; esta não estava lá | Virou `Auth::ExistingUserSessionService` — só casa usuário existente, e recusa conta bloqueada. Renomeado junto com a mudança: o nome antigo mentia duas vezes (`Visitor` é tipo que a DEC-41 removeu, e não havia "signup" a fazer) |
| AUTH-03 | **[observável]** Pedido de código **não enumera contas** (S1) | O serviço respondia "Usuário não encontrado" para destino sem conta. Num produto de crédito, "esta pessoa é cliente?" é informação de negócio, não só de segurança | Destino desconhecido e conta bloqueada recebem a **mesma** resposta de sucesso, sem envio nenhum. QA: pedir código para e-mail inexistente responde 200 — é o comportamento correto, não um falso positivo |
| AUTH-04 | Código de acesso endurecido (S1, `BE-020`) | `Kernel#rand` (Mersenne Twister, previsível a partir de amostras), comparação com `==` (vazamento por tempo) e **dois** tetos de tentativa para a mesma regra — 3 no `LoginCode`, 5 no serviço, valendo o mais permissivo | `SecureRandom`, `ActiveSupport::SecurityUtils.secure_compare`, **3** tentativas num lugar só, 5 min de vida e teto de 5 pedidos/15 min por destino normalizado. O código esgotado é marcado como usado, **não apagado**: apagar tira do banco a evidência da sequência de tentativas |
| AUTH-05 | Sessão personificada **expira** (S1, DEC-18.3) | O refresh da impersonação usava o TTL normal de **30 dias** — na prática, uma sessão que não expira, com a identidade de outra pessoa renovável por um mês | `IMPERSONATION_REFRESH_TTL` de 1 hora. Motivo é obrigatório (mínimo 5 caracteres) e a trilha em `versions` grava quem, quem foi personificado, quando, por quê e quando encerrou |
| AUTH-06 | Logout de quem já está deslogado **não é erro** (S1, `BE-005`, `FE-039`) | Respondia 422, e o front tratava como falha: susto na cara de quem sai de uma sessão já expirada. Do outro lado, erro **real** de logout era engolido pelo `catch` | 200 quando não há o que revogar; e falha de verdade agora avisa — "se estiver em um computador compartilhado, feche o navegador" —, porque nesse caso a sessão pode continuar válida em outro dispositivo |
| MAIL-01 | O log de e-mail guarda **metadados, sem corpo** (S1, **DEC-90**) | `livetat_mailer_contacts` guardava `message`, e a coluna foi promovida de `string` para `text` **justamente para caber o corpo** — inclusive de e-mails de credencial —, sem expurgo nenhum | `email_logs` **não tem coluna de corpo**. Não é convenção: é ausência deliberada, para que nenhum envio futuro consiga gravar a credencial por engano. Expurgo de 180 dias pelo `PurgeEmailLogsJob`, que já existia esperando a tabela |
| DEC-74-A | **Melhoria parcial por decisão**: o indicador de verificação é replicado, a **trava do telefone não** (S1, **DEC-74**) | No legado, `is_phone_checked = 1` deixava o campo de telefone `readonly` **para sempre** | O indicador de quatro degraus vai como está, com o degrau máximo dependendo da mesma flag. A trava não vai: no ai9 o telefone é canal de login (DEC-14), e replicá-la deixaria quem perdeu o número sem acesso e sem autoatendimento — cai na exceção de segurança do próprio DEC-30. Conferido pela tela: o campo WhatsApp do `/profile` é editável |
| DEC-75-A | **Consequência registrada**: o aviso de "sua credencial mudou" **desapareceu** (S1, DEC-75) | Os dois e-mails de senha saíram inteiros, e com eles o único gatilho que avisava o usuário quando o acesso dele era alterado — o sinal por onde uma pessoa percebe que mexeram na conta dela | Nenhum dos 3 e-mails vivos cobre isso. Se voltar, volta como **notificação de alteração de conta**, que é o nome certo num produto sem senha |
| IMP-A4 | Paginação **que existe de verdade** nas listagens de conta (S1, `BE-009`, `BE-034`, `FE-014`) | O legado descartava `limit`/`offset` (**D-20**) e a `UsersPage` da base tinha `page`/`perPage` no estado, mandava para a API e **não renderizava controle nenhum** — `setPage` só era chamado com `1`. E `GET /users/:id/memberships` fazia `Project.all`, sem paginação e sem filtro | Kaminari no servidor com **teto de `per_page`** (o legado aceitava `per_page=1000000`), envelope em cabeçalho (DEC-62) e `PaginationPill` na tela. A aba Projetos de `/users/:id` também é paginada |
| IMP-A5 | **[observável]** Cadastro público desligado **nos dois lados** (S1, `BE-011`, **DEC-49**) | Não bastava não portar a rota do legado: a **base ai9** já tinha `pre_register`, `complete_registration`, `visitor_signup` e `visitor_signup_with_link` na allowlist pública de `api/root.rb`. Se ficassem, o **D-39** (qualquer pessoa da internet virando Admin) renasceria por uma porta que nem veio do legado | As 4 rotas foram **removidas**, não só tiradas da allowlist — rota que não existe não reabre por engano. Specs 9.2.1–9.2.3 provam que nenhuma delas atende anônimo e que nenhum caminho cria conta com papel administrativo |
| IMP-A8 | Perfil estendido com **os tipos certos** (S1, `DB-004`, `BE-016`, `BE-507`) | Os 41 campos de `livetat_auth_user_infos` usavam `integer` fazendo papel de boolean e de enum, e `string` fazendo papel de data — a ponto de aceitar `"00/00/0000"` como emissão de documento fiscal | Entram em `users` (não viram tabela): `gender` enum string, `birthday` e `fiscal_document_issued_at` como `date`, booleanos como `boolean`. **Consertado nesta passada:** os seis campos eram aceitos pelo `PATCH /auth/v1/me` e **não eram expostos pela entity** — a tela gravava e, ao recarregar, voltavam vazios |
| IMP-A9 | **[observável]** Avatar com tipo real, teto de servidor e storage privado (S1+S13, `BE-017`, `OPS-505`, `OPS-506`) | O caminho da base gravava o arquivo cru em `public/uploads/avatars/`, servido como estático **sem autenticação** (D-82 vivo dentro do produto novo), validava o `Content-Type` **declarado pelo cliente** e **não tinha teto de tamanho** — o limite de 2 MB existia só no JavaScript da tela, e não batia com nada | `POST /api/v1/users/:id/avatar` pelo motor único: ActiveStorage privado, magic bytes (Marcel), 3 MB do catálogo. **O endpoint antigo da base continua montado** (dois consumidores do assistente interno) mas foi **endurecido nesta passada** com as mesmas duas checagens |
| IMP-A26 | Código de acesso **imprevisível**, com comparação de tempo constante (S1, `BE-020`) | O legado gerava com `Kernel#rand` (Mersenne Twister — previsível a partir de amostras) e comparava com `==`, que retorna cedo e vaza o prefixo correto pelo tempo de resposta. O token do magic link **nunca expirava** (D-37) | `SecureRandom`, 6 dígitos, 5 minutos, 3 tentativas, teto de 5 pedidos/15 min por destino normalizado, e comparação de tempo constante |
| IMP-A30 | Token de máquina **não herda papel de OG** (S1, `BE-047`, ver Q-B1) | Na base, `require_og!` passava para **qualquer** `ClientApplication` válido: um token de integração vazado virava CRUD total de contas de usuário | A policy separa "é máquina" de "é OG". A credencial de máquina entra pelo mesmo `Authorization: Bearer` do resto da API, e os headers `X-LAA-Agent`/`X-LAA-Token` do legado são descartados (DEC-58) |
| IMP-A32 | **[observável]** Na aba **WhatsApp** do login, o botão deixa de **não fazer nada em silêncio** (S1, `FE-002`) | `MagicLogin.handleSubmit` tinha três pré-checagens que davam `return` seco com um `console.warn` e o comentário *"a validação já é feita no hook"* — **e o hook não era chamado nesses casos**. Número curto: a tela ficava idêntica, sem mensagem, sem avanço. Na aba E-mail o estrago era menor só por sorte do `<input type="email">`, cuja validação nativa do navegador barrava antes | As três cópias saíram; quem valida é o `useAuth`, que **escreve o motivo** e a faixa vermelha renderiza. Exemplo em `AuthFlow.duas-abas.test.tsx` trava os dois lados: não avança **e** diz por quê |
| IMP-A33 | **[observável]** A faixa de "Vendo como" passa a aparecer em **toda tela e toda largura** (S1, `FE-037`) | O aviso de impersonação vivia só dentro da barra lateral — que **nasce recolhida** (`sidebar_collapsed` default `true`) e **não existe abaixo de `md`**. Na configuração padrão era um ícone de olho; no telefone, nada. Cada ação feita nesse estado é gravada na trilha em nome da pessoa personificada | `ImpersonationBanner` no `Layout`, ao lado do banner de aceite. **Não pode ser dispensada**: é estado de sessão, não pendência administrativa. O cartão duplicado da barra lateral foi removido no mesmo passo |
| IMP-A34 | **[observável]** O **Admin** ganha a porta de "Ver como" que já tinha no servidor (S1, `FE-036`, DEC-18.3) | `useRole()` dizia `canImpersonate: isOg`. A `Authorization::Hierarchy` do servidor já autorizava o Admin (limitado a hierarquia inferior), então o recurso existia e era **inalcançável pela tela** — o defeito mais comum do legado, repetido no produto novo | "Ver como" no menu do usuário e no cartão da barra lateral para OG **e** Admin, com teste chamando o hook de verdade. A trava de quem pode ser alvo continua sendo do servidor |
| IMP-A35 | O canal de permissões deixa de aceitar o `user_id` **que o cliente mandar** (S1, `BE-040`, flag **U2**) | `PermissionsChannel` da base fazia `stream_for("permissions:#{params[:user_id]}")` sem conferir nada: qualquer sessão autenticada assinava o fluxo de qualquer pessoa. `ProjectProgressChannel` e `RenegotiationChannel` já tinham recusado esse padrão por escrito | O fluxo é **sempre** o do usuário da conexão; pedir o de outra pessoa é `reject` (não um fluxo trocado em silêncio). O canal carrega **um aviso**, nunca o estado — quem recebe reconsulta pelo endpoint, que é onde a autorização mora |
| MAIL-02 | **[observável]** O e-mail mostra a marca em **texto**, não como imagem anexada (S1, `BE-535`, `OPS-504`) | O legado anexava `logo.png` inline em 8 pontos de `engines/mailer19/.../mailing.rb` (e o `avatar.png` do remetente no convite) com `File.new(...).read` **sem `rescue`**: arquivo ausente derrubava o envio. Além disso, boa parte dos clientes de e-mail bloqueia imagem por padrão — o logo simplesmente não aparecia para uma fatia dos destinatários | `layouts/mailer.html.erb` desenha SAFE**GOLD** em Work Sans com a faixa de ouro, sempre visível. **É diferença de aparência em relação ao legado, e é decidida**: quem comparar os dois e-mails vai ver o logo virar texto. O que se ganha junto é que falta de arquivo de marca não pode mais derrubar envio — agora vale por construção, não por `rescue` |
| IMP-A36 | **[observável]** Apagar um campo do perfil passa a funcionar (S1, `BE-015`, `BE-016`) | `Auth::MeService#update` fazia `params.slice(...).compact`, e o `.compact` descartava justamente o `nil` **explícito** — que é como o cliente diz "apague isto". **Medido contra o servidor de dev antes da correção:** `{"graduation": null}` respondia `422 Nenhum campo para atualizar`, e `{"birthday": ""}` também, porque o Grape converte data vazia para `nil`. Ou seja, **campo de data não tinha como ser limpo** — e nada acusava: a tela mandava e a resposta dizia que não havia o que atualizar | Campo esvaziado na tela vira `null` no `PATCH` e o servidor grava `NULL`. O que o cliente **não** mandou continua intocado. Quatro exemplos em `spec/requests/api/auth/v1/me_spec.rb`; recolocando o `.compact`, três deles voltam a 422 — conferido |
| IMP-S13-01 | **[observável]** Anexo do Safegold é **privado, com URL de prazo curto** (S13, `OPS-491`, `OPS-492`) | No legado o binário morava em `public/system/:attachment/:id/…`, servido como estático, com URL adivinhável e **sem autenticação nenhuma** (D-82) — inclusive os documentos de renegociação, que são documento financeiro. A base ai9 repete o padrão em `AssetsProxyController` (flag F-10) | Toda leitura passa por `GET /api/v1/attachments/:signed_id`, que **autoriza antes de assinar**. A URL expira (5 min para documento, 1 h para imagem). QA: link de anexo copiado e colado depois de um tempo **para de funcionar** — é o comportamento correto, não regressão |
| IMP-S13-02 | **[observável]** Upload é validado pelo **conteúdo real** do arquivo (S13, `OPS-623`) | `api/v1/uploads.rb:31` da base confia no `Content-Type` que o **cliente** declarou (`ct.start_with?('image/')`), e o legado tinha a detecção de spoof literalmente desligada (`MediaTypeSpoofDetector#spoofed? → false`) | Arquivo cujo tipo declarado mente é recusado com `422 ATTACHMENT_REJECTED`. **Medido nos dois estados:** sem `spoofing_protection: true` o `active_storage_validations` compara o tipo declarado e um texto puro enviado como `image/png` passava |
| IMP-S13-03 | **[observável]** Os limites de anexo passam a valer **no servidor** (S13, `OPS-495`, `FE-484`) | `MAX_FILES_PER_RENEGOTIATION = 4` e `MAX_FILE_SIZE = 5.megabytes` só existiam interpolados no JavaScript da view (`_tab_geral.js.erb:90-102`). O servidor aceitava qualquer quantidade e qualquer tamanho — bastava um `curl` | Mesmos números, mesmos textos ("Limite excedido", "O máximo de arquivos permitido para envio é de 4 arquivos"), agora vindos de `config/attachments.yml` e conferidos no servidor **contando o que já está anexado** — quatro requisições de um arquivo cada não passam mais |
| IMP-S13-04 | Limite de anexo vem de **configuração explícita**, nunca de introspecção (S13, CFG-02) | É a regra que sobrevive ao descarte da galeria polimórfica (`OPS-496` e cia.), onde o legado decidia o limite por `respond_to?` e a checagem de dimensão testava **duas vezes a largura** e nunca a altura | `config/attachments.yml` é a única fonte. Um portão de spec reprova model que declare `has_one_attached` fora do `Attachable` — ele já pegou um caso real |
| IMP-S13-05 | **[observável]** Falha de entrega de e-mail vira **linha de log**, não ausência de linha (S13, `DB-481`, `DB-596`) | O `EmailDeliveryLogger` é um `register_observer`, e observador do `Mail` só roda **quando a entrega dá certo**: erro de SMTP ficava indistinguível de "ninguém tentou mandar". No legado o problema era o espelho — a linha era gravada **antes** de enfileirar, então ela significava intenção, não entrega | `ApplicationMailer.delivery_job = LoggedMailDeliveryJob`, que grava `status: 'failed'` com classe e mensagem do erro **e relança** para o Sidekiq retentar. QA: agora dá para responder "a pessoa recebeu o código de acesso?" |
| IMP-S13-06 | Consulta de CNPJ volta **com teto por usuário/dia** (S13, `OPS-480`, **DEC-46**) | A integração é **paga por consulta** e o custo é do cliente. O legado não tinha teto nenhum, e um laço acidental na tela vira fatura | 30 consultas por usuário por dia (`RECEITAWS_DAILY_LIMIT_PER_USER`), com `remaining_quota` na resposta para a tela avisar antes de o botão parar. Cache de 365 dias **não** conta contra o teto. CNPJ com dígito verificador errado é recusado **antes** de gastar consulta — o legado mandava qualquer coisa para a API |
| IMP-S13-07 | **[observável]** Barra de progresso deixa de mentir (S13, `FE-482`, `OPS-463`) | `live_progress_percent` do legado devolvia **100** quando não havia job (entidade que nunca rodou nada aparecia como concluída) e **0** no fim do processamento, porque o registro do job já tinha saído da fila | `nil` quando não há job — a tela não desenha barra —, o número real enquanto roda, 100 só quando terminou. Travado por spec |
| IMP-S13-08 | Job que falha **é visto** (S13, `OPS-460`, `OPS-462`) | Os 7 jobs do legado capturam `rescue => e` sem relançar, e em `insert_projects_on_default_user_job.rb:11-12` o `rescue` é **literalmente vazio**: a fila marcava sucesso, nada era retentado e um usuário ficava sem projeto sem deixar rastro (D-79) | A exceção propaga, o Sidekiq retenta e o dead set guarda o job por 6 meses **visível e reenfileirável**. Um portão de spec reprova `rescue` largo sem `raise` — inclusive contra o exemplo de `ai9-conventions.md` §3.7, que é o antipadrão. Divergência consciente, registrada em `upstream-flags.md` |
| IMP-S13-09 | **[observável]** O avatar sai de `public/uploads/` (S13, `OPS-493`) | Gravação crua no disco da aplicação, servida como estático sem autenticação, sem limite de tamanho e com tipo conferido pelo que o cliente declarou | `POST/DELETE /api/v1/users/:id/avatar` sobre ActiveStorage. **O campo da API continua se chamando `avatar_url`** — regra de fronteira: nenhum consumidor do front precisou mudar. A validação de 2 MB que existia só no JavaScript saiu; vale o limite real do servidor (3 MB, o do legado) |
| IMP-S13-10 | **Melhoria adiada, registrada**: falha ao **enfileirar** e-mail ainda não gera linha em `email_logs` (S13) | `notify_safely` (em `observers_service.rb` e `admin_messages_service.rb`) captura a exceção do `deliver_later` e apenas loga — o que está certo (SMTP fora não pode derrubar a operação de domínio), mas se o Redis estiver fora o e-mail se perde deixando só uma linha de log | A falha de **entrega** já vira linha (IMP-S13-05); falta a de **enfileiramento**. O conserto é um ponto único de despacho, e ele toca services de outra fatia — por isso fica registrado em vez de feito às pressas |
| CAD-S3-01 | **[observável]** Paginação e ordenação dos catálogos passam a **funcionar** (S3, `BE-067`, `BE-072`, `BE-075`, `BE-077`, `BE-700` / D-20, DC-38) | No legado o `search` dos cinco cadastros lia `l`/`o` e **descartava**: a tela trazia a base inteira e o controle de paginação era decorativo. Ordenar `carrier_groups` por título respondia **500** (o controller chamava `CarrierGroup.prepare_ordering`, que o model nunca definiu — D-21) | Kaminari com envelope em cabeçalho (DEC-62) e `Sfg::Sortable` com allowlist. **QA: a tela deixa de trazer tudo de uma vez — é mudança intencional, não regressão.** Chave de ordenação desconhecida é ignorada em vez de virar `nil + " "` → 500 |
| CAD-S3-02 | **[observável]** Criar segmento passa a **funcionar** (S3, `BE-076` / **D-21**) | `Segment` validava `user_id` presente e `segment_params` **não o permitia**: a criação falhava **100% das vezes**, em produção, desde 2021. Ninguém conseguia cadastrar um segmento pela tela | O autor vem da **sessão** e o `user_id` do corpo é ignorado — a mesma regra que fecha o outro lado do defeito no `ProjectGuaranteeTypesController`, o único que **não** sobrescrevia o `user_id` recebido (D-23). **QA: uma feature que estava quebrada nasce funcionando** |
| CAD-S3-03 | **[observável]** O detalhe do Portador passa a ser **alcançável** (S3, `FE-068`, `BE-068` / **DC-08**, D-22) | O HTML e o SCSS da tela existem no legado (`pub/console/parts/carriers/detail/`) e **nenhuma rota chega neles**: a action `show` só respondia a `format.js` e não havia link em lugar algum | `/carriers/:id`, com abas Identificação / Estrutura de cotas / Relações, montada como `children` da área no registro de navegação. **QA: é tela nova para o usuário, embora seja trabalho que já existia** |
| CAD-S3-04 | **[observável]** Exclusão bloqueada responde **422 de verdade** (S3, `BE-070`, `BE-073`, `BE-078`, `BE-705` / **D-24**) | O legado respondia `:ok` mesmo quando não excluía — a tela dizia "excluído" e o registro continuava lá. Pior: `Carrier has_many :risk_controls, dependent: :destroy` fazia excluir um portador **apagar os limites de risco dele** | Bloqueio explícito, com a frase **nomeando o vínculo** ("2 projetos usam este segmento"), e **nenhuma associação de domínio em cascata**. Na tela, o botão de remover dá lugar ao ícone com a explicação, e o critério do botão **é** o critério do servidor. **QA: excluir passa a falhar onde antes "funcionava" sem apagar** |
| CAD-S3-05 | **[observável]** `bank_code` vira **string** e preserva `001` (S3, `DB-057`, `BE-069` / **DC-12**) | A coluna era `integer` e o código COMPE do Banco do Brasil virava `1`. Replicar seria replicar corrupção de dado | String da criação à serialização. **Consequência para o ETL:** os zeros à esquerda precisam ser reconstituídos pelo comprimento 3 do COMPE, e o que não converter é **reportado**, nunca inserido calado |
| CAD-S3-06 | **[observável]** "% de contas subordinadas" passa a ser **derivado no servidor** — com a **fórmula do legado preservada** (S3, `FE-065`, `BE-069` / **DC-09** + **DEC-30**) | Era calculado em JS a cada tecla **e** persistido como coluna editável — duas fontes de verdade para o mesmo número. A guarda de divisão por zero existia só no cliente: o servidor aceitava e gravava `Infinity`/`NaN` (família D-10) | Fórmula num lugar só (`Carrier#derive_subordinated_percent`) e campo **somente leitura** na tela. **A FÓRMULA NÃO MUDOU:** o legado divide as cotas subordinadas pelas **sênior** (não pelo total) e devolve **0** quando sênior é 0 — `250/750 = 33,33%`, não 25%. É contraintuitivo para quem conhece a proporção usual de cota subordinada num FIDC, e por isso está travado por golden (`carriers_percentual_golden_spec.rb`, DEC-30): mudar exige uma DEC nova. **QA: o campo deixa de ser editável; o número é o mesmo de hoje** |
| CAD-S3-07 | **[observável]** Tipo de garantia deixa de responder **para anônimo** (S3, `BE-700` / **D-23**) | `ProjectGuaranteeTypesController` declarava `requires_current_user? == false`: o endpoint respondia sem sessão, e o gate og/admin/gerente existia **só na view** | Montado dentro de `api/v1/base.rb`, sob o gate de credencial do `Api::Root`: **401 sem token** é o comportamento padrão. Escrita por `Authorization::Matrix` no servidor, com `R` ao Colaborador (DEC-18.4) |
| CAD-S3-08 | **[observável]** O logo do Portador **volta a existir** (S3, `FE-067`, `DB-062` / **DEC-47**, **DEC-91**) | Estava morto pela metade (**DC-10**): upload e exibição comentados no HTML, mas handler JS vivo apontando para input inexistente, `permit` aceitando `logo` e o model com anexo e validações completos | `has_one_attached` no próprio `Carrier` — ActiveStorage, **não** `Medium` (a tabela `media` não tem dono nem escopo) e **não** Paperclip. O tipo **real** do arquivo é conferido: `.exe` renomeado para `.png` é recusado (no legado o detector de spoof estava desligado). `Company` continua **sem anexo**, como o DEC-47 decidiu |
| CAD-S3-09 | **[observável]** Os tipos de garantia **existem** desde o primeiro boot, marcados como **provisórios** (S3, `DB-558` / **DEC-86**) | A tabela existe no legado desde 2022 e **nenhum seed a popula**: o select de garantias do projeto sobe **vazio** até alguém cadastrar à mão | Oito tipos plausíveis no seed de **referência** (aval, nota promissória, penhor, alienação fiduciária, cessão de recebíveis, fiança, hipoteca, seguro garantia), com selo "provisório" na tela. **QA: o conteúdo é suposição, não migração — a lista definitiva é do cliente**, e trocá-la é trocar linhas de seed, sem migration nem deploy |
| CAD-S3-10 | Busca deixa de interpretar o termo como **padrão SQL** (S3, `OPS-056`) | `Dev.ilike` interpolava o operador conforme o adapter e montava o padrão dentro da string: `100%` e `a'b` viravam padrão SQL em vez de texto literal | `ILIKE` com **bind** e `sanitize_sql_like` — `%`, `_` e contrabarra deixam de ser curinga. Buscar por "100%" acha o fundo que tem "100%" no nome, em vez de devolver tudo |
| CAD-S3-11 | Busca **simétrica** com e sem ordenação (S3, `BE-067`) | O ramo com `ordering_keys` e o sem montavam consultas **diferentes** — e só um deles fazia `q.upcase`. Com dado em caixa mista, o mesmo termo devolvia conjuntos diferentes conforme a coluna clicada | O `where` da busca é aplicado **antes** da ordenação e não depende dela. Spec compara os dois conjuntos |
| CAD-S3-12 | Cidade e UF deixam de depender de **geocodificação** (S3, `OPS-057` / Lacuna **L-11**) | O `geocoder` rodava com timeout de ~3h20 e **sem cache** para preencher dois campos de formulário de cadastro | `api/v1/br_states.rb` com as 27 UFs como constante, reusando a mesma lista de `countries.rb`. `geocoder` e `city-state` **não são portados** (coerente com o DEC-92, que descarta a geolocalização inteira) |
| CAD-S3-13 | Título de portador **duplicado continua permitido** — de propósito (S3, `BE-071`) | Não é bug: o comentário do legado ("Cloud #7036") registra que a carga trouxe contrapartes homônimas com usos distintos em projetos e renegociações, e a unicidade foi desligada | Preservado, **com teste de não-regressão**: quem acrescentar `uniqueness: true` sem passar por uma DEC reprova em `carriers_spec.rb`. **QA: não abrir defeito por haver dois portadores com o mesmo nome** |
| CAD-S3-14 | O seed da app se separa em **referência** × **demonstração** (S3, `OPS-054`, `OPS-540`) | O legado misturava os dois, a ponto de o bloco de empresas estar marcado **no próprio código** como "seed feito somente para vídeo de aprovação" — e aquilo rodava em qualquer ambiente | `rake reference:seed` (idempotente, roda em **todo** deploy: papéis, permissões, tipos de garantia) × `rake demo:seed` (S20, vitrine, não roda no cutover). Carregador **único**; S5/S6/S8/S17 plugam o catálogo delas com um arquivo e uma linha em `Runner::CATALOGS` |
| CAD-S3-15 | `sub_segments` **não ganha** hierarquia com `segments` — decisão registrada (S3, `DB-066` / **DC-13**) | Apesar do nome, o legado nunca ligou os dois: são duas listas planas e `projects` aponta para cada uma por uma coluna própria. Criar a hierarquia agora exigiria **inventar** o mapeamento dos dados existentes | Fica como candidato a feature futura, escrito no model e na migration para que ninguém "conserte" sem decidir. **QA: subsegmento não filtra por segmento — é o comportamento do legado** |
| CTR-S12-01 | **[observável]** Publicar contrato passa a **exigir papel** (S12, `BE-335`, **DEC-38**) | `contracts_controller.rb` tem **101 linhas e zero** `before_action`/`may?`/`admin?`/`og?`/`authorize`; as rotas não têm constraint (`routes.rb:30-31`) e o `create` (`:56-67`) só carimba `creator` e salva. **Qualquer autenticado publicava um novo Termos de Uso** — e, como o vigente é o de maior versão, o texto que todo mundo aceita passava a ser o dele | Recurso novo `contract_versions` na `Authorization::Matrix` (o 46º): **CRUD para OG e Admin**, `-` para Gerente e Colaborador. `contracts` (ler e aceitar) fica como a matriz aprovou. **Gate NOVO, não paridade** — verificado nos dois lados por API e por tela: OG/Admin 201, Gerente/Colaborador **403 do servidor**, e o item "Contratos" some do menu do Gerente |
| CTR-S12-02 | **[observável]** O ciclo de aceite **volta a existir** (S12, `BE-332`, `BE-333`, `FE-332`, `FE-333`, **DEC-65**) | Morto por **quatro** causas independentes: bloqueio inteiramente comentado (`pub_application_controller.rb:55-63`); os dois botões "ACEITAR" comentados com handler e rota `PUT` **vivos e inalcançáveis**; pendência levantando exceção por `source: :contract_deal` inexistente (`user_decorator.rb:40` → 500 em `/contract/:type`); e checkboxes pré-marcados que controller nenhum lia. **Não existia forma de aceitar pela interface** | Banner persistente + botão, **sem bloquear o acesso** (opção (b) da DEC-65 — bloquear numa demo comercial trava o cliente na primeira tela). Verificado renderizando o ciclo inteiro: banner com 2 pendências → aceite pelo rodapé da página pública → banner com 1 → "Aceitar" do banner → banner some, `contract_deals` com IP, user-agent, hash e texto |
| CTR-S12-03 | **[observável]** A prova de aceite guarda **o que a pessoa leu** (S12, `DB-331`, `OPS-333`, **DEC-80**) | `contract_deals` tinha `user_id`, `contract_id` e os dois `timestamps`. Nada mais: prova de que houve aceite, sem prova do que foi aceito (D-65) | Usuário, versão, data/hora, IP, user-agent, **hash SHA-256** e **o texto renderizado no momento do aceite**, com índice único `(user_id, contract_id)` e exportador CSV. Como a DEC-80 recusou o versionamento imutável, o corpo gravado é o que transforma "sei que mudou" em "sei o que ele leu". Spec prova que editar o contrato depois **não** altera a impressão registrada |
| CTR-S12-04 | **[observável]** Editar contrato com aceites **avisa quantos ficam divergentes** (S12, mitigação 2 da **DEC-80**) | Não existia no legado: o texto vivia (e vive) em `action_text_rich_texts` e podia ser editado sem que nada registrasse a consequência | `GET /api/v1/contract_versions/:id/impact` devolve `accepted_count` e `divergent_count`, e a etiqueta vermelha aparece no histórico. Com o gate da DEC-38, o aviso chega a quem pode decidir |
| CTR-S12-05 | **[observável]** Publicar deixa de ser **efeito colateral de digitar** (S12, `FE-342`) | No legado **não existe botão salvar**: qualquer `keyup` registra a ação na barra global, e revisar um parágrafo publicava uma versão nova dos Termos de Uso para todo mundo | Botão **"Publicar" explícito**, com confirmação nomeando o número da nova versão e avisando que **todos voltam a ter aceite pendente**; comparação lado a lado com a versão anterior e aviso de alterações não publicadas. Conferido na tela, nos dois modos |
| CTR-S12-06 | **[observável]** Vigente passa a ser o de **maior versão** (S12, `BE-331`, `BE-336`) | O `.last` sem `order` ordena por `id`: **re-salvar uma versão antiga a movia para o fim e o público passava a ler o texto errado**. E o `version_guess` era `before_save` **sem `on:`**, de modo que re-salvar **incrementava** o número | `Contracts::Resolver` usa `DISTINCT ON (kind) … ORDER BY version DESC`; a numeração é atribuída **só na criação**, com `pg_advisory_xact_lock` e índice único `(kind, version)` — a distinção entre concorrentes é do **banco**, não da aplicação |
| CTR-S12-07 | **[observável]** A página pública de contrato deixa de ser **vetor de XSS e open redirect** (S12, `BE-330`, `FE-334` / **D-69**) | O `redirect_url` era interpolado na view **e** usado como destino: as duas vulnerabilidades no mesmo parâmetro | `return_to` deixou de ser URL e virou **chave** (`login`, `console`, `profile`, `faq`) resolvida por allowlist **no servidor**. Não existe caminho em que texto do visitante vire `href`. Spec cobre os dois vetores; a tela avisa quando o destino foi recusado |
| CTR-S12-08 | **[observável]** A página que tem valor jurídico passa a ser a **mais fiel** (S12, `BE-345`, `FE-331`) | A pública fazia `CGI.unescape(...to_plain_text).html_safe` e jogava fora título, lista e negrito; o console mostrava o rich text. **A tela que o usuário assina era a menos fiel das duas** | `Contracts::Renderer` é o **único** caminho de HTML, com allowlist aplicada na renderização. Spec compara literalmente o corpo servido pela rota pública e pelo console |
| CTR-S12-09 | Q-B34 resolvida **sem quebrar link externo** (S12, `FE-335`, `OPS-331`) | As URLs do legado carregam o tipo em português, com espaço e com o typo consolidado (`Politicas de Privacidade`), e existem em links de terceiro | A rota pública aceita **as duas formas**: o slug (`termos-de-uso`) é o canônico das URLs novas e a string literal continua resolvendo. E `APP_HOST` passa a ser validado **no boot** — no legado a dependência de `ENV['alias']` era silenciosa, e sem ela **todos** os links quebravam sem erro no servidor |
| CTR-S12-10 | **Consequência registrada**: os aceites do legado entram marcados, e o novo é exigido (S12, **DEC-66**) | O passivo tem duas origens: o `after_create` que gravava aceite sem interação (`user_decorator.rb:234-240`) e o seed que **fabricou aceite retroativo para toda a base** (`db/seeds.rb:141-157`). A base atual não distingue quem aceitou de quem foi carimbado | `contract_deals.source = 'implicit_legacy'` preserva a data original (em `legacy_accepted_at`, quando o registro é promovido) e **não satisfaz a pendência**. O perfil mostra a etiqueta "carimbado pelo sistema antigo" — é o que faz o pedido de reaceite parecer razoável em vez de arbitrário. **QA: reaceite geral no cutover é comportamento esperado** |
| CTR-S12-11 | **Melhoria recusada por decisão**: o percentual de aceite **não é portado** (S12, `BE-344`, **DEC-54**) | `Contract#accept_users` está comentado nos **dois** únicos lugares em que aparecia, sem comentário explicando por quê — pode ter sido performance, pode ter sido conta errada | Entra no ledger como `dropped` com o motivo escrito. A divisão inteira que dava sempre 0 ou 100 fica registrada aqui como o defeito que ele tinha, para quem for ressuscitá-lo um dia |
| HLP-S12-01 | **[observável]** A busca da ajuda passa a achar **o conteúdo novo** (S12, `BE-350`, `BE-362` / **D-58**) | `HelpItem` tinha **dois acervos** — a coluna `description` até 04/2019 e o ActionText depois — e `has_rich_text` **sobrescrevia o leitor da coluna**. Nada criado depois de 04/2019 era encontrado por busca de conteúdo, e a tela não dava sinal nenhum disso | **Um campo só.** A busca é `ILIKE unaccent` sobre `action_text_rich_texts.body`, com as tags de HTML apagadas antes da comparação (senão "div" casaria tudo). A extensão `unaccent` entra por migration própria — a base só tinha `plpgsql` e `pgcrypto`. Verificado na tela: buscar "medio" acha "prazo **médio**" |
| HLP-S12-02 | **[observável]** O termo `0` deixa de casar **a base inteira** (S12, `BE-350`) | O `WHERE` tinha `OR help_items.id = ?` recebendo `@query.to_i`, e `"abc".to_i` é `0` | Texto e id são parâmetros distintos, declarados no Grape. Não existe mais o `OR id =` na busca textual |
| HLP-S12-03 | **[observável]** Com mais de 30 itens, **nada some da tela** (S12, `BE-351`) | O front pedia `l = 30` ignorando o default 20 do servidor, e o servidor não devolvia total nenhum: qualquer instalação com mais de 30 itens perdia itens **em silêncio** | Envelope em cabeçalho (`X-Total-Count`/`X-Total-Pages`, DEC-62), com spec cobrindo 35 itens em duas páginas sem interseção |
| HLP-S12-04 | **[observável]** O slug de categoria vira **coluna** (S12, `DB-368`) | `normalized_title` era calculado em runtime, sem persistência e **sem unicidade**: duas categorias que transliteram igual produziam o mesmo slug, e renomear uma quebrava o deep-link da outra | Slug persistido, único e **desambiguado** (`duvidas-frequentes`, `duvidas-frequentes-2`), e **imutável no renomeio** — que é o ponto inteiro de um deep-link |
| HLP-S12-05 | **[observável]** A área de ajuda deixa de responder **sem sessão** (S12, `BE-363` / **D-57**) | **Todos os 4 controllers de help** do legado respondiam sem usuário logado — não sobrescreviam `requires_current_user?`. Só o CSRF protegia as escritas, e a única restrição real era **de menu** | Sessão obrigatória em tudo; leitura do FAQ para qualquer autenticado (`faq`, `R R R R`), escrita só para papel administrativo (`help_*`, `CRUD CRUD - R`). Provado por spec nos quatro papéis e por tela: o Gerente vê "Ajuda" e **não** vê "Central de ajuda" |
| HLP-S12-06 | **[observável]** Excluir passa a **falhar quando falha** (S12, `BE-354`, `BE-359`, `BE-353`) | O ternário do legado é literalmente `@help_item.errors.any? ? :ok : :ok` — **200 sempre** — e o template de resposta é um arquivo de **0 bytes**. Do outro lado, `.where(...).first` seguido de `.update` estourava `NoMethodError` → 500 em id inexistente | Falha responde erro **com motivo**; id inexistente responde **404** em item, categoria e grupo. O fallback `params[:account_id]` (resíduo de copy/paste de outro CRUD) **não** é portado |
| HLP-S12-07 | **[observável]** A confirmação de exclusão diz **quanto se perde**, vinda do servidor (S12, `BE-357`, `BE-360`) | O único aviso era um texto fixo no JS; o servidor apagava a subárvore sem contar nada. ⚠️ Nem o legado nem o ai9 têm lixeira nestas tabelas | `GET /help_groups/:id/impact` e `/help_categories/:id/impact` devolvem a contagem exata, e é ela que a caixa exibe ("2 categorias e 5 itens"). A cascata roda numa transação — spec prova que falha no meio não deixa órfão |
| HLP-S12-08 | **[observável]** Editar item de outro autor **não reescreve a autoria** (S12, `FE-366`) | `user_id` viajava num campo escondido sempre com o `current_user`: depois da primeira revisão, "quem escreveu isto" não tinha mais resposta | Autor gravado **só na criação**, a partir da sessão; quem alterou por último vai em coluna própria. O `user_id` **não existe** no payload de edição. Junto: o toast deixa de dizer "Item criado" na edição, e a cor do avatar de fallback vira **determinística pelo id** (era `random_color`, mudando a cada render) |
| HLP-S12-09 | **[observável]** Corpo vazio de item passa a ser **recusado** (S12, `BE-352`) | `validates :description, presence: true` **nunca falhava**: `has_rich_text` faz o leitor devolver um `ActionText::RichText` construído na hora, que é sempre "presente". Dava para gravar item sem conteúdo | A validação olha o **texto puro**. E na tela o botão fica desabilitado enquanto o editor estiver vazio |
| HLP-S12-10 | **[observável]** A edição inline deixa de ser uma **corrida** (S12, `FE-365`) | Um `focusout` de 200 ms **revertia** a alteração competindo com o `Enter`: renomear e clicar fora perdia a edição — às vezes. E `setEmpty(false)` estava nos **dois** ramos do `if`, então o estado vazio **nunca** aparecia | Sem temporizador: `Enter` e `blur` gravam, `Escape` cancela. Travado por vitest que avança 1000 ms de relógio falso e prova que nada reverte. O estado vazio aparece de verdade |
| HLP-S12-11 | **[observável]** A busca do FAQ deixa de disparar **uma requisição por tecla** (S12, `FE-364`) | Busca no `keyup`, sem debounce; `lastQuery` setado para `" "` (um espaço) ao trocar de categoria, o que fazia **sumir itens de título curto sem espaço**; e callback de falha **vazio**, que fazia falha de rede não mostrar nada | `useDebouncedSearch` (300 ms, o mesmo do app), trocar de categoria **limpa** a busca, e o quarto estado mostra o erro com "tentar de novo". Os três travados por vitest |
| HLP-S12-12 | **Achado desta fatia, corrigido renderizando**: o editor de item abria **vazio** (S12) | A lista administrativa não devolve `description_html` (o entity só o expõe com `type: :full`, para a lista não carregar o texto de todos os itens). Abrir o formulário com o objeto da lista deixava o editor em branco — e "Salvar alterações" mandaria corpo vazio por cima do texto real. **`tsc` passava limpo**: `description_html` é opcional no tipo | O formulário busca o item completo ao abrir e só existe depois que o corpo chega; enquanto isso mostra "Carregando o texto do item…". Vitest reprova a volta. **Só apareceu abrindo a tela** — é o caso que a Regra de fronteira nomeia |
| HLP-S12-13 | **[observável]** A ajuda de campo nasce **com os 91 textos** (S12, `OPS-545`, **DEC-88**) | Os 3 YAML do legado têm 91 chaves e **um único texto**: *"Só um teste de informações do campo pra descrever para que serve cada campo"*. Não havia conteúdo a migrar | O **mecanismo** é desta fatia (`GET /api/v1/help/fields` + `<FieldHelp>`), e os textos já escritos sob a DEC-88 vêm dos YAML — trocar um texto é editar YAML, sem deploy. **As 4 chaves marcadas `TODO:` não exibem tooltip**: campo sem resposta não ganha ícone, porque "TODO: precisa saber…" na cara do operador é pior que tooltip nenhum. Duas travas (servidor filtra, cliente refiltra) |
| HLP-S12-14 | `unaccent` entra como **migration**, não como suposição (S12, `BE-350`) | O `schema.rb` da base declarava só `plpgsql` e `pgcrypto`. Usar `unaccent()` sem habilitar faria **toda** busca de ajuda levantar `PG::UndefinedFunction` — e só na primeira busca, não no boot | `EnableUnaccentExtension`. `pg_search` **não** entra: seria o primeiro uso na base e é dependência nova (Princípio 6b). Se o volume exigir, vira proposta própria |
| TEM-S17-01 | **[observável]** Dark mode **passa a existir** (S17, `BE-381` / **DEC-98**) | O legado **não tem** dark mode. `STYLE__DARK` está declarado em `SFG/theme.rb` e a opção que o ligaria está **comentada** — nunca houve o segundo modo | `globals.css` com os três blocos completos (`:root`, `.dark`, `.surface-dark`) e o `ThemeToggle` da base no ar. **GANHO, NÃO PARIDADE: o QA do Phase 4 não deve procurar dark mode no legado.** O custo é de QA e está assumido pelo DEC-98: toda tela nasce conferida nos dois modos, e a conferência é renderizando |
| TEM-S17-02 | **[observável]** A marca passa a ter **fonte única**, e trocar o logo passa a exigir **deploy** (S17, `BE-382`, `BE-383`, `OPS-603` / **DEC-55**) | No legado a marca vinha de um `GlobalTheme` no banco com 4 anexos, editável por uma tela. Só que **o motor não pintava nada**: `app_theme_template.css` tem 167 linhas e o arquivo inteiro está dentro de um comentário (`/*` na linha 1, `*/` na 167, zero regras fora — **D-55**), e o parser continuava rodando em cima disso (`app_theme.rb:207-231`), gravando um comentário CSS inteiro em `cached_css`. A área **também não tinha item de menu** (D-63) | Marca = token (`globals.css`) + arquivo versionado (`public/images/brand/`) + um componente (`components/brand/Logo.tsx`). **O custo está registrado e o usuário escolheu pagá-lo:** a alternativa (tema como configuração única editável) foi oferecida e recusada. **QA: não existe tela de temas, e a ausência é deliberada** — travada por teste |
| TEM-S17-03 | **Melhoria PROMETIDA e depois RECUSADA por decisão**: "o tema passa a pintar" e "`override_css` passa a ter efeito" **não acontecem** (S17, `BE-377`, `BE-378` / **DEC-55**) | A proposta original da S17 prometia as duas — eram a razão de ser da fatia. O DEC-55 as cancelou junto com a área inteira. **Está aqui porque um QA que leia a proposta antiga vai procurar as duas e não achar** | Nenhum. `BE-377` e `BE-378` entram no ledger como `dropped`. A ironia que fecha o caso: a definição de paridade de `BE-378` no inventário é literalmente *"o motor de temas não pinta nada — `override_css` inerte"*. Portar seria portar o defeito |
| TEM-S17-04 | **[observável]** `text-primary` deixa de ser **ilegível** no modo claro (S17, `BE-381` / achado desta fatia) | O ouro Safegold `#FFC107` sobre o card branco dá **1,63:1** — reprova AA por mais de três vezes. E o mesmo token pintava o **fundo** do botão primário, onde o ouro cheio é justamente o certo (grafite por cima, 10,4:1). Um valor só não serve aos dois usos: clarear conserta o texto e estraga o botão. Havia **125 chamadas** de `text-primary` no repo, todas nessa situação | Tokens `-text` separados (`--primary-text`, `--warning-text`, `--destructive-text`, `--success-text`, `--info-text`, `--negative-text`) nos três blocos, e `tailwind.config.js` mapeia **só** `textColor` para eles — `backgroundColor`, `borderColor` e `ringColor` continuam no token de fundo. **Nenhuma das 125 chamadas mudou**: a tela escreve `text-primary` e sai legível. **`--primary` continua sendo exatamente o ouro medido no logo (DEC-68) — a marca não se mexeu.** `text-brand-gold` também não é rebaixado, porque ali a cor é o assunto |
| TEM-S17-05 | **[observável]** O botão de apagar **volta a passar AA** no escuro (S17, `BE-381` / achado desta fatia) | `--destructive` no escuro era `357 74% 55%` e o `--destructive-foreground` branco por cima dava **4,36:1** — o próprio botão destrutivo reprovava. Achado renderizando `/ui-kit` no escuro, não lendo código | `357 74% 53%` → **4,55:1**, sem mudar a leitura da cor. O par foi conferido nas duas direções, porque a mesma cor também é usada como texto de erro de formulário — e essa metade puxava para o lado oposto (precisava clarear). Daí o `--destructive-text` a 72% |
| TEM-S17-06 | A varredura de fonte única vira **teste**, não checklist (S17 / §5.4.7) | A §5.4.7 do `ai9-conventions.md` pedia um `grep` antes de abrir PR de tela. Checklist que depende de alguém lembrar apodrece — e este apodreceria rápido, porque com a área de temas fora **nada mais impede** a marca de voltar a se espalhar em `#hex` pelas telas | `frontend/src/__tests__/marca-fonte-unica.test.ts`, 26 casos, comentários descontados: zero `#hex`, zero `rgb()/hsl()` com número cravado, zero paleta literal do Tailwind, zero `z-[…]`/`shadow-[…]`/`rounded-[…]`, zero `dark:` consertando cor, zero referência a CRUD de tema, e todo caminho de logo citado no `Logo.tsx` tem arquivo. **Não substitui olhar a tela** — e o teste diz isso no cabeçalho |
| TEM-S17-07 | **[observável]** O logo ganha variante **monocromática** — que no legado nunca existiu (S17, `OPS-635` / **DEC-93**) | Em `SFG/theme.rb:47-57` as constantes `_WHITE` e `_MONO` apontavam **todas para o mesmo arquivo colorido**. Não havia logo branco nem monocromático: havia três nomes para uma arte só | Seis artes derivadas do canal alfa da arte colorida, preenchido com uma cor só, expostas como `<Logo tone="mono">`. **Registradas como derivação, não como arte do cliente:** se o manual de marca aparecer, o original vence e a troca é de arquivo. A branca e o maskable já tinham sido derivados antes (`d068d3dc`), com **remoção de fundo por alpha real** — a fórmula por luminância apagava o ouro junto — e o maskable com **fundo cheio**, porque maskable transparente o Android recorta errado |
| TEM-S17-08 | **Dívida registrada, não corrigida aqui**: `users.app_theme_id` **não deveria existir** (S17, `DB-388` / **DEC-56**) | A coluna e o índice nasceram na migration de `users` de S1 (`20260825130000_add_safegold_identity_columns_to_users.rb:58,90`), escrita **antes** do DEC-56 descartar o `UserTheme`. Hoje é uma FK lógica para uma tabela que nunca vai existir | Não removida por esta fatia **de propósito**: `db/schema.rb` está sendo editado por outros agentes agora, e o `checkpoint.md` documenta duas armadilhas de banco que já morderam nesse cenário. **Ação para o dono de S1:** remover a coluna e o índice. Enquanto isso, o comentário da coluna já avisa que as telas de auth não a leem |
| TEM-S17-09 | **[observável]** A política de anexo `public_brand` **sai** antes de virar buraco (S17, `OPS-499` / **DEC-55**) | Era a **única** política do motor de anexos que devolvia `true` sem olhar o usuário — anexo legível antes de existir sessão, porque a tela de login precisava do logo do tema. Com o tema fora, ficou **sem nenhum consumidor** | Removida junto com o catálogo `app_theme` (4 anexos) de `config/attachments.yml`. Política de "todo mundo, inclusive anônimo" sem consumidor é arma carregada: bastaria a próxima linha de catálogo escolhê-la por engano para abrir o anexo à internet. Conferido que nenhuma entrada do catálogo referencia política inexistente. **QA: se algum dia existir anexo genuinamente público, ele nasce com política nomeada e teste provando que é público de propósito** |
| TEM-S17-10 | **Dívida registrada, não corrigida aqui**: `app_themes` continua na matriz de autorização (S17, `BE-384` / **DEC-55**) | `Authorization::Matrix` lista `app_themes` como recurso (`CRUD CRUD - R`) e `matrix_spec.rb:57-61` o usa como exemplo do gate og/admin. É um recurso que **não existe mais**: sem tabela, sem model e sem endpoint, a linha não autoriza nada | Não removida por esta fatia: `app/services/authorization/matrix.rb` é tabela compartilhada, está sendo estendida por outras fatias, e a remoção arrasta o spec junto. **Inofensiva enquanto ficar** (linha de matriz sem endpoint não abre nada), mas é linha morta. **Ação para o dono da matriz:** remover a entrada e trocar o exemplo do spec por outro recurso de gate og/admin — `help_items` já está na mesma asserção |
| ETL-S14-01 | **Divergência INTENCIONAL com o texto de `DB-ETL-05`: `legacy_password` NÃO é preservada** (S14, tarefa 10.5) | O texto de DB-ETL-05 fala em preservar as colunas `legacy_*`. `legacy_password` é hash Django e a senha do usuário importado é **derivada do primeiro nome + `#6230`** (`../sfg/app/models/legacy/u.rb:28,30` — D-106/D-109), num produto que **não tem senha** (DEC-14) | `legacy_id`, `legacy_project_id`, `legacy_user_id` e `legacy_carrier_id` existem nas tabelas de destino; `legacy_password` **não existe no ai9** e não é criada. **Sem este registro o QA do Phase 4 lê como item faltando** |
| ETL-S14-02 | **[correção ao material]** São **138** migrations no legado, não 139 (104 do app + **34** das engines) | Medido reexecutando: `db/migrate` tem 104 arquivos e `engines/*/db/migrate` tem 34 (auth19 14, feedback19 13, mailer19 5, auth_omni19 2). O material dizia "104 + 35" | O baseline (`backend/db/etl/legacy_schema.yml`) reporta `138/138 reexecutadas` e **67 tabelas** — o número de tabelas confere com o inventário |
| ETL-S14-03 | **[correção ao material / F-08]** São **30** tabelas órfãs herdadas da base ai9, não ~25 | Contadas pelo portão de schema: 30 tabelas no `schema.rb` que nenhuma migration cria (`fly_*` 16, `work_*` 8, `budget*` 3, `achievements`, `user_achievements`, `drops`, `point_events`) | Estão na allowlist **nominal** de `Sfg::Etl::TargetBaseline::INHERITED_ORPHANS`. Acrescentar uma órfã **nova** faz `rake sfg_etl:schema_gate` e o spec falharem; as antigas ficam **visíveis**, não invisíveis |
| ETL-S14-04 | **Achado pela introspecção, e era bloqueador de acesso silencioso**: `livetat_auth_user_infos.phone` **não existe** | A migration `20171201171448_add_phone_columns_to_user_info.rb` **removeu** `phone` em 2017 e a partiu em `phone_country_code` + `phone_area_code` + `phone_number` (e `20171213170127` trocou o default de `+55` para `55`). Um conversor que lesse `phone` traria `nil` **para toda a base** | No ai9 o telefone é **canal de login** (DEC-14): a migração inteira chegaria sem telefone e o dry-run do DEC-45 não teria como contar quem fica sem entrar. `Converters::Users#compose_phone` monta o número das três colunas. **É o tipo de defeito que só a introspecção pega — nenhum teste que estube o esquema o encontraria** |
| ETL-S14-05 | **Achado ao executar a carga**: a ordem de carga de 2021 é a **causa** do BE-452 (a), e não é replicada | No pipeline Django→Rails `users` era a 7ª entidade, depois de `carriers` e `segments` — e por isso o adaptador **forçava o autor para um identificador fixo**: quando o portador era gravado, não havia usuário a quem apontar. A primeira reconciliação desta fatia mostrou `carriers.user_id` gravado **nulo**, pelo mesmo motivo | `Users` passa a ser o **primeiro** da ordem (`backend/db/etl/load_order.yml`). O ciclo que sobra (`users.default_project_id` → `projects` → `segments` → `users`) é resolvido por um **segundo passo** declarado (`Converters::Base.deferred`), não por id inventado. Reconciliação: **100% das FKs resolvidas, 0 órfãs** |
| ETL-S14-06 | **Achado ao executar**: o ETL passa a gravar com `touch: false`, preservando o `updated_at` do legado | Numa **atualização** cujo `updated_at` atribuído coincide com o já gravado (logo, não conta como "changed") mas cujo registro ficou sujo por um callback do model, o Rails carimba `updated_at = Time.current`. Os portadores voltaram da 2ª carga com a data de hoje | A data de atualização do legado é **dado**, não metadado — é ela que responde "quando isto mudou pela última vez". Travado pela amostra campo a campo da reconciliação |
| ETL-S14-07 | **Achado ao executar**: `users.identifier` do legado pode **não caber** no formato do ai9, e não é normalizado | No legado `identifier` é `string` livre; no ai9 é `/\A[A-Z0-9]{6}\z/` com unicidade (`user.rb:122-125`). A carga estourou `RecordInvalid` na primeira execução | O usuário entra com `identifier` **nulo** e sai numa lista (`users:identifier_format`) para atribuição manual. **Não se inventa um identificador de seis caracteres a partir de outro** — seria fabricar dado |
| ETL-S14-08 | Colunas que o model do ai9 **calcula** são declaradas e não migradas (`Converters::Base.derived`) | `carriers.subordinated_accounts_percent` é derivado no servidor (DC-09, `carrier.rb:164-172`, replicando a fórmula do legado: subordinadas × 100 ÷ **sênior**). Atribuí-lo no ETL é escrever um valor que o `before_save` sobrescreve — e a reconciliação acusaria divergência em toda linha | Nenhuma mudança de valor. A reconciliação **exclui** essas colunas da comparação literal e **diz quais são**, em vez de gerar ruído que esconde a divergência de verdade |
| ETL-S14-09 | **Correção à tarefa 8.1/8.2 da S14**: o ensaio do ETL **não** roda contra o seed de demonstração | O seed da S20 grava tabelas do **ai9**; a origem do ETL é o esquema do **legado**. Rodar o dry-run contra o banco do seed não exercita conversor nenhum, porque nenhuma tabela legada existe lá | O ensaio roda contra `Sfg::Etl::Source::Fixture` (`backend/db/etl/fixtures/*.yml`), dado versionado com a forma do legado. Trocar para o dump de produção é mudar `SOURCE=`. **QA: não procurar o ETL rodando sobre o seed** |
| ETL-S14-10 | **[defeito no PRÓPRIO motor, achado ao rodar contra produção]** `SqlDump` lia **zero linha** de um dump `pg_dump` normal | `pg_dump` grava dado em `COPY … FROM stdin;` por padrão, e só usa `INSERT INTO` com `--inserts`. O parser entendia **só `INSERT`** — que é o formato do dump Django antigo, o único disponível quando ele foi escrito. Contra `sfg-31-may-25.sql` (56 blocos `COPY`, zero `INSERT`) ele devolvia **0 linha em toda tabela, sem erro nenhum**: volumetria, dry-run e reconciliação saíam **verdes e vazios**. É o modo de falha mais perigoso possível num ETL, e nenhum portão o pegava | Os dois formatos são suportados; o formato detectado entra no **cabeçalho de todo relatório** (`formato copy`), para que a pergunta se responda em um segundo. O bloco `COPY` é lido **em fluxo** — uma passada guarda esquema, deslocamento em bytes e contagem; as 642.447 linhas de `risk_entries` nunca viram 642.447 Hashes ao mesmo tempo. Travado por spec com dump sintético |
| ETL-S14-11 | **[defeito no próprio motor]** O gravador do baseline **engolia** toda DSL de migration que não conhecia | `Proxy` não implementava `change_table`, e `ActiveRecord::Migration#method_missing` embrulha a chamada em `say_with_time { … }` — que o `Proxy` estubava para `nil`. Resultado: a chamada **sumia sem erro e sem entrar em `migrations_failed`**. Duas migrations do legado usam `change_table`, e o baseline saiu sem as **4 colunas Paperclip de avatar** e as **3 de progresso de job**. A introspecção contra o dump real então acusou **7 "surpresas"** que não existiam — e um baseline incompleto é pior que baseline ausente, porque **aborta o cutover apontando o lugar errado**. Junto: `respond_to?(:up)` é `true` em TODA migration (o `up` de instância delega para o de classe), então as três migrations em `def self.up` do legado eram escolhidas certo por acidente e a de `change_table` por acidente errada | `change_table`, os predicados de existência (resolvidos **contra o gravador**, nunca contra um banco) e a DSL inerte entram em lista explícita; **o desconhecido levanta** e vira linha em `migrations_failed`. A escolha entre `change`/`up` passa a olhar o que a CLASSE define. Depois: **0 surpresas** contra o dump de produção. `TargetBaseline` ganhou o mesmo tratamento — o `rescue StandardError; next` dele escondia migration pulada, e migration pulada some com as tabelas dela |
| ETL-S14-12 | **[defeito no próprio motor]** O dry-run **estourava** na primeira anomalia real | `Scan#publish!` esperava `{key:, title:, line:}` de `Converter#anomalies`, mas **11 dos 13 conversores devolvem String** (o retorno de `Values.anomaly_line`). Contra o dump real: `TypeError: no implicit conversion of Symbol into Integer`. A fixture nunca alcançou o caminho porque as duas únicas anomalias que ela dispara vêm justamente dos dois conversores que usam Hash | O leitor aceita as duas formas (`Scan#normalized_custom`) e o contrato está escrito em `Converters::Base#anomalies`. **Quem tinha o defeito era o leitor** — 11 conversores de outras fatias não mudaram uma linha |
| ETL-S14-13 | **[SEGURANÇA — 72 contas]** O bloqueio de conta passa a usar `deactivated`, não `is_active` (**D-128**, DEC-39) | Medido no dump: `is_active = 0` são **13** usuários e `deactivated = true` são **85**, com os 13 sendo **subconjunto** dos 85. `deactivated` é o **único `boolean`** do schema do legado e é o que o produto lê — recusa o login (`sessions_decorator.rb:12`), derruba a sessão a cada request (`pub_application_controller.rb:45`), é o que os botões escrevem e o que a lista mostra. `is_active` **não tem um único leitor**. O conversor bloqueava só por `is_active`: **72 contas hoje impedidas de entrar no legado entrariam no ai9** | Bloqueio pela **união** das duas (85), com `blocked_reason` nomeando qual coluna desligou cada conta. Não é regra nova: é a **regra da DEC-39** aplicada à coluna certa, e cai na exceção de segurança/autorização do DEC-30. Travado por spec |
| ETL-S14-14 | **[correção ao material]** Nenhuma das duas "provas de schema fora do versionamento" existe (**D-108** muda de veredito) | O material tratava `availability_templates.default_position` e `contracts.description` como colunas presentes no banco e ausentes das migrations. Medido no dump de produção: `default_position` tem **zero ocorrência no arquivo inteiro**, e `contracts` tem **7 colunas** e nenhuma `description` — o `description` do contrato é `has_rich_text` (`contract.rb:11`), ou seja ActionText, com 2 linhas em `action_text_rich_texts` | **Não existe schema fora das migrations no Safegold.** O D-126 (`default_position`) continua defeito de código; o **D-108 deixa de ser defeito de schema**. A allowlist fica, porque descreve o que é *tolerado*, não o que existe |
| ETL-S14-15 | **[perda silenciosa evitada]** `action_text_rich_texts` ganha conversor: são **512** descrições que não moram em coluna nenhuma | Indicator/description **485**, HelpItem/description **25**, Contract/description **2**. Carregar `indicators` sem carregar essa tabela junto perde 485 "instruções" **sem deixar rastro** — não existe coluna vazia para alguém notar a falta. O aviso já estava escrito em `load_order.yml`; faltava o conversor | `Converters::ActionTextRichTexts`, religado pelo de-para **polimórfico** (o `record_id` do legado é `integer`, o do ai9 é `uuid`), por último na ordem de carga. A validação item a item que o aviso exigia foi feita: **zero** corpos URL-escapados (a única `%XX` do acervo é o texto literal `%DE PARTICIPAÇÃO`, no indicador 288) |
| ETL-S14-16 | **[reconciliação de conteúdo, não só de contagem]** A religação de anexo é **provada por assinatura** | "Acervo reconciliado 100%" é verdade em contagem e em tamanho — e insuficiente: `renegotiation_attachments#45` tem **0 byte** no banco E no disco, e passa nas duas conferências (**D-133**). Um instrumento de garantia vazio só aparece olhando o conteúdo | `Attachments#verify!` baixa o binário de volta do ActiveStorage e confere **tamanho e SHA-256** contra o que saiu do acervo; divergência levanta, e num anexo `critical` a seção sai como **aborto**. Provado com PDF real de produção: `Simulação.pdf`, 983.078 B, sha256 `126956878458bcf0…`, lido do `.tar` **sem extrair** |
| ETL-S14-17 | **[correção de desempenho que era bloqueio de janela]** A reconciliação fazia **~2 milhões de `SELECT`** | `relink_section` chamava `IdMap.resolve` **dentro do laço de linhas**: só `risk_entries` são 642.447 linhas × 3 referências. Contra o dump real a reconciliação **não terminava** | Uma consulta por **tabela referenciada** (`IdMap.cache_for`), não por linha. O de-para inteiro de uma tabela cabe em memória; 2 milhões de idas ao banco não cabem numa janela de cutover |
| ETL-S14-18 | **[cobertura]** A introspecção passa a responder "que tabela com dado ninguém reivindicou?" | A volumetria dizia quantas linhas cada tabela tem; nenhuma seção dizia se alguém as reivindicava. A lição do Phase 1 é que verificação se faz no **consolidado** — cinco agentes reportaram cobertura completa e havia 31 IDs órfãos nas fronteiras | Seção nova: tabela da origem precisa estar na ordem de carga, na lista `do_not_migrate` com motivo, ou na de infraestrutura. Conversor que **lê tabela junto** declara `also_reads` (o `Users` lê quatro). Medido em 26/08/2026: **6 sem dono** — `trackings` (6.076), `livetat_auth_abilities` (2.224), `livetat_feedback_states` (8), `livetat_feedback_contexts` (4), `livetat_auth_client_applications` (3) e `app_themes` (1) |
| PRJ-S4-01 | **[observável]** O projeto corrente deixa de morar num **cookie** (S4, `BE-098`/`FE-106`, **D-28**) | No legado o tenant vivia no cookie `cached_info`, escrito pelo servidor **e** pelo cliente, com codificações diferentes, 4 dias de vida e **nenhuma flag de segurança**. Trocar o cookie trocava de projeto | A peça é da S0 e a S4 a **consome em cada endpoint**: `project = current_project!`, numa linha visível. O `project_id` que chega no corpo **nem é declarado** nos `params do` — é a ausência que fecha a família D-01/D-16/D-29 |
| PRJ-S4-02 | **[observável]** `project_id` do corpo é ignorado **também no update** (S4, `BE-055`, `BE-062`, `BE-119`, **D-23**) | O legado forçava o projeto no `create` e **esquecia** no `update`: `@company.update(company_params)` com `:project_id` no `permit`. Um campo escondido de formulário movia empresa, fornecedor e garantia de tenant | `ProjectScopedService#update` reafirma `record.project_id = project.id` **depois** do `assign`, e nenhum gancho pode desfazer. Travado por spec nos três recursos (7.1.10) |
| PRJ-S4-03 | **[observável]** A **paginação e a ordenação passam a funcionar** em projetos, empresas, fornecedores e garantias (S4, `BE-050`/`BE-059`/`BE-080`/`BE-118`, **D-20**) | Em `companies#search`, `providers#search` e `projects#search` o legado fazia `@x.where!(...).order(...).limit(...).offset(...)`: `where!` muta em lugar, e `.order/.limit/.offset` devolvem **outra** relação, que era descartada. A lista voltava inteira e a UI de paginação era decorativa. Em garantias o limite default era **1000** | Kaminari + envelope em cabeçalho (DEC-62). O total do cabeçalho é o total **sem limite** — o serviço devolve a RELAÇÃO, e quem materializa é o endpoint |
| PRJ-S4-04 | **[observável]** Ordenar garantias por "Título" **funciona** (S4, `BE-118`, **D-32**) | `ProjectGuarantee.get_ordering_key("title")` devolvia `"risk_operations.title"` — tabela que o `joins` da busca nem incluía. Clicar no cabeçalho "Título" produzia **erro de SQL** | A allowlist aponta para a coluna que a tela mostra, e o `joins(:carrier).joins(:guarantee_type)` é aplicado sempre. Chave desconhecida é **ignorada**, nunca 500 (`Sfg::Sortable`) — no legado `nil + " "` derrubava o request a partir da barra de endereço |
| PRJ-S4-05 | **[observável]** Criar projeto com responsável novo **envia link**, e nenhuma senha é montada (S4, `BE-085`, **D-38**) | O legado montava `@data_hash` com `responsible_username` e `responsible_pass` **em texto plano** para a view de boas-vindas | `Auth::InviteService` (o mesmo do convite, DEC-18.7): `LoginCode` de uso único, 24 h, por e-mail. Spec verifica que a resposta **não contém** `password`/`senha`/`username` |
| PRJ-S4-06 | **[observável]** O criador **permanece com participação** ao indicar um responsável existente (S4, `BE-087`, DC-14) | No legado a posse ia para o responsável e o criador ficava **de fora do próprio projeto** — sem participação, o escopo o excluía na requisição seguinte | `ProjectService#create` grava participação para o dono **e** para o ator, em transação |
| PRJ-S4-07 | **[observável]** O **slug é imutável** depois da criação (S4, `BE-095`, DC-17) | `set_smart_id` rodava em **todo** `before_validation`: renomear o projeto mudava o slug e, com ele, toda URL baseada nele | Derivado do nome `on: :create` (algoritmo do legado replicado, inclusive o sufixo numérico de desambiguação) e **congelado no model** (`freeze_slug`), não só ausente do `permit` — job, seed e `update_column` distraído também não trocam a URL de um projeto em produção |
| PRJ-S4-08 | **[observável]** Exclusão bloqueada responde **422 real**, com a frase nomeando o vínculo (S4, `BE-056`/`BE-063`/`BE-091`/`BE-119`, **D-24**) | Os controllers de empresa, garantia e conexão respondiam `errors.any? ? :ok : :ok` — **`:ok` nos dois ramos** — e o JS redirecionava dizendo "removido com sucesso" sem ter removido | O mecanismo é `BlockingDependents`, **extraído** do `GlobalCatalog` da S3 em vez de copiado: duas cópias dariam duas semânticas de exclusão. **Achado renderizando:** com `dependent: :restrict_with_error` a mensagem era a genérica do Rails, com o nome da associação **em inglês no meio do português** ("existem companies dependentes"). Nenhuma associação desta fatia usa `dependent: :restrict_with_error` |
| PRJ-S4-09 | **[observável]** O detalhe de **Fornecedor** passa a existir (S4, `BE-065`, **D-22**) | A rota e a action existiam; o template **não**. Clicar na linha dava `MissingTemplate` → 500 | `ProviderDetailPage`, com uma informação que o legado não tinha em lugar nenhum: **quando** o cadastro veio da Receita Federal (`cnpj_fetched_at`). Sem ela não há como saber se a razão social na tela é de ontem ou de 2021 |
| PRJ-S4-10 | **[observável]** O autopreenchimento por **CNPJ** volta a funcionar (S4, `FE-073`, **D-27** / **DEC-46**) | Estava morto por **duas** pontas: o botão comentado no HTML (`providers/helper/_body.html.erb:54-56`) e a URL do JS com o ERB escapado (`<%%=`), apontando para um literal | `GET /api/v1/cnpj/:cnpj` (entregue por S3/S13, **consumido** aqui), com teto por usuário/dia — a consulta é paga e o custo é do cliente. Indisponibilidade **não impede** o cadastro manual |
| PRJ-S4-11 | **[observável]** O documento do fornecedor vira **um par** e passa a ser validado de verdade (S4, `BE-066`, `DB-053`, DC-11) | O legado tinha `cnpj` e `cpf` como colunas separadas, "desabilitava" o bloco não escolhido **só por CSS** (os dois iam no POST) e gravava **com máscara** — `12.345.678/0001-95` e `12345678000195` eram strings diferentes, e o mesmo fornecedor entrava duas vezes apesar da unicidade | `(document_type, document)`, só dígitos no banco, dígito verificador conferido por `Sfg::Document` (**um** lugar; `Sfg::ReceitaWs::LookupService` tinha a metade CNPJ, privada). Continua **opcional** (a regra "ao menos um" estava comentada no model legado). A tela distingue **incompleto** de **inválido** |
| PRJ-S4-12 | **[observável]** As conexões de portador deixam de instanciar **classe arbitrária** vinda da URL (S4, `BE-102`..`BE-105`) | `params[:owner_type].constantize` e `params[:connection_type].constantize` montavam a ação inteira. Além disso: `save` com retorno ignorado (duplicata falhava calada), **só o último item** inspecionado por erro, lote vazio derrubando com `NoMethodError`, e a action `destroy` lendo `@connection` quando o `before_action` preenchia `@connections` — **nunca funcionou** | Sentido fixo (projeto corrente ↔ portador do catálogo), ação de conjunto fechado, lote vazio → **400**, resultado **por item**, e desconectar portador com garantia é recusado **com a contagem na mensagem**. Verificado executando o lote na tela |
| PRJ-S4-13 | **[observável]** O reset do projeto de treinamento resolve o segmento por **chave**, nunca por id fixo (S4, `BE-092`, **D-26**) | `wipe_data` fazia `self.segment_id = 1` — o primeiro segmento cadastrado em 2021. Apagá-lo deixaria a limpeza apontando para nada. E o método chamava `Renegotiation…destroy_all` **duas vezes**, iterando no bloco sobre objetos já destruídos: as parcelas ficavam | `ProjectResetService`, em **transação**, com a ordem de apagamento declarada por nome (a tabela que ainda não nasceu é pulada) e o segmento resolvido por `integration_key`. Ausência é resposta válida: melhor sem segmento do que apontando para o que sobrou |
| PRJ-S4-14 | **[observável]** Os dois interruptores do detalhe do projeto têm **ids distintos** (S4, `FE-093`) | Os dois eram `id="is_active_{project.id}"`. Clicar no rótulo de "BI contratado" alternava "Gerido pela Safegold" | `has_safegold_management` e `has_bi`, cada um com o seu `htmlFor`. Travado por teste que lê o arquivo — o defeito é de atributo, e um teste de comportamento passaria com os dois ids iguais desde que só um estivesse na tela |
| PRJ-S4-15 | **[observável]** A marca de gestão da empresa passa a ser **derivada**, e não há coluna para divergir (S4, `DB-051`, **D-30**/DC-01) | O legado copiava `has_safegold_management` para **6** tabelas por `before_validation` e re-carimbava **só `companies`** por `update_all` quando a marca do projeto mudava. Qualquer relatório que filtrasse por ela mentia nas outras cinco | `Company#has_safegold_management` lê do projeto. **⚠ A decisão definitiva depende da Q-02, sem resposta:** a coluna nas 6 filhas **não foi criada**, e a de `projects` (BE-093) foi. Se a resposta for "carimbo histórico", a coluna volta com backfill — o caminho contrário (remover 6 colunas já em uso) seria mais caro |
| PRJ-S4-16 | **[observável]** Salvar o formulário de projeto dispara **uma** requisição (S4, `FE-089`, DC-23) | O legado registrava salvamento a cada `keyup` do formulário | Um `salvar.mutate` no arquivo inteiro, travado por teste. O sucesso mantém o usuário no formulário |
| PRJ-S4-17 | **[observável]** A observação de disponibilidade **recusa anexo no servidor** (S4, `BE-097`) | O ActionText aceita anexo por padrão. O legado bloqueava **só no cliente** (`trix-file-accept` + botão escondido por CSS): um `curl`, ou o mesmo navegador sem JS, embutia arquivo na nota — e o arquivo saía servido por `public/`, sem autenticação (D-82) | `validate :availability_note_has_no_attachments`. **Achado ao rodar:** a validação precisa ler `rich_text_availability_note` (a associação crua) e não `availability_note` — o leitor gerado por `has_rich_text` **constrói** o registro, o autosave o grava com `body` nulo, e criar projeto passava a estourar `NotNullViolation`. Uma validação que materializa o que está validando muda o mundo |
| PRJ-S4-18 | **[observável]** `projects.name` ganha **índice único** (S4, `DB-549`) | O legado tinha `validates :formal, uniqueness: true` e **nenhum índice**: duas abas criavam dois projetos com o mesmo nome, e a partir daí toda busca por nome ficava ambígua | Índice único no banco. **Regra de fronteira aplicada no mesmo passo:** o helper de spec e um spec de anexo usavam nome fixo e passaram a colidir; os dois foram ajustados junto, não deixados para depois |
| PRJ-S4-19 | **[correção de dado]** `projects.integration_key` recebe **backfill** (S4, migration `BackfillProjectIntegrationKeys`) | Acrescentar uma validação obrigatória é mudar o contrato de escrita: as linhas gravadas antes da coluna ficaram `NULL` e **todo `update` nelas** passou a falhar. `rspec` inteiro verde — a suíte cria projeto do zero e nunca vê o caso. Apareceu no primeiro `save` de um projeto existente | Migration de backfill, com a mesma regra de derivação do model e desambiguação por sufixo. `down` **irreversível de propósito**: apagar chave de integração de projeto em uso quebra consumidor externo (DEC-85) |
| PRJ-S4-20 | **[observável]** As rotinas de correção de dado passam a ter **pré-visualização, log e teste** (S4, `OPS-055`, `OPS-089`) | `fix_after_global_remove`, `fix__7412` e `fix_company_links` eram métodos soltos que alguém colava no `rails c` de produção — um deles com `update_all`, que não passa por validação, não passa por callback e não deixa versão na trilha | `rake sfg:fix_company_links` e `sfg:fix_project_data`, com **`DRY_RUN` como default** (aplicar exige `APPLY=1`), log persistente em `log/`, escrita pelo **model** (a trilha registra) e spec de idempotência |
| PRJ-S4-21 | **[GANHO, não paridade]** As telas desta fatia nascem com **versão mobile própria** (S4, **DEC-100**) | O legado é desktop-only | Lista vira `MobileCard`, ações de linha vão para uma **folha no rodapé** (`MobileRowActions`, novo na biblioteca compartilhada), lote ganha barra contextual (`MobileActionBar`, idem), paginação é `MobilePagination`. O `CatalogScreen` foi **melhorado no lugar** — as cinco telas da S3 herdaram a folha e a paginação de graça. **QA do Phase 4: não procurar equivalente no legado** |
| PRJ-S4-22 | **[correção de contraste, achada renderizando]** A cor do projeto vira **faixa**, não fundo de ícone (S4, `FE-080`) | A primeira versão pintava o fundo de um ícone com `projects.color` — valor **arbitrário do banco** atrás de um glifo, contraste que ninguém pode garantir. E projeto **sem cor** (toda a base anterior à coluna) ficava com o ícone invisível no modo escuro | Faixa de 6 px na borda do cartão, com fallback no token `bg-border`. O ícone volta para `bg-muted`/`text-muted-foreground`. **Só apareceu abrindo a tela no escuro** |
| IND-S10-01 | **[observável]** A lista de indicadores globais **passa a paginar** (S10, `BE-311`/`FE-310`, **D-20**) | O front do legado manda `l=50, o=0` **fixos** e **nunca incrementa o offset** (`indicators/_body.js.erb`), e o `search` não devolve total nenhum: a partir do 51º indicador a lista **trunca sem aviso**, sem controle de página e sem nada na tela que sugira que falta coisa | Kaminari + envelope em cabeçalho (DEC-62), `PaginationPill` no desktop e `MobilePagination` no telefone. O total do cabeçalho é o total **sem limite** |
| IND-S10-02 | **[observável]** A grade mensal **distingue "não lançado" de "lançado como zero"** (S10, `BE-324`/`FE-326`, **DEC-70**) | A view instancia `IndicatorEntry.new` para o mês vazio (`indicator_entries/list/_widget.html.erb:14`) e renderiza `entry.value.blank? ? 0 : entry.value`; como a coluna tem default `0.0`, **ausência e zero saem idênticos** nas quatro variantes, e a cor também não distingue | A distinção nasce **no serviço** (`entry: null` × `value: "0.0"`), nunca numa heurística do componente. **Exceção consciente ao DEC-30 pelo critério da própria DEC-70**: a grade **não tem linha nem coluna de total** (verificado), então distinguir não muda soma nenhuma. Conferido renderizando: março `R$ 0,00`, junho `—` |
| IND-S10-03 | **[observável]** O autosave da célula **avisa quando falha** (S10, `FE-329`) | Cada célula é um `form_for` que submete no `change` por `remote: true` e **não há handler de erro nenhum**: em 422 o campo destrava sem mensagem e o usuário **acredita que salvou**. Pior, `preventDoubleSubmission` marca o formulário como enviado e **nunca limpa a flag** — depois do primeiro auto-save o mesmo campo **não envia de novo** até a lista recarregar | `useMutation` com `onError`: três estados visíveis na célula (enviando / salvo / falhou), a falha **fica na tela** com o valor digitado preservado, e o `isPending` do React Query substitui a flag que não se limpava. Provado executando: digitar e sair grava a linha no banco; com perfil somente-leitura o campo não aceita e a tela explica |
| IND-S10-04 | **[observável]** Ordenar por "Chave" **funciona** (S10, `BE-312`, achado **A-5** do DEC-85) | `Indicator.get_ordering_key("key")` devolve `"integration_key"` — **coluna que não existe** em `indicators` (a coluna é `key`) → `PG::UndefinedColumn`, 500. E `prepare_ordering` chamava `Segment.get_ordering_key`, não os métodos do próprio model. Na UI só "Título" era clicável, o que **mascarava** os dois defeitos | `Sfg::Sortable` com allowlist real (`title`, `key`, `created_at`), ordenação persistida na URL, chave desconhecida **ignorada** em vez de 500 |
| IND-S10-05 | **[observável]** Excluir indicador **não apaga mais a série histórica** (S10, `BE-318`/`FE-315`/`FE-321`, **D-66**) | `has_many :entries, dependent: :delete_all` — **sem callbacks, sem backup**, com uma confirmação que dizia apenas "A operação não pode ser desfeita" e **não mencionava os lançamentos**. Na tela de indicadores específicos **não havia diálogo nenhum** | Exclusão **lógica** (`indicators.discarded_at`), os lançamentos ficam, e a confirmação pergunta ao servidor **antes de qualquer escrita** quantos lançamentos e quais projetos serão afetados. O expurgo (`PurgeDiscardedIndicatorsJob`) **nunca** apaga indicador que ainda tenha lançamento ou conexão |
| IND-S10-06 | **[observável]** A busca da tela de indicadores específicos **passa a filtrar** (S10, `BE-707`/`FE-319`) | Os ramos `if params[:connection_type] == "Carrier"/"Project"` (`project_indicator_connections_controller.rb:31,34`) **nunca casam** com `"Indicator"`: `q`, `limit` e `offset` são ignorados, e o front chama com `l=200`. O JS ainda registra listener para um campo de busca que **não existe no HTML dessa tela** | Busca `ILIKE` com bind, ordenação e paginação reais. E o `params[:owner_type].constantize` (6 ocorrências) sai: não há tipo dinâmico nenhum |
| IND-S10-07 | **[observável]** O escopo da tela de específicos passa a ser o **projeto corrente** (S10, `FE-319`, contrato C1) | O projeto vinha de `current_user.default_project` **hardcoded no `data-attribute` e na URL do proxy**: quem não tem projeto padrão **quebrava** em `default_project.id`, e quem tem vários via sempre o mesmo | `current_project!` no endpoint, numa linha visível. Nenhum `project_id` sai do cliente |
| IND-S10-08 | **[observável]** Os filtros da grade e a ordenação do catálogo **ficam na URL** (S10, `FE-312`/`FE-325`) | No legado os filtros de indicador/mês/ano se perdem entre visitas, e não há como mandar a tela pronta para alguém | `?indicator=&month=&year=` e `?ordering=&dir=&q=`. É deep-link, não feature nova |
| IND-S10-09 | **[observável]** Os dois itens de menu chamados "Indicadores" ganham **rótulos distintos** (S10, **Q-R33**) | `create_console_menu` registra `indicator_entries` (Gestão) e `indicators` (Cadastro) **com o mesmo título** | Gestão vira **"Lançamentos de indicadores"**; Cadastro continua "Indicadores" |
| IND-S10-10 | **[correção de comparação]** A unicidade de título deixa de tratar `%` e `_` como **curinga** (S10, `BE-320`) | A regra do legado é `where("title #{Dev.ilike}", title)` → `title ILIKE LOWER(?)`. Sem escape, um indicador chamado `TAXA_MEDIA` faz o banco **recusar** `TAXAXMEDIA` como duplicado — e o que se perde é uma criação legítima | Comparação de **igualdade** insensível a caixa e acento (`unaccent(lower(...))`), com o resultado das cinco combinações travado no golden test `G1`. É a mesma correção que a S3 fez no `Dev.ilike` da busca (OPS-056), pelo mesmo motivo |
| IND-S10-11 | **[GANHO, não paridade]** A "Instrução" passa a ser **sanitizada na leitura** (S10, **UF-1**) | `RichTextInput` renderiza com `dangerouslySetInnerHTML` **sem sanitização** e não há DOMPurify no `package.json`. A Instrução é HTML escrito por um usuário e **lido por todos os outros do projeto**: XSS armazenado com alcance de tenant | `components/ui/RichTextField.tsx` — **membro da biblioteca** (Princípio 11), com allowlist fechada aplicada sobre documento parseado por `DOMParser` (não regex sobre string). O `RichTextInput` compartilhado **não foi tocado**: sanitizá-lo afeta todo consumidor da base, e isso é mudança de plataforma. **QA do Phase 4: não procurar equivalente no legado** |
| IND-S10-12 | **[GANHO, não paridade]** `GET /api/v1/permissions/me` ganha **consumidor** (S10, `FE-314`/`FE-318`/`FE-718`) | O endpoint existe desde a S0 e o comentário dele diz *"é o que o front usa para esconder botão"* — e **nenhuma tela o chamava**. Sem ele, a única forma de a tela saber que o perfil é somente-leitura era tentar gravar e ler o 403 | `hooks/useMyPermissions.ts` + `useIsReadonly()`, na biblioteca compartilhada. **Achado ao executar:** o endpoint responde **plano** (`{user_id, permissions}`), não `{data:{...}}` — o hook lia só a forma embrulhada e o modo somente-leitura **nunca acendia**, com `tsc` e `vitest` verdes |
| IND-S10-13 | **[GANHO, não paridade]** As três telas nascem com **versão mobile própria** (S10, **DEC-100**) | O legado é desktop-only | Catálogo em `MobileCard` com ações em folha de rodapé; grade com célula por mês empilhada e filtros acima; paginação `MobilePagination`. **Achado renderizando em 390×844:** a chave de integração (`inadimplencia_da_carteira`, monoespaçada) **invadia a coluna vizinha** do cartão — passou a ocupar a linha inteira com `break-all`. `tsc` e `vitest` passavam |
| DEMO-S20-01 | **[GANHO, não paridade]** O seed de demonstração tem **um** catálogo de referência, e ele é o da S3 | O `scaffolding` aplicava só `UserTypes` e `Permissions` com uma lista própria, e os escritores de limite e de movimentação **criavam os tipos por conta própria** — com flags que divergiam da referência em duas das quatro linhas (`fomento` nascia com `has_pre_faturamento`, `comissária` sem). A flag é `create_only` e decide se o limite abre par estático: a divergência não aparecia como erro, aparecia como operação estática no tipo errado | `Seeds::Reference::Runner.call!` no `scaffolding` (os seis catálogos, idempotente, pulando o que a fatia dona não entregou) e os escritores **resolvendo por `integration_key`**, nunca criando. Duas listas para o mesmo catálogo é sempre isso |
| DEMO-S20-02 | **[observável]** Um escritor que estoura **não derruba mais os outros dezesseis** | `Base#run` deixava a exceção subir: com cinco fatias entregando model na mesma semana, o primeiro escritor que encontrasse uma validação nova abortava o seed inteiro. O que sobrava na véspera da demonstração era um banco vazio, não um banco com um agregado a menos | Falha **barulhenta e parcial**: a transação do escritor volta atrás sozinha, o relatório imprime `✖ FALHOU` com a mensagem, e `rake demo:seed` termina com status ≠ 0. Como "não levantou erro" deixou de significar "gravou", o spec ganhou o exemplo que separa os dois — pular por model ausente é esperado, falhar gravando é defeito |
| DEMO-S20-03 | **[correção de dado, achada pela contagem da segunda execução]** Duas empresas do mesmo grupo colidiam numa linha só | `companies` tem índice único `(project_id, title)`. O grupo cuja razão social já termina em "Participações S.A." colidia com a própria filial 2, porque o sufixo dela é exatamente esse: as duas viravam **uma** linha, a segunda sobrescrevia a primeira em toda execução, e o limite de uma delas sumia. O banco aceitava; o que denunciou foi a segunda rodada reportando "atualizado" para sempre | Título único por construção (`company_title` recebe os já usados e pula o sufixo que colide). O mesmo sintoma achou outras duas fontes de "atualizado eterno": `document_type` do fornecedor gravado em minúsculo contra um domínio em caixa alta, e o título do indicador mandado como se escreve contra a coluna que a DEC-89 grava em caixa alta e sem acento |
| DEMO-S20-04 | **[correção de coerência]** A cadeia de movimentos é ordenada por **data**, e o saldo é recalculado nessa ordem | Os movimentos nasciam na ordem lógica (liberação, IOF, ad valorem, juros, liquidação), que **não** é a cronológica — os juros vencem perto do vencimento e a liquidação parcial costuma vir antes. O `balance` gravado na operação era o da ordem lógica e o `RiskOperation#balance_on` (BE-266) devolvia o da ordem cronológica: **dois números diferentes para a mesma operação**, um na lista e outro no painel de risco | `normalize_chain!` reordena por `(date, ordem de inserção)` — o mesmo `(date, created_at)` que o banco usa — e recalcula `sequence` e saldo. A soma não muda; a ordem, sim, e neste domínio a ordem é o resultado (DEC-02). Pelo mesmo motivo a mora de operação vencida passou a ser lançada **no vencimento**: `RiskMovement` recusa data fora da janela da operação |
| DEMO-S20-05 | **[GANHO, não paridade]** Os agregados da renegociação nascem pelo **serviço da S9**, não por conta própria | O escritor escrevia os ~20 agregados (R$ Pago, R$ A Pagar, percentual, contagens, estado) com números do razão. Bateriam com a tela por coincidência, e divergiriam na primeira vez que a S9 mudasse a fórmula | O escritor grava o **contrato** (fornecedor, empresa, datas, taxa, valores, parcelas e pagamentos) e chama `Renegotiations::AggregateService.recalculate!` — o cálculo único (contrato C2). O rodapé da demonstração bate com a lista **pela mesma conta que bate em produção**. `Renegotiations::Formulas.installment` deriva `saldo` (negativo) e `pending_value` (piso em zero) em vez de reimplementar a assimetria que a S9 passou a fatia eliminando |
| DEMO-S20-06 | **[correção de risco]** O reset **descobre** as tabelas a limpar em vez de tê-las escritas à mão | A lista fixa tinha os quinze agregados que o seed escreve — e o reset morreu no meio na primeira execução contra `sfg9_dev`, porque `availability_entries` (de outra fatia) referencia `companies` e não estava nela. O estado que sobrou foi o pior possível: metade apagada, metade não | Toda tabela com `project_id`, ordenada por FK (em cada passada saem as que nenhuma das restantes referencia), `DELETE ... WHERE project_id IN (...)`. A fatia que nascer amanhã entra sozinha. **Continua não sendo `TRUNCATE ... CASCADE`**, e é essa a diferença que protege as contas de login: o `CASCADE` segue a FK para fora do escopo (`users.current_project_id` → `projects`), que foi como a base de usuários foi apagada uma vez |
| DEMO-S20-07 | **[cobertura de estado]** As quatro cores da tela de renegociação existem no banco | "Sem parcela cadastrada" e "Inconsistente" não apareciam: o primeiro é o estado em que a renegociação nasce, e o segundo era **inalcançável no legado** (D-45 — a linha seguinte o sobrescrevia), a ponto de o filtro da tela nunca devolver nada. A S9 consertou; sem uma linha assim no banco, ninguém vê o conserto | Duas renegociações de vitrine no cliente em recuperação: uma sem parcela, outra com parcelas cobrindo 62% da dívida. Travado por spec (`os quatro estados de renegociação têm pelo menos um exemplo cada`) |
| DISP-S11-01 | **[segurança]** O painel de disponibilidade **passa a exigir sessão e escopo** (S11, `BE-117`/`BE-149`, **D-01**) | `api/v1/project_availability_controller.rb` herda de `ApplicationController` — **não** do `PubApplicationController` — e a primeira linha do `before_action` é `Project.find(params[:id] \|\| params[:project_id])`. Sem sessão e sem escopo: **qualquer requisição lia a disponibilidade de qualquer projeto por id**. Não é "o filtro é descartado quando chega um id" (D-16/D-29/D-76/D-100) — é **não havia filtro nenhum**, e é daí que a família tira o nome | Montado em `api/v1/base.rb` (autenticação global); o projeto vem de `current_project!`; o `:id` da rota é **conferido** contra a participação, nunca usado para buscar. Sem credencial → **401**; projeto alheio → **404**, igual a id inexistente. Travado por request spec |
| DISP-S11-02 | **[observável]** A busca do catálogo de padrões **volta a funcionar** (S11, `BE-132`/`FE-136`, **D-06**/**D-126**) | `availability_templates_controller.rb:21-22` monta `where!("title #{Dev.ilike} ", "#{@query}%")` — fragmento SQL **sem placeholder** — e ordena por `default_position`. A análise do dump (26/08/2026) confirmou que **essa coluna não existe** (zero ocorrências, nenhuma migration a cria): `ORDER BY default_position` levanta `UndefinedColumn`. **A listagem está quebrada em produção há anos**, e a tela só parava de atualizar, sem mensagem | Busca por **substring** com `sanitize_sql_like` e bind; ordem pela **hierarquia** (`sort_key`). A coluna `default_position` existe no ai9 (DEC-79) e o ETL a carrega se aparecer, mas **não ordena nada**. **QA do Phase 4: a ordem do ai9 não tem equivalente no legado — lá não havia ordem, havia erro 500** |
| DISP-S11-03 | **[observável]** A paginação e a ordenação **passam a existir** (S11, `BE-132`/`BE-120`, **D-07**/**D-20**) | `q`, `l` e `o` eram lidos nos três controllers do módulo e **nunca aplicados**: a lista vinha inteira, sempre, e a grade montava **uma consulta por padrão ativo** (`project.rb:216-233`) | Kaminari + envelope em cabeçalho (DEC-62), `PaginationPill` no desktop; a grade sai em **duas** consultas agregadas |
| DISP-S11-04 | **[observável]** Abrir a grade **nunca cria registro** (S11, `BE-130`, **DC-30**) | Os derivados (`parent_entry`, `next_level_entries`, `update_mirror!`) **salvam no banco**, e o `destroy` do controller chamava `parent_entry` **antes** de apagar — criando o pai justamente quando o usuário estava excluindo (havia um `TODO #7408` admitindo que o cenário multiempresa não foi fechado) | Leitura é `SELECT` puro. Os derivados continuam sendo materializados **na gravação**, que é onde o legado também os materializava. Travado por spec que conta as linhas antes e depois de abrir a grade |
| DISP-S11-05 | **[observável]** Padrão bloqueado por job **termina desbloqueado**, com ou sem sucesso (S11, `BE-147`, **D-05**) | Quatro operações em segundo plano, **quatro** implementações do par bloquear/desbloquear. Em três o `unlocked!` era a penúltima linha do **caminho feliz**; na quarta (`background_removal`) **não existia**. Somado a `destroy_failed_jobs? false` e à exceção engolida, uma falha deixava o padrão **bloqueado para sempre**, sem recuperação pela interface | `Availability::TemplateLock.around`, um serviço só, com `unlock!` em **`ensure`**. Spec que **força a exceção** em cada job e confere que o padrão fica utilizável |
| DISP-S11-06 | **[observável]** Desativar padrão obrigatório é **recusado no serviço**, antes de enfileirar (S11, `BE-114`/`BE-145`, **D-04**/**D-33**) | A guarda existia em `deactive_and_reorder!` — método que **nenhum caminho real chamava**: o controller enfileirava o job, e o job usava `background_deactivate`, que faz `is_active = 0` direto. Pior, a consulta de dependentes filtrava `project_id: self.id` — o **id do padrão** no lugar do id do projeto —, então nunca achava dependente nenhum | As três guardas rodam no serviço, antes da fila. Spec confere que **nenhum job é enfileirado** |
| DISP-S11-07 | **[dado financeiro]** Remover padrão **não apaga mais lançamento** (S11, `BE-115`/`BE-146`, **DC-20**) | `has_many :entries, dependent: :restrict_with_error` **e** `background_remove_templates` fazendo `self.entries.destroy_all` logo antes — o código contornava a própria restrição, sem transação e sem aviso. Remover um padrão apagava todos os lançamentos dele e da subárvore | O servidor **recusa 422** e os lançamentos **permanecem**; a remoção é atômica e a subárvore sai do mais fundo para a raiz com `destroy!` (não `destroy_all`, que engole a recusa). Travado por spec |
| DISP-S11-08 | **[observável]** A obrigatoriedade escolhida no formulário **é a gravada** (S11, `BE-134`/`DB-132`) | `is_mandatory \|= 1` no `before_validation`: **todo padrão global nascia obrigatório**, dissesse o formulário o que dissesse. E `should_insert_on_existing_projects` tinha default `1` e **nunca era exposto**: **toda** criação enfileirava job em **todos** os projetos, sem ninguém pedir | Os dois viraram campo na tela, com a consequência escrita ao lado. **Muda o que o usuário vê acontecer ao salvar** |
| DISP-S11-09 | **[muda número exibido]** `is_adjusted` passa a ser **copiado** do catálogo para o projeto (S11, `OPS-120`/`OPS-121`) | Os dois caminhos do legado divergiam: `create_templates_from_global` copiava `is_mandatory` e **não** copiava `is_adjusted`; `create_template_from_global` forçava `is_mandatory: 1` **literal** (`project.rb:359`) e também não copiava `is_adjusted`. O mesmo global gerava padrões diferentes conforme tivesse vindo da semeadura ou da propagação | `Availability::GlobalSeeder`, um código para os dois caminhos, copiando os atributos fielmente. **⚠ Copiar `is_adjusted` faz o padrão passar a ser corrigido por dias úteis onde antes não era: o número na tela muda. Não é regressão** |
| DISP-S11-10 | **[observável]** O valor digitado e o valor gravado ficam **ambos visíveis** (S11, `FE-134`) | O campo vinha preenchido com o valor **já corrigido** (`_widget.html.erb:56,131`) e não havia nada na tela dizendo que houve correção: o usuário digitava X e via Y | Selo "base R$ …" ao lado do valor, com o multiplicador de dias úteis no tooltip e o aviso de que salvar de novo aplica a correção sobre o valor já corrigido. **A fórmula não mudou — DEC-24 mandou replicar; o que mudou é o usuário conseguir ver o que ela faz** |
| DISP-S11-11 | **[observável]** O sinal negativo vai **no próprio valor** (S11, `FE-125`) | `_body.js.erb` exibia o **módulo** (`baseEntry.total * -1`) e sinalizava só pintando de vermelho: ambíguo para todo mundo e invisível para quem não distingue as cores | `formatarValor` devolve `-R$ …`, e a cor é reforço. Travado por teste |
| DISP-S11-12 | **[observável]** As duas regras de soma passam a ser **rotuladas** (S11, **DEC-26**/**DEC-27**, **D-08**) | A consolidação geral soma **bruto** (`availability_entry.rb:188`) enquanto o nó com filhos aplica `is_cumulative` e o sinal (`:191`); o total geral usa `value` e os cards usam `virtual_value` — mesma palavra, duas métricas. Nada na tela dizia qual era qual | **As fórmulas ficam** (DEC-26/27 mandaram replicar). O que entra é o rótulo: "Consolidação geral — soma bruta" × "Total do grupo — respeita cumulatividade e sinal", e "Saldo acumulado" nos cards. **A tarefa de rotulagem não é cosmética: ela É a decisão** — sem ela o D-08 volta a ser defeito silencioso |
| DISP-S11-13 | **[código morto removido]** O "total geral" **não é portado** (S11, **DEC-27**) | `values[:total]` (`project.rb:406`) é calculado a cada requisição e **nenhuma view o renderiza** — o painel só usa `by_entry[].total` | Não existe no ai9. **QA do Phase 4: não procurar esse número na tela; ele nunca esteve lá** |
| DISP-S11-14 | **[observável]** A ordenação da árvore deixa de ser **lexicográfica** (S11, `DB-131`/`BE-137`) | `position` era **string** e a árvore ordenava por ela: com 12 irmãos a ordem saía `1, 10, 11, 12, 2, 3…`. E o nível era derivado com ` \|= ` — **OR bit a bit** —, então um filho de nível 2 herdando de 5 virava **7** (funcionava por acidente enquanto os valores fossem `0`/`nil`) | `sort_key` zero-padded (`"0001.0002.0003"`) como **única** chave de ordem, `level = parent.level + 1`, e 4º nível recusado. As nove colunas redundantes saem; o caminho legível "1.2.3" é **derivado**, nunca guardado em segundo lugar |
| DISP-S11-15 | **[desempenho, mesmo resultado]** A reordenação deixa de ser **quadrática** (S11, `BE-116`/`BE-138`) | Os três `import` rodavam **dentro** do laço de 1º nível (`project.rb:266-268`): com N raízes, cada uma reimportava a lista acumulada. E a ordem da árvore vinha de `all_ids_by_position`, uma consulta **por nó**, remontada com `join (VALUES …)` construído por interpolação — que com projeto zerado virava **SQL inválido** | Uma leitura, renumeração em memória, uma gravação por nó **fora de laço aninhado**. Travado por spec que conta consultas. A árvore sai por `ORDER BY sort_key` |
| DISP-S11-16 | **[correção de risco]** A rotina destrutiva `destroy_existing` **não é portada** (S11, `OPS-129`) | `AvailabilityTemplate.destroy_existing` são três linhas — `AvailabilityEntry.destroy_all`, `ProjectAvailabilityTemplate.destroy_all`, `GlobalAvailabilityTemplate.destroy_all` — **sem uma única guarda**, chamáveis do console por quem tivesse acesso: todos os lançamentos financeiros e todos os padrões de todos os projetos, sem confirmação, sem pré-visualização, sem trilha | `rake sfg:availability:report` (só leitura, os cinco relatórios do cutover) e `sfg:availability:reconcile_virtual_values` (exige `APPLY=1`, roda em transação e só reescreve um valor **derivado**). Não existe tarefa destrutiva nesta fatia |
| DISP-S11-17 | **[GANHO, não paridade]** O progresso das operações em segundo plano **chega à tela** (S11, `OPS-121`/`FE-108`, Princípio 10) | O `console_progress_delegate` do legado só imprimia no **stdout**: nenhuma tela consumia o progresso. E cada propagação escrevia `job_id`/`job_state` na linha do **projeto**, então duas rodando ao mesmo tempo se atropelavam | `ProjectProgressChannel` (S0) invalidando a consulta, **um evento por projeto**. **Provado executando, nos dois sentidos:** mudei o dado no banco sem emitir evento e a tela **não** mudou em 14 s (zero requisições — não é polling); emiti **um** evento e a tela mudou sozinha com **uma** requisição. A primeira versão fazia **duas** — `invalidateKeys` e `onDone` invalidando a mesma chave —, que é o mesmo modo de falha do `WhatsappInstanceChannel`; corrigido e re-medido. **QA do Phase 4: não procurar equivalente no legado** |
| DISP-S11-18 | **[GANHO, não paridade]** As três telas nascem com **versão mobile própria** (S11, **DEC-100**) | O legado é desktop-only, e decidia "é mobile" **no servidor por user agent** (`mobile_device?`) **e** no cliente por detecção própria (`isMobile.any()`) — os dois podiam discordar | Uma fonte de verdade (`hooks/useMobile.ts`, no cliente). Lista vira `MobileCard`, ações vão para `MobileRowActions`, indicadores viram `MobileKPI`, o calendário abre em folha ancorada no rodapé. **Achado renderizando em 390×844:** `MobileKPI` arredonda moeda para zero casas (convenção de KPI da S0, correta para indicador) e o saldo saía "R$ 225.990" **sem os centavos** — passou a receber o valor já formatado, o mesmo da tela larga. `tsc` e `vitest` passavam |
| RNG-S9-01 | **[observável]** O escopo de projeto deixa de sumir quando chega `renegotiation_id` (S9, `BE-190`, **D-01/D-16/D-29/D-76/D-100**) | `pub/renegotiations_controller.rb:23-24` escreve o filtro de projeto e, na linha seguinte, **reatribui** a relação: `@renegotiations = Renegotiation.where(id: params[:renegotiation_id])`. Qualquer sessão lia a renegociação de qualquer projeto passando o id na query string — **o maior risco do bloco** | `current_project!` no endpoint, filtro por id **dentro** de `for_project`, e id de outro projeto responde **404 idêntico** ao de id inexistente. 20 exemplos em `renegotiations_cross_project_spec.rb`, um por endpoint que aceita id, comparando corpo e status das duas respostas |
| RNG-S9-02 | **[observável]** O estado **"Inconsistente" volta a existir** (S9, `BE-209`, **D-45** + **D-49**) | `renegotiation.rb:118-123` escreve o estado e a **linha seguinte o sobrescreve em todos os ramos**. "Inconsistente" nunca chegava ao banco — e o `case` do filtro não tinha `when "empty"`, com um `return` que abortava a action: a tela dava **500**. **Dois dos quatro filtros eram inertes** | `Formulas.state_for`, uma expressão. A sobrescrita só acontece no ramo "liquidado", que era o que ela pretendia cobrir. ⚠ **É a única mudança de VALOR da fatia**: uma renegociação em aberto cujas parcelas não cobrem a dívida passa a aparecer como "Inconsistente" onde hoje aparece como "Pago". Travado pelo cenário C do golden test, que mostra os dois valores lado a lado |
| RNG-S9-03 | **[observável, perda de dado]** A exclusão de parcelas em lote deixa de reportar o contrário do que aconteceu (S9, `BE-202`, **D-51**) | `batch_destroy_installments!` devolve `inst.blank?` **depois** do `destroy_all` — a relação já esvaziada. O retorno era `true` (sucesso) quando **nada** havia para apagar e `false` (falha) quando **apagou**. O usuário via erro depois de uma remoção bem-sucedida, reexecutava, e **apagava parcelas a mais** | Ou o lote inteiro sai, ou nada sai — numa transação. Id desconhecido, id de outra renegociação e parcela **com pagamento** interrompem **antes** de qualquer `DELETE`, e a resposta diz qual. Lote vazio **não é sucesso** |
| RNG-S9-04 | **[observável]** Excluir renegociação passa a responder a verdade (S9, `BE-201`, **D-24**) | `errors.any? ? :ok : :ok` — **`:ok` nos dois ramos** — com o template de resposta **vazio**. A tela dizia "removido com sucesso", a lista recarregava e o registro voltava | 422 com a frase nomeando o vínculo ("tem 3 parcela(s) vinculado(s)"), e o registro continua existindo. Os **anexos** vão junto na remoção legítima: são documento DELA, não dado independente |
| RNG-S9-05 | **[observável]** O painel de consistência passa a mostrar o que calcula (S9, `BE-195`, **D-48**) | `geral_update_values` monta um hash `values` com sete valores formatados e **o descarta**: a resposta é `@renegotiation.to_json`. E o `to_json` estava sobrescrito com assinatura errada (`def to_json` sem `*args`), levantando `ArgumentError` quando o Rails o chamava com opções | `GET …/general_values` **recalcula** e devolve os sete valores + `unposted_value` + a flag de "remover todas". A serialização é `Grape::Entity` |
| RNG-S9-06 | **[observável]** "Parcelas vencidas" deixa de ser fotografia de 24 h (S9, `BE-207`/`OPS-190`, **D-54**) | Um cron diário (`CRONFacade.update_renegotiations_counters`, 00:01) percorria `where.not(state: LIQUIDADO)` chamando `update_values!`. Três defeitos juntos: até 24 h desatualizada; a renegociação **liquidada era excluída** e nunca mais reprocessada, mesmo voltando a ter saldo; e o `save` **sem bang** pulava em silêncio quem falhasse na validação | A fórmula é a mesma. O que muda é **quando** ela roda: `recalculate!` em toda alteração, e `live_overdue_for` apura na **consulta** da leitura — uma consulta para a página inteira. **O cron não é recriado**, e a ausência está documentada em `config/schedule.yml`, no arquivo onde alguém procuraria antes de recriá-lo |
| RNG-S9-07 | **[observável]** Criar previsão em lote deixa de dizer "criadas com sucesso" sem criar (S9, `BE-214`/`BE-216`, **D-52**) | Quatro caminhos para o mesmo desfecho: `repetitions.to_i` transformava `"abc"` em 0 e o laço não rodava; intervalo 0 gerava N parcelas na mesma data, todas menos a primeira inválidas; duplicata **dentro do próprio lote** não era conferida (só contra as existentes); e o retorno de `create` era **ignorado**. Em todos, resposta 200 | `create!` com bang, em transação, com as datas conferidas **antes**; não numérico, intervalo < 1 e tipo desconhecido respondem **422 com o motivo** |
| RNG-S9-08 | **[observável]** A cascata pagamento → parcela → renegociação roda **uma vez** (S9, `BE-220`/`BE-224`) | `after_save` no pagamento chamava a parcela, que chamava a renegociação; o controller ainda fazia `update` **e** `save`. A corrente inteira rodava em duplicidade, e cada elo gravava com `save` sem bang — falha no meio deixava agregado e parcela divergentes **sem erro nenhum** (**D-79**) | Cascata escrita, explícita, numa transação, com `save!`. Teste conta as chamadas de `recalculate!`: exatamente uma por criar/editar/excluir. Falha é **revertida e reportada** |
| RNG-S9-09 | **[observável]** O pagamento ganha teto no pendente e a data volta a ter sentido (S9, `BE-223`, **D-52** + **D-B12**) | Não havia checagem de valor em **camada nenhuma** — o "pendente" era só rótulo do seletor de previsão. E o campo de data era travado em "hoje": `days_late` era **sempre 0** e pagamento retroativo era impossível | Teto conferido contra o pendente (descontando os outros pagamentos), mora negativa recusada, e a **data é editável**. ⚠ **Muda o que hoje é aceito** e pode barrar lançamento que o operador fazia — registrado como tal na tarefa 2.24 |
| RNG-S9-10 | **[observável, segurança]** O anexo de renegociação deixa de ser um arquivo público (S9, `BE-226`/`BE-227`/`BE-229`, **D-82**, cinco defeitos) | (a) o binário ficava em `public/system/:attachment/:id/…`, servido como estático, **sem autenticação**; (b) o download respondia `disposition: 'inline'` com o content-type do **uploader** — XSS armazenado na mesma origem; (c) a validação de tipo estava desligada (`do_not_validate_attachment_file_type` **e** o spoof detector monkey-patchado para `false`); (d) "só o autor exclui" era regra **só visual**; (e) os limites (4 arquivos, 5 MB) só existiam no JavaScript da tela | ActiveStorage privado pelo motor único (S13); `Content-Disposition: attachment` **sempre** + `X-Content-Type-Options: nosniff`; tipo pelos **magic bytes** (`.svg`/`.html` renomeado para `.pdf` é rejeitado, com teste); exclusão só pelo autor **checada no servidor**; limites do catálogo, aplicados contra o **estado** e não contra o lote |
| RNG-S9-11 | **[observável]** Os limites de anexo passam a ser lidos por quem os aplica (S9, `BE-226`, **D-50**) | O JavaScript da tela lia `.lesson_attachment_content_wrapper` — seletor **de outro produto**, que não existe nesta página — e comparava com `NaN`. O atributo `data-locked` era **escrito no HTML e nunca lido**. Ou seja: o bloqueio não existia em lugar nenhum | `GET …/attachments/limits` devolve teto, consumo e quanto falta; o `FileDropzone` escreve a frase a partir desses números e **desabilita** com o motivo. Nem a tela nem o servidor têm "4" escrito neles |
| RNG-S9-12 | **[observável]** Renomear anexo **passa a existir** (S9, `BE-228`, **DEC-53**) | `pub/renegotiation_attachments_controller.rb:51` chama `update_attributes(renegotiation_params)`: o método `renegotiation_params` **não existe** naquele controller (só `renegotiation_attachment_params`) e `update_attributes` foi removido no Rails 6.1 — dois erros na mesma linha, com o `respond_to` inteiro comentado. `NameError` garantido, sem resposta | Endpoint, serviço e edição em linha na galeria. ⚠ **Contraria o `proposal.md` desta fatia**, que marcava `BE-228` como `dropped`: a **DEC-53 é posterior e manda implementar**, e a DEC vence a tarefa |
| RNG-S9-13 | **[observável]** A prévia da previsão e a gravação passam a produzir o **mesmo número** (S9, `FE-221`, contrato C2 / **D-09**) | Não havia prévia: o usuário digitava e só via o total depois de salvar | `POST …/installments/preview` chama `AggregateService.preview`, que compartilha `Renegotiations::Formulas` com `recalculate!`. Um teste manda o **mesmo rascunho** aos dois endpoints e compara campo a campo. **A tela não soma nada** — enquanto a resposta não chega, mostra o traço, nunca um número provisório |
| RNG-S9-14 | **[observável]** A confirmação de operação irreversível **para de se autodescartar** (S9, `FE-218`) | A caixa de confirmação da exclusão em lote sumia sozinha em **6 segundos**. Quem parasse para pensar perdia a caixa e recomeçava — e recomeçar, com o D-51, era como se apagavam parcelas a mais | `ConfirmDialog` (novo primitivo compartilhado) sem temporizador nenhum. **Medido renderizando**: aberta, continua aberta depois de 9 s. O rótulo do botão diz o que vai acontecer, e é prop obrigatória — no legado o texto de uma tela vazava na outra ("Excluir previsão" ao excluir um **pagamento**) |
| RNG-S9-15 | **[observável]** As abas do detalhe ganham endereço e histórico (S9, `FE-204`, **D-92**) | O estado de navegação vivia só em memória JS: recarregar voltava para a primeira aba, o endereço da aba não podia ser compartilhado, e o **Voltar do navegador saía do console inteiro** | `?tab=previsoes` na URL, lido e escrito por `useSearchParams`. Endereço inválido é normalizado uma vez, com `replace`, sem empilhar no histórico |
| RNG-S9-16 | **[observável]** O cartão de resumo deixa de parar de atualizar por causa de um campo (S9, `FE-206`/`FE-207`) | O legado montava o objeto de valores em JS e escrevia campo a campo; **um campo ausente na resposta levantava `TypeError` e parava a atualização dos demais** — três cartões novos e um velho, sem aviso | Cada cartão lê o seu campo com o traço como padrão. A atualização chega por **Action Cable** (`RenegotiationChannel`), nunca por polling, e **quando o canal cai a tela avisa** em vez de mostrar número velho como se fosse novo |
| RNG-S9-17 | **[observável]** A cor do lote de parcelas passa a **terminar** (S9, `OPS-196`) | `RenegotiationInstallment.color_defined` é um laço de rejeição **sem limite**: sorteia cor até achar uma não usada na renegociação, com uma consulta ao banco por tentativa. Quando o espaço de cores se aproxima do esgotamento o tempo esperado explode; no limite, **não termina** | Paleta finita (12 matizes), determinística e sem gem: a próxima cor é a primeira livre; esgotada a paleta, ela **recomeça**. Repetir cor é incômodo visual; travar a criação de parcela é perda de função |
| RNG-S9-18 | **[GANHO, não paridade]** A renegociação nasce com versão mobile própria (S9, **DEC-100**) | O legado é desktop-only | Lista vira `MobileCard` com quatro campos, ações de linha em folha no rodapé (`MobileRowActions`), seleção em lote com `MobileActionBar`, cartões com `MobileKPI`, carregando/vazio/erro com `MobileListState`. Conferido em 390×844: **zero rolagem horizontal na página**; a tabela de 13 colunas rola **dentro do próprio contêiner**. **QA do Phase 4: não procurar equivalente no legado** |
| RNG-S9-19 | **[correção achada renderizando]** A tabela de 13 colunas ganhou invólucro de rolagem própria (S9, `FE-190`) | Primeira versão: `DataTable` solto. Em 1440 px as quatro últimas colunas — inclusive "VP" e "Data próxima" — ficavam **cortadas**, sem barra de rolagem e sem sinal de que existiam. `tsc` limpo, `vitest` verde, e a coluna sumida | `overflow-x-auto` no contêiner, como o `CatalogScreen` já fazia. **Só apareceu abrindo a tela** — é a lição do checkpoint outra vez: portão verde prova que carrega, não que funciona |
| IND-S10-14 | **[GANHO, não paridade]** Os dois 409 de escopo de projeto viram **estado de tela**, não erro (S10, contribuição à biblioteca compartilhada) | `current_project!` passou a distinguir "pediu projeto invisível" (404) de "tem projetos e não escolheu" (409 `PROJECT_NOT_SELECTED`) e "não participa de nenhum" (409 `PROJECT_NONE_AVAILABLE`). Sem tratamento, os dois 409 caem no `ErrorState` genérico — "Não foi possível carregar" acusa o usuário de um erro que ele não cometeu, e o vazio genérico afirma que **não há indicador** quando o que falta é o projeto | `components/ProjectScopeNotice.tsx` + `codigoDeEscopoDeProjeto(erro)`, **membro da biblioteca** (Princípio 11): toda tela escopada por projeto — recebível, renegociação, limite, indicador — recebe exatamente esses dois códigos, e escrita em cada tela cada uma inventaria uma frase |
| DEMO-S20-08 | **[cobertura de recurso]** A tela de risco ganha a única linha vermelha da demonstração | O desenho prometia "dois controles em ~92%, **nenhum estoura**". Só que a S5 entregou o aviso de estouro (**NEW-005**, DEC-95), que lê `limite_disponivel < 0`: com a promessa cumprida ao pé da letra, o aviso era um recurso entregue que a apresentação **nunca conseguiria mostrar** | **Um** grupo acima de 100% (100,57% medido na tela), no cliente em recuperação, onde a história já o explica. Os dois em ~92% continuam. O alvo passou a valer para o **grupo** (cliente × portador × modalidade) porque é assim que a tela agrega — marcando um controle só, o que aparecia era a média diluída pelas outras empresas, 53% onde o razão dizia 92% |
| DEMO-S20-09 | **[correção de coerência, achada abrindo a tela]** Exposição **negativa** num limite | O painel mostrava `-6.322,61  (-1,41%)` de utilização. A soma final da operação estava certa; o meio do caminho, não: a liquidação de uma operação encerrada estava datada **antes** do lançamento de juros, e `balance_on` lê o último movimento ATÉ a data — entre as duas datas a operação ficava **credora** | A liquidação é o último lançamento da cadeia, sempre (`settle!` recebe piso na maior data já lançada). Travado por spec: `nenhum saldo intermediário fica positivo — a operação nunca é credora`. **Nenhum teste de soma pegaria isso**: a soma fecha; o que não fecha é a leitura por data |
| DEMO-S20-10 | **[para o QA, não é defeito da S20]** A coluna "LIMITES" da lista de Empresas mostra a soma de **todos** os dependentes | Com o seed dentro, a tela mostra 43/41/42/45 onde o banco tem 4/2/3/6 limites. `usage_counts` (`api/v1/companies.rb`) soma **todos** os `blocking_dependents` da empresa — limites **mais** operações — e a entity expõe esse total como `risk_controls_count`. Só ficou visível quando as empresas passaram a ter operação | Reportado para a **S4/S5**; não corrigido aqui (regra de fronteira). O dado no banco está certo, quem conta errado é a leitura |
| JOB-S13-20 | **[observável, recurso que não acontecia]** Projeto novo **volta a nascer com os padrões de disponibilidade** (S13 dívida, `OPS-465`) | O `SeedGlobalTemplatesJob` foi entregue pela S11 — correto, com progresso item a item, idempotente e com spec — e **nenhum lugar do repositório o enfileirava**. O comentário de `ProjectService#create` reservava a linha *"quando existir"*, o job passou a existir, e a linha nunca foi escrita. Resultado: **criar projeto não trazia nada do catálogo global**, e nada acusava — não há erro quando ninguém chama. No legado as duas tarefas saíam juntas do `after_create` (`../sfg/app/models/project.rb:74,82`) | Enfileirado em `ProjectService#create` **e** em `ProjectResetService#call` (o `WIPE_ORDER` apaga `ProjectAvailabilityTemplate`, então sem a segunda linha o projeto de treinamento voltava da limpeza sem painel; o legado também recriava, `project.rb:738`). **A lição é a do checkpoint outra vez, com uma variante nova: aqui nem o spec mentia — ele testava o job, que estava certo. O que faltava era o chamador, e chamador ausente não reprova em portão nenhum** |
| JOB-S13-21 | **[correção de comportamento]** Falha de um projeto na propagação **deixa de derrubar os outros** (S13, `OPS-466`, **D-80**) | `PropagateGlobalTemplateJob` era um job só com `projetos.find_each` dentro. O `rescue` do bloco publicava a falha e terminava em `raise` — a regra certa do `ApplicationJob` —, só que **dentro de um laço o `raise` aborta o `find_each`**: o primeiro projeto que falhasse deixava todos os seguintes sem o padrão, e a retentativa recomeçava do zero. Ao lado dessa linha estava escrito *"e o job segue"*. **O comentário prometia a garantia que o código não dava** | Coordenador + **um filho por projeto**. O coordenador conta, escreve o total e despacha; nenhuma escrita de domínio nele. **Medido rodando** (Sidekiq no ar, 3 projetos, conflito plantado em um): os outros dois receberam o padrão e o relatório fechou `completed: 2, failed: 1`. O contrato do payload de cabo **não mudou** — mesmo `job_id`, um evento por projeto |
| JOB-S13-22 | **[GANHO, não paridade]** Padrão travado por processo **morto** volta a ser utilizável sozinho (S13, melhoria de `OPS-469`/`OPS-470`) | O `TemplateLock.around` da S11 já garante o desbloqueio para **exceção** (`unlock!` em `ensure`), e isso fecha o D-05. O que ninguém cobria era o processo morrer **sem rodar `ensure` nenhum**: `SIGKILL` do OOM killer, container reciclado no meio do deploy, máquina caída. Aí o padrão fica `is_locked` com `job_state = 'running'` para sempre e a interface não oferece saída — o estado terminal do legado chegando por outra porta | `UnlockStaleTemplatesJob`, de 10 em 10 minutos: destrava **e escreve o motivo** (desde quando, por quantos minutos, qual era o limite), marca `failed` e **avisa a tela pelo cabo**. Prazo de 60 min por padrão, generoso de propósito — prazo curto destravaria operação em andamento, e duas escritas concorrentes na mesma subárvore são piores que o problema. **QA do Phase 4: não procurar equivalente no legado** |
| JOB-S13-23 | **[segurança]** A chave privada de DKIM sai do repositório e o boot para de depender dela (S13, `OPS-485`/`OPS-608`) | `../sfg/config/application.rb:110-114` fazia três coisas erradas de uma vez: lia a chave de `lib/dkim_private_key.pem` — **versionado no repositório**, ou seja, quem clonasse podia assinar e-mail como o domínio —, lia com `open()` **no boot** (arquivo ausente = a aplicação inteira não sobe, que é o que acontecia em toda máquina de desenvolvimento) e tinha domínio e seletor assados no código | `Sfg::DkimSigner`: domínio e seletor por ENV, chave em `DKIM_PRIVATE_KEY` **ou** no `Credential` encriptado (DEC-61, trocável por tela sem deploy), resolvida **na hora do envio**. Sem chave o envio é **degradado** — o e-mail sai sem assinatura e o aviso vai ao log uma vez por processo — em vez de o boot cair. Um exemplo de spec reprova qualquer `.pem` que volte a ser versionado |
| JOB-S13-24 | **[armadilha de framework, registrada]** `after_discard` do ActiveJob roda a **cada tentativa**, não só na última (S13) | A primeira versão do relatório do coordenador incrementava contadores dentro do `after_discard`. **Rodando de verdade**, com três projetos e um falhando, o relatório fechou `completed: 2, failed: 2` — soma 4. A causa: quando o job **não declara `retry_on`**, a retentativa do Sidekiq é opaca para o ActiveJob, que trata toda exceção não tratada como descarte e dispara os `after_discard_procs` **em toda tentativa**. Nenhum spec pegaria: com adapter `:inline` não há retentativa | O desfecho passou a ser gravado **por projeto**, num mapa, e os contadores são **derivados** dele. Registrar duas vezes o mesmo projeto sobrescreve a chave; e uma retentativa que **dá certo** troca `failed` por `completed`, então o relatório se corrige sozinho. **Vale para qualquer job desta base que conte alguma coisa no `after_discard`** — `DefaultMemberJob` e `LinkDefaultMembersJob` escapam porque declaram `retry_on`, o que faz o bloco rodar só na exaustão |
| JOB-S13-25 | **[para o QA, não é defeito da S13]** As 3 falhas que sobram na suíte inteira em 26/08/2026 são de **fronteira com a S6**, que está em voo | `bundle exec rspec` no banco da S13: **2086 exemplos, 3 falhas**. (a) `spec/lib/sfg/audit_trail_spec.rb:82` — o `Sfg::AuditTrail` já trocou a chave de `'Receivable'` para `'ReceivableEntry'` (a tabela é `receivable_entries`, a classe é `ReceivableEntry`) e o spec ainda exige `'Receivable'`; (b) `:112` — `ReceivableEntry`, `Charge` e `Receipt` foram declarados no catálogo e **não têm verbete em `config/locales/pt-BR.yml`** (rótulo e gênero), então a trilha os mostraria sem nome em português; (c) `spec/lib/demo/orchestrator_spec.rb:52` — o escritor de `receivable_entries` do seed de demonstração falha em `Wallet é obrigatório`, `Resource source` e `Status`: os campos obrigatórios do model novo da S6 chegaram depois do escritor da S20 | **Não corrigidas aqui — regra de fronteira**: o dono do `Sfg::AuditTrail` é S0/S19, o dos models é **S6** e o do escritor é **S20**, e as três estão sendo escritas agora. Registrado para que a próxima rodada de portão não as leia como regressão da S13. As fatias que a S13 toca (`spec/jobs`, `spec/config`, `spec/channels`, `spec/lib/sfg/dkim_signer_spec.rb`) e a suíte de `spec/models` + `spec/services` + `spec/requests` (**1655 exemplos**) estão em **zero falhas** |
| REC-S6-01 | **[o achado que sustenta a fatia]** O motor de cálculo do borderô foi conferido contra **produção**, não contra a leitura do código (S6, `BE-155`…`BE-180`, **D-114**) | O legado **não tem um único teste**. A tentação era extrair os valores esperados lendo `receivable_entry.rb:38-118` e recalculá-los à mão — o que só provaria que dois leitores concordam | O dump de 31/05/2025 traz **28.131 borderôs com os ~33 derivados como o legado os gravou**, ao longo de três anos de uso. O motor foi rodado contra as **28.099** linhas limpas × 33 colunas = **927.267 comparações**, com **zero** divergência. **131** dessas linhas entraram na suíte como golden, escolhidas para cobrir cada ramo de guarda, cada extremo e os quatro casos que caem exatamente no meio do arredondamento |
| REC-S6-02 | **[armadilha de linguagem, medida]** `BigDecimal ⊗ Float` mudou de comportamento entre o Ruby de produção e o desta base (S6) | O legado mistura `BigDecimal` (colunas `decimal`) com `Float` (colunas `float`) na **mesma expressão**. No **Ruby 2.6.1** de produção (DEC-03/DEC-11) a conversão usa `Float::DIG + 1` = **16 dígitos significativos**; no Ruby 3.4 passou a usar a representação decimal mais curta. Sem replicar isso sobravam **22 divergências em 927.267 comparações**, todas de último dígito | `Receivables::Calculator#bd`, aplicada em cada ponto onde o legado mistura os dois tipos. Com ela, zero. **Nenhum teste escrito de cabeça teria pegado isto** — só a comparação contra dado real, e em quantidade |
| REC-S6-03 | **[observável, corrupção de dado]** O `Infinity`/`NaN` do D-10 **não era hipótese**: são 30 borderôs em produção (S6, `BE-162`/`BE-166`/`BE-172`/`BE-176`/`BE-179`) | No legado o cálculo roda num `before_validation` — **antes** de qualquer validação —, e a única guarda existia no JavaScript da tela. Medido no dump: **30 linhas com `NaN` gravado** em coluna de dinheiro (`recompra`, `tarifas_desagio`, `valor_liquido`, `total_deducoes`, `vlr_liq_recebido`); num deles o `NaN` de uma tarifa contaminou o total, o líquido e os quatro percentuais | `Receivables::InputGuard` roda **antes** do cálculo, nos dois caminhos (prévia e gravação), e enumera os divisores concretos da cadeia; `InputGuard.result_errors` é a rede do outro lado, olhando o **resultado** em vez de uma lista que envelhece; e o model recusa `NaN` mesmo quando a escrita vem de rake ou ETL — que é por onde os 30 entraram, não pela tela. **O DEC-02 exclui o D-10 do "replicar" explicitamente** |
| REC-S6-04 | **[observável]** A prévia da tela e o valor gravado passam a ser **o mesmo número** (S6, `FE-171`, contrato C2 / **D-09**) | A conta existia em dois lugares: o `before_validation` do model e uma reimplementação **parcial** em JavaScript (`receivables/new/_body.js.erb:339-504`), que não calculava `taxa_desconto_nominal_*`, `custo_efetivo_com_float_*`, `multiplicador_*` nem os `*_percent`, e arredondava o total de tarifas de outro jeito | `Receivables::Calculator` é o único lugar; `POST /receivables/preview` e a gravação montam o **mesmo `Input`**. Um request spec envia o mesmo payload aos dois e compara **os 37 derivados campo a campo**; uma varredura de vitest reprova se qualquer marca da fórmula (`Math.pow`, as alíquotas, o expoente `0.0333…`) reaparecer num arquivo do front que fale de borderô. **Conferido também executando**: o painel reproduziu na tela os 37 valores do borderô 19086 de produção |
| REC-S6-05 | **[correção achada renderizando]** O formulário de borderô era **impossível de enviar** (S6, `FE-165`) | Faltava o campo **Fonte de recurso**, que o servidor exige (`resource_source_id`, preenchido em 28.131 de 28.131 linhas de produção). O Salvar respondia 422 e a tela mostrava a mensagem — mas o usuário não tinha como corrigir, porque o campo não existia. **`tsc` limpo, `rspec` verde, `vitest` verde**: nenhum portão pega um campo ausente | Campo acrescentado, mais um `GET /api/v1/resource_sources` de **leitura** (a tabela e a superfície de escrita são da **S8**; o `GET` nasceu aqui por dependência dura, com o dono escrito no arquivo). Só apareceu dirigindo o formulário pela tela — é a lição do checkpoint outra vez |
| REC-S6-06 | **[correção achada renderizando]** Duas linhas do painel de cálculo colapsavam na **mesma linha** (S6, `FE-171`) | O `Tooltip` da base tem `className` padrão `inline-block`. As linhas do painel que têm texto de ajuda ficavam **inline**, e "CET com float total 2,06%" grudava em "CET com float sem IOF —". Ilegível num painel de 37 números | `className="block w-full"` no gatilho. **Nenhum teste automático pegaria** — o DOM está correto, o que estava errado era o desenho. Foi a captura de tela |
| REC-S6-07 | **[GANHO, não paridade]** A lista de borderôs ganha rodapé com o total da **consulta**, não da página (S6, `BE-150`) | O legado não tinha total nenhum, e a lista vinha inteira do servidor: somar no cliente era possível porque as 28 mil linhas chegavam todas | `GET /receivables/summary` agrega no banco com os mesmos filtros, em **uma** consulta. **QA do Phase 4: não procurar equivalente no legado** |
| REC-S6-08 | **[para o QA, não é defeito]** Quatro borderôs de produção têm os `tarifas_*` **defasados** em relação às próprias tarifas (S6, `BE-155`) | Somando `receivable_taxes` por borderô e comparando com o denormalizado: **28.127 de 28.131 batem**. Os quatro que não batem (21608, 21871, 21872, 26246) são o **D-09 no dado** — no legado o recálculo do pai dependia de o JavaScript da tela chamar `update_and_save()`, e quem editasse tarifa sem salvar o borderô deixava o total velho | Vêm **como estão** na carga, marcados no fixture (`buckets_defasados`) e excluídos da comparação daqueles quatro campos. "Consertar" a soma apagaria a evidência do defeito |
| REC-S6-09 | **[correção de contrato]** Erro de validação de parâmetro do Grape voltava **500** nos endpoints financeiros (S6, `BE-150`/`BE-187`) | `Grape::Exceptions::ValidationErrors` é `StandardError`, e o `rescue_from StandardError` local (que existe para não vazar backtrace, F-3) o engolia — o handler local vence o do pai, e o de `api/v1/base.rb`, que já responde 400, nunca era alcançado. Uma data malformada ou um `state` fora do domínio respondiam "servidor quebrou" | `rescue_from Grape::Exceptions::ValidationErrors → 400` **antes** do handler genérico, nos dois endpoints. Confirmado reproduzindo: `POST /api/v1/receivables` com `date: 'nao-e-data'` dava 500, agora dá 400 |
| REC-S6-10 | **[GANHO, não paridade]** O borderô nasce com versão mobile própria (S6, **DEC-100**) | O legado é desktop-only | Lista vira `MobileCard` com quatro números, ações de linha em folha (`MobileRowActions`), paginação estreita (`MobilePagination`) e o formulário empilha o painel de cálculo abaixo dos campos. **Medido em 390×844, nos dois modos: zero rolagem horizontal em 24 renderizações** (6 telas × 2 modos × 2 larguras), zero erro de console e zero violação de CSP. **QA do Phase 4: não procurar equivalente no legado** |
| REC-S6-11 | **[para o QA, não é defeito da S6]** Os catálogos de produção divergem do que o mapa previa | Medido no dump: **12** carteiras (o mapa dizia 10, e não existem "Caução" nem "Domicílio"; existem Risco Sacado, Pré-faturamento, Boleto Escrow e Intercompany), **7** tipos de recebível (o mapa dizia 5, e o quinto é "Cartão", não "Cartão de crédito"), **18** tipos de movimentação (o mapa dizia 17 — falta "Regresso", de 25/03/2022) e **6** fontes de recurso (o mapa da S8 dizia 7, e não há "Defasagem" nem "Retenção"; há "13º salário") | Os seeds reproduzem **o que está em produção**, com o `legacy_id` de lá como chave natural — DEC-30: o sistema validado é o que **rodou**. A divergência com o mapa é do mapa |
| ABL-S1-01 | **[segurança, o coração da DEC-108]** As 6 abilities que voltaram passam a ser checadas **no servidor**, não no CSS | No legado o gate de cada uma vivia dentro de um `if` de view — `may_create_users` em `my_account/_body.html.erb:13`, `may_delete_users` em `users/detail/tabs/_tab_geral.html.erb:3`, `may_invite_users` em `registrations/index.html.erb:6`, `may_modify_public_entries` em `projects/detail/tabs/_tab_geral.html.erb:76`. O `if` escondia o botão e **a rota continuava aberta**: bastava chamar fora da tela (a família do **D-34**) | `require_permission!` no endpoint, com **403 `PERMISSION_REQUIRED`**, e `abilities_enforcement_spec.rb` provando os **dois** lados de cada uma. A DEC-30 manda replicar, mas a exceção de **segurança e autorização** manda corrigir: replicar um gate de CSS não é paridade, é portar a vulnerabilidade |
| ABL-S1-02 | **[observável]** Os dois `max_*` deixam de ser enfeite de barra de título e passam a **valer** | `max_users_amount` só aparecia em `registrations/index.html.erb:7`, como `"#{@users.size}/#{max}"` no cabeçalho da lista, e **nada** comparava; `max_invitations_amount` não tinha nem isso — só o default em `config/application.rb:71`. Um teto que ninguém aplica é um número que mente para o operador | `enforce_limit!` com **422 `LIMIT_EXCEEDED`**, dizendo o teto e a contagem atual. A contagem de `max_users_amount` é a mesma do legado (o total de contas do sistema, `Livetat::Auth::User.all`). ⚠ **Muda o que hoje é aceito**: um Admin que ultrapasse o teto do papel dele passa a receber 422 onde antes criava |
| ABL-S1-03 | **[divergência deliberada]** O **OG fica sem teto**, contra os 100/200 do seed da engine | `../sfg/engines/auth19/db/seeds.rb:25-27` dá ao OG `max_users_amount = 100` e `max_invitations_amount = 200`, enquanto `../sfg/db/seeds.rb:58-59` dá **9999** ao Admin. Enquanto ninguém aplicava, a contradição era inerte. Aplicando (ABL-S1-02), o **fornecedor** (DEC-18.1) ficaria com teto MENOR que o Admin do cliente — e a produção já tem **135 contas**, logo o OG nasceria bloqueado no primeiro `POST /users` enquanto o Admin seguiria criando | `limit_value = NULL` para o OG, que o catálogo e a tela mostram como "sem limite". Não é um número inventado: é a ausência de teto, visível e editável na tela. `0` continua significando "nenhum permitido", que é o que o legado dá ao Gerente e ao Colaborador |
| ABL-S1-04 | **[correção de dado / atribuição falsa]** A tela de Permissões deixa de atribuir ao usuário uma decisão que foi do orquestrador | A docstring de `PermissionsPage.tsx` citava *"DEC-18.6, decisão #6 do usuário"* para justificar o catálogo de uma linha. As duas metades eram falsas: a **DEC-18.6** é sobre `Membership.role` ser rótulo descritivo (outro assunto), e a **decisão #6** está registrada na seção *"decisões de baixo impacto que eu tomei sozinho"* — o usuário nunca a tomou. A justificativa dela também era falsa: afirmava que **nenhuma** das 16 era consultada, e isso valia para **10** | A docstring cita a **DEC-108**, diz o que estava errado antes e por quê. Mesma família do erro que queimou o número 104: **nenhuma decisão do orquestrador pode ser escrita como sendo do usuário** |
| ABL-S1-05 | **[GANHO, não paridade]** O convite passa a saber **quem convidou** | `login_codes` não guardava o emissor, então "teto de convites" não tinha o que contar. No legado o convite também não guardava — mas lá o teto também não era aplicado | `login_codes.invited_by_id`, nula, com índice parcial, preenchida só por `Auth::InviteService`. O teto conta **convites em aberto** (não usados e não expirados) do próprio ator: convite usado ou vencido libera a vaga. **QA do Phase 4: não procurar equivalente no legado** |
| ABL-S1-06 | **[para o QA, não é defeito]** `may_modify_public_entries` gateia a **adição** de membro, não a remoção | O rótulo do legado é *"Modificar dados em Módulos"* (`public_entities_alias = "Módulos"`, `config/application.rb:67`), o que sugeriria cobrir toda a escrita. Mas o **único** call site é a caixa «Adicionar Membro» (`projects/detail/tabs/_tab_geral.html.erb:76`); a remoção, três arquivos adiante, está atrás de `user_is_readonly` (`memberships/list/_widget.html.erb:23`) | Portado o **efeito**, não o rótulo: `POST /memberships` e `GET /memberships/candidates` (a mesma caixa) exigem a ability; `DELETE /memberships/:id` **não**. Travado por spec dos dois lados. Estender o gate à remoção inventaria uma restrição que nunca existiu |
| EST-S8-01 | **[o defeito que só apareceu executando]** Desmarcar um recibo do pacote respondia **500** — e o caminho nunca tinha sido percorrido (S8, correção em `Charges::BulkReceiptsService`) | `remove_unlisted!` destruía o recibo **primeiro** e só depois zerava `operation.receipt_id`. Como `risk_operations.receipt_id` (S6) e `structured_operations.receipt_id` (S8) têm **FK real** para `receipts`, o Postgres recusava: `PG::ForeignKeyViolation: update or delete on table "receipts" violates foreign key constraint on table "structured_operations"`. **Até a S8 o caminho era inalcançável**: sem o model `Remuneration`, o serviço parava no 422 que nomeava a fatia, e nenhum teste chegou a remover um recibo de verdade — o lado LIQ estava igualmente quebrado desde a S6, sem ninguém saber | Ordem invertida: solta o lado da operação, **depois** destrói o recibo, tudo na mesma transação. O motivo ficou escrito no método, porque a ordem parece arbitrária e não é. **Nenhum portão pegaria**: `ruby -c`, `zeitwerk:check` e a suíte inteira da S6 estavam verdes com o defeito dentro — o que faltava era um caminho que ninguém conseguia executar |
| EST-S8-02 | **[o model que não carregava]** `Remuneration` levantava `ArgumentError` na carga da classe (S8, auditoria do material não commitado) | O model fazia `include Auditable` sem estar declarado em `Sfg::AuditTrail::VERSIONED`, e o portão do **DEC-78 #1** levanta para model não declarado — que é o portão funcionando. Qualquer requisição que nomeasse a constante virava **500**. O material estava no disco, bem escrito e **não verificado**: nada tinha sido marcado, e o `db:migrate:status` mostrava a migration ainda `down`, com o `schema.rb` descrevendo três tabelas que não existiam no banco | Linha declarada com o motivo (a taxa multiplica todo o faturamento e o recibo a congela na emissão, então a trilha é a única prova da taxa anterior), mais o verbete pt-BR que o spec da trilha exige. `StructuredOperationType` e `ResourceSource` entraram em `EXCLUDED`, com motivo, para que a lista curta não seja lida como esquecimento |
| EST-S8-03 | **[armadilha de polimorfismo, achada escrevendo o teste]** Ler `operation_type` com classe desconhecida levantava `NameError` **dentro do `before_validation`** (S8, `BE-304`) | `operation_type` é o getter polimórfico do ActiveRecord: ele **constantiza** `operation_type_type`. Com `"Qualquer"` na coluna, a simples leitura dentro de `copy_title_from_operation_type` levanta `NameError: uninitialized constant Qualquer` **antes** de a validação de `inclusion` conseguir responder 422 — ou seja, **500 pela porta dos fundos**, exatamente o modo de falha que o BE-304 existe para eliminar | `rescue NameError` no callback, deixando a validação falar. O endpoint já barra pelo `values:` do Grape e o banco pelo `check_constraint`, mas **seed, console, rake e ETL não passam por nenhum dos dois** — o model precisa aguentar sozinho |
| EST-S8-04 | **[observável]** A **prévia** de recibo passa a mostrar o número que vai ser cobrado (S8, tarefa 3.2, `BE-305`) | O legado não arredonda em lugar nenhum: `operation_value * (fee.to_f / 100.0)` devolve o produto cru — para `99.999,99 × 7,77%` isso é `7769.9992229999990000001`, com o artefato do float na **décima** casa — e quem corta em 2 casas é o **cast da coluna** no INSERT. A tela de candidatos mostrava `31,48128` e o banco gravava `31,48` | `ROUND_HALF_UP`, 2 casas, declarada em `Charges::ReceiptGenerator::ROUNDING_SCALE`/`ROUNDING_MODE`, aplicada ao valor final. **Reproduz** o resultado do cast — os cinco goldens passam antes e depois —, e há exemplo travando `HALF_UP` contra `HALF_EVEN` (255,005 → 255,01), que é a diferença que quatro dos cinco goldens não veriam. Quanto o cliente paga deixa de depender de um efeito colateral do adapter do banco |
| EST-S8-05 | **[IMP-R2, observável]** As listas da unidade passam a **paginar de verdade** (S8, `BE-283`, `BE-284`, `BE-296`, `BE-300`) | Três listas diferentes, três jeitos de não paginar: `structured_operations` calculava o `X-Total-Count` **depois** do `limit!/offset!`, devolvendo no máximo o tamanho da página (toda a habilitação de paginação da tela dependia de um número truncado) e aceitava `l=999999`; `structured_operation_types` chamava `.limit().offset()` **sem `!`**, e no Grape do legado eles eram silenciosamente descartados; `remunerations` **não tinha nenhum dos três** — nem paginação, nem `ORDER BY`, nem total | Kaminari + envelope de cabeçalho (DEC-62) nas três, com o total **sem limite** e teto de `per_page`. `Charges::ReceiptGenerator.candidates` também pagina (`Kaminari.paginate_array`, porque os candidatos vêm de **duas** tabelas e não há `ORDER BY` que atravesse as duas) — com `page: nil` devolvendo tudo, que é o que a gravação em lote precisa: otimização não pode mudar linha devolvida |
| EST-S8-06 | **[IMP-R4, observável]** Operação com data **nula** passa a aparecer quando não há filtro de período (S8, `OPS-283`) | Sem `from`/`to`, o legado mandava `DateTime.dinosaurs` e `DateTime.mars` — um monkey-patch de ±2000 anos em `config/initializers/date_time.rb` — como limites do range. O predicado continuava sendo `DATE(due_date) >= DATE(?) AND DATE(issue_date) <= DATE(?)`, e `DATE(NULL)` é NULL: a operação sem data era **excluída em silêncio**, inclusive na listagem sem filtro nenhum | Sem `from`/`to`, o predicado **não é aplicado**. Efeito idêntico para dados com data preenchida — travado por spec nos dois casos, com e sem filtro |
| EST-S8-07 | **[segurança, `FE-309`]** Os cinco controllers da unidade deixam de ser alcançáveis **sem login** (S8) | Todos herdam `requires_current_user? == false`: `search`, `create`, `update` e `destroy` **não exigiam login pelo `before_action`**. Funcionavam só porque `current_user` precisava existir para `current_user.default_project_id` não quebrar — o que é acidente de implementação, não autorização | `authenticate_user!` + `authorize!` da `Authorization::Matrix` em todo endpoint, mais o gate global de `user_is_readonly`. Travado por spec dos **dois lados** (C3): o Colaborador **escreve** operação estruturada (é o que a matriz diz) e **não escreve** no catálogo de tipos. ⚠ **Escrevi o primeiro exemplo esperando 403 para o Colaborador e recebi 201 — a expectativa é que estava errada, não o servidor** |
| EST-S8-08 | **[segurança, duas IDORs]** O id por parâmetro deixa de descartar o escopo de projeto (S8, `BE-280`/`BE-286`/`BE-301`/`BE-302`) | `structured_operations#search` **reatribuía** a relation quando recebia `structured_operation_id` (`structured_operations_controller.rb:25`), e o filtro de projeto desaparecia: qualquer autenticado lia operação de qualquer projeto pela query string. O `update` buscava por id **sem escopo**. Em `remunerations`, o `project_id` vinha de `hidden_field` e não era conferido — dava para **criar a taxa no projeto de outro cliente**, e taxa é preço | `ProjectScopedService` nas duas: o filtro por id entra **dentro** do escopo, id alheio devolve vazio na lista e **404** no detalhe (nunca 403, que confirmaria a existência do registro alheio), e `project_id` do corpo **nem é declarado** nos `params do` |
| EST-S8-09 | **[observável]** Exclusão bloqueada responde **422 de verdade** nos três recursos da unidade (S8, `BE-287`/`BE-299`/`BE-729`) | O ternário degenerado `errors.any? ? :ok : :ok` fazia a exclusão bloqueada responder **200**: o front tratava como sucesso, recarregava a lista e o registro continuava lá. Em `resource_sources` é o caso que mais dói, porque a guarda **realmente dispara** — a coluna está preenchida em **28.131 de 28.131** borderôs de produção | 422 com a dependência nomeada nos três. `Remuneration` ganhou `dependent: :restrict_with_error` (o legado tinha `has_many :receipts` **sem `dependent:`**, deixando **recibo órfão** — e como `Receipt belongs_to :remuneration` é obrigatório, qualquer save posterior daquele recibo falhava, meses depois) |
| EST-S8-10 | **[observável]** Tipo de operação estruturada desativado volta a ser **reativável pela tela** (S8, `BE-296`) | A listagem do legado era sempre `.active` (com o SQL literal `where('is_active = 1 ')`, espaço sobrando incluído). Desativar um tipo o fazia sumir da tela de administração, e **não havia como reativá-lo pela UI** — só por SQL | O filtro de ativo passa a ser **opcional**: `?active=true` filtra, a ausência do parâmetro mostra tudo. O `scope :active` do `GlobalCatalog` é bind + boolean |
| EST-S8-11 | **[observável]** Imutabilidade prometida pela UI passa a valer no servidor — e **com mensagem** (S8, `BE-298`/`BE-728`) | `title` e `integration_key` são `readonly` no formulário do legado e mesmo assim viajavam no `permit`. Alterar a chave a qualquer momento quebra integração externa em silêncio | Os dois campos são **declarados** nos `params do` de propósito, para serem **recusados com 422 e o motivo**. Deixá-los fora faria o Grape descartá-los em silêncio: o cliente mandaria um título novo, receberia 200 e nada teria mudado — que é pior do que recusar, porque parece ter funcionado |
| EST-S8-12 | **[observável]** Os textos de ajuda param de ler o disco a cada campo renderizado (S8, `OPS-284`) | O helper do legado fazia `YAML.load_file` **uma vez por campo** — 13 leituras de disco por abertura do formulário — e, se o arquivo sumisse do deploy, o formulário inteiro respondia **500** | `Structured::HelpTexts` sobre `Rails.cache`, reusando o mecanismo que a S6 entregou. Arquivo ausente devolve `{}`. As **13** chaves do legado têm todas o **mesmo** placeholder ("Só um teste de informações do campo…"), conferido linha a linha: o mecanismo vem, o conteúdo nasce vazio (Q-R9) e **campo sem chave não exibe indicador de ajuda** |
| EST-S8-13 | **[dead code registrado, não corrigido]** `validates :operation_value, presence: true` **nunca dispara** — nem no legado (S8, `BE-293`) | A validação existe em `structured_operation.rb:19`, e a coluna nasce `default: 0` na migration de 2022 (`20220701125757:11`). Um registro novo já chega com `0`, a presença passa, e **capital zero é aceito** | Replicados **os dois lados** — a validação e o default —, com um exemplo de spec dizendo que a validação é dead code. Sem ele, a próxima pessoa "descobre" que o capital pode ser zero e acha que a migração perdeu uma regra. Não perdeu: a regra nunca existiu de fato |
| EST-S8-14 | **[para o QA, não é defeito da S8]** O `db:migrate` da S8 arrastou junto o `schema.rb` da migration da S1 (DEC-108) | As duas fatias estão em voo na mesma árvore. `20260826234000_as_sete_abilities_com_efeito_real_voltam.rb` estava pendente neste banco quando rodei a migration da S8, e o dump seguinte trouxe as colunas dela (`login_codes.invited_by_id`, `permissions.kind`, `default_limit_value`, `limit_value`) | Conferido linha a linha: **nenhuma remoção** além da versão do esquema, e nenhuma feature do Phase 1b reintroduzida. As linhas são da S1 e estão corretas; ficam neste commit porque `schema.rb` é um arquivo só. Registrado para que a próxima leitura de diff não as tome por terceiro |
| EST-S8-15 | **[correção achada RENDERIZANDO]** A tabela de operações estruturadas **estourava o container** e escondia as ações da linha (S8, `FE-280`) | São **dez** colunas (as mesmas do legado) e os valores usam `font-numeric` (Fira Mono), mais larga que a de texto. Declarar `width` na coluna **não basta**: a `Table` da base usa `table-layout: auto`, onde a largura é uma sugestão e o conteúdo manda — "Cooperativa de Crédito Ipiranga" empurrava a tabela para fora do container, "Portador" saía cortado à esquerda e a coluna de **AÇÕES ficava fora da tela**. `tsc --noEmit` em 0 erro o tempo todo | O limite passou a ser `max-w` no **conteúdo** (`<span className="block max-w-[7rem] truncate">`), que é o que a truncagem precisa para valer com `table-layout: auto`, mais larguras remedidas coluna a coluna. Conferido por captura em 1440×900 nos dois modos: as dez colunas cabem e os três gatilhos de ação aparecem. **Nenhum portão pega isto** — é a lição do `Sheet` sem `SheetContent` outra vez |
| EST-S8-16 | **[correção achada RENDERIZANDO]** "Saldo" quebrava o **sinal negativo** em duas linhas (S8, `FE-289` / DEC-01) | `original_balance` e `balance` chegam negativos por convenção do legado (DEC-01, Q-R20) e a tela os exibe assim. Com a largura apertada, `-R$ 50.000,00` quebrava: o `-` ficava sozinho na linha de cima e o valor embaixo. Um traço solto sobre um número em coluna de dinheiro lê-se como "sem valor", que é o oposto do que a DEC-01 manda preservar | `whitespace-nowrap` nas células monetárias e 9,5rem na coluna. O sinal é dado, não decoração: preservá-lo legível é parte de replicar a convenção |
| EST-S8-17 | **[correção achada RENDERIZANDO]** A taxa do candidato a recibo saía com **ponto decimal de JavaScript** (S8/S6, `FE-184`) | A célula "Valor da remuneração" mostra a `fee` como rótulo ao lado do valor, e ela saía de `Number(c.fee)` cru: `R$ 5.100,01 (2.55%)` — ponto no meio de uma coluna em reais formatada em pt-BR. É exatamente o que o **OPS-289** existe para eliminar, e passou porque `tsc` não tem opinião sobre separador decimal | `Intl.NumberFormat` pt-BR com **4 casas** — a escala real de `remunerations.value` (`decimal(7,4)`). Com duas casas, `0,6255%` viraria `0,63%` na tela e o operador não fecharia a conta com o valor ao lado |
| EST-S8-18 | **[GANHO, não paridade]** Trocar a empresa para outro projeto passa a **perguntar** antes de mover a operação (S8, `BE-291`, lado da tela) | `project_id` é derivado de `company.project_id` em **todo** save. No legado bastava trocar um select para a operação mudar de tenant, levando junto o vínculo com a remuneração e o recibo, que ficavam para trás em outro projeto — sem aviso nenhum | O servidor recusa com `422` + `code: PROJECT_CHANGE_REQUIRES_CONFIRMATION`; a tela lê o **código** (não o texto), abre confirmação destrutiva com a frase do servidor e só então reenvia com `confirm_project_change: true`. **QA do Phase 4: não procurar equivalente no legado** |
| EST-S8-19 | **[resíduo achado no fechamento da paridade]** `resource_kinds` sai da matriz de autorização — era o **último** lugar vivo de uma entidade descartada (S8/S6, `13.3` / **DEC-110**) | O portão **T-D7** foi respondido pelo dump (**0 linhas** na tabela, **0 de 28.131** `receivable_entries` com `resource_kind_id`) e os 10 IDs da família viraram `dropped`. A tabela nunca foi criada, o model não existe, não há endpoint nem item de menu — mas `backend/app/services/authorization/matrix.rb:91` ainda declarava `'resource_kinds' => %w[CRUD CRUD CRUD R]`, e a tela de **PERMISSÕES** lê a matriz: ela publicava permissão configurável para algo **inalcançável**. A própria nota do `DB-286` no razão já sinalizava o resíduo, com dono "S6/S8" | A linha saiu, e **no lugar dela ficou o motivo escrito** (a mesma forma usada no `Seeds::Reference::Runner` e no `consoleNavigation.tsx`), para que ninguém a recrie por simetria com `resource_sources`. `spec/services/authorization` + `permissions_spec.rb`: **32 exemplos, 0 falhas** depois da remoção. O outro resíduo apontado na mesma nota — o conversor no `load_order.yml` — já estava em `do_not_migrate` |
| EST-S8-20 | **[a prova que faltava para o DEC-02]** O `ROUND_HALF_UP` explícito foi conferido contra o **cast do próprio Postgres**, golden a golden (S6, `2.35`/`4.27`, `BE-188`) | A S8 tornou a política de arredondamento **explícita** — antes quem cortava em 2 casas era o cast de `decimal(15,2)` no INSERT. Trocar um efeito colateral do adapter por uma regra escrita só é seguro se os dois produzirem **o mesmo número**, e isso ninguém tinha medido: os goldens comparavam o resultado consigo mesmo | `SELECT CAST(<produto cru> AS numeric(15,2))` mandado ao banco para os 5 goldens, e comparado com `ReceiptGenerator.round_value`: **5 de 5 iguais**. É a única conferência contra um sistema externo possível nesta família — e continua **não** sendo oráculo de produção, porque prova que a conta é a mesma, não que o número esteja certo para o negócio (DEC-103b, tarefa 13.5) |
| EST-S8-19 | **[reuso, não componente novo]** A barra de ação do formulário vira **membro da biblioteca** e o borderô passa a consumi-la (S8, `FE-295`) | O legado não tem botão "Salvar" em tela nenhuma de cadastro: cada `change` empilhava uma ação anônima em `dashBottomHolder.getData().stack`, e o `handle.js.erb` **removia a ação da pilha sem mensagem** assim que um obrigatório ficasse vazio. O usuário perdia a possibilidade de salvar e não tinha como descobrir por quê. A S6 já tinha resolvido isso **dentro** do formulário de borderô, inline | `DS/FormActionBar.tsx`, que **exige** a lista de pendências como prop — não existe caminho em que ela desabilite o botão em silêncio. `ReceivableFormPage` (S6) foi convertido para consumi-la no mesmo commit, para o componente ser reuso e não uma segunda implementação |
| EST-S8-20 | **[para o QA, não é defeito]** Existem **dois** mecanismos de texto de ajuda para o mesmo formulário, e a tela usa o da DEC-88 | `Structured::HelpTexts` (`OPS-284`, `GET /structured_operations/help_texts`) lê `config/structured_operations_help_inputs.yml` e nasce **vazio** por decisão da **Q-R9**. A **DEC-88**, posterior, mandou **escrever** os 91 textos, e eles vivem em `db/seed_assets/structured_operations_help_inputs.yml`, servidos por `Help::FieldHelp` (`GET /field_help`, escopo `structured_operations`, já com as 13 chaves preenchidas) | A tela usa o `<FieldHelp scope="structured_operations">` da DEC-88, que é a decisão mais recente e a que tem conteúdo. O mecanismo do `OPS-284` fica **portado e sem consumidor de tela** — registrado aqui em vez de removido (Princípio 6b); a chave `is_on_variable`, marcada `TODO:`, corretamente **não exibe ícone** |
| PRJ-S4-16 | **[bloqueio fóssil derrubado]** As 9 linhas "BLOQUEADO por S10" do razão eram **falsas há semanas** (S4, `BE-106`..`BE-109`, `FE-102`..`FE-104`, `DB-092`) | O razão dizia *"a tabela `indicators` nao existe"*. Ela existe desde `db/migrate/20260826210000_create_indicators.rb`, a **S10 fechou 80/80**, e o endpoint que a S4 pedia como `project_indicator_connections` foi entregue por ela com o nome `indicator_connections` — **mesmo controller legado** (`Pub::ProjectIndicatorConnectionsController`), mesmos defeitos corrigidos. Ninguém re-conferiu porque a nota do razão não tem data de validade | O contrato da S4 foi **verificado executando** contra o servidor de dev, não relido: `GET` 200 sem o específico de outro projeto; lote 200/422/422/200-idempotente; `DELETE` de global **422**, de outro projeto **404**, malformado **404**, do específico **200** com exclusão lógica. `spec/requests/api/v1/indicator_connections_spec.rb` (13 exemplos) trava o contrato **da S4**, não a implementação da S10 |
| PRJ-S4-17 | **[o campo nulo deixa de governar a interface]** `indicators.scope` vira **coluna**, com CHECK (S4, `DB-092`) | `project_id IS NULL` decidia "global" em **quatro** camadas — `Indicator#global?`, a entity (`scope` derivado na serialização), `ConnectionService.connectable` e a linha da tela (`indicador.scope === 'global'`). O front **já falava** `scope: 'global' \| 'project'`; o banco não. Três custos reais: nada impedia um `UPDATE` que zerasse `project_id` e promovesse um específico a global em silêncio, levando o histórico junto; filtrar globais era `IS NULL` em vez de igualdade indexável; e o contrato ficava implícito para quem chegasse depois | Coluna `scope` (`global`/`project`), **`CHECK` que a amarra a `project_id`** — sem ele a coluna seria só mais um campo para sair de sincronia —, índice `(scope, title)` e `before_validation` que a deriva em **todo** save (promover/rebaixar por `update(project_id:)` esbarraria no CHECK e viraria 500; derivando, vira o resultado certo). Backfill num `UPDATE`, e é a **última** leitura em que `IS NULL` decide escopo |
| PRJ-S4-18 | **[o defeito é replicado de propósito]** O carimbo de `has_safegold_management` volta às **6 tabelas**, com a inconsistência (S4, `DB-051`, `DB-090`, `DEC-112`) | O razão marcava os dois como `blocked` por *"Q-02 SEM RESPOSTA"*. A **Q-02 é a matriz de autorização**, fechada pela DEC-08; a pergunta certa é a **Q-17** dos mapas (F-10 / P-019), que o **DEC-30 destravou** — opção **(b), manter o carimbo**. Enquanto isso, quatro models (`Company`, `AvailabilityEntry`, `RiskControl`, `RiskEntry`) tinham sido escritos para **derivar** a marca na leitura, com specs afirmando que a coluna *não existia* | As quatro colunas criadas (`receivable_entries` e `renegotiations` já as tinham), `before_validation` **sem `on:`** em todas — como o legado, que recopia em todo save, não só na criação — e o `update_all` de ressincronização **só em `companies`** (`project.rb:298-303`). As outras cinco ficam com o carimbo velho **para sempre**: é o **D-30**, e há spec provando que o limite de risco não é ressincronizado. O que torna isso seguro está medido: **zero leitor interno** (nenhum `where`, nenhum scope, nenhum `if` de regra) — o consumidor é externo (BI/planilha) e quer a foto do momento. `Renegotiation` também perdeu o `new_record?` que a fazia divergir do legado numa edição posterior |
| PRJ-S4-19 | **[correção achada RENDERIZANDO]** O interruptor do indicador **mentia** para o perfil somente-leitura (S4, `FE-103`) | O `FE-323` manda deixar o controle **visível** e explicar a restrição no clique — e a implementação fazia isso com `e.preventDefault()` no `onClick` do Radix Switch. `preventDefault` impede o Radix de chamar `onCheckedChange`, mas **não desfaz o `aria-checked` que ele já escreveu no DOM**; e como `checked` (controlado) não muda, o React nunca re-renderiza para reverter. Resultado medido em `/indicator-connections` com `user_is_readonly`: o interruptor ia de `false` a `true`, **zero requisição, zero aviso**. É o pior estado possível numa tela de permissão — parece que funcionou. `tsc --noEmit` em 0 erro, suíte verde | `Switch` passa a ser `disabled` quando somente-leitura (o Radix não toca em nada) e o aviso vem do `span` que o envolve, que é quem recebe o clique. Medido depois: estado **não muda** (`true → true`) e o toast *"Você não possui permissão para alterar o estado do indicador."* aparece. ⚠ **Lição de método:** minha primeira medição pegou o `button[role="switch"]` errado — o primeiro da página é de outro componente do console. Seletor por `aria-label` (`*="onectar"`), não por `.first()` |
| PRJ-S4-20 | **[perda de dado que nenhum portão pegaria]** O ETL apagaria o carimbo histórico das 6 tabelas ao carregar o dump (S4, `DEC-112`, achado ao fechar `DB-090`) | `Sfg::Etl::Converters::Base#write!` grava por `record.save!` — que dispara o `before_validation` de carimbo. Ou seja: cada linha carregada do dump teria `has_safegold_management` **recopiado do valor de hoje do projeto**, apagando exatamente a inconsistência que a DEC-112 mandou preservar. Silencioso, sem erro, sem linha de log: **28.131 recebíveis** e **642.447 posições de risco** (a maior tabela do dump) passariam a mentir sobre o próprio passado, e o consumidor externo (BI/planilha do cliente) — o **único** leitor da marca — é justamente quem quer o histórico. O defeito já existia para `receivable_entries` desde a S6; criar as outras quatro colunas o multiplicaria por seis | `app/models/concerns/safegold_stamped.rb`: um `before_validation` único, com a origem declarada (`safegold_stamp_source :project` / `:company` — `RiskControl` e `RiskEntry` copiam da **empresa**, e a cadeia importa), mais `preserve_safegold_stamp`, ligado **só** pelo `write!` do ETL e **só quando a origem trouxe o valor**. Os quatro converters (`companies`, `availability_entries`, `risk_controls`, `risk_entries`) passam a ler a coluna do dump e a declará-la em `booleans`, para o `scan` conferir anomalias. `spec/models/safegold_stamped_spec.rb` trava os dois lados: com a chave o valor da origem sobrevive, sem ela o callback vence |
| GRA-S15-01 | **[a premissa do DEC-10 estava errada, e vale registrar]** Não havia gráfico a migrar (S15, `NEW-001`) | O **DEC-10** mandava "usar as libs de gráfico do ai9", supondo haver um gráfico legado a portar. A medição do Phase 2 mostrou que não: `Doughnut` é importado e exposto como global em `../sfg/app/frontend/vendor/js/index.js.erb:31,37` e **nenhuma view o instancia** — grep recursivo em `app/views/`, `app/frontend/js/`, `*.erb`, `*.js`, `*.scss` devolve **zero** ocorrências fora de `vendor/`. `OPS-746` preserva a mesma prova pelo lado dos shims de Webpacker | O DEC-10 continua valendo onde tem alcance real (`vendor/dialog` → `ui/dialog.tsx`, moeda → `Intl`/`date-fns`). O gráfico entra como **feature nova** por decisão posterior do usuário (DEC-21), o que **supera** a nota de escopo do DEC-09 em `openspec/specs/indicators/spec.md` em vez de contrariá-la por engano. As duas coisas continuam verdadeiras ao mesmo tempo: não existe no legado, e passa a existir no ai9 |
| GRA-S15-02 | **[correção achada RENDERIZANDO]** O rótulo do eixo Y saía **cortado** e o tooltip imprimia o número **cru** (S15, `NEW-001`/`NEW-002`) | `RechartsLine` da base imprime `{val}` direto no tooltip: um total de borderô aparecia como `605602.54` — ponto decimal de JavaScript, sem separador de milhar e sem `R$`, na frente do cliente. E a largura do `YAxis` é a padrão do Recharts (60 px), calculada sobre nada: com sete dígitos o eixo mostrava `00000` e `0`, com o começo do número comido pela borda da área de plotagem. `RechartsBar` erra ao contrário — crava `R$ ` no prefixo, então série que não é dinheiro sai com símbolo de moeda. Nenhum dos dois aceita prop de formatação: `{ labels, values }` é a assinatura inteira. **`tsc --noEmit` em 0 erro o tempo todo** | Os três arquivos de `components/charts/` da base **não foram tocados** (Princípio 6b, tarefa 3.4 da fatia) — há varredura em `dashboard.test.tsx` travando a assinatura dos dois. Nasceram **membros novos** da biblioteca: `SeriesLineChart` (largura do eixo calculada a partir do rótulo mais largo que a série realmente produz, com folga para o topo "bonito" que o Recharts escolhe), `CategoryBarChart` e `LimitMeters`, com `chartFormat.ts` decidindo num lugar só que **valor exato** vai ao tooltip e à tabela e **notação compacta** ao eixo (`R$ 1,6 mi`). Reusa `formatMoney` (FE-431); `formatMoneyCompact` entrou no **mesmo** arquivo da base, não num segundo helper. Limitações registradas em `UF-S15-01` |
| GRA-S15-03 | **[o ouro da marca NÃO pode carregar dado — medido]** A paleta de gráfico da fatia sai de `--info`, não de `--primary` (S15) | Medido com o validador do skill `dataviz` (`scripts/validate_palette.js`), superfícies `#ffffff` (claro) e `#20201d` (escuro): `--primary` (`#ffc105`, o ouro Safegold) dá **1,63:1** sobre o card claro e `--brand-gold-deep` (`#eb9500`) dá **2,38:1** — o mínimo para uma marca de dado é **3:1**. A paleta categórica de `charts/theme.ts` reprova em quatro das cinco verificações nos **dois** modos (faixa de luminosidade, piso de croma, piso de visão normal, contraste). Não é defeito da marca: ouro é cor de **ação**, e continua sendo a cor do botão primário | `components/charts/chartTokens.ts` registra a medição e os tokens de dado: `--info` para a medida, `--success`/`--negative` **só** quando a cor significa estado (e sempre com ícone e rótulo junto, nunca cor sozinha). Todos os gráficos da fatia são de **uma medida só**, onde a verificação categórica não se aplica. `theme.ts` não foi tocado; o achado é `UF-S15-02` |
| GRA-S15-04 | **[correção achada RENDERIZANDO]** O valor do cartão de KPI era **cortado** pela borda (S15, `NEW-002`) | O `KpiCard` da base usa `text-4xl` (36 px) com `font-numeric` (Fira Mono, ~18 px por dígito) e **não encolhe nem trunca**. Com quatro cartões numa linha em 1440×900 o card tem ~316 px úteis, e `R$ 174.493.854,98` (17 caracteres) pede ~354 px: o último dígito ficava **fora da borda**. Num sistema de crédito isso não é detalhe de layout — é um número errado na tela, e aconteceu **duas vezes** (na faixa de quatro e depois na metade da tela) | Hierarquia em vez de quatro iguais: o **Total operado** ocupa a largura inteira com o corpo grande (onde 17 dígitos cabem com folga) e leva o contorno de marca (`card-highlight`); os outros três descem para `text-2xl` numa fileira. O rebaixamento é por **seletor de descendente** no elo que envolve o cartão (`[&_.text-4xl]:text-2xl`), que tem especificidade maior sem `!important` — **nada é editado no componente compartilhado**. Flag em `UF-S15-03` |
| GRA-S15-05 | **[correção achada RENDERIZANDO]** Dois idiomas para a mesma grandeza, lado a lado (S15) | O painel "Consumo de limite por tipo" imprimia `51.76%` (o texto pronto de `Money.percent`, que usa `%.2f`) enquanto o painel vizinho, da DEC-116, imprimia `109,0%` — **na mesma tela**. É a família do `OPS-289`/`FE-184`: formatação decidida em dois lugares | `localizePercentLabel` em `lib/utils/number.ts` troca **só o separador**, preservando os dígitos que o domínio decidiu (inclusive os arredondamentos que a DEC-01 manda manter). Converter para número e reformatar introduziria uma segunda decisão de arredondamento sobre um valor já decidido — aqui não há aritmética, é ortografia. A porcentagem da DEC-116 sai do domínio como **número** e é formatada por `formatPercent` |
| GRA-S15-06 | **[correção achada RENDERIZANDO]** A linha do limite repetia a si mesma, e duas linhas dividiam a mesma chave (S15, DEC-116) | A lista "prestes a estourar" mostrava `FIDC Aurora Crédito · FIDC Aurora Crédito`: o título do limite é **derivado do portador** (`RiskControl#derive_title_and_project`), então a segunda metade da linha repetia a primeira. E o React avisava no console — `two children with the same key` —, porque a chave era o rótulo e dois limites do mesmo portador têm o mesmo título | A segunda linha passa a ser o **tipo de limite** ("Fomento", "Auto Liquidável"), que é o que diferencia dois limites do mesmo portador. A chave passa a ser o `id` do limite. Os avisos de console do `browser.js` foram lidos: é justamente o que a README da ferramenta diz que some do relatório quando ninguém olha |
| GRA-S15-07 | **[texto errado achado RENDERIZANDO]** "O resumo do projeto **SÃO** de um projeto por vez" (S15) | O `ProjectScopeState` monta a frase no **plural** ("… são de um projeto por vez") a partir do `recurso` que a tela passa. Com `recurso="o resumo do projeto"` a concordância quebrava. Nenhum type-check tem opinião sobre concordância verbal | `recurso="os números do resumo"`. Registrado porque é uma armadilha do componente compartilhado que a próxima tela vai encontrar: o `recurso` **tem** de ser um sintagma plural, e isso não está dito na assinatura |
| GRA-S15-08 | **[ferramenta]** `browser.js` ganhou `--viewport`, e foi assim que o telefone virou verificável (S15, DEC-100) | A ferramenta abria sempre em 1440×900. Sem outro tamanho, "conferido em 390×844" não passava de afirmação — e a passada anterior de mobile achou **35 destinos inalcançáveis** justamente porque ninguém conseguia olhar a tela estreita | `--viewport=<L>x<A>` (padrão `1440x900`), documentado no `README.md` da pasta. Altura grande (`390x2200`) também serve para capturar a página inteira quando o app rola num contêiner interno, onde `fullPage` não alcança |
| GRA-S15-09 | **[achado de bancada]** `src/test/setup.ts` não registra o `cleanup()` do testing-library (S15) | A tela de um exemplo continua montada durante o exemplo seguinte, com a consulta dela viva. O sintoma **não aponta para a causa**: a rejeição do exemplo seguinte aparece como *rejeição sem dono*, e o teste que prova o tratamento do erro reprova por causa disso. Rodar só aquele exemplo com `-t` **reproduz** o problema (o `beforeEach` continua rodando), o que despista ainda mais | `cleanup()` explícito no `beforeEach` dos dois arquivos de teste da fatia. O conserto certo é `afterEach(cleanup)` no setup compartilhado — `UF-S15-04`, porque alcança a suíte inteira e hoje toda tela herda o mesmo modo de falha |
| GRA-S15-10 | **[acabamento, a tela é a primeira impressão comercial]** A tela inicial passa a ter **duas zonas nomeadas** e o escopo escrito na primeira linha (S15, `NEW-002`) | Sete blocos soltos obrigavam a pessoa a descobrir sozinha onde termina uma leitura e começa a outra — e numa primeira impressão ninguém descobre, apenas passa os olhos. E o cabeçalho dizia "Bem-vindo de volta, Helena": simpático e sem informação, num painel cujo número muda inteiro conforme o projeto selecionado na barra lateral | `SectionHeading` novo na biblioteca (`h2` + filete que mede a largura da zona), e cada zona vira `<section aria-labelledby>` — a mesma hierarquia serve o olho e o leitor de tela. **Carteira** responde como o volume evoluiu; **Risco**, se ainda cabe operação e onde ele está. O cabeçalho passa a dizer **de qual projeto** e **de quando** é o número: o nome do projeto entrou no payload (é nome, não número) e o período vai por extenso, porque `09/2025` é notação de coluna de tabela |
| GRA-S15-11 | **[número certo, pergunta errada]** "Limites no teto" contava só o que **estourou**, e a lista nasceu com a faixa aberta em cima (S15, **DEC-116**) | O cartão vinha **zerado nos 12 projetos**. A primeira investigação conferiu o cálculo, achou-o correto e parou ali — e essa foi a parte errada do método: o cartão respondia **com precisão** uma pergunta que ninguém tinha feito. `limite_disponivel < 0` é *estourado*; "no teto" lê como *chegou*, e um limite a 98% não entrava em lugar nenhum. Depois, com a lista criada, a faixa saiu aberta em cima (`>= 90%`, incluindo os estourados) — e o usuário, olhando rodando, apontou que **quem já estourou não está "prestes" a nada** | Dois indicadores, faixas que **não se sobrepõem**: o cartão conta `>= 100%` (`disponivel <= 0`); a lista mostra `>= 90%` **e** `< 100%`, nomeando cada limite e a porcentagem em que ele está — 89% e 99% têm urgências diferentes. Cada limite aparece em exatamente um lugar. O corte de cima é o **sinal do disponível** aplicado junto com a porcentagem, porque com teto zero a porcentagem não existe e filtrar só por ela deixaria esses limites escaparem pela borda da divisão por zero. **A lição de método fica registrada:** "o cálculo confere" é o começo da investigação, não o fim |
| GRA-S15-12 | **[melhoria DECLINADA, com motivo]** Série plurianual no gráfico de indicadores (S15, `NEW-001`, tarefa 5.6) | O `demo-seed-design.md` §8 descreve **24 meses** de indicador com a inflexão do mês 9, e o dado está lá: há lançamento em 2024, 2025 e 2026. A tentação é fazer o gráfico atravessar os dois anos | **Declinada por decisão de orquestração (26/08/2026).** A grade de lançamentos é **por ano** — contrato do legado — e o design **G3** proíbe o gráfico de ter filtro próprio, justamente para não haver duas verdades na mesma tela. Uma série plurianual ali seria **feature nova**, e a DEC-09 é explícita sobre não inventar o que o legado não tinha. O painel principal **já atravessa dois anos** (09/2025 a 08/2026), então a capacidade existe no produto; não existir nesta tela é coerente com a grade anual. Fica registrado para que a próxima leitura saiba que foi decisão, e não esquecimento |
| GRA-S15-13 | **[o título de seção era a CAUSA do buraco, não um detalhe ao lado dele]** O painel vira um fluxo único de masonry (S15) | Título de seção obriga cada grupo a ser um **contêiner próprio**, e masonry só empacota **dentro** de um contêiner. Com "Carteira" e "Risco" separando os blocos havia **dois masonry independentes**: o vazio no fim do primeiro não podia ser preenchido pelo primeiro bloco do segundo. A intuição do usuário (*"talvez se tirar esses títulos fica até mais bonito"*) estava tecnicamente certa | As **duas versões foram montadas e capturadas**, claro e escuro, e comparadas lado a lado. Sem os títulos, "Consumo de limite por tipo" sobe para o espaço que sobrava embaixo dos KPIs e o empacotamento passa a valer de ponta a ponta. **O que substitui o título como orientação:** cada bloco já se nomeia (o cartão diz TOTAL OPERADO, o painel diz "Consumo de limite por tipo" e o subtítulo diz de quando é o número) — o rótulo da zona repetia um nível acima o que o bloco já dizia. O agrupamento continua pela **ordem** e pela proximidade, e cada painel segue sendo uma `<section>` com nome acessível, então a estrutura para leitor de tela não se perdeu com o título. O `SectionHeading` **fica na biblioteca**: a decisão é desta tela, não remoção do componente |
| GRA-S15-14 | **[a escolha de masonry foi MEDIDA, não presumida]** Três bibliotecas baixadas e lidas antes de decidir (S15) | A instrução inicial dizia "não instale biblioteca" por uma limitação de ambiente que **não existia** (o `pnpm` roda; o `vitest` roda). Com a restrição revogada, a escolha teve de ser refeita como decisão técnica | Os pacotes foram baixados e o algoritmo de distribuição lido: **`react-masonry-css` 1.0.16** faz `columnIndex = i % columnCount` — rodízio por índice, que distribui mas **não empacota por altura**, então o buraco continuaria; **`react-plock` 3.6.1** só empacota com `useBalancedLayout`, e esse modo **reordena entre colunas pela altura**, quebrando a ordem de leitura que aqui é requisito duro; **`masonic` 4.1.0** é **virtualizado**, e desmontar painéis com Recharts e assinatura de React Query vivos é custo sem benefício. O CSS Grid com linhas finas + `ResizeObserver` é a **única** opção que empacota por altura **e** mantém o posicionamento automático do Grid, que é o que garante que o item *n* nunca renderize antes do *n−1*. Para 6–8 blocos, 140 linhas sem dependência é a engenharia certa — e agora está escrito por que |
| ETL-S14-01 | **[medição que virou DECISÃO do usuário — a `DEC-117`]** A carga cortava na 6ª casa em **6 colunas**, e o legado não cortava. **CORRIGIDO** (S14, tarefa 4.8, `DB-ETL-06`) | No legado, 25 colunas de `receivable_entries` são **`float`** (`../sfg/db/migrate/20210315183541_create_receivable_entries.rb:32-38,64-77`) e no ai9 eram `decimal`. Em 19 delas isso não muda nada, porque a própria fórmula do legado arredonda antes de gravar. Nas outras 6 — `recompra_percent`, `retencao_percent`, `fomento_percent`, `outros_percent`, `float_calculado`, `diferenca_float` — a fórmula **não arredonda**: `recompra_percent` é `100 × recompra / valor_liquido` cru (`../sfg/app/models/receivable_entry.rb:59`), e produção guardou `19.704917111218396`. Com `decimal(15,6)`, **o ai9 acrescentava um ponto de arredondamento que o legado não tinha**. Medido em `spec/lib/sfg/etl/values_precision_spec.rb` contra o dump de 31/05/2025: **5.321 comparações, 45 colunas decimais, 131 borderôs reais → 48 divergências, TODAS nessas 6 colunas de escala 6, e ZERO nas 39 colunas de dinheiro e de taxa** (escala 2 e 4). Reconferido depois contra o dump **inteiro**, sem fixture: **1.154.980 comparações em 28.131 borderôs → 8.976 divergências**, as mesmas 6 colunas e mais nenhuma | **A entrada anterior dizia "mantido como está, com dono nomeado", e o dono decidiu.** Esta linha é reescrita, não apagada: a medição acima é o que justifica a `DEC-117`, e sem ela a decisão fica sem fundamento no registro. O que mudou foi o veredito. O argumento de então — *"1e-6 num percentual derivado e exibido, e trocar esquema às vésperas da apresentação é o precedente DEC-116"* — **caiu por dois motivos escritos na própria DEC-117**: a DEC-02 liberou o `decimal` **sob a condição** de os totais ficarem *idênticos*, e a carga está adiada pela **DEC-102**, então a troca acontece longe do cutover (adiá-la é que seria arriscado — depois da carga a correção exigiria recarregar 28.131 borderôs). **O que foi feito:** `20260826235200_seis_colunas_de_escala_6_voltam_a_float.rb` troca **só essas 6** para `float`; o conversor passa a usar `Values.to_float` nelas (`FLOAT_COLUMNS`); o `Receivables::Calculator` devolve `Float` nas 6, que é o que o legado tem em memória depois de atribuir a uma coluna `float` — sem isso a **prévia** respondia `BigDecimal` (string JSON) e a **gravação** respondia `Float` (número JSON) para o mesmo payload, quebrando o contrato **C2** na serialização. **Resultado medido, executando contra o dump real:** `1.154.980 comparações → 0 divergências`, e as 168 células `NaN`/`Infinity` do **D-10** continuam sendo barradas pelo conversor (`NAN_SENSITIVE`), não pelo cast. **O spec inverteu de sentido:** travava a lista das divergentes como *fechada em 6*; agora reprova se **qualquer** coluna divergir — uma divergência nova não entra mais por omissão. **As outras 19 `float` do legado continuam `decimal` de propósito** (0 divergências medidas): trocá-las é risco sem ganho. `DB-ETL-06` é ID de desenho da S14 e **não existe como linha do `parity-ledger.md`** — conferido; inventar linha nova para poder marcar tarefa seria o inverso de registrar |
| ETL-S14-19 | **[medição nova, feita ao fechar a DEC-117 — e ela impede um aperto errado do golden]** Com as 6 colunas em `float`, a tentação seguinte é comparar o golden de C2 em igualdade EXATA de `double`. **Não passa, e o motivo não é a fórmula** | Rodado contra o dump inteiro (28.131 linhas, 32 puladas pelo D-10), comparando as 6 em `Float` exato: **168.594 comparações → 9 divergências**, todas de último dígito — `produção 0.0018867924528302` × `ai9 0.0018867924528301887` (2,00 / 106.000,00). A causa é a **precisão padrão da divisão `BigDecimal / BigDecimal`**, que difere entre o **Ruby 2.6.1** de produção (DEC-03) e o Ruby 3.4 desta base: é a mesma família das 22 divergências que o `Calculator#bd` já resolvia nas outras colunas, e aqui ela cai **abaixo da 6ª casa** | **A distinção que importa, e que fica escrita no cabeçalho de `calculator_spec.rb`:** o **transporte** (ETL) é exato — 1.154.980 comparações, 0 divergências — porque o conversor **copia** os derivados de produção, não os recalcula (DEC-30). O que tem tolerância de 1e-6 é o **recálculo**, e por isso `SCALES` continua comparando essas 6 em 6 casas. Quem apertar para igualdade exata sem ler isto ganha 9 falhas e vai procurar defeito na fórmula, onde ele não está |
| ETL-S14-20 | **[defeito latente que a troca de tipo revelou]** `Receivables::InputGuard.nonfinite?` devolvia `nil`, não `false`, para `Float` finito | `Float#infinite?` responde **`nil`** (não `false`) quando o número é finito, e o ramo era `value.nan? \|\| value.infinite?`. Enquanto `float_calculado` era `decimal` o valor caía no ramo `BigDecimal`, que já usava `!value.finite?` e devolvia booleano; a **DEC-117** o mandou para o ramo `Float` e três exemplos que assertam `be(false)` reprovaram de uma vez | Funcionalmente a guarda **nunca falhou** — `nil` é falsy e o `filter_map` de `derived_values_must_be_finite` se comportava igual. O conserto é o predicado passar a valer para os dois tipos (`when Float, BigDecimal then value.nan? \|\| !value.finite?`), devolvendo `true`/`false` sempre. **A guarda do D-10 saiu mais forte da troca de tipo, não mais fraca** — que era a condição escrita na própria DEC-117 |
| DS-01 | **[defeito achado pelo USUÁRIO, corrigido no COMPONENTE]** `DataTable`/`Table` ganham rolagem horizontal **com afordância**, coluna de identificação congelada e cartões no telefone | Em `/renegotiations` (Admin, 1440×900) treze colunas não cabem: a última visível vinha **cortada no meio de um valor** (`R$ 2…`), as seguintes não existiam para quem olha, e **nada** na tela dizia que dava para rolar — `components/ui/Table.tsx:9` tem `overflow-auto`, mas `globals.css` apaga a barra de TODO container do app (`*::-webkit-scrollbar { height: 0 }`), e rolagem invisível é o mesmo que rolagem inexistente. Num sistema de crédito meio número não é layout torto: `R$ 2…` pode ser lido como dois reais. **Alcance medido: 18 telas** usam `DataTable`/`Table` fora de `components/ui` | Quatro peças, todas **aditivas** e no compartilhado (mesmo padrão do `layout='auto'\|'fixed'` de hoje): (1) `.table-scroll-x` devolve uma barra de **8 px** nos dois dialetos — `::-webkit-scrollbar` e `scrollbar-width`/`scrollbar-color`, este último obrigatório porque o `scrollbar-color: transparent` do `html` é herdado; (2) **cortina opaca** na borda direita com a largura EXATA do pedaço de coluna que sobrou (medida por `offsetLeft`/`offsetWidth` do `th`), então a coluna que não cabe fica fora da área visível **inteira** — nunca meia célula — e, quando a borda cai numa divisa, sobra só a sombra `--elevation-scroll-edge`; (3) `scroll-snap-align: start` com `scroll-padding-left` = largura da coluna congelada, para a coluna seguinte nunca parar metade escondida sob ela (`2.500,00` pela metade vira `500,00`, que é plausível e errado); (4) primeira coluna `sticky` com `--elevation-sticky-col`, **só quando há transbordo de verdade** — sem rolagem ela não teria efeito visual e ainda assim mexeria no realce de linha das 18 telas. A região que rola vira alcançável pelo teclado (`role="region"` + `tabIndex`, WCAG 2.1.1) só quando rola. **Nenhuma tela precisou mudar.** Dois tokens novos de elevação, claro e escuro |
| DS-02 | **[GANHO, e é o telefone virando primeira classe no componente]** Abaixo de 768 px o `DataTable` deixa de ser tabela: cada linha vira um `MobileCard` com o cabeçalho da coluna como rótulo | Numa tabela de 10+ colunas o telefone não tem para onde encolher: rolagem lateral esconde da 5ª coluna em diante e — como o próprio `MobileCard` já registrava — *coluna fora da tela é coluna que o usuário nunca descobre*. A régua do produto: desktop é onde o trabalho acontece, **telefone é onde ele é conferido**, e conferir mal é pior que não conferir. Conferir exige ver TODOS os campos | Não é invenção: **treze** telas desta migração já escreviam essa mesma lista à mão (renegociações, borderôs, cobranças, operações de risco, estruturadas, indicadores, catálogos, disponibilidades, extensões, movimentos, recibos, remunerações). O componente passa a fazer de fábrica o que treze cópias provaram ser o padrão, e as treze **continuam intactas** porque trocam por `useMobile` antes de montar o `DataTable`. Rótulo e valor saem das MESMAS `Column` da tabela (`conteudoDaCelula`), sem segunda fonte de formatação; `Column.hideOnMobile` tira a coluna do cartão; `mobile='scroll'` é a saída para quando a grade é a informação. Quem ganha hoje: `UiKitPage` e **toda tela futura**, que nasce com telefone resolvido em vez de precisar da décima quarta cópia |
| DS-03 | **[TERCEIRA e QUARTA vez do mesmo descuido]** `beauty_state` deixa de chegar cru à tela: `66.87% Pago` vira `66,87% Pago`, na lista **e no detalhe** | Ponto decimal de JavaScript/Ruby numa tela em português, na mesma captura do DS-01. O servidor compõe `"#{paid_percent}% Pago"` (`Renegotiation#beauty_state`, réplica de `../sfg/app/models/renegotiation.rb:129`). A S8 já tinha achado `2.55%`, a S15 achou `51.76%` ao lado de `109,0%` **na mesma tela** — e foi ela que escreveu `localizePercentLabel` justamente para isto | Passa por `localizePercentLabel`, que troca **só o separador**: os dígitos, inclusive o arredondamento, são do domínio e a DEC-01 manda preservá-los — reformatar introduziria uma segunda decisão de arredondamento sobre valor já decidido. A varredura pedida achou a **quarta** ocorrência que ninguém tinha visto (`RenegotiationDetailPage.tsx:117`), mais cinco lugares de número cru: o tooltip de correção por dias úteis (`72.5%`), o andamento do job em duas telas (`33.33333333333333%`, de `Number(bruto)` sem arredondar) e o tamanho de anexo em dois componentes (`2.5 MB`). Duas varreduras novas travam a volta: `src/__tests__/numero-em-pt-br.test.ts` proíbe `toFixed` com casa decimal costurado dentro de texto (e deixa passar o `toFixed(2)` de `paraDecimalString`, que é payload de API, não tela), e o portão da S9 proíbe `.beauty_state` fora de `localizePercentLabel` |
| DS-04 | **[três defeitos do próprio DS-01, achados RENDERIZANDO — nenhum deles reprova `tsc` nem a suíte]** | Provado com `browser.js`/Playwright em `/renegotiations`, `/risk-operations`, `/receivables`, `/structured-operations`, `/risk-controls` e `/ui-kit`, claro e escuro, 1440×900 e 390×844 | (1) **A cor da coluna congelada não pode ser cravada.** `bg-card` fixo deixava um bloco claro sobre a tabela escura em `/risk-operations`, que põe a tabela direto na página (`--background`), não num cartão. Passou a ser **medida**: o componente sobe a árvore até a primeira superfície **totalmente opaca** e a publica em `--table-surface`. A primeira versão da medição aceitava qualquer alfa acima de zero e pegou um `bg-muted/20` — a coluna saiu 20% opaca, ou seja, sem resolver nada; agora só `rgb(…)` ou `rgba(…, 1)` valem. (2) **Trocar de tema não remedia sozinho:** a cor ficava a do modo anterior, porque nada tinha rolado nem redimensionado desde a troca — um `MutationObserver` na classe do `<html>` refaz a medida. (3) **No cartão do telefone, `R$ 1.204.000,00` quebrava depois do penúltimo dígito**, com `0` sozinho na linha de baixo: número partido em duas linhas é a mesma família do número cortado que originou a tarefa. Coluna de moeda passou a ocupar a largura inteira do cartão e valor numérico não quebra mais |
| DS-05 | **[medido, e é o que fecha o ponto 2]** A cortina de borda existe nos DOIS lados, e a largura de cada uma é medida a cada rolagem | Encaixe (`scroll-snap`) resolve o lado esquerdo na maioria das posições, mas **não** no fim da rolagem: essa posição é sempre alcançável e nem sempre é ponto de encaixe. Visto renderizando em `/renegotiations`: no fim, a coluna "Lançado" mostrava `3.619,58` — o começo de `R$ 493.619,58` escondido sob a coluna congelada. Cortado à esquerda é **pior** que à direita: `R$ 2…` denuncia que falta algo, `500,00` é um número plausível e errado | Medido nas quatro telas, 1440×900: cortina direita de 42 a 100 px na abertura, **`ausente` ao chegar ao fim** (o aviso some quando não há mais o que avisar); cortina esquerda de 35 a 160 px no fim, `ausente` nas posições de encaixe. Teto de 45% da área visível para nenhuma das duas comer metade da tabela. `/risk-controls` cabe em 1302 px: **nenhuma cortina, nenhuma coluna congelada, nada muda** — que é o comportamento certo para as tabelas que já cabiam |
| DS-06 | **[defeito apontado pelo USUÁRIO com captura]** Os três filtros da trilha de auditoria saem do canto direito e viram uma **faixa horizontal** acima da lista (`/admin/audit-trail`) | Eles moravam no `CardHeader`, dentro de um `flex flex-wrap gap-2` estreito. Como o `Select` do design system é `block` (`w-full`) por padrão, cada um ocupava a linha inteira do contêiner e os três **empilhavam numa coluna de 480 px à direita**, com metade da largura do cabeçalho vazia. Palavras do usuário: *"pq esses filtros ficaram todo entulhados no canto direito?"*. A passada de `impeccable` tinha sido feita no painel, não aqui | Faixa própria em `bg-card`, três campos de peso igual (`flex-1 min-w-[12rem]`), **rótulo visível** em cada um ("Tipo de registro", "Evento", "Período" — "Todos os tipos" sozinho não diz o que filtra), botão **Limpar** só quando há filtro, e o **total do filtro** no cabeçalho da lista, que é a pergunta seguinte a qualquer filtro. Nenhum filtro novo, nenhum removido. É o mesmo padrão de `RenegotiationsPage`: reuso, não invenção. No telefone os três empilham, que é o que cabe em 390 px |
| DS-07 | **[defeito apontado pelo USUÁRIO, e o componente estava errado]** O detalhe do evento de auditoria deixa de ser `Sheet` e vira **`Dialog`** | Palavras dele: *"o detalhe de auditoria ficou péssimo de design, e também poderia ser um diálogo ao invés de usar o componente de form"*. Ele está certo nas duas metades: `Sheet` é a **gaveta de formulário** — estreita e alta de propósito, porque formulário é uma coluna de campos. Para um registro de leitura (pares rótulo/valor, UUID de 36 caracteres, diff antes→depois) ela picotava identificador, espremia o diff e deixava **metade da altura vazia**. O desenho só ficou visível hoje: até a correção do `SheetContent` ausente, o detalhe não abria de jeito nenhum | `AuditEventDialog` (membro da biblioteca `components/audit/`, não peça de tela): `sm:max-w-3xl`, chip do evento no cabeçalho, `DetailList` de 3 colunas, e o diff virou **grade campo · antes · depois** com cabeçalho de coluna. O valor antigo **perdeu o `line-through`** — numa auditoria ele é dado que se lê e se copia, não texto cancelado; a distinção passou a ser cor **e** rótulo (cor nunca é o único portador). UUID em `font-mono` com quebra nos hífens que ele já tem, nunca no meio de um segmento. Evento sem alteração de campo (impersonação) passou a **dizer isso** em vez de deixar a seção com título e nada embaixo. A foto completa do estado anterior (**DEC-78**) segue colapsada, com rolagem própria |
| DS-08 | **[aditivo na base, com o padrão preservado]** `DialogContent` ganha `fullScreenOnMobile?: boolean` (padrão `false`) | No telefone a caixa centrada nasce `max-w-lg` com margem dos dois lados: em 390 px sobram ~300 px úteis, ilegíveis justamente para o diff. Tela cheia é o padrão nativo para "abrir um registro" no aparelho, e é o que a **DEC-100** pede — no telefone a densidade é **repensada**, não encolhida | Mesmo molde do `layout?: 'auto' \| 'fixed'` do `DataTable`: com o padrão `false` a string de classes é **byte a byte** a de antes, então as 6 telas que já usam `Dialog` não mudam. O botão de fechar subiu para alvo de **44 px** abaixo do `sm` (`p-3 sm:p-2`) — `p-2` dava 32, e fechar é a única saída de um diálogo em tela cheia |
| DS-09 | **[o portão que faltava, e o que ele prova]** `auditTrailPage.test.tsx` — 7 testes que **clicam** em "Ver detalhes" e exigem o conteúdo dentro do `role="dialog"` | Portão verde prova que o código **carrega**, não que ele **funciona**: o `Sheet` sem `SheetContent` passou por `tsc` e por suíte verde desde sempre, e o detalhe simplesmente não abria. Um `getByText` solto teria passado também — o conteúdo estava no DOM, só que no fluxo da página | Os testes travam: (1) o detalhe mora **dentro** do diálogo; (2) o diff mostra antes E depois rotulados, em `font-mono` e **sem** `line-through`; (3) evento sem alteração de campo diz isso; (4) o estado completo anterior nasce colapsado e com rolagem própria; (5) os três filtros continuam existindo, com rótulo visível, numa faixa com `role="region"`. Suíte do front: **466 passando** |
| DS-10 | **[achado da passada de layout, corrigido no componente]** "Ver detalhes" perde o `ml-auto` no `AuditTrailItem` | Em 1440 px a ação ia parar a ~900 px do texto que descreve, e o olho tinha de atravessar um rio de vazio para ligar as duas coisas. Modo **Operate**: a ação mora ao lado do conteúdo sobre o qual ela age | Vale também na trilha **do próprio objeto**, que usa o mesmo item — em coluna estreita nada muda, porque lá não havia rio |
| DS-11 | **[defeito apontado pelo USUÁRIO, corrigido no COMPONENTE]** `DataTable` passa a **arrastar para rolar** — o tratamento do `apl9`, extraído para `hooks/useDragToScroll.ts` | Palavras dele: *"o apl9 tem o tratamento de tabelas com scroll que ficou bom"*. A barra que a DS-01 devolveu resolve o mouse no desktop e **não resolve** dois casos que são o dia a dia: o **iOS** não mostra barra persistente (é overlay do sistema) e o **Chromium headless reserva 0 px e não pinta nada** — ou seja, a afordância da DS-01 sequer é *comprovável* renderizando. Arrastar o conteúdo não depende de o sistema desenhar coisa alguma. No `apl9` o hook é **inline numa página** (`apl9/frontend/src/app/pages/UserProfilePage.tsx:14-47`), instanciado três vezes ali mesmo; aqui quem usa é o `DataTable`, e o alcance é de **18 telas** — por isso ele nasce na biblioteca, não copiado | Três diferenças em relação ao `apl9`, todas por causa do alcance: (1) **limiar** — a página do `apl9` não tem linha clicável e rola no primeiro pixel; aqui a linha tem `onRowClick`, então o gesto só vira rolagem depois de **5 px** e o `click` que nasce de um arrasto é engolido em `onClickCapture` (fase de captura no container: o evento morre antes de chegar ao `<tr>`); (2) **`clientX` em vez de `pageX`** — o `apl9` faz `e.pageX - el.offsetLeft`, e `offsetLeft` é constante durante o gesto, então subtraí-lo não muda nada: o que importa é a diferença entre dois pontos, e `clientX` a dá igual (a página nunca rola na horizontal, §5.4.8 regra 3) **e é reproduzível em `fireEvent`**; (3) **`enabled`** — cursor de arraste em tabela que **cabe** é promessa falsa, então ele liga só com transbordo medido, a mesma regra da coluna congelada. Enquanto arrasta: `cursor-grabbing`, `select-none`, `scroll-snap-type: none` **em linha** (classe contra classe é empate de especificidade decidido pela ordem do CSS gerado) e `[data-table-dragging]` no `globals.css` para o cursor valer sobre a linha, que declara `cursor-pointer`. `speed: 2` é o do `apl9` e fica. A barra segue com **8 px**, não os 4 px do `apl9`: a DS-01 mediu essa largura como "vista e arrastável", e estreitá-la agora seria trocar uma decisão medida por uma cópia. **Nenhuma das 18 telas mudou**. Visto renderizando em `/renegotiations` a 1440×900: arrasto de 3 px sobre a linha **abriu o registro**; arrasto de 240 px rolou de 0 a 505 px, com `cursor: grabbing` e `scroll-snap-type: none` medidos no meio do gesto, e **não navegou** |
| DS-12 | **[o portão que separa clique de rolagem — 8 exemplos]** `dataTableArraste.test.tsx` | O risco que o arraste traz é exatamente o que `tsc` não vê: um arrasto que termina sobre uma linha emite `click`. Sem limiar, rolar 40 px **abre um registro que ninguém pediu**; com limiar mal posto, o tremor de 3 px do clique deixa de abrir o registro que o usuário pediu. Os dois casos são o mesmo código e apontam para lados opostos | Travados: (1) 3 px **é clique** — a linha abre e `scrollLeft` continua 0; (2) 40 px **é rolagem** — rola 80 px (× `speed`) e a linha **não** abre; (3) o clique **seguinte** volta a abrir (a supressão vale para um só, senão a linha para de funcionar e ninguém entende por quê); (4) durante o gesto: `cursor-grabbing` + `select-none` + `data-table-dragging` + `scroll-snap-type: none`; (5) soltar o mouse **fora** encerra (senão o cursor fica agarrado e o próximo movimento rola sem ninguém apertar nada); (6) tabela que **cabe** não promete arraste; (7) arrasto que **começa num controle** da célula é do controle (o `preventDefault` do movimento mataria o clique do botão); (8) botão do meio e direito não arrastam. `scrollLeft` é falsificado no jsdom, que aceita a atribuição e devolve 0 |
| DS-13 | **[defeito apontado pelo USUÁRIO, e de novo o componente estava errado]** O detalhe da cobrança deixa de ser `SideDrawer` e vira **rota própria**, `/charges/:id` | Palavras dele: *"o detalhe de cobrança ficou muito ruim assim, ou fazemos em diálogo ou em uma página separada"*. Página — e a razão é **medida**, não estética: o detalhe é a porta da **seleção de recibos**, que numa cobrança real desta base tem **214 candidatos** e **9 persistidos**, e o que ele já mostra (extrato por remuneração) é a mesma coisa em miniatura: uma tabela. Gaveta é estreita e alta **de propósito**, porque formulário é uma coluna de campos; espremer tabela ali brigaria com a rolagem horizontal, a coluna congelada e os cartões de telefone que o `DataTable` acabou de ganhar (DS-01/DS-02/DS-11) | `ChargeDetailPage`, filha de `/charges` no mesmo padrão de `/receivables/:id`, com `:id/receipts` declarado **antes** de `:id` (o router casa na ordem). O extrato virou `DataTable` de verdade — e por isso ganhou de fábrica os cartões abaixo de 768 px, sem a décima quarta cópia do mesmo `MobileCard`. O par de páginas fica coerente: `/charges/:id` lê, `/charges/:id/receipts` seleciona, as duas com endereço e com Voltar do navegador (no legado a segunda era `{section: 'receipts'}` em memória com `history.replaceState`, e Voltar saía do console — **D-92**). **A gaveta de CRIAÇÃO fica**, e foi avaliada: um `DatePicker` e duas frases é formulário curto, que é o caso em que a gaveta está certa. A regra que sai disto: **formulário curto = gaveta; leitura densa = página**. O rótulo da coluna de valor virou "Valor da remuneração" e a classe virou a sigla `LIQ`/`EST` com explicação — o MESMO vocabulário de `ChargeReceiptsPage`, para as duas telas da cobrança não falarem duas línguas |
| DS-14 | **[defeito apontado pelo USUÁRIO, corrigido no COMPONENTE — e 14 telas herdam]** A folha de ações vira **menu ancorado acima de 768 px**; no telefone continua folha | Palavras dele: *"o `more` de disponibilidade não faz sentido ser assim no desktop; no mobile faz, no desktop tem que ser dropdown"*. Ele está certo, e o motivo é proporção: a folha toma a metade de baixo da janela para oferecer **três** itens, e o contexto — de que LINHA são essas ações — sai de vista junto. No telefone esse tamanho é o acerto (zona do polegar, e a linha some de qualquer jeito); em 1440 px é desperdício com perda de contexto. **Hoje só `/project-availabilities` dispara a folha no desktop**; as outras 13 telas a disparam de dentro do ramo do telefone e passam a herdar o menu no dia em que precisarem — a correção é do componente, não da tela, que foi a lição do `KpiCard` (cinco remendos em cinco telas antes de alguém consertar no lugar certo) | O corte é `useMobile` (768 px, o mesmo do `DataTable`) dentro de `MobileRowActions`/`MobileActionsSheet`: **uma lista de ações, duas apresentações** — nunca duas listas, que é como o menu do legado ficava vazio numa combinação e cheio noutra (FE-145). **Nenhum segundo componente de menu foi criado**: o painel é o `FloatingPanel` da biblioteca, que já resolve portal no `body`, virada quando não cabe embaixo, reposicionar na rolagem e fechar por clique fora — os mesmos problemas que o `MobileMenuActions` (obsoleto, sem consumidor) resolvia errado, com `absolute` preso ao contexto de empilhamento do ancestral. O item bloqueado continua **aparecendo com o motivo** (D-24). Teclado: abre com foco no primeiro item utilizável, setas percorrem, `Escape` fecha e **devolve o foco ao gatilho** (visto renderizando: o anel de foco volta ao "…" da linha certa); escolher uma ação **não** devolve o foco, porque ela abre diálogo ou navega e disputar foco com eles é como se perde o foco de um formulário. `MobileActionsSheet` ganhou `anchorRef?` — **sem ela o desktop continua caindo na folha**, que é o que mantém a mudança aditiva para quem ainda não a passa. Largura fixa de 240 px de propósito: o `useAnchoredPanel` só conhece a largura do painel com `matchWidth`, e sem ela **estima 240**; com os 248 px medidos, o menu encostava na borda direita da janela. Igualar a largura real à estimativa acerta a conta **sem tocar no componente compartilhado**, que é do `Select`, do `DatePicker` e do `Autocomplete` (Princípio 6b) |
| DS-15 | **[o portão do corte, e um teste que MENTIA sem saber]** `rowActionsDesktop.test.tsx` (8 exemplos) — e `projetoEmpresas.test.tsx` passa a declarar o aparelho | Os três exemplos da folha em `projetoEmpresas.test.tsx` chamam-se *"a folha de ações do TELEFONE (DEC-100)"* e rodavam na largura padrão do jsdom (**1024 px**). Passavam porque a folha era incondicional — o nome dizia telefone e o exemplo media desktop. No instante em que a folha virou condicional, os três reprovaram, e isso é o teste dizendo a verdade pela primeira vez | Os três agora **declaram 390 px**, e as asserções ficam intactas. O lado de desktop ganhou portão próprio: o menu é `role=menu` com `menuitem` e **não** existe `role=dialog`; no telefone é o contrário; **sem `anchorRef` o desktop continua na folha** (é o exemplo que prova que a mudança é aditiva); a ação bloqueada aparece com o motivo; o foco nasce no primeiro item utilizável; escolher dispara a ação e fecha. Mais 8 exemplos do detalhe da cobrança (`chargeDetailPage.test.tsx`), que **montam a página na rota de verdade** — a lição do `Sheet` sem `SheetContent`: portão verde prova que o código carrega, não que ele funciona. Suíte do front: **491 passando** |
| DS-04 | **[defeito achado pelo USUÁRIO no aparelho, e um token cosmético derrubava o layout mobile INTEIRO]** Sai `interactive-widget=resizes-content` do `meta viewport`, entra `viewport-fit=cover` | No iPhone **em retrato** a barra de abas sumia. Ela é `md:hidden` (esconde a partir de 768 px) e um iPhone em retrato tem 390–430 px — para sumir, a página tinha de estar sendo diagramada em **largura de desktop**. Era o `meta viewport` sendo **ignorado por inteiro** pelo Safari por causa de um token desconhecido: `interactive-widget=resizes-content` é o valor **padrão** da especificação (não muda nada onde é suportado) e é chave que o parser do Safari não conhece. Sem viewport honrado, a página cai nos ~980 px padrão — e nessa largura **a barra some E a tabela deixa de virar cartão**, que foram exatamente os dois sintomas relatados na mesma frase | Token removido. Entrou `viewport-fit=cover`, que **faltava e importa**: sem ele os `env(safe-area-inset-*)` valem **zero** no iPhone, e a barra usa `pb-[env(safe-area-inset-bottom)]` — o respiro da faixa inicial nunca chegou a existir no aparelho. **Confirmado pelo usuário: "resolvido a botbar".** ⚠ **Limitação de teste que este caso expõe:** o Playwright honra o `viewport` que você passa **independentemente** do `meta` da página, então a varredura headless mostrou a barra visível nos cinco iPhones em retrato e **não reproduziu o defeito**. Erro de `meta viewport` é invisível para o harness — só aparece em navegador móvel de verdade |

