# Inventário — jobs & cron (460–479) e integrações (480–499)

> Legado: `../sfg` (Safegold, Rails 6.1.4 / Ruby 3.0.2). Alvo: ai9 (Rails 8 API, Sidekiq + Action Cable).
> Todas as linhas `arquivo:linha` referem-se ao repositório legado, lido em modo somente-leitura.
> Constantes de negócio citadas aqui (`MAX_FILE_SIZE`, `RWS_TOKEN`, `GOOGLE_*`, `FACEBOOK_APP_ID`)
> estão detalhadas em `.migration-ai9/brand-and-metadata.md`.

## Operacional (OPS)  ← tabela principal desta unidade

| ID | Área | Item (job/cron/integração/e-mail/PDF/upload) | Fonte (arquivo:linha) | Gatilho | Comportamento | Falha/timeout | Dependência externa |
| -- | ---- | -------------------------------------------- | --------------------- | ------- | ------------- | ------------- | ------------------- |
| OPS-460 | jobs | **Backend de fila: `delayed_job` + `delayed_job_active_record`** (fila em tabela `delayed_jobs` do Postgres, não Redis) | `Gemfile.linux:22-23`; `engines/mailer19/db/migrate/20170505110720_create_mailer_delayed_jobs.rb:4-18` | Todo `Delayed::Job.enqueue` e todo `.delay` | Job serializado em YAML na coluna `handler`; worker faz *polling* na tabela por `priority, run_at`. Nenhuma fila nomeada é usada (coluna `queue` sempre nula). Prioridade sempre 0 (default) — não há priorização real | Se não houver worker rodando o job fica na tabela indefinidamente sem nenhum aviso ao usuário (o registro fica com `job_state = "Em progresso"` para sempre) | PostgreSQL |
| OPS-461 | jobs | **Daemon do worker** `bin/delayed_job` (gem `daemons`) | `bin/delayed_job:1-5`; `Gemfile.linux:45` | Start manual: `RAILS_ENV=... bin/delayed_job -i worker_N start` | Escreve pidfile em `tmp/pids/delayed_job.<worker>.pid`. Não há supervisor/systemd/Procfile que o suba — é operação manual | Worker morto = fila parada silenciosamente. Nenhum healthcheck, nenhuma métrica, nenhum alerta | Sistema de arquivos (`tmp/pids`) |
| OPS-462 | jobs | **Política de retentativa e falha** `Delayed::Worker` | `config/initializers/delayed_job_config.rb:1-2` | Global | `max_run_time = 7.days` (um job travado ocupa um worker por 7 dias antes de ser considerado morto); `destroy_failed_jobs = false` (jobs falhos permanecem na tabela com `failed_at` + `last_error`); `max_attempts` fica no default do delayed_job = **3 tentativas** com backoff `attempts**4 + 5` segundos | Após 3 tentativas o job é marcado `failed_at`; **ninguém é notificado**. Além disso todos os jobs de negócio (OPS-465..471) capturam `rescue => e` internamente, então o delayed_job **nunca vê o erro** e nunca retenta — a falha só aparece em `job_report` no registro e em `Tracking` | — |
| OPS-463 | jobs | **`progress_job` (`ProgressJob::Base`)** — progresso de job persistido | `Gemfile.linux:46`; `engines/mailer19/db/migrate/20170505114146_add_progress_to_mailer_delayed_jobs.rb:29-33`; jobs em `lib/*_job.rb:1` | Todo job de negócio herda de `ProgressJob::Base` | Adiciona `progress_stage`, `progress_current`, `progress_max` na tabela `delayed_jobs`. O job chama `delegate.update_stage_progress(...)`, `update_progress_max(n)`, `update_progress` (ex.: `app/models/project.rb:312-345`, `app/models/project_availability_template.rb:619-700`) | Se o job morre, os contadores param congelados; o registro-pai fica com `job_id` apontando para um job que não avança | — |
| OPS-464 | jobs | **`Runner`** — orquestrador caseiro de workers (attach/start/kill/force_start, `@@max_workers = 4`) | `lib/runner.rb:1-99` | **Nenhum.** Grep em todo o repo: a classe só é definida, nunca chamada | Faria `Open3.capture3("kill -9 #{pid}")` e `rm -rf` em pidfiles, e subiria workers com `bin/delayed_job -i worker_N start` | Código morto com injeção de shell latente (`kill -9 #{pid}`, `rm -rf #{path}`) | Shell do SO |
| OPS-465 | jobs | **`CreateGlobalTemplateForProjectJob`** — gera todos os templates de disponibilidade globais dentro de um projeto novo | `lib/create_global_template_for_project_job.rb:1-59`; enfileirado em `app/models/project.rb:82-88` (after_create) e `app/models/project.rb:738-742` | `after_create` de `Project`; e ação explícita em `project.rb:738` | Grava `Tracking` de start; chama `project.create_templates_from_global(self)` (`project.rb:312-345`, itera `GlobalAvailabilityTemplate.count`, progresso 1 a 1, depois "Ordenando templates"); ao fim seta `job_state = COMPLETED`, `job_id = nil` e salva | `rescue => e` → `job_state = "Falhou"`, `job_report = ["Falhou: #{e}"]`, `Tracking` de falha. **Não relança**, então o delayed_job marca sucesso e não retenta. Usuário só descobre abrindo o projeto | — |
| OPS-466 | jobs | **`InsertGlobalTemplateOnProjectsJob`** — propaga um template global recém-criado para **cada** projeto existente | `lib/insert_global_template_on_projects_job.rb:1-59`; enfileirado em `app/models/global_availability_template.rb:23-38` | `after_create` de `GlobalAvailabilityTemplate`, quando `should_insert_on_existing_projects?` | Faz `Project.all.each` e enfileira **um job por projeto** (fan-out N jobs numa única request), sobrescrevendo `project.job_id` de cada um. Cada job chama `project.create_template_from_global(template, self)` (`project.rb:349-379`) | `rescue => e` silencioso por projeto → `job_state = FAILED` + `job_report`. Se houver 500 projetos, 500 jobs; o `Project.all.each` roda **dentro do request web** | — |
| OPS-467 | jobs | **`InsertDefaultUserOnProjectJob`** — associa todos os usuários "membro padrão" a um projeto novo | `lib/insert_default_user_on_project_job.rb:1-53`; enfileirado em `app/models/project.rb:74-79` (after_create) | `after_create` de `Project` | Chama `project.insert_default_users(self)` (`project.rb:381-390`), criando `Membership` para cada default user com progresso | `rescue => e` → `job_state = FAILED` + `job_report`. **Não gera Tracking** (diferente dos demais), então não há trilha de auditoria dessa falha | — |
| OPS-468 | jobs | **`InsertProjectsOnDefaultUserJob`** — associa todos os projetos a um usuário que virou "membro padrão" | `lib/insert_projects_on_default_user_job.rb:1-49`; enfileirado em `app/decorators/models/user_decorator.rb:249` | `after_commit :create_memberships` em `Livetat::Auth::User` (create/update), quando `is_default_member?` e não é chamada do console | `insert_projects_on_default_user` (`user_decorator.rb:255-262`) itera `Project.all` criando `Membership` de participante | `rescue => e` **vazio** (`lib/insert_projects_on_default_user_job.rb:11-12`): o erro é engolido sem log, sem `job_report`, sem `Tracking`. **Pior caso da unidade**: usuário fica sem acesso a projetos e nada registra o motivo | — |
| OPS-469 | jobs | **`ProjectAvailabilityTemplateActivateJob`** — ativa um template de disponibilidade e recalcula as entradas | `lib/project_availability_template_activate_job.rb:1-62`; enfileirado em `app/controllers/pub/project_availabilities_controller.rb:66-73` | `POST` na ação `activate` do console | Enfileira, seta `job_id`, `job_state = IN_PROGRESS` e **bloqueia o item na UI** com a mensagem "Esse item foi ativado e ficará bloqueado até a conclusão da atualização das entradas". Chama `ProjectAvailabilityTemplate.background_activate` | `rescue => e` → `job_state = FAILED`, `job_report`, `Tracking` de falha. Se o worker não estiver rodando o item fica **travado (locked) para sempre** | — |
| OPS-470 | jobs | **`ProjectAvailabilityTemplateDeactivateJob`** — desativa o template e recalcula entradas | `lib/project_availability_template_deactivate_job.rb:1-62`; enfileirado em `app/controllers/pub/project_availabilities_controller.rb:85-92` | Ação `deactivate` do console | Idem OPS-469, mensagem de bloqueio "Esse item foi desativado…" | Idem OPS-469 (item permanece bloqueado se a fila não roda) | — |
| OPS-471 | jobs | **`ProjectAvailabilityTemplateRemovalJob`** — remove template, templates-filhos e suas entradas | `lib/project_availability_template_removal_job.rb:1-61`; enfileirado em `app/controllers/pub/project_availabilities_controller.rb:104-111` | Ação `destroy` do console | O `destroy` do controller **não apaga nada de forma síncrona**: apenas enfileira o job e bloqueia o item. A remoção real acontece em `ProjectAvailabilityTemplate.background_removal` (`app/models/project_availability_template.rb:619-700`), incluindo remoção de templates correlatos e reordenação | `rescue => e` → `FAILED` + `job_report` + `Tracking`. Falha no meio pode deixar remoção parcial (sem transação envolvendo tudo) | — |
| OPS-472 | cron | **`whenever` — arquivo base `config/schedule.rb`** | `config/schedule.rb:1-3`; `Gemfile.linux:35` | `whenever`/`crontab` | Define apenas `output = log/cron_development.log` e `environment = development`. **Não declara nenhuma tarefa** — é um arquivo vazio de jobs. Os agendamentos reais estão nos arquivos por ambiente (`schedule.dev.rb`, `schedule.prod.rb`), que precisam ser apontados manualmente no `whenever --load-file` | Se o deploy usar o `schedule.rb` default, **nenhum cron é instalado** e o recálculo diário some silenciosamente | cron do SO |
| OPS-473 | cron | **`CRONFacade.update_renegotiations_counters` — diário às 00:01** | `config/schedule.prod.rb:4-6` e `config/schedule.dev.rb:4-6`; implementação em `lib/cron_facade.rb:1-8` | `every :day, at: '00:01 am'` via `runner` | Percorre `Renegotiation.where.not(state: "Liquidado")` e chama `update_values!` em **cada uma**, que recalcula e persiste (`app/models/renegotiation.rb:83-127`): `installments_count`, `first_due_date`/`last_due_date`, `correct_value = total_debt`, somatórios das parcelas (`installments_main_value`, `installments_interest_value`, `..._with_interest`, `..._monetary_correction_value`, `..._with_interest_cm`), `main_value`, `paid_value_with_interest_cm`, `pending_main_value`, `paid_percent`, `late_payment_value`, `paid_value`, `remaining_value`, `paid_installments`, **`overdue_installments`** (parcelas com `due_date < hoje` e `is_paid = 0` — é *este* contador que muda por passagem de data), `due_installments`, `total_value_with_desagio`, `current_installment_value`, `current_value` e o **`state`** da renegociação (`Sem parcela cadastrada` / `Inconsistente` / `Pago` / `Liquidado`) | Sem tratamento de erro: **uma renegociação que exploda aborta o laço inteiro** e todas as posteriores ficam desatualizadas naquele dia. Sem lock: se rodar concorrente com edição do usuário, dá *lost update*. Roda síncrono no processo do `rails runner`, sem batching (`.each` carrega tudo) | cron do SO + PostgreSQL |
| OPS-474 | jobs/e-mail | **`NotificationFacade`** — porta de entrada assíncrona dos 3 e-mails transacionais do app base (alias global `NF`) | `lib/notification_facade.rb:1-15` | Chamado por `registrations_decorator.rb:38` e `auth_ux19_notification_decorator.rb:8,12` | Usa `Livetat::Mailer19::Mailing.delay.<método>` — ou seja, **estes 3 e-mails vão para a fila `delayed_jobs`** (diferente dos e-mails do mailer19/feedback19, ver OPS-479) | Se o worker estiver parado, o e-mail de boas-vindas (que carrega a **senha do usuário**, ver BE-480) e os de recuperação de senha **nunca saem**, sem nenhum aviso na UI | SMTP (OPS-484) |
| OPS-475 | jobs | **`TrackingFacade`** — trilha de auditoria dos jobs (`kind = "JOB"`) | `lib/tracking_facade.rb:1-296` | Chamado por OPS-465, 466, 469, 470, 471 nos momentos *request / start / success / failure* | Cria um registro `Tracking` (polimórfico em `trackable` + `trackable_parent`) com um `resume` em texto livre em pt-BR (ex.: `"A criação de templates no novo projeto #12 finalizou com sucesso, gerando 40 novos templates"`). É a **única** forma de o operador saber o que aconteceu com um job | `t.save` sem `!` — se o Tracking falhar a validação, perde-se a auditoria silenciosamente. Note os typos gravados em produção: `"A inerção do template…"` (`tracking_facade.rb:78,90,103,117`) | — |
| OPS-476 | infra | **`Procfile` / `foreman`** — processos declarados | `Procfile:1-2`; `Gemfile.linux:49` | `foreman start` | Declara apenas `server: bin/rails s -p 8192` e `assets: bin/webpack-dev-server`. **Não há processo `worker` nem `cron`** | Rodar o app pelo Procfile deixa a fila e o cron desligados: todo job fica pendente e o recálculo diário não ocorre | — |
| OPS-477 | infra | **`FileToStringDecoder`** — leitura de arquivo (BOM/UTF-8) para string, com variantes que trocam `\n` por `<br/>` e escapam via `URI::DEFAULT_PARSER.escape` | `lib/file_to_string_decoder.rb:1-24` | Utilitário chamado sob demanda (usado para embutir conteúdo de arquivo em HTML/URL) | Abre com `'r:bom|utf-8'`, lê tudo em memória, fecha | Sem `rescue`: arquivo inexistente levanta `Errno::ENOENT`. Sem limite de tamanho (carrega o arquivo inteiro na RAM) | Sistema de arquivos |
| OPS-478 | infra | **`CustomFailure`** — `Devise::FailureApp` custom | `lib/custom_failure.rb:1-16` | Qualquer falha de autenticação Devise | Se `http_auth?` responde HTTP auth; senão faz `store_location!`, seta `flash[:alert] = i18n_message` (a menos que já haja `flash[:notice]`) e **redireciona sempre para `root_path`** (nunca para `/users/sign_in`) | Em request `.json`/`.js` o usuário recebe um redirect HTML em vez de 401 — no ai9 (API) isso vira **401 JSON** | Devise |
| OPS-479 | jobs/e-mail | **ActiveJob sem adapter configurado → `deliver_later` roda in-process** | `config/environments/production.rb:32-34` (linha do `queue_adapter` **comentada**); comparação em `engines/mailer19/lib/livetat/mailer19/grind_mailer.rb:17,37,57,77,95` e `engines/feedback19/app/decorators/grind_mailer_decorator.rb:29,68` | Todos os e-mails do `GrindMailer`/feedback19 | **Bug real do legado:** `config/application.rb:106` faz `c.smtp_settings.delivering = ENV['smtp_settings_delivering']`, que devolve a **String** `"async"` (`config/application.centos.yml:11`). O código compara `delivering == :async` (**Symbol**) → sempre falso. Logo o ramo `Mailing.delay.…` (delayed_job) **nunca executa** e cai sempre em `.deliver_later`. Como não há `config.active_job.queue_adapter`, o Rails usa o adapter `:async` (thread pool **in-process**) | E-mails de feedback/convite/observador ficam numa fila em memória: **reinício do processo ou deploy perde os e-mails pendentes**, sem retentativa e sem registro. Só os 3 e-mails do `NotificationFacade` (OPS-474) são realmente duráveis | SMTP |
| OPS-480 | integração | **ReceitaWS — consulta de CNPJ** (gem `receitaws ~> 0.1.2`) | `config/initializers/receitaws.rb:1-16`; wrapper `app/helpers/cnpj_api.rb:1-5`; chamada em `app/controllers/pub/providers_controller.rb:124` (`get_api_info`) | Usuário digita CNPJ no cadastro de fornecedor → `GET` na rota `get_api_info` do console | `Receitaws::ConsultarCnpj.executar(cnpj)`; devolve o payload cru como JSON (`status: :ok`) ou `[]` com `404` se `info.blank?`. **Credencial:** `config.token = ENV['rws_api_token']` (= `SFG::Metadata::RWS_TOKEN`). `config.days = 365` (aceita dados de até 1 ano do cache da própria ReceitaWS); `config.timeout = 10s` | **Sem `rescue`**: timeout/HTTP 429 (a ReceitaWS limita a 3 req/min no plano free) sobe exceção e o endpoint retorna 500. **Sem cache local** — cada consulta bate na API. O preenchimento automático de `cnaes`/`atividades` no `create` está **comentado** (`providers_controller.rb:67-77`), ou seja, hoje o retorno só alimenta a tela, não persiste | receitaws.com.br (HTTPS) |
| OPS-481 | integração | **Geocoder — geocodificação reversa (lat/lng → endereço)** | `config/initializers/geocoding.rb:1-6`; uso em `app/models/geolocation.rb:138-159` e `:166-171` | `before_save :update_address` de **toda** `Geolocation` | `Geocoder.search([lat, lng], lookup: :google).first` preenche `address`, `full_address`, `city`, `state`, `cep`, `country`, `street_number`, `neighborhood` a partir de `address_components` (route, sublocality, administrative_area_level_2). `Geocoder::Calculations.distance_between` calcula `distance` em km. Config: `timeout: 12000`, `units: :km`, `distances: :spherical`, `language: :pt_br` | **`timeout: 12000` é interpretado em segundos pelo Geocoder → ~3h20 de timeout efetivo**: uma chamada travada trava a request web inteira (o geocode acontece **síncrono dentro do `before_save`**). Sem `rescue`: erro de API aborta o save da entidade dona da geolocalização. **Sem cache** (`Geocoder.configure` não define `cache`) | Google Geocoding API |
| OPS-482 | integração | **Google Maps JavaScript API** (mapa + `places.Autocomplete`) | `app/definitions/SFG/metadata.rb:8-10,25-27`; carregado em `app/views/pub/console/parts/my_account/parts/address/_container.js.erb:4-14,54-78` | Abertura da aba "Endereço" em Minha Conta | Carrega `https://maps.googleapis.com/maps/api/js?key=…&libraries=geometry,places` via `scriptLoader`, espera 200 ms (`setTimeout`) e instancia `google.maps.places.Autocomplete` no campo `#query`. Ao escolher um lugar, faz parse de `place.address_components` e preenche o formulário; se `!place.geometry`, mostra toast vermelho "Endereço inválido — Escolha uma das opções oferecidas durante a busca". Constantes de viés (`AUTOCOMPLETE_BIAS_LAT/LNG/RADIUS`, `GOOGLE_MAPS_DEFAULT_PLACE`) existem em `metadata.rb` mas **não são aplicadas** neste `Autocomplete` (nenhuma referência a elas fora do arquivo de definição) | Se o script não carregar (offline, key inválida, quota estourada), o autocomplete simplesmente não funciona — **sem mensagem de erro**; o `setTimeout(200)` também é uma corrida (se o Maps demorar >200 ms, `loadAutocomplete` quebra com `google is not defined`) | Google Maps JS API |
| OPS-483 | integração | **`city-state` (gem) — estados e cidades do Brasil** | `Gemfile.linux:17`; `app/controllers/pub/console_controller.rb:259-270`; `app/views/pub/console/parts/my_account/parts/address/_container.html.erb:33,41`; `app/models/geolocation.rb:67-76` | Selects encadeados País → Estado → Cidade (AJAX em `_container.js.erb:18-52`) e `Geolocation#abbreviated_state` | `CS.states(alpha2)`, `CS.cities(uf)`, `CS.transliterated_match(...)` para casar o valor vindo do parâmetro. `abbreviated_state` converte nome do estado → sigla usando `CS.states(:br)` | Biblioteca **local** (baixa/cacheia dados IBGE em arquivo). Primeira chamada pode tentar baixar dados; se falhar, os selects vêm vazios. `abbreviated_state` retorna `nil` se o nome não bater exatamente | Nenhuma em runtime (dados empacotados/cacheados) |
| OPS-484 | integração | **SMTP (Office 365) via `mailer19` / ActionMailer** | `config/application.rb:92-115`; `engines/mailer19/lib/livetat/mailer19/engine.rb:33-52`; defaults em `engines/mailer19/lib/livetat/mailer19/smtp_settings.rb:7-17`; valores em `config/application.centos.yml:3-11` (figaro) | Todo envio de e-mail | `address: smtp.office365.com`, `port: 587`, `domain/user/sender: noreply@safegold.com.br`, `authentication: login`, `enable_starttls_auto: true`, **`openssl_verify_mode: VERIFY_NONE`** (`engine.rb:43`). `config.action_mailer.raise_delivery_errors = false` (`engine.rb:39`) | **`raise_delivery_errors = false` = falha de SMTP é totalmente silenciosa** — nem exceção, nem log de erro, nem retentativa. Combinado com OPS-479, um e-mail pode sumir sem deixar rastro. `VERIFY_NONE` desabilita validação de certificado TLS (MITM). ⚠ **Segurança:** o default hardcoded em `smtp_settings.rb:13` traz a senha `lvt&ticket251` da conta `ticket@livetat.com` versionada no repositório; `config/initializers/ssl_for_win.rb:1` também força `VERIFY_NONE` no Windows | Office 365 SMTP |
| OPS-485 | integração | **DKIM — assinatura das mensagens** (gem `dkim`) | `config/application.rb:110-114`; chave em `lib/dkim_private_key.pem` | Interceptor global do ActionMailer (`ActionMailer::Base.register_interceptor(Dkim::Interceptor)`) | Assina **todo** e-mail com `domain = 'safegold.com.br'`, `selector = 'dk'`, chave privada lida de `lib/dkim_private_key.pem` no boot | O `open(...)` roda dentro do `before_initialize`: **se o .pem sumir, a aplicação não sobe**. Se o registro DNS `dk._domainkey.safegold.com.br` não bater com a chave, os e-mails passam a cair em spam sem nenhum sinal na aplicação. ⚠ **Segurança:** a chave privada DKIM está **commitada no repositório** (`lib/dkim_private_key.pem`) — deve ser **rotacionada** e movida para secret/ENV no ai9 | DNS (registro DKIM) |
| OPS-486 | integração | **Google Analytics** (`GOOGLE_ANA_APP_ID = G-7E78XXZX5X`) | `app/definitions/SFG/metadata.rb:7`; partial `app/views/livetat/analytics/_google.js.erb:1-4`; incluída em `pub/start/_index.js.erb:1`, `pub/console/_index.js.erb:1`, `pub/contracts/_index.js.erb:2`, `pub/users/sessions/_new.js.erb:1` | Render das telas públicas/console (guardado por `unless @ga_has_been_loaded`, exceto em `contracts` que carrega sempre) | Injeta o snippet clássico `analytics.js` (`https://www.google-analytics.com/analytics.js`) com o ID. **Divergência:** o ID `G-…` é formato GA4, mas o snippet carregado é o de **Universal Analytics** (`analytics.js`, descontinuado desde jul/2023) — provavelmente **não coleta nada hoje** | Bloqueador de anúncios ou script indisponível: nenhuma quebra funcional (falha silenciosa) | Google Analytics |
| OPS-487 | integração | **Redis** | `Gemfile.linux:42`; `config/cable.yml:7-10` | Só em `production` | **Único uso é o adapter do Action Cable** (`url: ENV['REDIS_URL']` com fallback `redis://localhost:6379/1`, `channel_prefix: sfg_production`). **Não** é usado para cache (`config.cache_store` não é definido em nenhum ambiente), **não** é usado para sessão (cookie store), **não** é backend de fila (a fila é a tabela `delayed_jobs`, OPS-460) | Redis fora do ar em produção: o Action Cable falha ao subir — mas como não há channel algum (OPS-488), o impacto observável hoje é nulo | Redis |
| OPS-488 | integração | **Action Cable** | `config/cable.yml:1-10`; `config/application.rb:4-12` | — | `cable.yml` existe (`async` em dev, `test` em test, `redis` em prod) mas: (a) **`action_cable/engine` não é requerido** em `config/application.rb` (o app carrega railties à la carte e omite Action Cable); (b) **não existe nenhum `*_channel.rb`, nenhum diretório `app/channels`, nenhum `createConsumer`/`ActionCable` no frontend**; (c) não há `mount ActionCable.server` em `config/routes.rb`. **Conclusão: nada é realtime no legado hoje** | N/A | Redis (só se ativado) |
| OPS-489 | integração | **Login com Facebook (`auth_omni19`)** — **desativado** | `config/application.rb:75-80`; `app/definitions/SFG/metadata.rb:4-5` | Configuração no boot | `Livetat::AuthOmni19` é configurado com `facebook_app_id = SFG::Metadata::FACEBOOK_APP_ID` = **`0`** e `facebook_app_secret = 0`; callback `#{ENV['alias']}/users/auth/facebook/callback`. **Confirmado desativado**: com app_id `0` nenhum fluxo OAuth do Facebook completa | Qualquer tentativa de login social retorna erro do provedor | Facebook Login (inativo) |
| OPS-490 | integração | **`wicked_pdf` + `wkhtmltopdf-binary` — declarados e NUNCA usados** | `Gemfile.linux:34,36` | — | Busca exaustiva (`grep -ri pdf` em `app/`, `engines/`, `config/`, `lib/`; `find -iname '*pdf*'`) retorna **zero** ocorrências fora do Gemfile: nenhum `WickedPdf`, nenhum `format.pdf`, nenhum template `.pdf.erb`, nenhuma rota `.pdf`. **O legado não gera nenhum PDF.** (Único `.pem` encontrado é a chave DKIM) | N/A | — |
| OPS-491 | integração | **`kt-paperclip ~> 7.0` + ImageMagick / `image_processing`** — motor de anexos | `Gemfile.linux:38,47`; `config/initializers/paperclip.rb:1-14`; caminho do binário em `config/application.centos.yml:2` (`paperclip_path: /usr/local/bin/identify`) | Todo `has_attached_file` (OPS-493..499) | Armazenamento **em disco local** sob `:rails_root/public/system/:attachment/:id/:basename_:style.:extension`, servido diretamente como estático (`config.public_file_server.enabled = true`, `production.rb:19`). Conversões padrão: `-set colorspace sRGB -strip -alpha remove -background white -flatten +matte` | ⚠ **Segurança:** o initializer **monkey-patcha `Paperclip::MediaTypeSpoofDetector#spoofed?` para retornar sempre `false`** (`paperclip.rb:2-8`) — a proteção contra arquivo com extensão/MIME falsificados está **desligada em todo o sistema**. Se o ImageMagick não estiver no caminho configurado, o upload falha na validação. Arquivos em `public/` são **publicamente acessíveis sem autenticação** por quem souber a URL | ImageMagick (binário do SO) |
| OPS-492 | integração | **ActiveStorage / `config/storage.yml` + ActionText** | `config/storage.yml:1-34`; `config/environments/production.rb:22`; `app/models/project.rb:60` (`has_rich_text :availability_description`) | Rich text do projeto | `config.active_storage.service = :local` → disco em `Rails.root/storage`. Todos os serviços de nuvem (S3/GCS/Azure/Mirror) estão **comentados**. ActiveStorage só é exercitado pelo ActionText (imagens embutidas na descrição de disponibilidade); os anexos "de negócio" usam Paperclip (OPS-491) | Disco cheio / volume não persistido no deploy = perda de anexos. **Convivência de dois sistemas de anexo (Paperclip + ActiveStorage) é dívida a resolver no ai9** | Sistema de arquivos |
| OPS-493 | upload | **Avatar de usuário** — `Livetat::Auth::User#avatar` | `engines/auth19/app/models/livetat/auth/user.rb:4-14,60-61`; **redefinido** em `app/decorators/models/user_decorator.rb:11-22` | Upload no perfil; também **lido pelos e-mails** (`account_invitation`, `generic_message`) | Engine define styles `thumb 80 / preview 250 / medium 500 / large 1200 / retina 1500` (jpg); o decorator do app **sobrescreve** para apenas `thumb 80 / preview 250 / original 1500`. Tipo aceito `image/*`, **limite 3 MB** (`user.rb:61`). Disco: `public/system/avatars/:id/…` | ⚠ Divergência entre engine e decorator: e-mails que pedem `sender.avatar.path` sem style podem apontar para style inexistente. `Mailing#account_invitation` faz `File.new(who_invited.avatar.path, 'rb')` (`engines/mailer19/app/mailers/livetat/mailer19/mailing.rb:31`) — **se o admin não tiver avatar, o e-mail de convite estoura `TypeError`/`Errno::ENOENT`** e (por OPS-479) some silenciosamente | ImageMagick |
| OPS-494 | upload | **Avatar de projeto** — `Project#avatar` | `app/models/project.rb:48-58,126-127` | Cadastro/edição de projeto | Styles `thumb 80 (jpg q100) / preview 250 (jpg q70) / original 1500 (jpg q70)`; `content_type` `image/*`; **limite 5 MB** | Upload não-imagem é rejeitado pela validação (mas o spoof detector está desligado, OPS-491) | ImageMagick |
| OPS-495 | upload | **Anexos de renegociação** — `RenegotiationAttachment#file` | `app/models/renegotiation_attachment.rb:4-13,38-40`; limites de UI em `app/views/pub/console/parts/renegotiations/detail/tabs/_tab_geral.html.erb:200` e `_tab_geral.js.erb:90-102` | Aba "Geral" da renegociação, upload múltiplo (remotipart/AJAX) | Sem styles (arquivo bruto), path `public/system/files/:id/:basename.:extension`. **`do_not_validate_attachment_file_type :file`** → aceita **qualquer** tipo. `title` recebe `default_file_name` se vier em branco. Limites **apenas no JavaScript**: máx. **4 arquivos** por renegociação (`SFG::Metadata::MAX_FILES_PER_RENEGOTIATION`) e **5 MB por arquivo** (`MAX_FILE_SIZE`), com mensagens "O máximo de arquivos permitido para envio é de 4 arquivos" / "O tamanho máximo de cada arquivo permitido para envio é de 5 MB"; o form fica `data-locked` quando já há 4 anexos | ⚠ **Os dois limites são só client-side** — não há `validates_attachment_size` nem contagem no model. Um POST direto sobe qualquer tamanho e qualquer quantidade. Combinado com `do_not_validate_attachment_file_type` + spoof detector desligado (OPS-491) + arquivos servidos de `public/`, é **upload arbitrário de arquivo publicamente acessível**. No ai9 estes limites **devem** virar validação de servidor | ImageMagick (opcional) |
| OPS-496 | upload | **Imagens genéricas** — `Picture#image` (polimórfico via `imageable`) | `app/models/picture.rb:6-22,25-36` | Galerias de entidades que declaram `imageable` | Styles `thumb 80 (jpg q100) / preview 250 (jpg q70) / original 1500 (jpg q50)`; `content_type image/*`; presença obrigatória. Limites **dinâmicos**: `imageable.max_image_count` (via `exceeding_max_allowed_images?`) e `imageable.max_image_size` em MB, com **fallback de 5 MB** | Usa `defined?(self.imageable.max_image_count)` — que em Ruby avalia a chamada e retorna `nil` se o método não existir, então o fallback funciona, mas de forma frágil. `image_file_size` nulo em caso de anexo ausente levanta `NoMethodError` | ImageMagick |
| OPS-497 | upload | **Logo de fornecedor** — `Provider#logo` | `app/models/provider.rb:12-24,28-29` | Cadastro de fornecedor | Styles `thumb 80 / preview 250 / medium 500 / large 1200 / retina 1500` (todos **png** q100); `content_type image/*`; **limite 1 MB** | Limite bem mais apertado que os demais (1 MB) — atenção à paridade | ImageMagick |
| OPS-498 | upload | **Logo de banco/carrier** — `Carrier#logo` | `app/models/carrier.rb:16-28,32-33` | Cadastro de carrier (banco) | Idêntico a OPS-497: 5 styles png, `image/*`, **limite 1 MB** | Idem | ImageMagick |
| OPS-499 | upload | **Identidade visual do tema** — `AppTheme#symbol_logo`, `#full_logo`, `#text_logo`, `#login_bkg_image` (4 anexos) | `app/models/app_theme.rb:6-45,48-55` | Tela de configuração de tema (white-label por usuário/manager) | Os 3 logos: styles `thumb 80 / preview 250 / original 1500` (png q100). `login_bkg_image`: `thumb/preview/medium/large/retina` (jpg, q100→50) com o convert padrão. **Todos com `image/*` e limite 5 MB**. **Consumidos pelos e-mails transacionais** (`app/decorators/models/mailer_decorator.rb:9,11,23,25,38,40` usam `app_theme.full_logo.path(:original)` e `symbol_logo.path(:original)`) | ⚠ **Se o `AppTheme` do usuário não tiver `full_logo`/`symbol_logo` carregados, os 3 e-mails de BE-480/481/482 estouram no `File.new(...)`** e (por OPS-474/479) o usuário nunca recebe as credenciais nem o link de recuperação de senha. **Risco alto de paridade** | ImageMagick |

## Backend (BE)

> Catálogo exaustivo de **cada e-mail/notificação** disparado pelo sistema. IDs no intervalo 480–499 conforme reservado.

| ID | Área | Feature / comportamento | Fonte legado (arquivo:linha) | Entradas | Saídas / efeitos | Regras & edge cases | Ambiguidade? |
| -- | ---- | ----------------------- | ---------------------------- | -------- | ---------------- | ------------------- | ------------ |
| BE-480 | e-mail | **Boas-vindas / credenciais de acesso** — assunto `"Credenciais pra acesso ao <display_name do tema, ou app.safegold.com.br>"` | Trigger: `app/decorators/controllers/registrations_decorator.rb:22-38`; fachada `lib/notification_facade.rb:2-4`; mailer `app/decorators/models/mailer_decorator.rb:3-14`; template `app/views/livetat/mailer19/mailing/send_welcome_email_to_new_generic_user.html.erb` | Hash com `responsible_formal`, `responsible_username`, `responsible_mail`, **`responsible_pass` (senha em texto puro)**, `responsible_role_type`, `manager_formal`, `manager_role_type` + `app_theme` | E-mail para `"<primeiro nome> <e-mail do novo usuário>"`, `from` = `"Safegold <noreply@safegold.com.br>"`; anexa inline `app_logo_250.png` e `mobile_app_logo_250.png` do `AppTheme`. Enfileirado via `.delay` (delayed_job, OPS-474) | Disparado só quando `@_action_name == "create"` **e** `@user.errors.blank?` **e** `params[:is_pub_domain] == 1`. Antes, faz `@user.update_theme(manager.app_theme_id)` — **se `manager_id` for inválido, `manager.app_theme_id` levanta `NoMethodError` e o cadastro quebra**. ⚠ **Segurança: a senha do usuário trafega em texto puro no corpo do e-mail** — no ai9 deve virar link de definição de senha com token expirável | **sim** — confirmar com o Vinícius se o ai9 pode trocar "senha no e-mail" por "link de primeiro acesso" (mudança de comportamento observável) |
| BE-481 | e-mail | **Recuperação de senha ("Perdeu a senha?")** | Trigger: `app/decorators/facades/auth_ux19_notification_decorator.rb:7-9` (sobrescreve `Livetat::AuthUx19::Notification.send_password_reset_instructions`); fachada `lib/notification_facade.rb:6-8`; mailer `app/decorators/models/mailer_decorator.rb:17-28`; template `app/views/livetat/mailer19/mailing/send_email_to_recovery_password_user.html.erb` | `user` (objeto `Livetat::Auth::User`) + `user.app_theme` | E-mail para `"<primeiro nome> <user.email>"`, assunto `"Perdeu a senha?"`, logos do tema inline. Via `.delay` (delayed_job) | O decorator faz `alias_method :old_send_password_reset_instructions` mas **nunca chama o original** — o fluxo padrão do auth_ux19 fica totalmente substituído | não |
| BE-482 | e-mail | **Confirmação de nova senha ("Nova senha configurada")** | Trigger: `app/decorators/facades/auth_ux19_notification_decorator.rb:11-13` (sobrescreve `notify_password_reset_success`); fachada `lib/notification_facade.rb:10-12`; mailer `app/decorators/models/mailer_decorator.rb:32-43`; template `app/views/livetat/mailer19/mailing/send_email_to_reset_password_user.html.erb` | `user` + `user.app_theme` | E-mail para `"<primeiro nome> <user.email>"`, assunto `"Nova senha configurada"`, logos do tema inline. Via `.delay` | E-mail puramente informativo (não contém link nem token) | não |
| BE-483 | e-mail | **Confirmação de recebimento de feedback** — assunto `"Obrigado, <primeiro nome> :)"` | Trigger: `engines/feedback19/app/controllers/livetat/feedback19/messages_controller.rb:91` → `engines/feedback19/lib/livetat/feedback19/notification.rb:4-8` → `engines/feedback19/app/decorators/grind_mailer_decorator.rb:4-36`; template `engines/mailer19/app/views/livetat/mailer19/mailing/confirm_feedback_to.html.erb` (+ `.text.erb`) | E-mail e nome de quem enviou a mensagem de feedback | Cria um registro `Livetat::Mailer19::Contact` e envia. Corpo varia: se `Livetat::Feedback19.is_generic_admin_active` usa `generic_team_name`; senão, `"A equipe do <app_name>"` ou `"A equipe da Safegold"` conforme o e-mail já pertença a um usuário existente. Anexa `logo.png` inline | Enviado só se `@message.errors.empty?`. Cai no ramo `deliver_later` (OPS-479) → **não durável** | não |
| BE-484 | e-mail | **Confirmação de conta ("Bem-vindo")** | Trigger: `engines/mailer19/app/controllers/livetat/mailer19/contacts_controller.rb:27-35` (rota `POST /mailer/contacts/confirm_account`); `engines/mailer19/lib/livetat/mailer19/grind_mailer.rb:26-44`; mailer `engines/mailer19/app/mailers/livetat/mailer19/mailing.rb:14-21`; template `.../mailing/confirm_account_of.html.erb` | `params[:contact][:target]` (e-mail); busca o usuário e usa `authentication_token` como token | Cria `Contact` (assunto `"Bem-vindo"`, mensagem "Sua conta foi criada com sucesso…") e envia com `logo.png` inline | **Endpoint disparado manualmente** (ferramenta de teste/reenvio no console), não automático no cadastro. Se `params[:contact][:target]` não existir na base, `user` é `nil` → `NoMethodError` (500). ⚠ Expõe o `authentication_token` do usuário por e-mail | **sim** — confirmar se esse endpoint é usado em produção ou é resquício de teste |
| BE-485 | e-mail | **Convite para a conta** — assunto `"<Nome do admin> te convidou para trabalhar em seu projeto!"` | Trigger: `engines/mailer19/app/controllers/livetat/mailer19/contacts_controller.rb:37-45` (rota `POST /mailer/contacts/account_invitation`); `engines/mailer19/lib/livetat/mailer19/grind_mailer.rb:46-64`; mailer `engines/mailer19/app/mailers/livetat/mailer19/mailing.rb:23-34`; template `.../mailing/account_invitation.html.erb` | e-mail, nome, `authentication_token`, `deadline`, `who_invited` (usuário atual) | E-mail com token de convite, **avatar do admin inline** (`avatar.png`) e `logo.png` | ⚠ Duas quebras conhecidas: (a) o controller chama `send_account_invitation_to(email, formal, token, current_user)` com **4 argumentos**, mas o método exige **5** (`email, name, token, deadline, who_invited`) → **`ArgumentError` garantido**; (b) `File.new(who_invited.avatar.path, 'rb')` falha se o admin não tiver avatar. **Este e-mail provavelmente está quebrado em produção** | **sim** — confirmar com o Vinícius se o convite por e-mail é usado; hoje o código não pode funcionar |
| BE-486 | e-mail | **Notificação: você foi incluído como observador** — assunto `"<Primeiro nome> te incluiu no <app_name>"` | Trigger: `engines/feedback19/app/controllers/livetat/feedback19/observers_controller.rb:20` → `engines/feedback19/lib/livetat/feedback19/notification.rb:10-21` → `send_generic_message`; template `engines/mailer19/app/views/livetat/mailer19/mailing/generic_message.html.erb` (sobrescrito por `engines/feedback19/app/decorators/mailing_decorator.rb:16-33`) | `observer` (e-mail, título) + `observer.user` como remetente | Corpo HTML explicando que o admin adicionou o destinatário como observador e que ele receberá e-mail a cada mensagem em aberto; inclui e-mail do admin para contestação | Enviado só se `@observer.errors.empty?`. Ramo `deliver_later` (OPS-479) | não |
| BE-487 | e-mail | **Notificação: você foi removido como observador** — assunto `"<Primeiro nome> te removeu no <app_name>"` | Trigger: `engines/feedback19/app/controllers/livetat/feedback19/observers_controller.rb:70` → `notification.rb:23-34` | `observer` + `observer.user` | Corpo HTML informando que as capacidades de observador foram desabilitadas | Idem BE-486 | não |
| BE-488 | e-mail | **Notificação aos observadores: nova mensagem de feedback** — assunto `"Nova mensagem no <app_name>"` | Trigger: `engines/feedback19/app/controllers/livetat/feedback19/messages_controller.rb:91` → `notification.rb:7` → `notification.rb:36-74` | Mensagem de feedback (`m`) e a lista `m.context.observers` | **Um e-mail por observador**, com bloco "DADOS DO USUÁRIO": nome, situação (`state.name`), e-mail, tipo (`context.name`), data/hora (`%d.%m.%Y %H:%M`) e os campos extras `hadouken`/`shoryuken` quando ativos | Pula o observador quando `o.is_intern == 0 && m.is_intern == 1` (mensagem interna só vai para observadores internos). HTML é montado por concatenação de string e marcado `html_safe` — ⚠ **a mensagem do usuário entra sem escape (risco de injeção de HTML no e-mail)** | não |
| BE-489 | e-mail | **Resposta/nova nota em um feedback** — assunto `"<Equipe> respondeu sua mensagem"` / `"Você tem uma nova mensagem #<id>"` / `"Conversa com <nome> #<id>"` | Trigger: `engines/feedback19/app/controllers/livetat/feedback19/notes_controller.rb:29` e `:36` → `notification.rb:76-106` | `note`, e-mail e nome do destinatário, `user` (autor) | Corpo com trecho da nota e link `#{ENV['alias']}/feedbacks/messages/<public_token>`. O assunto/texto muda conforme `note.sender?(user)` e `Livetat::Feedback19.is_generic_admin_active` | Enviado só se `@note.errors.empty?`. O link usa `public_token` — **acesso à conversa sem autenticação por quem tiver a URL** | não |
| BE-490 | e-mail | **`generic_message_with_link` — mailer definido sem nenhum chamador** | `engines/mailer19/lib/livetat/mailer19/grind_mailer.rb:84-100`; mailer `engines/mailer19/app/mailers/livetat/mailer19/mailing.rb:50-65`; template `.../mailing/generic_message_with_link.html.erb` | e-mail, nome, assunto, mensagem, remetente, link, título do link | Enviaria mensagem genérica com um CTA | **Código morto**: grep em `app/`+`engines/` não encontra nenhuma chamada a `send_generic_message_with_link`. Não migrar sem confirmação | **sim** — confirmar se pode ser descartado |

## Frontend (FE)

| ID | Área | Tela / fluxo | Fonte legado | Estados | Interações | Regras de UI | Ambiguidade? |
| -- | ---- | ------------ | ------------ | ------- | ---------- | ------------ | ------------ |
| FE-480 | polling | **`PollingManager` — helper genérico de polling** (`setInterval`) | `app/frontend/js/polling_helper.js:1-97`; exposto global em `app/frontend/js/index.js.erb:4,31` | período default **5000 ms**; `start/stop/reset/execute`, listeners `didStart/didStop/didExecute` | Executa um callback a cada N ms | Único consumidor é FE-481. **→ converter para Action Cable no ai9** (helper inteiro deve deixar de existir) | não |
| FE-481 | polling | **Monitor do usuário no widget de navegação** — `GET /users/<id>.json` a cada **1000 ms** | `app/views/pub/base/nav/user/_widget.js.erb:1-20` | `w.data("user-monitor")` | Instancia o `PollingManager` com período de 1 s batendo em `users/{id}.json` — o `success` é **vazio** (não faz nada com a resposta) | ⚠ **O `.start()` está comentado** (`_widget.js.erb:18-19`: `// Desativado na Versão 2.0 - Revisão 2 - Build #5695`) → **o polling está desligado hoje**; o objeto é criado mas nunca inicia. **→ não reintroduzir; se a necessidade voltar, converter para Action Cable no ai9** | não |
| FE-482 | jobs | **Visibilidade do progresso de job para o usuário** (o "progresso" do `progress_job`) | `app/views/pub/console/parts/projects/list/_widget.html.erb:1,10-19`; `app/views/pub/console/parts/projects/detail/_body.html.erb:12-15`; `app/models/project.rb:145-147,672-684` | `has_ongoing_job?` = `!job_id.blank?` | Na **lista de projetos**: ícone de alarme vermelho (`zmdi-alarm`) que abre um dropdown com "Existe uma atualização em andamento sobre os dados financeiros da projeto: **<X>%**" (`live_progress_percent` = `progress_current/progress_max*100`, arredondado em 2 casas; retorna 100 se `job` for nil e 0 se `progress_max == 0`). No **detalhe do projeto**: subtítulo vermelho "Atualização em andamento" (sem percentual) | ⚠ **O percentual é renderizado no servidor e NUNCA se atualiza sozinho** — o usuário precisa recarregar/renavegar para ver o progresso mudar. Não há polling nem websocket. **→ no ai9 isto vira Action Cable** (broadcast de progresso do job Sidekiq para o canal do projeto). ⚠ Só `Project` implementa `has_ongoing_job?`; os widgets de receivables/renegotiations/risk_operations/structured_operations/companies/availability_templates/project_guarantees leem `w.data("ongoing")` (`.../list/_widget.js.erb:10`) mas **o atributo `data-ongoing` não é emitido nesses HTMLs** → sempre `undefined`, bloco morto | **sim** — confirmar se o alerta de "atualização em andamento" deve existir para as outras entidades (parece intenção não concluída) |
| FE-483 | integração | **Autocomplete de endereço (Google Places) + selects encadeados Estado/Cidade** | `app/views/pub/console/parts/my_account/parts/address/_container.js.erb:1-80`; `.../address/_container.html.erb:33,41`; endpoints `app/controllers/pub/console_controller.rb:259-270` | script Maps carregado sob demanda; `#query`, `#country_select`, `#state_select`, `#city_select`, `#user_info_delivery_location_cep` (máscara `99999-999`) | Ao trocar país → AJAX `pub_console_state_select_path.js`; ao trocar estado → AJAX `pub_console_city_select_path.js`; ao escolher um lugar no autocomplete, preenche o formulário com `place.address_components` invertidos | Endereço sem `geometry` → toast vermelho "Endereço inválido / Escolha uma das opções oferecidas durante a busca". Ver OPS-482 (corrida do `setTimeout(200)`) | não |
| FE-484 | upload | **Upload de anexos da renegociação (limites na UI)** | `app/views/pub/console/parts/renegotiations/detail/tabs/_tab_geral.html.erb:200`; `_tab_geral.js.erb:90-102` | `data-locked` no wrapper quando `attachments.count >= 4` | Seleção múltipla de arquivos (remotipart) | Bloqueia envio e mostra toast de erro: "O máximo de arquivos permitido para envio é de **4 arquivos**" (título "Limite excedido") e "O tamanho máximo de cada arquivo permitido para envio é de **5 MB**". **Validação existe apenas aqui** — ver OPS-495 | não |

## Data (DB)

| ID | Área | Tabela / model | Fonte | Colunas / tipos | Relações & índices | Volume aprox. | Notas de migração |
| -- | ---- | -------------- | ----- | --------------- | ------------------ | ------------- | ----------------- |
| DB-460 | jobs | **`delayed_jobs`** (fila + progresso) | `engines/mailer19/db/migrate/20170505110720_create_mailer_delayed_jobs.rb:4-18`; `.../20170505114146_add_progress_to_mailer_delayed_jobs.rb:29-33` | `priority:int(0)`, `attempts:int(0)`, `handler:text` (YAML do objeto job), `last_error:text`, `run_at`, `locked_at`, `failed_at`, `locked_by:string`, `queue:string`, `progress_stage:string`, `progress_current:int(0)`, `progress_max:int(0)`, timestamps | Índice `delayed_jobs_priority (priority, run_at)`. Referenciada por `Project#job` e `ProjectAvailabilityTemplate#job` via `belongs_to :job, class_name: Delayed::Job.name` (`app/models/project.rb:4`, `app/models/project_availability_template.rb:7`) | Baixo (fila transitória) + jobs falhos acumulados (`destroy_failed_jobs = false`) | **No ai9 a fila vira Sidekiq/Redis.** Atenção: `projects.job_id` e `project_availability_templates.job_id` são **FKs para a tabela de fila** — no ai9 esse acoplamento deve ser substituído por um `job_state`/`job_progress` próprio da entidade (ou tabela de execuções), pois o Sidekiq não tem registro relacional. Jobs pendentes na migração de dados **não** são portáveis (handler YAML de classes Rails 6) |
| DB-461 | jobs | **`trackings`** (auditoria de jobs e ações, `kind = "JOB"`) | `db/migrate/20180724162731_create_trackings.rb:3-18`; escrita por `lib/tracking_facade.rb` | `user_id:int`, `target_id:int`, `trackable_type:string`, `trackable_id:int`, `resume:string` (texto livre pt-BR), `type:string` (STI), `target_group_id:int`, `target_group_type:string`, `trackable_parent_id:int`, `trackable_parent_type:string`, `kind:string`, timestamps | Polimórfico duplo (`trackable` + `trackable_parent`), **sem índices declarados na migration** | Cresce com o uso (4 registros por execução de job) | `resume` é string (limite 255 no PG) e recebe `"… falhou com o erro: #{e}"` — **mensagens de erro longas são truncadas ou falham**. No ai9 usar `text` e campos estruturados (evento, entidade, payload JSON) em vez de frase montada |
| DB-480 | integração | **`geolocations`** (destino do Geocoder) | `db/migrate/20160302002809_create_geolocations.rb:3-24`; model `app/models/geolocation.rb` | `geolocatable_type/id` (polimórfico), `lat`/`lng`/`ref_lat`/`ref_lng` `decimal(10,6)`, `city`, `state`, `cep`, `neighborhood`, `country`, `street_number:int`, `address`, `complement`, `full_address`, `auto_loading:int(0)`, `distance:float`, `distance_unity:string`, timestamps | Índice em `geolocatable` (type+id) | Uma por entidade geolocalizada | `auto_loading = 1` faz o `before_save` **apagar** todos os campos de endereço e re-geocodificar; `auto_loading = 0` preserva. Validação `check_uniqueness` (`geolocation.rb:174-176`) roda **em todo save** e só barra na criação. No ai9, mover o geocode para **job assíncrono** (hoje é síncrono no save, OPS-481) |
| DB-481 | integração | **`livetat_mailer_contacts`** (registro de todo e-mail enviado pelo mailer19) | `engines/mailer19/db/migrate/20160409121840_create_livetat_mailer_contacts.rb`; `.../20170519223014_*` e `20170519223026_*` (remove/readiciona `message`); model `engines/mailer19/app/models/livetat/mailer19/contact.rb` | `sender`, `target` (e-mail destino), `target_name`, `subject`, `message`, timestamps | Listado paginado em `Livetat::Mailer19::ContactsController#index` (6 por página, offset em blocos de 5 — ⚠ **paginação inconsistente: `limit(6)` com `offset(offset*5)`**) | Cresce com cada e-mail enviado | É o **único log de e-mails** do sistema; **não registra sucesso/falha de entrega** (só a intenção). No ai9, considerar tabela de `notifications` com status de entrega |
| DB-482 | uploads | **Colunas Paperclip nos models com anexo** | `app/models/{project,picture,provider,carrier,app_theme,renegotiation_attachment}.rb`; `app/decorators/models/user_decorator.rb:11`; `engines/auth19/app/models/livetat/auth/user.rb:4` | Para cada anexo: `<attr>_file_name:string`, `<attr>_content_type:string`, `<attr>_file_size:int`, `<attr>_updated_at:datetime` | 11 anexos no total: `users.avatar`, `projects.avatar`, `renegotiation_attachments.file`, `pictures.image`, `providers.logo`, `carriers.logo`, `app_themes.{symbol_logo, full_logo, text_logo, login_bkg_image}` | Proporcional ao nº de entidades | **Migração de arquivos**: os binários vivem em `public/system/:attachment/:id/…` no disco do servidor legado — precisam ser copiados e re-anexados. No ai9 padronizar em **ActiveStorage** e sair de `public/` (hoje qualquer anexo é acessível sem autenticação, ver OPS-491/495) |

## Cobertura

**Arquivos lidos (todos no legado `../sfg`, somente leitura):**
- `lib/`: `cron_facade.rb`, `runner.rb`, `notification_facade.rb`, `tracking_facade.rb`, `file_to_string_decoder.rb`, `custom_failure.rb`, `create_global_template_for_project_job.rb`, `insert_global_template_on_projects_job.rb`, `insert_projects_on_default_user_job.rb`, `insert_default_user_on_project_job.rb`, `project_availability_template_activate_job.rb`, `project_availability_template_deactivate_job.rb`, `project_availability_template_removal_job.rb` (+ existência de `dkim_private_key.pem`)
- `config/`: `application.rb`, `schedule.rb`, `schedule.dev.rb`, `schedule.prod.rb`, `cable.yml`, `storage.yml`, `application.centos.yml`, `credentials_by_environment.rb`, `routes.rb` (mounts), `environments/{production,development,test}.rb`, `initializers/{delayed_job_config,geocoding,paperclip,receitaws,dev,content_security_policy,ssl_for_win,mime_types,date_overload}.rb`
- Raiz: `Gemfile.linux`, `Gemfile.prod` (diff), `Procfile`, `bin/delayed_job`, `README.md`
- `app/`: `definitions/SFG/metadata.rb`, `helpers/cnpj_api.rb`, `models/{geolocation,project,picture,provider,carrier,app_theme,renegotiation_attachment,global_availability_template,renegotiation,project_availability_template}.rb`, `controllers/pub/{project_availabilities,providers,projects,console}_controller.rb`, `decorators/models/{user_decorator,mailer_decorator}.rb`, `decorators/controllers/registrations_decorator.rb`, `decorators/facades/auth_ux19_notification_decorator.rb`
- Views: `pub/base/nav/user/_widget.js.erb`, `pub/console/parts/projects/{list/_widget.html.erb,list/_widget.js.erb,detail/_body.html.erb}`, `pub/console/parts/receivables/list/_widget.js.erb`, `pub/console/parts/my_account/parts/address/{_container.html.erb,_container.js.erb}`, `pub/console/parts/renegotiations/detail/tabs/{_tab_geral.html.erb,_tab_geral.js.erb}`, `livetat/analytics/_google.js.erb`, `livetat/mailer19/mailing/*` (listagem)
- Frontend: `app/frontend/js/polling_helper.js`, `app/frontend/js/index.js.erb`
- Engines: `mailer19/` (mailer, grind_mailer, smtp_settings, engine, configuration, contacts_controller, routes, migrations), `feedback19/` (`grind_mailer_decorator.rb`, `mailing_decorator.rb`, `lib/livetat/feedback19/notification.rb`, `{messages,notes,observers}_controller.rb`), `auth19/app/models/livetat/auth/user.rb`
- `db/migrate/`: `20180724162731_create_trackings.rb`, `20160302002809_create_geolocations.rb`, migrations do `delayed_jobs`/`progress`

**Lacunas / dúvidas para o Phase 2:**
1. **`send_account_invitation_to` é chamado com 4 argumentos e exige 5** (`contacts_controller.rb:40` vs `grind_mailer.rb:46`) → `ArgumentError` certo. Confirmar se o convite por e-mail (BE-485) existe de fato em produção ou se é fluxo morto.
2. **Bug do `delivering`**: `"async"` (String) nunca é igual a `:async` (Symbol) → o ramo `.delay` do mailer19/feedback19 é inalcançável e os e-mails saem por `deliver_later` em adapter in-process (OPS-479). Decidir se o ai9 replica o comportamento **observado** (não durável) ou corrige para Sidekiq (recomendado — é claramente um bug).
3. **Cron**: `config/schedule.rb` (o arquivo default do `whenever`) **não declara nenhuma tarefa**; só `schedule.dev.rb`/`schedule.prod.rb` têm o job diário. Não há script de deploy no repo — confirmar como o crontab é instalado hoje em produção (`whenever --load-file config/schedule.prod.rb`?) e se o job realmente roda.
4. **Nenhum processo de worker no `Procfile`** e nenhum supervisor: confirmar como o `bin/delayed_job` é iniciado/mantido em produção hoje (systemd? manual? `Runner`?).
5. **`Runner` é código morto** (`lib/runner.rb`, zero chamadores) — confirmar descarte. Não portar (contém `kill -9`/`rm -rf` com interpolação).
6. **`wicked_pdf`/`wkhtmltopdf-binary` declarados mas sem nenhum uso**: **o legado não gera PDF algum**. Confirmar com o Vinícius se existe geração de PDF esperada pelo cliente (relatório de renegociação?) que nunca foi implementada — nesse caso é *feature nova*, não paridade.
7. **`generic_message_with_link` (BE-490) sem chamadores** — confirmar descarte.
8. **`AUTOCOMPLETE_BIAS_*` e `GOOGLE_MAPS_DEFAULT_PLACE` não são usados em lugar nenhum** fora de `metadata.rb`: o "viés geográfico" do autocomplete descrito no briefing **não está implementado** no legado. Decidir se o ai9 implementa (melhoria) ou mantém sem viés (paridade).
9. **Google Analytics**: ID no formato GA4 (`G-…`) sendo carregado com o snippet Universal Analytics descontinuado (OPS-486) — provavelmente não coleta nada. Confirmar se o cliente ainda quer GA e migrar para `gtag.js`.
10. **`has_ongoing_job?` só existe em `Project`**, mas 7 widgets de lista leem `data-ongoing` (FE-482). Confirmar se o alerta de job em andamento deveria valer para as outras entidades.
11. **Não há `config.cache_store`** em nenhum ambiente → o Redis do Gemfile só serve ao Action Cable (que não tem channels). Confirmar se algum cache é esperado.
12. Achados de **segurança** a NÃO propagar (registrar no `improvements-log.md`): senha SMTP hardcoded (`smtp_settings.rb:13`), token da ReceitaWS versionado (`application.centos.yml:12`), **chave privada DKIM commitada** (`lib/dkim_private_key.pem`), `MediaTypeSpoofDetector` neutralizado (`initializers/paperclip.rb:2-8`), `openssl_verify_mode: VERIFY_NONE` (`engine.rb:43` e `ssl_for_win.rb:1`), senha do usuário em texto puro no e-mail de boas-vindas (BE-480), anexos servidos de `public/` sem autenticação (OPS-491/495), limites de upload apenas client-side (OPS-495).

**Polling encontrado (a converter para Action Cable):**
- **FE-480** — `app/frontend/js/polling_helper.js:47` (`setInterval`), helper `PollingManager` com período default de 5 s. → converter para Action Cable no ai9 (o helper deve deixar de existir).
- **FE-481** — `app/views/pub/base/nav/user/_widget.js.erb:7-16`: polling de 1 s em `GET /users/{id}.json`. **Já está desativado** no legado (`.start()` comentado, "Desativado na Versão 2.0 - Revisão 2 - Build #5695") e o callback de sucesso é vazio. → **não reintroduzir**; se a necessidade voltar, Action Cable.
- **FE-482** — *quase-polling por recarga manual*: o progresso de job (`live_progress_percent`) é renderizado server-side e só muda quando o usuário recarrega a página. → **converter para Action Cable no ai9** (broadcast de progresso do job Sidekiq).
- Nenhum outro polling de aplicação foi encontrado: as demais ocorrências de `setInterval`/`setTimeout` estão em bibliotecas de terceiros (`jquery.mobile.js` em `app/frontend/{site_gems,pub_gems}/js/` e `engines/navkit/.../jquery.mobile.min.js`) e não refazem requisições da aplicação.
- Não há long-poll, nem `meta refresh`, nem `location.reload()` periódico.
