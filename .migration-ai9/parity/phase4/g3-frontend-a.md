# Phase 4 — lote `g3-frontend-a` (130 itens)

Cada linha e um item ABERTO do razao. Preencha `veredito` com `verified`, `blocked` ou `dropped` e escreva a EVIDENCIA — arquivo:linha do ai9, ou o comando que provou. Sem evidencia o item continua aberto.

| ID | estado atual | feature | veredito | evidencia |
| --- | --- | --- | --- | --- |
| FE-004 | migrated | Painel Esqueceu a senha (recuperação) | verified | Nao ha senha no ai9 (DEC-14: "o produto nao tem senha"), entao o painel de recuperacao vira "pedir novo codigo": `frontend/src/features/auth/AuthFlow.tsx:14-24` (o comentario nomeia os tres paineis do legado que somem) + `features/auth/MagicLogin.tsx:147-203` (pedir codigo) e `CodeValidation.tsx`. Teste: `src/features/auth/__tests__/AuthFlow.duas-abas.test.tsx` (5 casos, passou). |
| FE-005 | migrated | Login com Facebook | verified | `frontend/src/features/auth/MagicLogin.tsx:215-218` — botao Facebook renderizado no rodape "Ou entre com", chamando `loginWithFacebook`, com `authService.getFacebookAuthUrl` em `lib/api/auth.ts:165` e retorno em `features/auth/OAuthCallbackPage.tsx:14,50`. No legado (`_sign_in.js.erb:22-52`) so existia o handler: o botao nao existia no HTML. Teste: `features/auth/__tests__/MagicLogin.test.tsx`. |
| FE-006 | migrated | Aviso "você precisa entrar na sua conta antes de acessar essa página" quando há redirect_url | verified | `frontend/src/features/auth/LoginNotice.tsx:52-60` renderiza o aviso do `?next=`; `safeNextPath` (`:71-89`) so aceita caminho same-origin. No legado o container `.warning_message` estava comentado (`_sign_in.html.erb:9-10`) e a mensagem nunca aparecia. Teste: `src/features/auth/__tests__/LoginNotice.test.tsx` (8 casos, passou). Divergencia de texto registrada: o ai9 diz "Depois de entrar voce volta para X" em vez da frase do legado. |
| FE-007 | migrated | Página de definir nova senha (versão do app, com tema) | verified | Nao ha "definir nova senha" porque nao ha senha (DEC-14). O equivalente e a pagina de entrada por link: `frontend/src/features/auth/MagicLinkCallbackPage.tsx:1-62`, com tema e marca via `components/brand/Logo` (`:44`). Teste: `src/features/auth/__tests__/MagicLinkCallback.test.tsx` (2 casos, passou). |
| FE-010 | migrated | Estado "link de recuperação expirado" | verified | `frontend/src/features/auth/MagicLinkCallbackPage.tsx:33-35` grava "Link expirado ou ja utilizado. Solicite um novo acesso." e `:45-57` renderiza o estado com link "Voltar ao login". No legado o estado era inalcancavel (500 por `user.app_theme` fora da guarda). Teste: `src/features/auth/__tests__/MagicLinkCallback.test.tsx`. |
| FE-014 | migrated | Paginação da lista de usuários | verified | `frontend/src/app/pages/UsersPage.tsx:681-690` monta o `PaginationPill`; `components/ui/PaginationPill.tsx:165-221` tem primeira/anterior/proxima/ultima com `disabled={atFirst}`/`disabled={atLast}` e o seletor "por pagina" (`:183-193`) — os mesmos controles do legado (`_body.js.erb:104-226`). Testes: `src/app/pages/__tests__/UsersPage.dados-reais.test.tsx` e `components/ui/__tests__/Pagination.test.tsx`. |
| FE-016 | migrated | Dropdown de ações do widget de usuário | verified | `frontend/src/app/pages/UsersPage.tsx:752-802` (`MenuActions`): Visualizar sempre; Editar/Bloquear-Desbloquear/Remover sob `canWrite`; "Ver como" sob `canImpersonate` (`:786-788`), com `ReasonDialog` (`:693-700`). Corrige o defeito do legado ("Ver como" para qualquer papel). Teste: `src/app/__tests__/permissoes-gate.test.tsx` — "OG e Admin sim; Gerente e Colaborador nao" (passou). |
| FE-017 | migrated | Copiar identificador do usuário para a área de transferência | blocked | O `identifier` e EXIBIDO (`frontend/src/app/pages/UsersPage.tsx:536-540` e `app/pages/users/UserDetailPage.tsx:200`) mas nao ha acao de copiar. `grep -rn "clipboard.writeText" frontend/src` retorna so `ProfilePage.tsx:843` (conta propria, FE-031), `WhatsappPage.tsx:386` e `CodeValidation.tsx:40`. FALTA: acao de copiar o `identifier` de outra conta no card da lista e no card do detalhe, com o retorno visivel que o legado dava (`list/_widget.js.erb:163-167`). |
| FE-018 | migrated | Botão "Cadastrar" e abertura do helper de criação | verified | Botao em `frontend/src/app/pages/UsersPage.tsx:443-449`; deep-link por rota filha de verdade (`/users/new` e `/users/:id/edit`) em vez do `history.replaceState` do legado. Teste: `src/app/pages/__tests__/UsersPage.dados-reais.test.tsx` — "/users/new abre o drawer de criacao" e "/users/:id/edit abre o drawer PREENCHIDO" (passaram). |
| FE-019 | migrated | Formulário de cadastro/edição de usuário (console) | blocked | O drawer existe (`frontend/src/app/pages/UsersPage.tsx:573-679`) com UM campo de papel (`:625-641`, fecha o defeito do `hidden`+`select` do legado) e sem senha (DEC-14, `:669-676`; teste "o drawer NAO tem campo de senha"). FALTA o campo "Membro padrao": o legado o oferecia a OG/Admin (`helper/_body.html.erb:1-76`), o ai9 so o EXIBE como selo (`UsersPage.tsx:531`, `UserDetailPage.tsx:143`) e o endpoint nao aceita o parametro (`backend/app/controllers/api/v1/users.rb:85-101` e `:231-260`), apesar de a flag disparar o `DefaultMemberJob` (`backend/app/models/user.rb:378-379`). Nenhuma DEC autoriza a remocao (busca por "membro padrao"/"default_member" em `decisions.md` e `removed-features.md` = vazio). `username` (DEC-45) tambem nao e editavel no formulario. |
| FE-021 | migrated | Upload e pré-visualização de avatar no formulário | blocked | Upload existe em `frontend/src/app/pages/UsersPage.tsx:409-436` (campo em `:588-621`, limite de 3 MB dito na tela). FALTA o caso de CRIACAO: `:413-416` recusa com "Salve o usuario antes de enviar a foto." porque o POST do avatar precisa de `editingUser.id`. No legado o `input[type=file]` fazia parte do proprio form (`helper/_body.js.erb:78-103`), com previa local por `FileReader`, e a foto ia junto no cadastro. Falta tambem a previa local antes do envio. |
| FE-022 | migrated | Detalhe do usuário — abas Geral / Projetos | verified | `frontend/src/app/pages/users/UserDetailPage.tsx:147-180` — `TabsList` (Geral/Projetos/Permissoes) e `TabsContent` saem do MESMO bloco, sem condicao de visibilidade propria; botao "Contas" de voltar em `:119-122`. Fecha o defeito do legado (aba na linha 14 e conteudo na linha 22 com condicoes diferentes). Teste: `src/app/__tests__/permissoes-gate.test.tsx`. |
| FE-023 | migrated | Detalhe do usuário — card "Dados do usuário" | verified | `frontend/src/app/pages/users/UserDetailPage.tsx:183-224` (`AbaGeral`/`DetailList`) com Nome, Tipo, CPF/CNPJ, E-mail, Codigo, Usuario, Telefone, Criada em, Ultimo acesso, Verificacao e Bloqueio; "Editar" gateado por `podeEscrever` (`:124-129`), nao mais por `may_delete_users?`. DEFEITO REGISTRADO: o telefone sai cru da entity (`backend/app/controllers/api/entities/user.rb:8`) e a tela nao formata — o legado mostrava `+55 (48) 99999-9999`; nao existe formatador de telefone no front (busca por `formatPhone`/`formatTelefone` = vazio). |
| FE-024 | migrated | Detalhe do usuário — painel de permissões individuais com toggles | verified | `frontend/src/app/pages/users/UserDetailPage.tsx:297-425` (`AbaPermissoes`): catalogo vindo do servidor via `usersApi.permissions`, `PermissionControl` decidindo toggle x campo numerico pelo `kind` (`components/PermissionControl.tsx`), toasts de sucesso e erro (`:340-364`), estado `HIERARCHY_LOCKED` proprio (`:372-381`) e a nota das 7 de 17 (DEC-108, `:418-424`). Teste: `src/app/__tests__/permissoes-gate.test.tsx` (passou). |
| FE-025 | migrated | Detalhe do usuário — aba Projetos: associar/desassociar projetos | verified | `frontend/src/app/pages/users/UserDetailPage.tsx:227-292` — lista paginada e escopada (`usersApi.memberships` + `PaginationPill` em `:272-281`) com carregando/vazio/erro. Sem associar/desassociar por decisao assinada DC-18 (`.migration-ai9/map/projects-cadastros.md:598`): a aba e informativa, e conceder/revogar e da aba Membros do projeto sob as tres condicoes do DEC-18.5 — a tela diz isso em `:284-289`. O fluxo de conceder/revogar foi conferido em FE-096/FE-097. |
| FE-028 | migrated | Console → Minha conta: perfil (avatar, nome, sobrenome, CPF, e-mail, código, telefone) | verified | `frontend/src/app/pages/ProfilePage.tsx` — avatar (`:565-597`), Codigo em leitura com copiar (`:604-609`), E-mail em leitura (`:613-618`), Nome (`:652-658`), WhatsApp (`:668-672`), CPF/CNPJ (`:674-683`), mais os campos estendidos do DEC-74 (genero em `:686-700`, endereco em `:759-765`). Divergencia registrada: "sobrenome" do legado virou um unico campo "Nome" (a entity expoe so `name`). |
| FE-029 | migrated | Minha conta — validação de CPF em tempo real | blocked | Nao ha validacao de CPF EM TEMPO REAL. `frontend/src/app/pages/ProfilePage.tsx:459-464` (`trocar`) so aplica a mascara (`mascararDocumento`, `:320-333`) e limpa o erro; a checagem de digito (`cpfValido`, `:336-345`) roda so no `save` (`:493-500`). O endpoint existe e nao e chamado por tela nenhuma: `frontend/src/lib/api/endpoints.ts:271-274` (`validateCpf`) — a busca por `validateCpf` no `src` so acha a definicao. FALTA: disparar a consulta ao completar 11 digitos e mostrar o retorno ("CPF valido" / mensagem do servidor) ao lado do campo, como `essential/_container.js.erb:19-62`. |
| FE-030 | migrated | Minha conta — máscara e bloqueio do telefone | verified | Mascara dinamica 8/9 digitos pelo `PhoneInputGroup` (`frontend/src/app/pages/ProfilePage.tsx:668-672`; componente em `src/components/PhoneInputGroup.tsx`). O bloqueio do legado (`is_phone_checked` deixava o campo `readonly` para sempre) nao e replicado de proposito — DEC-74, com o motivo escrito em `ProfilePage.tsx:660-668`: telefone e canal de login, e trava-lo deixaria quem trocou de numero sem acesso. |
| FE-031 | migrated | Minha conta — copiar "Código" (identifier) | verified | `frontend/src/app/pages/ProfilePage.tsx:604-609` (`CampoIdentidade ... copiavel`) e o botao em `:855-863` chamando `navigator.clipboard.writeText` (`:843`) com toast "Codigo copiado" (`:845`) e tratamento de clipboard negado (`:846-850`). Corrige o defeito do legado (campo dentro de `fields_for :info`, que nao tem coluna `identifier`). |
| FE-033 | migrated | Minha conta — remover a conta | verified | `frontend/src/app/pages/ProfilePage.tsx:939-1050` (`EncerrarContaSection`): confirmacao em duas etapas com codigo de 6 digitos enviado ao destino cadastrado (`:955-965`), botao destrutivo so com `codigo.length === 6` (`:1044`) e `usersApi.delete(user.id, codigo)` (`:976`). No legado nao havia modal e o gate era `may_create_users?` (permissao de CRIAR). |
| FE-034 | migrated | Minha conta — aceite de Termos de Uso / Política de Privacidade | verified | `frontend/src/components/contracts/MyTermsSection.tsx` montado em `app/pages/ProfilePage.tsx:775`: a caixa nasce DESMARCADA (`:33`), habilita o botao, e o aceite e gravado pelo servidor (`contractsApi.acceptAllPending`, `:38`). Fecha os tres defeitos do legado (pre-marcada, nunca lida por controller, e o `if (jbb.val()==false)` que nunca era verdadeiro). Historico com `implicit_legacy` por DEC-66 (`:24-29`). Teste: `src/components/contracts/__tests__/termsBanner.test.tsx`. |
| FE-035 | migrated | Minha conta — salvamento automático via fila do rodapé | verified | Sem autosave por tecla, por decisao assinada DC-23 (`.migration-ai9/map/projects-cadastros.md:603`): `frontend/src/app/pages/ProfilePage.tsx:783-788` monta o `FloatingActions`, que so aparece com alteracao (`showActions` + `dirtyFields`, `:429-449`), e o `save` (`:524-551`) manda so os campos alterados em UM `authApi.updateMe(dirtyFields)` (`:530`). Cancelar restaura o original (`:466-473`). |
| FE-040 | migrated | Projeto → adicionar membro (autocomplete de usuários) | verified | `frontend/src/app/pages/projects/ProjectDetailPage.tsx:317-346` — `Autocomplete` sobre `membershipsApi.candidates(termo)` (`:275-279`), que ja exclui quem participa (fecha o BE-044 do legado, dito na tela em `:334` e `:350-353`); toast de sucesso/erro em `:281-291`. O campo some para quem nao pode adicionar (`podeGerir`, `:318`). |
| FE-041 | migrated | Projeto → lista de membros e remoção | verified | `frontend/src/app/pages/projects/ProjectDetailPage.tsx:399-430` (lista) e `:363-394` (telefone). As tres condicoes do DEC-18.5 estao espelhadas em `motivoDeNaoRemover` (`:302-309`) e o lugar do botao e ocupado pela razao (`:415-418`) em vez de a acao sumir calada. Mostra o papel da MEMBERSHIP (`m.role`/`MEMBERSHIP_ROLE_LABELS`, `:409-411`), corrigindo o legado, que mostrava o papel global. |
| FE-042 | migrated | Feedback de erros nas operações de usuário (console) | verified | Toasts de sucesso E de erro: `frontend/src/app/pages/UsersPage.tsx:377-390` (criar/editar, com erro por campo em `fieldErrors` e o resto em toast), `:158-171` (bloquear/desbloquear) e `:433` (avatar). Erro por campo aparece embaixo do campo com `aria-invalid` (`:643-667`). Teste: `src/app/pages/__tests__/UsersPage.dados-reais.test.tsx` — "campo obrigatorio reprova SOZINHO, com a mensagem embaixo do campo" (passou). |
| FE-043 | migrated | Feedback de erros nas operações de permissão | verified | `frontend/src/app/pages/users/UserDetailPage.tsx:340-364` — toast de sucesso com o texto certo por tipo (DEC-108: um teto diz o numero; esvaziar diz "Excecao de teto removida — volta a valer o do papel") e toast de erro distinguindo `HIERARCHY_LOCKED`. Na tela por papel: `app/pages/users/PermissionsPage.tsx:149-165`. No legado o ramo `else` era vazio (sucesso invisivel). |
| FE-056 | migrated | Helper (painel lateral) de criar/editar Empresa | verified | `frontend/src/app/pages/projects/CompaniesPage.tsx:103-114` — painel lateral (`CatalogScreen` -> `SideDrawer`, `app/pages/catalogs/CatalogScreen.tsx:513-533`) com o UNICO campo do legado, "Razao social", placeholder e dica de unicidade. Titulo do painel e rotulo de criar vem de `texts` (`:52-60`). Some o texto herdado "Essa construtora nao pode ser alterada". Teste: `src/app/pages/catalogs/__tests__/CatalogScreen.test.tsx` (4 estados + escopo de projeto, passou) e `src/app/pages/projects/__tests__/projetoEmpresas.test.tsx`. Sem polling e sem cor literal (teste "nenhuma tela escreve cor literal"). |
| FE-057 | migrated | Exclusão de Empresa a partir da lista | verified | Confirmacao explicita em `frontend/src/app/pages/catalogs/CatalogScreen.tsx:535-561` ("Excluir a empresa «X»?" + "A exclusao e definitiva. Se houver algum vinculo, o servidor recusa e nada e apagado"), acionada por `:301` / `:417`. A contagem que esconde a acao e a MESMA que o servidor usa para responder 422 (`CompaniesPage.tsx:92-99`), fechando o D-24 (o legado respondia `:ok` sempre). Teste: `CatalogScreen.test.tsx` — "COM vinculo: o botao some E a razao fica visivel no lugar dele" e "excluir chama o servidor e recarrega a lista" (passaram). |
| FE-064 | migrated | Formulário de Portador (helper lateral) | verified | `frontend/src/app/pages/catalogs/CarriersPage.tsx:146-303` — todos os campos do legado: Razao social (`:157-165`), Descricao (`:167-175`), Agente financeiro (`:177-188`), Grupo (`:190-198`), Codigo COMPE como TEXTO (DC-12, `:200-207`), Cidade e UF (`:209-226`), Patrimonio liquido (`MoneyInput`, `:234-240`), Cotas senior e subordinadas (`NumericInput`, `:242-259`), % subordinadas (`:261-275`), Chave de integracao e Logo na edicao (`:277-290`), Ativo (`:292-296`). Vocabulario de CONTRAPARTE FINANCIADORA documentado em `:20-38`. Teste: `src/app/pages/catalogs/__tests__/CatalogScreen.test.tsx`. |
| FE-065 | migrated | Cálculo automático de "% contas subordinadas" | verified | `frontend/src/app/pages/catalogs/CarriersPage.tsx:152-153` mantem a formula do legado (subordinadas x 100 / senior, com 0 quando senior = 0 — DEC-30, com golden `carriers_percentual_golden_spec.rb`), mas o campo e `<output>` SOMENTE LEITURA (`:264-271`, DC-09 em `.migration-ai9/map/projects-cadastros.md`), com o valor real gravado pelo servidor. No legado o campo era editavel e sobrescrito a cada tecla — duas fontes de verdade. |
| FE-067 | migrated | Upload de logo do Portador | blocked | O upload existe e esta LIGADO (DEC-47/DC-10): `frontend/src/app/pages/catalogs/CarrierLogoField.tsx:70-84` (`ImageCropper`), com tipo e limite conferidos ANTES de enviar (`:39-47`), remocao (`:60-68`) e anexo no proprio model por ActiveStorage (DEC-91). FALTA o caso de CRIACAO: `CarriersPage.tsx:277-290` so monta o campo dentro de `{editing && ...}`, entao um portador novo nao pode receber logo no mesmo passo. Mesmo padrao em FE-021, FE-074 e FE-087 — convem decidir uma vez. |
| FE-071 | migrated | Formulário de Fornecedor e alternância CPF/CNPJ | verified | `frontend/src/app/pages/projects/ProviderDocumentField.tsx:40-79` — UM campo, com um grupo de botoes Sem documento / CPF / CNPJ que decide mascara e validacao. Fecha o defeito do legado, onde "desabilitar" era so CSS e os dois blocos iam no envio (`:9-13`). Trocar de tipo LIMPA o numero (`:63-66`) e so digitos vao ao servidor (`:75`). DC-11 (`.migration-ai9/map/projects-cadastros.md:591`) manda o par `(document_type, document)` com documento opcional — e o e (`:86-88`). Teste: `src/app/pages/projects/__tests__/projetoEmpresas.test.tsx`. |
| FE-072 | migrated | Máscara e validação client-side de CNPJ/CPF | verified | Mascara em `frontend/src/lib/api/projects.ts` (`mascararDocumento`, usada em `ProviderDocumentField.tsx:73`) e o aviso distingue INCOMPLETO de invalido (`:36` e `:86-92`: "Faltam N digito(s) para completar o CPF"), que o legado nao sabia fazer. Divergencia registrada e deliberada (DC-11): o digito verificador e conferido no SERVIDOR, nao mais duplicado em JS — a tela diz isso em `:91`. No legado o toast "CNPJ invalido" so saia com 14 digitos exatos, e documento incompleto passava calado. |
| FE-073 | migrated | Consulta automática de CNPJ (ReceitaWS) no formulário | verified | `frontend/src/app/pages/projects/ProvidersPage.tsx:205-224` — botao "Preencher pela Receita Federal", habilitado so com CNPJ de 14 digitos (`:186`), chamando `GET /api/v1/cnpj/:cnpj` (`:155`); preenche 12 campos (`:157-172`), mostra a cota restante (`:220-223`, DEC-46: teto diario por usuario) e, no erro, diz para preencher a mao sem travar o cadastro (`:180-182`). No legado era duplamente morto (botao comentado no HTML e a URL com `<%%=` escapado). |
| FE-074 | migrated | Upload de logo do Fornecedor | blocked | O upload existe: `frontend/src/app/pages/projects/ProvidersPage.tsx:249-259` monta o `ScopedLogoField` (`app/pages/projects/ScopedLogoField.tsx`) com limite de 1 MB e invalidacao de cache. FALTA o caso de CRIACAO: `:248-265` so oferece o campo com `editing` e, sem ele, imprime "O logo e enviado depois de salvar, na edicao deste fornecedor". No legado (`providers/helper/_body.js.erb:60-90`) o input fazia parte do form e a imagem ia junto no cadastro, com previa local. Mesmo padrao de FE-021, FE-067 e FE-087. |
| FE-076 | migrated | Helper de criar/editar Grupo de Portadores | verified | `frontend/src/app/pages/catalogs/CarrierGroupsPage.tsx:75-108` — painel lateral do `CatalogScreen` com "Nome do grupo" (o unico campo do legado), mais Chave de integracao na edicao e o interruptor Ativo. Duas correcoes registradas em `:9-17`: o toast fala do GRUPO (o legado dizia "O portador foi excluido") e a contagem que esconde o botao e a mesma do servidor (`counter_cache`, 422 no vinculo, `:60-66`). Teste: `src/app/pages/catalogs/__tests__/CatalogScreen.test.tsx` (passou). |
| FE-081 | migrated | Busca incremental na lista de projetos | verified | `frontend/src/app/pages/projects/ProjectsPage.tsx:59` usa `useDebouncedSearch` (`src/hooks/useDebouncedSearch.ts:20-46`): 300 ms e entrada so de espacos ignorada (`initial.trim()` / `minLength`) — as duas regras do legado. Campo em `:174-186`; vazio de busca cita o termo em `:205-209` ("Nenhum resultado para «X»"), como o legado. Teste: `src/app/pages/projects/__tests__/projetoEmpresas.test.tsx`. |
| FE-083 | migrated | Alerta de atualização em andamento no widget | verified | `frontend/src/app/pages/projects/ProjectsPage.tsx:404-447` (`ProgressoDoProjeto`): barra + percentual, com duas origens — `job_state`/`job_progress` da propria linha (para quem abre a tela com o job rodando) e o evento do `useJobProgress` (Action Cable) para quem esta com a tela aberta. Fecha o D-86: no legado o percentual so mudava recarregando a pagina. Sem polling — teste `projetoEmpresas.test.tsx` "nenhuma tela da fatia usa `refetchInterval` nem `setInterval`" (passou) e `src/__tests__/no-api-polling.test.ts` (passou). |
| FE-084 | migrated | Remoção de projeto pela lista | verified | `frontend/src/app/pages/projects/ProjectsPage.tsx:355-384` — confirmacao com o nome do projeto e o aviso de que o servidor recusa se houver vinculo; `is_sandbox` troca o texto para "Limpar projeto de treinamento". O erro APARECE (`:119`, `mensagemDoServidor`) — no legado o callback de erro era no-op comentado e, como o backend respondia 200 sempre (BE-091), a remocao bloqueada parecia ter dado certo. |
| FE-085 | migrated | Formulário de projeto — campos | verified | `frontend/src/app/pages/projects/ProjectForm.tsx:143-345` tem todos os campos do legado: Nome (`:143`), Chave de integracao e slug em leitura na edicao (`:155-167`, DC-17), Segmento (`:172-180`), Subsegmento (`:182-198`), Tipo/Logradouro/Numero/Complemento/Bairro/CEP/Cidade/UF (`:202-232`), Logo (`:236-252`), Data de baixa (`:254-260`), Responsavel (`:263-322`), Observacao em rich text (`:324-340`) e Ativo (`:342-345`). Teste: `src/app/pages/projects/__tests__/projetoEmpresas.test.tsx`. |
| FE-086 | migrated | Formulário — seletor de responsável (3 modos) | blocked | Os TRES modos existem, mas so na CRIACAO: `frontend/src/app/pages/projects/ProjectForm.tsx:263` embrulha o `fieldset` inteiro em `{!editing && (...)}`. Na EDICAO nao ha nenhum controle de responsavel — apesar de `toForm` ja mapear `responsible_mode`/`responsible_user_id` (`:95-98`) e de o payload envia-lo quando `editando` (`ProjectsPage.tsx`, `paraPayload`). FALTA: no legado, editar um projeto que ja tem responsavel mostrava um select restrito a `@project.users` (`new/_body.html.erb:132-178`); hoje o responsavel so pode ser trocado pela API. |
| FE-087 | migrated | Formulário — upload e preview do logo | blocked | O upload existe e a escolha SOBREVIVE a erro de validacao em outro campo (`frontend/src/app/pages/projects/ProjectForm.tsx:236-252` com `ScopedLogoField`, requisicao propria) — isso corrige o defeito do legado, em que o `ajax:error` resetava o input. FALTA o caso de CRIACAO: `:236` so monta o campo com `editing`, e no legado o logo ia junto no cadastro com previa por `FileReader` (`new/_body.js.erb:106-131`). Mesmo padrao de FE-021, FE-067 e FE-074. |
| FE-088 | migrated | Formulário — data de baixa (datepicker) | verified | `frontend/src/app/pages/projects/ProjectForm.tsx:254-260` — `DatePicker` (`src/components/ui/DatePicker.tsx`) com `clearable` e dica "Formato dd/mm/aaaa"; a formatacao e pt-BR por `formatDate` com `locale: ptBR` (`src/lib/utils/date.ts:59-62`). O valor vai como `closing_date` no payload. Melhoria sobre o legado: o campo aceita DIGITACAO alem do calendario (`DatePicker.tsx:12-21`), enquanto o do legado era `readonly`. |
| FE-089 | migrated | Formulário — salvamento automático (stack) e retorno | verified | Sem autosave, por decisao assinada DC-23 (`.migration-ai9/map/projects-cadastros.md:603`): uma requisicao por salvamento (`frontend/src/app/pages/projects/ProjectsPage.tsx:96-108`) e, no sucesso, o usuario PERMANECE na tela com confirmacao — o legado fazia `window.location = /u/console/projects`. Teste: `src/app/pages/projects/__tests__/projetoEmpresas.test.tsx` — "o formulario salva com UMA requisicao (FE-089 / DC-23): nao ha autosave; nenhum `onKeyUp`/`onBlur` dispara mutacao" (passou). |
| FE-090 | migrated | Feedback de erros do formulário | verified | `frontend/src/app/pages/projects/ProjectsPage.tsx:102` distingue "Projeto atualizado." de "Projeto cadastrado." — o legado usava o mesmo template e dizia "cadastrado" ao editar. O erro sai por `mensagemDoServidor` (`:107`), com a frase do servidor, em vez de um toast por chave tecnica em negrito como no legado. |
| FE-092 | migrated | Switch "GERIDO PELA SAFEGOLD" | verified | `frontend/src/app/pages/projects/ProjectDetailPage.tsx:213-229` — `Switch` com `id="has_safegold_management"` PROPRIO (FE-093: no legado os dois interruptores usavam o mesmo id e o rotulo de um alternava o outro), `disabled` quando `!podeEscrever` (papel + `useIsReadonly`, `:186-191`) e a razao escrita em `:248-252`. Toast de sucesso e de erro em `:194-201`. Teste: `projetoEmpresas.test.tsx` — "os dois interruptores tem ids DISTINTOS, e cada rotulo aponta para o seu" (passou). Divergencia registrada: o toast diz "Marca de gestao atualizada." em vez de nomear o projeto como o legado. |
| FE-094 | migrated | Detalhe — "Ações rápidas" | blocked | Nao ha bloco de "Acoes rapidas" no detalhe do projeto. `frontend/src/app/pages/projects/ProjectDetailPage.tsx:80-88` (cabecalho) so traz selos e o botao de voltar, e as abas (`:92-95`) sao Resumo/Membros/Portadores. FALTA no detalhe: "Editar projeto" e "Remover projeto"/"Limpar projeto" — hoje existem so no menu de linha da LISTA (`app/pages/projects/ProjectsPage.tsx:144-158` e a confirmacao em `:355-384`). O atalho "Portadores" do legado esta coberto pela aba (`:94` e `:167`). |
| FE-096 | migrated | Detalhe — adicionar membro (autocomplete) | verified | `frontend/src/app/pages/projects/ProjectDetailPage.tsx:317-346` — `Autocomplete` com estados de carregando e vazio ("Ninguem encontrado — quem ja participa nao aparece nesta lista"), alimentado por `membershipsApi.candidates(termo)` (`:275-279`). O bloco inteiro some para quem nao pode gerir (`podeGerir`, `:318`), espelhando o gate do servidor. |
| FE-097 | migrated | Detalhe — remover membro | verified | `frontend/src/app/pages/projects/ProjectDetailPage.tsx:292-300` — a mensagem diz "«Fulano» saiu deste PROJETO", corrigindo o texto do legado, que dizia "empresa". As tres condicoes do DEC-18.5 estao em `motivoDeNaoRemover` (`:302-309`) e ocupam o lugar do botao (`:415-418`); no telefone viram `disabledReason` da folha de acoes (`:381`). Teste: `projetoEmpresas.test.tsx` — "a mensagem de remocao fala do PROJETO, nao da empresa (FE-097)" (passou). |
| FE-102 | migrated | Tela "Indicadores específicos" | verified | `frontend/src/features/indicators/pages/ProjectIndicatorsPage.tsx` — tela montada em `/indicator-connections`, escopada no projeto corrente; carregando/vazio/erro via `AsyncSection` (`:193-227`); o botao "Cadastrar" SOME com `useIsReadonly` (`:168`) e no lugar dele aparece o aviso (`:182-190`), em vez do bloqueio fossil do legado. Sem polling (teste `src/__tests__/no-api-polling.test.ts`, passou). |
| FE-103 | migrated | Widget de indicador: global (switch) vs específico (menu) | verified | `frontend/src/features/indicators/components/ConnectionRow.tsx:57-129` — indicador GLOBAL ganha `Switch` (`:118-129`), indicador DO PROJETO ganha os tres botoes Editar/Ativar-Desativar/Excluir (`:130-158`); selo "Global"/"Do projeto" e selo "Inativo" em `:69-75`; a descricao abre em acordeao um por vez (`ProjectIndicatorsPage.tsx:193` — `Accordion type="single" collapsible`), como o legado. |
| FE-104 | migrated | Ações do indicador específico | verified | `frontend/src/features/indicators/pages/ProjectIndicatorsPage.tsx:88-140` — as quatro acoes (conectar/desconectar, ativar/desativar, editar, excluir) com toast de erro do servidor. O travamento do legado morre: o estado de envio e o `isPending` do React Query, que termina sozinho (`ConnectionRow.tsx:14-19`). Em somente-leitura o controle fica visivel e EXPLICA (`:118-121` e `:136,146,154`), com `disabled` no Radix Switch para o `aria-checked` nao mentir. "Excluir" ganhou confirmacao (`DeleteIndicatorDialog`, `:266-272`) — no legado apagava o indicador e todos os lancamentos sem dialogo. |
| FE-109 | migrated | Drawer de criar/editar padrão de disponibilidade | verified | `frontend/src/app/pages/availability/ProjectAvailabilitiesPage.tsx:582-586` monta `DialogoDeCriacao` (`:620-745`) e `DialogoDeRenomear` (`:760-835`). A restricao do legado (na edicao so o Titulo e editavel) e PRESERVADA e agora EXPLICADA (`:790-793`), com os campos imutaveis exibidos como dados (`:805-820`). Divergencia registrada: virou `Dialog` centralizado, nao painel lateral. Teste: `src/app/pages/availability/__tests__/availability.test.tsx` (passou). |
| FE-110 | migrated | Drawer — dependência entre "Faz parte de" e níveis | verified | `frontend/src/app/pages/availability/ProjectAvailabilitiesPage.tsx:686-703` — "Faz parte de" alimentado por consulta ESCOPADA no servidor (`available-parents`, `:637-641`), e o nivel e derivado do pai pelo servidor (dito em `:671-673`), sem os campos "Nivel 1/Nivel 2" travados do legado. Some o `data-templates` com `AvailabilityTemplate.all` embutido no HTML (vazamento entre projetos). Teste: `availability.test.tsx` — "nenhuma tela busca a lista GLOBAL de padroes para montar o campo «Faz parte de»" e "o cliente nunca envia `project_id`" (passaram). |
| FE-112 | migrated | Remover padrão de disponibilidade | verified | `frontend/src/app/pages/availability/ProjectAvailabilitiesPage.tsx:588-607` — `Dialog` de confirmacao com o nome do padrao, o aviso de que os itens abaixo saem junto e de que o servidor recusa se houver lancamento. Corrige o `M.SUCESS` (constante inexistente) do legado, citado em `:79`. O menu tambem diz por que NAO da para remover, em vez de sumir (`:223-232`: global, com lancamento, ou bloqueado). Caveat: a execucao destrutiva nao foi disparada — banco de desenvolvimento compartilhado. |
| FE-115 | migrated | Formulário de garantia | verified | `frontend/src/app/pages/projects/ProjectGuaranteesPage.tsx:136-200` — Titulo, Portador, Tipo de garantia, Valor (`MoneyInput`) e Observacao. UM criterio unico de "o projeto tem portador": a mesma consulta de conexoes que o servidor usa para aceitar `carrier_id` (BE-119, `:138-150`), com o estado vazio dizendo onde conectar (`:166-170`). Lista em `DataTable` paginada (`:42-116`). Teste: `src/app/pages/projects/__tests__/projetoEmpresas.test.tsx` (o filtro vai como `project_guarantee_id`, dentro do escopo). |
| FE-118 | migrated | Aba "Projetos" no detalhe do usuário | verified | A aba existe e esta MONTADA em `frontend/src/app/pages/users/UserDetailPage.tsx:154-158` e `:169-171`, com o conteudo em `AbaProjetos` (`:227-292`): lista escopada e paginada, com carregando/vazio/erro, informativa por DC-18 (`.migration-ai9/map/projects-cadastros.md:598`), e a razao escrita em `:284-289`. ATENCAO: o componente que o razao aponta, `src/app/pages/projects/UserProjectsSection.tsx`, e CODIGO MORTO — `grep -rn "UserProjectsSection" frontend/src` so acha a propria definicao; nenhuma tela o importa. |
| FE-128 | migrated | Estados da grade de entradas (container availability_entry_collection_container). | verified | `frontend/src/app/pages/availability/AvailabilityPage.tsx:400-437` — os quatro estados, cada um distinto: carregando (`:402-404`), ERRO com a mensagem do servidor e "tentar de novo" (`:406-417` — no legado o `failure` era vazio e a lista so parava de atualizar), vazio sem data ("Selecione uma data", `:419-425`) e vazio com data (`:427-437`, com o portugues corrigido: o legado escrevia "Voce ainda nao possui"). Teste: `src/app/pages/availability/__tests__/availability.test.tsx` (passou). |
| FE-129 | migrated | Campo monetário da entrada: máscara, sufixo de operação (D/C) e sanitização de digitação. | verified | `frontend/src/app/pages/availability/AvailabilityPage.tsx:593-600` usa o `MoneyInput` compartilhado (`src/components/ui/NumericInput.tsx`), que ja faz mascara e sanitizacao — some a limpeza a mao por `keyup` do legado. O sufixo `C`/`D` cru foi trocado por icone + rotulo legivel com tooltip (`:546-552`: `operation_type_label`). Testes: `src/components/ui/__tests__/MoneyInput.test.tsx` e `NumericInput.test.tsx` (passaram). |
| FE-130 | migrated | Salvamento inline: alterar o valor e sair do campo submete o form via AJAX. | verified | `frontend/src/app/pages/availability/AvailabilityPage.tsx:601-605` — o `onBlur` do `MoneyInput` grava, e so quando o valor MUDOU (`if (entry && Number(entry.value) === valor) return`), o que o legado nao checava. Toast de sucesso/erro na mutacao (`:519-529`). Sem `preventDoubleSubmission` pendurado: o `disabled` sai do `gravar.isPending` (`:598`). |
| FE-131 | migrated | Excluir entrada: ícone de lixeira no título, com confirmação. | blocked | O icone de lixeira existe e aparece so onde se aplica (`frontend/src/app/pages/availability/AvailabilityPage.tsx:608-618`: celula editavel e com lancamento gravado). FALTA A CONFIRMACAO: `onClick={() => excluir.mutate()}` apaga direto. No legado havia modal ("A operacao nao pode ser desfeita. Tem certeza?", `list/_widget.js.erb:106-135`). Busca por `confirm / Confirm / Tem certeza / desfeita` em `AvailabilityPage.tsx` nao retorna nada. |
| FE-132 | migrated | Modo somente-leitura / consolidação: input bloqueado. | verified | `frontend/src/app/pages/availability/AvailabilityPage.tsx:588-592` — `editable` vem da RESPOSTA do servidor (D-23), nao de uma regra da tela: onde nao e editavel a celula vira leitura com o rotulo da semantica do valor (`:620-637`). No legado o bloqueio era exclusivamente de UI e um `PUT` direto gravava em consolidacao geral. Teste: `availability.test.tsx` — "a grade decide pela resposta, nao por uma regra propria de tela" (passou). |
| FE-136 | migrated | Busca de templates globais com debounce. | verified | `frontend/src/app/pages/availability/AvailabilityTemplatesPage.tsx:44-63` usa o `CatalogScreen`, que busca por `useDebouncedSearch` (`app/pages/catalogs/CatalogScreen.tsx:187`) — 300 ms e entrada so de espacos ignorada (`src/hooks/useDebouncedSearch.ts:20-46`). E a busca FUNCIONA: o BE-132 do legado (`default_position` inexistente) derrubava a requisicao e o container caia num `failure` vazio. Teste: `src/app/pages/catalogs/__tests__/CatalogScreen.test.tsx` — "o vazio de BUSCA cita o termo procurado" (passou). |
| FE-137 | migrated | Estados da lista de templates globais. | verified | Os quatro estados vem do `CatalogScreen` e estao testados um a um em `src/app/pages/catalogs/__tests__/CatalogScreen.test.tsx` ("mostra o CARREGANDO", "mostra a LISTA", "mostra o VAZIO com o texto PROPRIO da tela", "mostra a FALHA e oferece tentar de novo" — todos passaram). Textos proprios desta tela em `app/pages/availability/AvailabilityTemplatesPage.tsx:59-63`. O erro deixa de ser silencioso, e a lista e paginada de verdade (`CatalogScreen.tsx:488-510`), no lugar do limite fixo de 50 sem paginacao. |
| FE-139 | migrated | Helper (painel lateral) de cadastro/edição de template global. | verified | `frontend/src/app/pages/availability/AvailabilityTemplatesPage.tsx:222-320` — Titulo, "Faz parte de", Natureza da operacao, Prazo, Obrigatorio, "Entra na soma do nivel acima" (cumulativo), "Corrigido por dias uteis" e ainda "Levar para os projetos que ja existem". O painel e o `SideDrawer` do `CatalogScreen` (`CatalogScreen.tsx:513-533`). O `data-templates` com `AvailabilityTemplate.all` embutido no HTML nao existe mais: a lista de pais e consulta escopada sob demanda (`:210-219`). |
| FE-140 | migrated | Detalhe do template (card "Dados do template"). | blocked | Nao existe tela de detalhe do padrao global: `app/consoleNavigation.tsx:502` registra so `/availability-templates` (lista), `AvailabilityTemplatesPage.tsx` nao tem `onRowClick` nem rota filha. A lista cobre parte do card (No, Titulo, Natureza, Prazo e os marcadores Obrigatorio/Corrigido/Nao soma, `:64-123`). FALTA: Escopo (Global/Projeto), criado por / criado em, "ha X desde a atualizacao" e sobretudo o bloco "Projetos" do legado (`availability_templates/detail/_body.html.erb:76-91`), que dizia em quais projetos o padrao esta ativo — hoje nao ha onde ver isso. |
| FE-147 | migrated | Helper de cadastro/edição de disponibilidade do projeto. | verified | Mesmo par de dialogos de FE-109: `frontend/src/app/pages/availability/ProjectAvailabilitiesPage.tsx:620-745` (criar, com Titulo / Faz parte de / Natureza / Prazo) e `:760-835` (editar, so o titulo — restricao do legado preservada e explicada). O texto de outro dominio do legado ("Essa construtora nao pode ser alterada") nao existe mais. Teste: `src/app/pages/availability/__tests__/availability.test.tsx` (passou). |
| FE-148 | migrated | Auto-preenchimento dos níveis a partir do template pai selecionado. | verified | `frontend/src/app/pages/availability/ProjectAvailabilitiesPage.tsx:614-618` e `:671-673` — o nivel e DERIVADO do pai pelo servidor, em vez de preenchido e travado por JS a partir de um JSON embutido. A lista de pais e escopada no projeto corrente (`:637-641`), fechando o vazamento do legado, em que um `parent_template_id` manipulado preenchia com dados de outro projeto. Teste: `availability.test.tsx` — "o cliente nunca envia `project_id` — o escopo e do servidor (C1)" (passou). |
| FE-149 | migrated | Botão de atualizar a lista de disponibilidades do projeto. | blocked | Nao ha botao de atualizar a lista. O cabecalho (`frontend/src/app/pages/availability/ProjectAvailabilitiesPage.tsx:236-268`) traz busca, o interruptor "Mostrar desativados" e "Novo padrao" — nada de recarregar. A lista so se atualiza por mutacao, por evento do `ProjectProgressChannel` e pelo "tentar de novo" do estado de ERRO (`:300`, `:377`). O comentario da tela (`:81`) declara morto o defeito do legado (dois handlers, um para seletor inexistente), mas o CONTROLE tambem sumiu, e nao ha DEC autorizando. Se a decisao e que o canal ao vivo substitui o botao, falta registrar isso. |
| FE-151 | migrated | Estado de carregamento da lista | verified | `frontend/src/features/receivables/pages/ReceivablesPage.tsx:418-424` e `:512-518` — `AsyncSection`/`DataTable` com `loadingLabel="Carregando borderos…"`; a recarga por filtro nao pisca a lista inteira (`loading={consulta.isFetching && !consulta.isLoading}` no `PageHeader`, `:326`), que e o `silent` do legado. Teste: `src/features/receivables/__tests__/receivables.test.tsx` (passou). |
| FE-152 | migrated | Estado vazio da lista (sem busca) | verified | `frontend/src/features/receivables/pages/ReceivablesPage.tsx:425-431` — vazio sem busca: "Nenhum bordero lancado" + "O bordero e onde o dinheiro entra no sistema. Lance o primeiro para comecar." (no telefone e no desktop, `:425` e o bloco equivalente do `DataTable`). |
| FE-153 | migrated | Estado vazio com termo de busca | verified | `frontend/src/features/receivables/pages/ReceivablesPage.tsx:425-430` — com busca ativa o titulo vira «Nenhum resultado para «X»» e a descricao sugere limpar a busca, citando o termo como o legado fazia. |
| FE-154 | migrated | Estado de erro (exclusão) | verified | Dois erros distintos, e os dois APARECEM. Lista: `AsyncSection` com `error` e `onRetry` (`frontend/src/features/receivables/pages/ReceivablesPage.tsx:419-422`) — no legado a falha do `search` deixava a tela como estava, sem mensagem. Exclusao: `:157-162` mostra a mensagem do SERVIDOR (`mensagemDoServidor`), nao o generico "Houve um problema". |
| FE-155 | migrated | Busca textual de recebíveis (debounce) | verified | `frontend/src/features/receivables/pages/ReceivablesPage.tsx:86` (`useDebouncedSearch`) — 300 ms e entrada so de espacos ignorada (`src/hooks/useDebouncedSearch.ts:20-46`). E o escopo da busca agora e COMUNICADO: placeholder "Buscar por portador, no do bordero ou descricao…" (`:339-340`), contra o "Procurar" generico do legado que so buscava por titulo do portador. |
| FE-156 | migrated | Filtro de período (datepicker de intervalo) | verified | `frontend/src/features/receivables/pages/ReceivablesPage.tsx:399-417` — dois `DatePicker` pt-BR ("De…" e "Ate…"), ambos `clearable`, que resetam a paginacao e entram na consulta como `de`/`ate` (`:107-117`). O bug do rotulo do legado (`from.getYear()` no lugar de `to.getYear()`) desaparece porque nao ha rotulo montado a mao. Divergencia registrada: o mapa (`.migration-ai9/map/receivables-renegotiations.md:150`) previa um `components/ui/DateRangePicker.tsx` unico com rotulo "De … a …"; o componente nao existe (`ls src/components/ui + grep -i range` = vazio) e a funcao foi entregue com dois campos. |
| FE-157 | migrated | Filtro por carteira | verified | `frontend/src/features/receivables/pages/ReceivablesPage.tsx:368-382` — `Select` "Filtrar por carteira" com a opcao "Todas as carteiras" e a lista vinda de `walletsApi.list` (`:139-141`). Some o `wallaetId` (chave com erro de grafia) do legado: o parametro vai como `walletId` (`:107`). |
| FE-158 | migrated | Filtro por portador | verified | `frontend/src/features/receivables/pages/ReceivablesPage.tsx:383-397` — `Select` "Filtrar por portador" alimentado por `carrierConnectionsApi.list` (`:145-147`), ou seja, os portadores CONECTADOS ao projeto corrente, com "Todos os portadores" como opcao neutra. Os dois filtros ficam sempre visiveis, sem o bloco escondido atras do botao "Filtros" do legado. |
| FE-159 | migrated | Ordenação multi-coluna | blocked | A ordenacao FUNCIONA e e do servidor (`frontend/src/features/receivables/pages/ReceivablesPage.tsx:519` com `sortMode="server"`, `:114-115` mandando `orderingKey`/`orderingStyle`), com o ciclo asc -> desc -> neutro em `components/ui/DataTable.tsx:386-393`. FALTA a parte MULTI-COLUNA: `sort` e um unico `SortState` (`:88`) e cada clique substitui o anterior — nao ha acumulo de chaves. O mapa pedia explicitamente "chaves acumuladas" (`.migration-ai9/map/receivables-renegotiations.md:153`) e nao existe `components/ui/SortableTableHeader.tsx`. Contraponto para a decisao: no legado o acumulo NUNCA teve efeito (o controller reordenava por `date DESC` no fim), entao pode ser caso de `dropped` com DEC — falta a decisao escrita. |
| FE-160 | migrated | Paginação por limite/offset | verified | `frontend/src/features/receivables/pages/ReceivablesPage.tsx:560-575` monta `PaginationPill` (desktop) e `MobilePagination` (telefone); os controles primeira/anterior/proxima/ultima com `disabled` nas bordas e o seletor de itens por pagina estao em `components/ui/PaginationPill.tsx:165-221`. O `total_pages` vem do servidor (DEC-62), sem depender do `@total_count` quebrado do legado. Teste: `src/components/ui/__tests__/Pagination.test.tsx`. |
| FE-161 | migrated | Botão "Cadastrar" com guarda de portador | verified | `frontend/src/features/receivables/pages/ReceivablesPage.tsx:344-350` — "Novo bordero" so existe com `podeEscrever` (papel + `useIsReadonly`, `:82-84`). A guarda de portador virou estado da propria tela de cadastro, com o texto CERTO: `features/receivables/pages/ReceivableFormPage.tsx:386-391` ("Este projeto ainda nao tem nenhum portador conectado") com atalho para conectar — o legado falava de "controle de risco" numa tela de recebivel. Teste: `src/__tests__/somente-leitura-esconde-escrita.test.ts` (passou). |
| FE-162 | migrated | Widget de linha do recebível (formatação) | verified | `frontend/src/features/receivables/pages/ReceivablesPage.tsx:166-270` — data em dd/mm/aaaa, dinheiro por `formatMoney` (que distingue nulo de zero, D-117) e o CET por `formatPercent(..., 4)` (`:266`), em pt-BR. Corrige o legado, que imprimia `12.3456%` cru com ponto decimal. Testes: `src/features/receivables/__tests__/receivables.test.tsx` — "o CET e formatado em pt-BR com 4 casas (FE-162)" e "`formatMoney(null)` rende travessao e `formatMoney(0)` rende R$ 0,00" (passaram). |
| FE-163 | migrated | Menu do widget: editar e remover | verified | `frontend/src/features/receivables/pages/ReceivablesPage.tsx:296-315` (linha) e `MobileRowActions` no telefone; a remocao passa pelo `ConfirmDialog` (`:586-599`) que NOMEIA o bordero, o portador e o valor liquido antes de confirmar — mais especifico que o "Tem certeza?" do legado. O tooltip pendurado num seletor inexistente (`.risk_operation_info_tippy_content`) nao existe mais. |
| FE-164 | migrated | Modo somente leitura | verified | `frontend/src/features/receivables/pages/ReceivablesPage.tsx:82-84` — `podeEscrever` consulta `useIsReadonly()` (DEC-108), e com ele somem "Novo bordero" (`:344`) e as acoes de linha. E agora o SERVIDOR tambem recusa (fecha o D-17/BE-153). Teste guarda: `src/__tests__/somente-leitura-esconde-escrita.test.ts` — "toda definicao de `podeEscrever` consulta o modificador" e "quem define `podeEscrever` importa o hook" (passaram). |
| FE-166 | migrated | Estado vazio: projeto sem portador | verified | `frontend/src/features/receivables/pages/ReceivableFormPage.tsx:385-391` — o formulario e suprimido e o estado vazio DIZ a razao ("Este projeto ainda nao tem nenhum portador conectado"), com atalho para `/project-carrier-connections`. O endereco foi conferido contra `app/consoleNavigation.tsx` (o comentario em `:376-383` registra o defeito de rota ja corrigido). |
| FE-167 | migrated | Estado vazio: projeto sem empresa (com criação inline) | verified | `frontend/src/features/receivables/pages/ReceivableFormPage.tsx:392-397` — estado vazio de empresa com o botao "Cadastrar empresa". Divergencia registrada: no legado a criacao era INLINE (montava o `company_create_helper_container` na propria tela); aqui navega para `/companies`. Custo zero na pratica, porque neste estado o formulario esta bloqueado e vazio. O atalho funciona tambem na edicao — no legado a URL era montada com variavel indefinida e o link nao ia a lugar nenhum. |
| FE-168 | migrated | Máscara de campos monetários | verified | `frontend/src/features/receivables/pages/ReceivableFormPage.tsx:562-570` e `:660-670` usam o `MoneyInput` compartilhado (`src/components/ui/MoneyInput.tsx`), que centraliza mascara, prefixo e limite de 2 casas — no lugar dos handlers de `keyup`/`focus`/`focusout` reescritos por tela no legado. Teste: `src/components/ui/__tests__/MoneyInput.test.tsx` (passou). |
| FE-169 | migrated | Máscara de campos decimais (prazos, floats, taxas) | verified | `frontend/src/features/receivables/pages/ReceivableFormPage.tsx:597-652` — `NumericInput` com `kind="decimal" casas={2}` nos prazos e no float, e `kind="percent"` no custo efetivo acordado e na taxa nominal. Virgula decimal na tela e numero no payload (`:441-452`), sem a reconversao manual do legado. Teste: `src/components/ui/__tests__/NumericInput.test.tsx` (passou). |
| FE-170 | migrated | Máscara de campos inteiros (borderô, quantidades) | verified | `frontend/src/features/receivables/pages/ReceivableFormPage.tsx:571-589` — `qtd_titulos` e `qtd_recusada` aceitam so digitos (`inteiro()`), com o comentario registrando que o exemplo "Ex: 789" do legado contradizia o campo. O `nro_bordero` (`:472-486`) e TEXTO LIVRE de proposito — a coluna e `string` desde 2021 e o legado tinha dois handlers em conflito no mesmo campo; a dica na tela diz que letras, barras e zeros a esquerda sao preservados. |
| FE-171 | migrated | Recálculo em tempo real no navegador | verified | O recalculo ao vivo EXISTE, e agora bate com o que e gravado: `frontend/src/features/receivables/pages/ReceivableFormPage.tsx:229` chama `useReceivablePreview` (debounce de 400 ms, `src/features/receivables/hooks/useReceivablePreview.ts:104-110`), que bate em `POST /receivables/preview` — o MESMO `Receivables::Calculator` da gravacao (contrato C2). O painel nao faz uma conta sequer (`features/receivables/components/CalculationPanel.tsx:8-38`). Fecha o D-09: no legado a formula era reimplementada em JS, PARCIAL e com outro arredondamento, e o numero da tela nao batia com o de depois de salvar. Teste: `src/features/receivables/__tests__/receivables.test.tsx` — "nenhum arquivo que fala de bordero reimplementa uma formula do calculador" e "a varredura esta OLHANDO os arquivos do bordero" (passaram). |
| FE-172 | migrated | Bloqueio de salvamento por incongruência | verified | `frontend/src/features/receivables/pages/ReceivableFormPage.tsx:414-428` — `podeSalvar` exige os obrigatorios (`faltando`, montado campo a campo) E `previa.problema === null`, que e a incongruencia apontada pelo SERVIDOR. O botao fica desabilitado e o tooltip DIZ o que falta (`:780-786`), em vez do "Alguns campos podem…" generico do legado. |
| FE-173 | migrated | Salvamento pela barra inferior (stack) | verified | `frontend/src/features/receivables/pages/ReceivableFormPage.tsx:740-787` — `FormActionBar` da biblioteca (a barra inferior), com o liquido em destaque (`:749-753`), Cancelar e Salvar. Sem revalidacao duplicada e sem religar handlers a cada recalculo, como fazia o legado. Teste: `src/components/ui/__tests__/FormActionBar.test.tsx` (passou). |
| FE-174 | migrated | Conversão de moeda no submit | verified | Nao ha mais conversao de mascara no envio, e por isso some a janela em que o exibido divergia do enviado: `MoneyInput`/`NumericInput` guardam NUMERO no estado, e `frontend/src/features/receivables/pages/ReceivableFormPage.tsx:432-461` monta o payload direto desses numeros. As datas saem em ISO por `iso()` (`:433`). |
| FE-175 | migrated | Tarifas: adicionar linha | verified | `frontend/src/features/receivables/components/TaxRows.tsx:89-96` — a linha nova entra NO TOPO, como no legado, com chave estavel de renderizacao; duplicidade de tipo continua permitida (Q-B15, `:29-32`). O recalculo vem junto pela previa do servidor. Sem o efeito do legado de re-registrar as mascaras do formulario inteiro a cada clique. |
| FE-176 | migrated | Tarifas: remover linha (nova vs. existente) | verified | `frontend/src/features/receivables/components/TaxRows.tsx:80-88` — linha ainda nao gravada some na hora; linha JA PERSISTIDA fica riscada com botao de desfazer e so e apagada no Salvar. E a DEC-72 (`:12-27`): no legado o `DELETE` saia direto na confirmacao, fora de qualquer submit, e o servidor apagava antes de o usuario decidir salvar. |
| FE-177 | migrated | Datepickers de data de operação e data de crédito | blocked | Os dois seletores existem e sao pt-BR (`frontend/src/features/receivables/pages/ReceivableFormPage.tsx:470-471` e `:486-493`, `components/ui/DatePicker.tsx`). FALTA A RESTRICAO MUTUA: nenhum dos dois recebe `min`/`max` (o `DatePickerProps` os aceita, `components/ui/DatePicker.tsx:26-27`). No legado escolher a data de operacao definia `minDate` no seletor de credito e escolher a de credito definia `maxDate` no de operacao (`receivables/new/_body.js.erb:74-108`), e o mapa pediu isso explicitamente ("Restricao mutua operacao/credito", `.migration-ai9/map/receivables-renegotiations.md:171`). |
| FE-178 | migrated | Retorno do salvamento do recebível | verified | `frontend/src/features/receivables/pages/ReceivableFormPage.tsx:252` distingue "Bordero atualizado." de "Bordero cadastrado." — o legado dizia "cadastrado" tambem na edicao. O erro sai por `mensagemDoServidor` (`:258`), em portugues, no lugar do toast por chave de atributo em ingles (`risk_operation_subtype_id`) que o legado mostrava porque o `translate_every_key` nunca era chamado no fluxo de recebiveis. |
| FE-179 | migrated | Console → Cobranças: lista | verified | `frontend/src/features/receivables/pages/ChargesPage.tsx:165-230` — colunas Data, Situacao (com selo e icone de faturado), Valor e Operacoes ("N liq. · M est."), mais a coluna de acoes; carregando/vazio/erro pelo `DataTable`/`AsyncSection` e paginacao REAL (`:344` e `meta`), no lugar do limite fixo de 1000 no cliente do legado. Teste: `src/features/receivables/__tests__/chargeDetailPage.test.tsx` — "a lista NAVEGA para o detalhe" (passou). |
| FE-180 | migrated | Filtros de cobrança (situação, mês, ano) | verified | `frontend/src/features/receivables/pages/ChargesPage.tsx:266-300` — os tres filtros (situacao, mes, ano), **todos com opcao em branco**: "Todos os meses" (`:285`) e "Todos os anos" (`:299`). Fecha o defeito do legado, em que o ano vinha pre-selecionado sem opcao vazia e era impossivel ver todas as cobrancas. Registrado em `:45-47`. |
| FE-181 | migrated | Widget e menu da cobrança | blocked | A linha abre o detalhe ("Ver mais", `frontend/src/features/receivables/pages/ChargesPage.tsx:128`) e a exclusao existe (`:209-229` + `ConfirmDialog` em `:414-424`), com o menu sumindo para `podeEscrever === false` e para o pacote faturado. FALTA "EDITAR": nao ha nenhuma acao de editar a cobranca em tela alguma. `chargesApi.update` existe (`src/lib/api/receivables.ts:412-413`) e o `PUT /api/v1/charges/:id` existe (`backend/app/controllers/api/v1/charges.rb:187`), mas `grep -rn "chargesApi.update" frontend/src` NAO retorna nenhum consumidor. Consequencia: a situacao nunca muda pela interface e o estado "Faturado" e inalcancavel (no legado so se chegava a ele editando). |
| FE-182 | migrated | Detalhe da cobrança | verified | `frontend/src/features/receivables/pages/ChargeDetailPage.tsx:160-247` — pagina propria em `/charges/:id` (no legado era secao em memoria), com titulo "Cobranca de dd/mm/aaaa", subtitulo com situacao e contagem de recibos, os quatro `KpiCard` (valor cobrado, valor das operacoes, operacoes de risco, estruturadas) e o "Extrato por remuneracao" em `DataTable`. O aviso de pacote faturado esta em `:182-193`, com o bloqueio agora no SERVIDOR (D-18). Teste: `src/features/receivables/__tests__/chargeDetailPage.test.tsx` (7 casos, passaram). DEFEITO REGISTRADO: falta o cartao "cadastro" do legado (criado por, criado em, atualizado em) — a entity ja expoe `user_id`, `created_at` e `updated_at` (`backend/app/controllers/api/entities/charge.rb:26-28`), a tela e que nao os mostra. |
| FE-183 | migrated | Ações do detalhe da cobrança | blocked | Das tres acoes do legado, so uma existe: "Escolher/Ver operacoes da cobranca" (`frontend/src/features/receivables/pages/ChargeDetailPage.tsx:174-177`), e ela agora NAO fica desabilitada no pacote faturado — vira leitura, com o bloqueio no servidor. FALTAM "Editar a cobranca" (a situacao clicavel do legado; ver FE-181 — nenhum consumidor de `chargesApi.update`) e "Remover a cobranca" (a exclusao existe so na LISTA, `ChargesPage.tsx:209-229`). |
| FE-184 | migrated | Tela de seleção de operações da cobrança | verified | `frontend/src/features/receivables/pages/ChargeReceiptsPage.tsx:162-235` — Classe (selo LIQ/EST com tooltip), Data, Tipo (titulo da remuneracao), Valor da operacao, Valor da remuneracao e a coluna de selecao; os vinculados ja vem marcados pelo `temp_id` persistido (`:99-101`, `:143`). Rota filha de verdade em `/charges/:id/receipts` (`:41-45`) — o legado trocava `route.section` em memoria. Cobranca `done` abre somente leitura (`:139`, `:266-271`). |
| FE-185 | migrated | Seleção/desseleção de recibo e persistência em lote | verified | `frontend/src/features/receivables/pages/ChargeReceiptsPage.tsx:113` — `chargesApi.setReceipts` manda o ESTADO FINAL em `temp_ids` (`src/lib/api/receivables.ts:421-422`, `PUT /charges/:id/receipts`): o que nao esta na lista e removido, tudo numa transacao. A falha REVERTE a marcacao, porque a selecao e reconstruida do refetch (`:99-101`, `:143`) — no legado o `toggleClass` acontecia ANTES da requisicao e a tela ficava fora de sincronia com o servidor ate recarregar. Barra de acao pela `FormActionBar` (`:13`). |
| FE-186 | migrated | Helper de criação/edição de cobrança | blocked | So a CRIACAO existe: `frontend/src/features/receivables/pages/ChargesPage.tsx:387-412` (`SideDrawer` "Nova cobranca") com a data padrao hoje + 30 dias (`:155-158`) e a nota de que "Faturado" nao e escolhido no nascimento — as duas regras do legado, preservadas. FALTA A EDICAO: nao ha painel nem tela para alterar situacao ou data de uma cobranca existente (ver FE-181: `chargesApi.update` sem consumidor). No legado o mesmo helper servia aos dois casos. |
| FE-187 | migrated | Console → Carteiras | verified | `frontend/src/app/pages/catalogs/WalletsPage.tsx` — lista com Carteira, Chave de integracao e contagem de borderos (`:33-47`), e painel lateral com "Nome da carteira" (`:62-70`), "Chave de integracao" na edicao (`:72-79`) e o interruptor Ativo (`:81`) — os tres campos do helper do legado. Estados e exclusao pelo `CatalogScreen` (teste `src/app/pages/catalogs/__tests__/CatalogScreen.test.tsx`, passou). |
| FE-188 | migrated | Console → Tipos de Recebíveis | verified | `frontend/src/app/pages/catalogs/ReceivableKindsPage.tsx` — mesma estrutura de FE-187: lista com Tipo, Chave de integracao e contagem de borderos (`:28-46`) e painel com "Nome do tipo" (`:57-65`), "Chave de integracao" na edicao (`:67-74`) e Ativo. Teste: `src/app/pages/catalogs/__tests__/CatalogScreen.test.tsx` (passou). |
| FE-189 | migrated | Console → Tipos de Movimentação (tarifas) | verified | `frontend/src/app/pages/catalogs/MovementKindsPage.tsx` — os NOVE campos do helper do legado estao la: titulo (`:178`), chave de integracao (`:188`), Ativo (`:257-261`), "Aparece no bordero" (`is_operation`, `:227-240`), marcador "titulo" (`is_title`, `:242-255`) e os quatro classificadores (`is_advalorem`/`is_desagio`/`is_iof`/`is_liquidation`) reunidos num `RadioGroup` de escolha UNICA (`:213-226`) — que fecha o caminho para o erro cru "Multiplos tipos" do legado sem esconder nada (o servidor continua recusando dois com 422). Mais o "Sentido contabil" (`:195-209`). Lista com Classificador, Sentido, "No bordero", Chave e contagem de tarifas (`:91-130`). |
| FE-191 | migrated | Lista — estados do container | verified | `frontend/src/app/pages/renegotiations/RenegotiationsPage.tsx:255-290` (desktop, via `DataTable`) e `:242-250` (telefone, `ListaEstreita` com `MobileListState`): carregando, lista, vazio, vazio-com-filtro citando o termo (`:270-275`) e ERRO com "tentar de novo" (`:261-262`). No legado o `failure` do proxy era no-op e a tela ficava no ultimo estado, sem mensagem. Teste: `src/app/pages/renegotiations/__tests__/renegotiations.test.tsx` (passou). |
| FE-192 | migrated | Lista — busca incremental por fornecedor | verified | `frontend/src/app/pages/renegotiations/RenegotiationsPage.tsx:76` — `useDebouncedSearch({ delay: 300 })`, com entrada so de espacos ignorada (`src/hooks/useDebouncedSearch.ts:20-46`). A busca casa NOME e FORNECEDOR, corrigindo o BE-190 do legado (que so olhava `provider_name`, e digitar o titulo da renegociacao nao achava nada). Teste: `renegotiations.test.tsx` (passou). |
| FE-193 | migrated | Lista — filtros (toggle + estado + tipo) | verified | `frontend/src/app/pages/renegotiations/RenegotiationsPage.tsx:201-224` — `Select` de estado e de tipo, com as opcoes vindas do endpoint `options` do servidor (`opcoes.data?.states` / `.kinds`), mais o botao "Limpar" quando ha filtro (`:229-235`). Divergencia registrada: os filtros ficam sempre visiveis, sem o botao "Filtros" que no legado escondia o bloco. |
| FE-194 | migrated | Lista — ordenação clicando no cabeçalho | blocked | A ordenacao FUNCIONA no servidor com o ciclo asc -> desc -> neutro (`frontend/src/app/pages/renegotiations/RenegotiationsPage.tsx:263-268` com `sortMode="server"`; ciclo em `components/ui/DataTable.tsx:386-393`). FALTA o acumulo MULTI-COLUNA: `ordenacao` guarda um unico `SortState` e cada clique substitui o anterior. No legado a flag `odrenacaoMultipla` era `true` fixa e acumulava chaves em arrays paralelos, e o mapa pediu "reuse do `SortableTableHeader` (FE-159) — ciclo asc/desc/neutro, ACUMULADO" (`.migration-ai9/map/receivables-renegotiations.md:276`). Mesmo caso de FE-159 — convem uma decisao unica para os dois. |
| FE-195 | migrated | Lista — navegação/paginação | verified | `frontend/src/app/pages/renegotiations/RenegotiationsPage.tsx:294-315` — `MobilePagination` no telefone e `PaginationPill` no desktop; primeira/anterior/proxima/ultima com `disabled` nas bordas e o seletor de itens por pagina em `components/ui/PaginationPill.tsx:165-221`. O `total`/`total_pages` vem do servidor (DEC-62), sem o calculo de "ultima pagina" a mao do legado. |
| FE-196 | migrated | Lista — widget da renegociação e menu de ações | blocked | A linha abre o detalhe (`frontend/src/app/pages/renegotiations/RenegotiationsPage.tsx:269`, e o widget mostra titulo E fornecedor, corrigindo o `data-title` que usava so `provider_name`). "Editar" existe, uma camada depois, no detalhe (`app/pages/renegotiations/RenegotiationDetailPage.tsx:129-131`, rota `/renegotiations/:id/edit` em `app/consoleNavigation.tsx:370`). FALTA O MENU DE ACOES NO DESKTOP: `acoesDaLinha` (`:132-149`) so e consumido pelo `MobileActionsSheet` (`:317-322`), que por sua vez so e alimentado pelo `ListaEstreita` (`onAcoes={setAcoesDe}`, `:248`), renderizado apenas quando `mobile`. Em 1440 px nao ha `⋮` nenhum na linha — e o componente ja sabe virar dropdown ancorado no desktop (`components/mobile/MobileRowActions.tsx:46-58`, com teste `components/mobile/__tests__/rowActionsDesktop.test.tsx`). |
| FE-197 | migrated | Lista — botão "Cadastrar" com guarda de fornecedor | verified | `frontend/src/app/pages/renegotiations/RenegotiationsPage.tsx:184-190` e `:281-287` — "Nova renegociacao" so aparece com `!readonly`. A guarda de fornecedor virou estado da tela de cadastro, com atalho: `app/pages/renegotiations/RenegotiationFormPage.tsx:201-212` ("sem fornecedor" -> botao para `/providers`). Teste: `src/app/pages/renegotiations/__tests__/fornecedorEmUso.test.tsx` (2 casos, passaram). |
| FE-198 | migrated | Lista — exclusão com confirmação | blocked | A remocao mostra o erro REAL do servidor (`frontend/src/app/pages/renegotiations/RenegotiationsPage.tsx:114`, fecha o D-24) e o criterio de quando NAO da para remover fica escrito (`podeRemoverMotivo`, `:331`). Mas FALTAM duas coisas: (1) nao ha CONFIRMACAO — `:144` chama `remover.mutate(registro)` direto no clique, enquanto o legado abria "A operacao nao pode ser desfeita. Tem certeza?"; (2) no DESKTOP nao ha acao de remover nenhuma (ver FE-196: o menu so existe no telefone). O `ConfirmDialog` da biblioteca ja e usado para isso em `components/renegotiations/InstallmentsTab.tsx:355-410`. |
| FE-200 | migrated | Formulário — estados de bloqueio | verified | `frontend/src/app/pages/renegotiations/RenegotiationFormPage.tsx:201-224` — os dois bloqueios com explicacao e atalho: sem fornecedor leva a `/providers`, sem empresa leva a `/companies`; o formulario so e montado com os dois presentes (`:226`). No legado o formulario abria com selects vazios e a mensagem de empresa apontava para um handler que nao ia a lugar nenhum. Teste: `src/app/pages/renegotiations/__tests__/renegotiations.test.tsx` (passou). |
| FE-201 | migrated | Formulário — máscaras de dinheiro e percentual | verified | `frontend/src/app/pages/renegotiations/RenegotiationFormPage.tsx:307-360` — `MoneyInput` nos cinco campos monetarios e `PercentInput` na taxa (`components/ui/NumericInput.tsx`), no lugar dos handlers de `focus`/`keyup`/`focusout` reescritos por tela. O truncamento da 3a casa do legado e PRESERVADO por DEC-02. Testes: `src/components/ui/__tests__/MoneyInput.test.tsx` e `NumericInput.test.tsx` (passaram). |
| FE-202 | migrated | Formulário — datepicker de datas | verified | `frontend/src/app/pages/renegotiations/RenegotiationFormPage.tsx:286-292` — `DatePicker` compartilhado (`components/ui/DatePicker.tsx`), pt-BR por `formatDate` com `locale: ptBR` (`src/lib/utils/date.ts:59-62`), aceitando digitacao `dd/mm/aaaa` alem do calendario e com limpeza do campo. |
| FE-203 | migrated | Formulário — feedback de sucesso/erro | verified | `frontend/src/app/pages/renegotiations/RenegotiationFormPage.tsx:102` distingue "Renegociacao criada." de "Renegociacao atualizada." — o legado dizia "atualizada com sucesso" tambem na criacao. O erro sai por `mensagemDoServidor` (`:108`), e o formulario ainda reprova em tela o que falta antes de enviar (`:147`). |
| FE-207 | migrated | Detalhe — atualização assíncrona dos cards (update_card_values) | verified | `frontend/src/hooks/useRenegotiationChannel.ts:33-68` — os cartoes atualizam por EVENTO do `RenegotiationChannel`, que invalida a consulta (o payload nao carrega estado, entao a leitura continua passando pela autorizacao do endpoint). Sem temporizador: `src/__tests__/no-api-polling.test.ts` (2 casos, passaram) e `spec/channels/renegotiation_channel_spec.rb`. A queda do canal e SINALIZADA (`connected`, `:29-31`), no lugar do `error` no-op do legado — e um campo ausente nao derruba mais os demais (o legado escrevia campo a campo e levantava `TypeError`). |
| FE-208 | migrated | Detalhe → GERAL — galeria de imagens dos anexos | verified | `frontend/src/components/renegotiations/AttachmentGallery.tsx:51-151` — grade de miniaturas com o derivado nomeado `thumb` do ActiveStorage (`:162-170`), e imagem indisponivel vira MARCADOR sem derrubar a lista (`:153-176`). Substitui os dois processos externos por imagem a cada renderizacao do legado. Teste: `spec/requests/api/v1/renegotiation_attachments_spec.rb`. Divergencia registrada: nao ha lightbox (o legado abria PhotoSwipe no clique da miniatura); o clique leva ao download autorizado (FE-212). |
| FE-209 | migrated | Detalhe → GERAL — seção "Arquivos" | verified | `frontend/src/components/renegotiations/FilesSection.tsx:116-117` — o titulo traz a contagem E o teto ("Documentos (2/4)"), lidos do catalogo do servidor (`limites.data.max_files`, `:100-107`). No legado o `data-locked` era escrito no HTML e NUNCA lido pelo JS, entao o limite existia sem ninguem comunicar. Teste: `src/app/pages/renegotiations/__tests__/renegotiations.test.tsx` — "mostra quantos arquivos cabem e quanto cada um pode pesar" e "quando o teto foi atingido, DIZ isso e desabilita" (passaram). |
| FE-210 | migrated | Detalhe → GERAL — upload por drag & drop / seleção múltipla | verified | `frontend/src/components/ui/FileDropzone.tsx:59-105` — arrastar e soltar (`onDrop`, `:102`) e selecao multipla (`multiple`, `:72`, conforme `maxFiles`), com `accept` montado a partir dos tipos do servidor (`:73`) e validacao por `validatePick` de `features/attachments/validation.ts` (`:6,59`) — a MESMA funcao do resto do app. Montado em `components/renegotiations/FilesSection.tsx:122-125`. Teste: `renegotiations.test.tsx` e `src/features/attachments/__tests__/validation.test.ts`. |
| FE-211 | migrated | Detalhe → GERAL — exclusão de anexo | verified | `frontend/src/components/renegotiations/AttachmentGallery.tsx:129-142` — a acao de excluir aparece so para o AUTOR e, quando nao aparece, o lugar dela e ocupado por "Sem permissao para excluir" em vez de sumir calada. E o servidor recusa (BE-229): `spec/requests/api/v1/renegotiation_attachments_spec.rb`. No legado o botao era escondido com `style="display:none"` e nada checava do lado de la. |
| FE-212 | migrated | Detalhe → GERAL — visualizar/baixar anexo | verified | `frontend/src/components/renegotiations/AttachmentGallery.tsx:104-110` — botao de baixar/ver chamando `onDownload`, que passa por `lib/api/renegotiations.ts` (`attachments.download`), o endpoint AUTORIZADO da renegociacao, com credencial — no lugar do link `target="_blank"` para um caminho publico. Teste: `spec/requests/api/v1/renegotiation_attachments_spec.rb`. |
| FE-213 | migrated | Detalhe → aba PREVISÕES — cabeçalho e colunas | blocked | A barra de acoes esta certa e SOME inteira no somente-leitura (`frontend/src/components/renegotiations/InstallmentsTab.tsx:217`). FALTAM COLUNAS: a linha mostra quatro valores — Principal, Total, Pago e Pendente (`components/renegotiations/InstallmentRow.tsx:118-121`) — mais `#` e Vencimento (`:104-106`) e o selo de situacao. O legado tinha DOZE colunas e o mapa pediu as doze (`.migration-ai9/map/receivables-renegotiations.md:295`). Nao aparecem: Juros, Principal + Juros, CM, Principal + Juros + CM, e Mora e Atraso — e o dado JA VEM do servidor (`src/lib/api/renegotiations.ts:132-138`: `interest_value`, `main_value_with_interest`, `main_value_with_interest_cm`). |
| FE-214 | migrated | PREVISÕES — lista de parcelas | verified | `frontend/src/components/renegotiations/InstallmentsTab.tsx:202-213` (carregando e ERRO com "tentar de novo") e `:269-280` (vazio, com atalho para cadastrar quando `podeEscrever`). A lista do legado NAO tinha tratamento de erro nenhum. Teste: `src/app/pages/renegotiations/__tests__/renegotiations.test.tsx` (passou). |
| FE-215 | migrated | PREVISÕES — pagamentos aninhados (expandir/colapsar) | verified | `frontend/src/components/renegotiations/InstallmentRow.tsx:173-226` — os pagamentos aninhados abrem e fecham pelo proprio cabecalho da linha (`:89-113`, com `aria-expanded`), mostrando numero, data, dias de atraso, valor com juros/CM, mora e total pago; vazio explicito em `:176`. No modo selecao o clique e IGNORADO (`:91` e `:95`), como no legado. |
| FE-216 | migrated | PREVISÕES — menu de ações da parcela | verified | `frontend/src/components/renegotiations/InstallmentRow.tsx:133-166` — "Pagar" (desabilitado na parcela quitada), "Editar previsao" e "Remover previsao"; no telefone as mesmas acoes saem pelo `⋮` (`:157-165`). "Remover" segue o MESMO criterio do servidor: com pagamento lancado o icone fica inerte e o tooltip diz "Remova os pagamentos antes de excluir a previsao" (`:143-149`), em vez de simplesmente sumir. Teste: `renegotiations.test.tsx` (passou). |
| FE-217 | migrated | PREVISÕES — modo de seleção múltipla | verified | `frontend/src/components/renegotiations/InstallmentsTab.tsx:217-267` — "Selecionar para remover" entra no modo, "Sair da selecao" sai, "Selecionar todos" pega SO as previsoes sem pagamento (`selecionarTodas` sobre `semPagamento`, com o contador "N de M selecionavel(is)" em `:231-233`), e o rotulo do botao destrutivo traz a contagem ("REMOVER N PREVISAO(OES)", `:256`). A parcela com pagamento nao ganha caixa e o motivo aparece no lugar (`InstallmentRow.tsx:75-83`), e no modo selecao "Nova previsao" e o `⋮` somem (`:259` e `InstallmentRow.tsx:133`). Nada selecionado vira AVISO, nao caixa vazia (`:249-252`), e o lote passa por `ConfirmDialog` (`:355-376`). |
