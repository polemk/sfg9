# Phase 4 — lote `g5-ops` (118 itens)

Cada linha e um item ABERTO do razao. Preencha `veredito` com `verified`, `blocked` ou `dropped` e escreva a EVIDENCIA — arquivo:linha do ai9, ou o comando que provou. Sem evidencia o item continua aberto.

---

## Provas executadas nesta passada (27/08/2026), Ruby 3.4.9, banco `sfg9_test`

Referenciadas na coluna de evidencia como **P1**..**P5**. Nenhuma integracao externa foi
disparada; nenhum job foi enfileirado no Sidekiq em execucao; `sfg9_motor_load` e
`sfg_legacy_dump` nao foram tocados.

| # | Comando | Resultado |
| --- | --- | --- |
| **P1** | `bundle exec rspec spec/jobs spec/config` | **119 exemplos, 0 falhas** |
| **P2** | `bundle exec rspec spec/lib spec/channels spec/models/attachable_spec.rb spec/models/indicator_spec.rb spec/services/seeds spec/services/renegotiations spec/services/structured spec/services/auth/impersonate_service_spec.rb` | **717 exemplos, 3 falhas, 5 pendentes** — 1 falha era `PG::TRDeadlockDetected` em `reference_seeds_spec.rb:29` por execucao paralela de outro agente no mesmo `sfg9_test`, **reexecutada isolada: verde**; as outras 2 sao o defeito **P4-G5-01** (abaixo) |
| **P3** | `bundle exec rspec` nos 11 arquivos de `spec/requests/api/v1/` citados neste lote | **165 exemplos, 0 falhas** |
| **P4** | `npx vitest run src/app/__tests__/routing.test.tsx src/__tests__/marca-fonte-unica.test.ts src/features/receivables/__tests__/receivables.test.tsx` + `npx vitest run src/features/auth` | **54 testes** + **21 testes**, 0 falhas |
| **P5** | `bundle exec rspec spec/lib/sfg/etl` | **301 exemplos, 2 falhas, 3 pendentes** — as 2 sao o **P4-G5-01** |

## Defeitos achados nesta passada (registrados, NAO corrigidos — QA nao edita codigo)

- **P4-G5-01** — `spec/lib/sfg/etl/indicator_converters_spec.rb:131` e `:135` **falham de
  verdade e reproduzivelmente**: `#<InstanceDouble(Sfg::Etl::Source::Base)> received
  unexpected message :table? with ("project_indicator_connections")`. O conversor
  `app/lib/sfg/etl/converters/indicator_entries.rb:129` ganhou uma guarda `source.table?`
  e o dublê do spec nunca foi atualizado. Familia do lote **g6-dados**, registrado aqui
  porque apareceu ao provar OPS-150.
- **P4-G5-02** — `frontend/scripts/check-coercion-golden.mjs` **nao roda mais**:
  `ERR_MODULE_NOT_FOUND: Cannot find package '@/lib'`. `frontend/src/lib/utils/number.ts:14`
  passou a importar `@/lib/config/currency` (commit `8ae521873`) e o alias do Vite nao
  resolve em `node` puro, nem com `--experimental-strip-types`, que e como o proprio
  cabecalho do script manda rodar. O script tambem **nao esta em `package.json`** e
  **nenhum vitest le `golden/coercion.json`**. Consequencia: o portao que "reprova
  divergencia nao declarada" do lado do front esta morto em silencio. Bloqueia OPS-619.
- **P4-G5-03** — `backend/bin/delayed_job` **existe e esta versionado**
  (`git ls-files backend/bin/delayed_job` devolve o caminho), e faz
  `require 'delayed/command'` — gem **ausente** do `Gemfile.lock`
  (`grep -c delayed_job backend/Gemfile.lock` = **0**). E um daemon morto apontando para
  uma fila que nao existe. Contradiz a evidencia escrita no razao para OPS-461/OPS-633
  ("Sem bin/delayed_job, sem pidfile"). Bloqueia OPS-633.
- **P4-G5-04** (base ai9, **fora** do escopo de paridade sfg) —
  `app/controllers/assets_proxy_controller.rb` herda de `ActionController::Base`, **sem
  nenhuma autenticacao**, e serve `public/uploads/**` pela rota
  `config/routes.rb:7` (`GET /media_vault/*path`). `app/controllers/api/v1/uploads.rb:111`
  continua gravando binario cru em `public/uploads/avatars/` — **22 arquivos ja la**.
  Nao quebra OPS-192/OPS-505 porque **nenhum anexo do Safegold** passa por esse caminho
  (User/Project/Provider/Carrier usam `sfg_attachment`); o unico consumidor restante e o
  chat-builder da base (DEC-13.2, desacoplado).

---

| ID | estado atual | feature | veredito | evidencia |
| --- | --- | --- | --- | --- |
| OPS-001 | migrated | E-mail de boas-vindas com credenciais para usuário recém-criado | verified | `app/mailers/auth_mailer.rb:34` (`def invite`) enfileirado por `app/services/auth/email_service.rb:44` (`.invite.deliver_later`); porta unica `POST /api/v1/users/:id/invite` em `app/controllers/api/v1/users.rb:377`, com `require_permission!('may_invite_users')` na :383 (DEC-108: no legado a permissao so escondia o botao). **Nao ha credencial no corpo do e-mail** — o produto nao tem senha (DEC-14/DEC-75), o convite leva codigo. Teste: `spec/requests/api/v1/users_account_lifecycle_spec.rb` (**P3**). |
| OPS-002 | migrated | E-mail de instruções de recuperação de senha | verified | Equivalente funcional entregue: `app/mailers/auth_mailer.rb:4` (`magic_login_code`), enfileirado em `app/services/auth/email_service.rb:23` (`deliver_later`) a partir de `app/services/auth/magic_login_service.rb:179`. Fila `<APP_NAME>_mailers` (`config/sidekiq.yml:6`). Entrega durável por `app/mailers/application_mailer.rb:13` (`self.delivery_job = LoggedMailDeliveryJob`). Teste: `spec/jobs/logged_mail_delivery_job_spec.rb` (**P1**). |
| OPS-005 | migrated | Job InsertProjectsOnDefaultUserJob — associa o usuário "membro padrão" a todos os projetos | verified | `app/jobs/default_member_job.rb:18` (`queue_as :low_priority` — fila declarada em `config/sidekiq.yml:11`), `:22` `perform(user_id)`. Mesmo efeito do legado (`../sfg/lib/insert_projects_on_default_user_job.rb`), com ids explicitos. Teste: `spec/jobs/default_member_job_spec.rb` (**P1**). |
| OPS-006 | migrated | Reexecução do job em todo update do usuário | verified | **Divergencia deliberada e provada**: `app/models/user.rb:168` (`after_commit :enqueue_default_member_job, on: %i[create update]`) e `:377-379` — o job so e enfileirado quando `saved_change_to_is_default_member?` **e** `is_default_member?`. O legado reenfileirava em TODO update. Comentario da regra na :165. Teste: `spec/jobs/default_member_job_spec.rb` (**P1**). |
| OPS-007 | migrated | Configuração OmniAuth/Facebook | verified | `config/initializers/devise.rb:35-42` — estrategia so registrada com `OAUTH_FACEBOOK_ENABLED=true`; default `false`. Chaves em `Rails.application.credentials.dig(:oauth, :facebook, …)` (⚠ o razao dizia "ENV FACEBOOK_*": e `credentials`, nao ENV — evidencia corrigida aqui; o que importa, chave fora do codigo, se mantem). Teste: `spec/config/platform_config_spec.rb:90` (`Devise.omniauth_providers` nao inclui `:facebook`) — **P1**. |
| OPS-008 | migrated | Configuração de autenticação por aplicação cliente e realm | verified | Metade "aplicacao cliente" migrada: `app/models/client_application.rb:3` (token unico, escopo `active`), autenticado em `app/controllers/api/root.rb:161` (`ClientApplication.active.find_by(token:)`), consumido por `app/lib/sfg/whats/access.rb:35` e `app/services/users_service.rb:68`. Teste: `spec/models/client_application_spec.rb`. ⚠ **`realm` NAO e portado, e e inerte**: no legado era `Devise.http_authentication_realm = 'auth #livetatmobile'` (`../sfg/engines/auth19/config/initializers/devise.rb:6`), string do desafio HTTP Basic; o ai9 e `api_only = true` (`config/application.rb:23`), o `User` nao tem `:http_authenticatable` e o 401 e JSON. Zero efeito observavel. ⚠ o razao citava `spec/requests/api/v1/credentials_spec.rb`, que e do model `Credential` (chaves de terceiro) — **outra coisa**; evidencia corrigida aqui. |
| OPS-009 | migrated | Seeds de tipos de usuário, permissões e reset em massa | verified | `db/seeds/reference/user_types.rb` (4 papeis, DEC-41) e `db/seeds/reference/permissions.rb` (7 das 17 abilities, DEC-108), plugados em `app/services/seeds/reference/runner.rb:35` (`CATALOGS`). O **"reset em massa"** do legado (`../sfg/db/seeds.rb:137-139`, `Livetat::Auth::User.all.each(&:save)` + fabricacao de aceite retroativo nas :141-157) **nao e portado de proposito** — o cabecalho do `CATALOGS` escreve o motivo no bloco de `Seeds::Reference::Contracts`. Testes: `spec/services/seeds/reference_seeds_spec.rb` e `reference_runner_spec.rb` (**P2**). |
| OPS-050 | migrated | Integração ReceitaWS para consulta de CNPJ | blocked | **Falta a credencial paga da ReceitaWS** — decisao comercial, nao tecnica: **DEC-130** ("vamos deixar esse para se o cliente comprar"). O codigo esta pronto e provado nos dois caminhos de erro (`app/services/sfg/receita_ws/lookup_service.rb`, teste `spec/requests/api/v1/cnpj_lookup_spec.rb` verde em **P3**), mas **sem token nao ha integracao para verificar**, e a instrucao proibe disparar integracao real. Para fechar: contratar/registrar o token e grava-lo no `Credential` de provedor `receitaws` (DEC-61), depois exercitar uma consulta real de CNPJ. |
| OPS-051 | migrated | Armazenamento de anexos (logos de Provider e Carrier) | verified | `app/models/carrier.rb:90` e `app/models/provider.rb:32` — ambos por `sfg_attachment :logo` (motor unico, DEC-91). Validacao por conteudo real: `app/models/concerns/attachable.rb:67` (`spoofing_protection: true`), com o motivo escrito em `carrier.rb:83`. Limite e derivados vem de `config/attachments.yml:89` e `:104`. Testes: `spec/models/attachable_spec.rb` (**P2**) e `spec/requests/api/v1/carriers_spec.rb` (**P3**) — o `.exe` renomeado para `.png` e RECUSADO. |
| OPS-054 | migrated | Seeds de Segment, Carrier e Company | verified | Seed de REFERENCIA separado do de DEMONSTRACAO: `app/services/seeds/reference/` + `db/seeds/reference/`, carregador unico em `app/services/seeds/reference/runner.rb:35` (`CATALOGS`), alvo `bin/rails reference:seed` (`lib/tasks/reference.rake`) — **conferido em `bin/rails -T`**. Demo em `db/seeds/demo/` sob `bin/rails demo:seed`. Testes: `spec/services/seeds/reference_runner_spec.rb` e `reference_seeds_spec.rb` (**P2**). |
| OPS-055 | migrated | Rotinas manuais de correção de dados | verified | `lib/tasks/fix_project_data.rake:27` (`task fix_company_links`) — **`DRY_RUN=1` e o DEFAULT** (cabecalho :13: rodar sem argumento nao altera nada; `APPLY=1` aplica). Alvo visivel em `bin/rails -T` como `sfg:fix_company_links`. Escrita pelo MODEL, entao a trilha do paper_trail registra. Teste: `spec/lib/tasks/fix_project_data_spec.rb` (**P2**). Nao executado contra o banco de desenvolvimento compartilhado, de proposito. |
| OPS-058 | migrated | counter_cache de carriers_count | verified | **A reprovacao P4-01 de 26/08 esta CORRIGIDA e provada.** `app/models/carrier.rb:37` (`counter_cache: :carriers_count`); o `delete_all` de `db/seeds/demo/reset.rb:178` agora e seguido de `::CarrierGroup.reset_counters(id, :carriers)` na **:188**, com o motivo escrito nas :150 e :164. Testes rodados nesta passada: `spec/lib/demo/reset_carrier_counter_spec.rb` + `spec/requests/api/v1/carrier_groups_spec.rb` → **11 exemplos, 0 falhas**, incluindo *"depois de DOIS ciclos reset+seed cada grupo continua declarando a contagem real"* e *"nao mexe em portador que nao e do seed"*. |
| OPS-080 | migrated | InsertDefaultUserOnProjectJob | verified | `app/jobs/link_default_members_job.rb:19` (`queue_as :low_priority`), `:27` `perform(project_id)`, `:63` `after_discard` — a falha vira log nomeado e evento `failed` no canal do projeto (D-05), ao contrario do `rescue` do legado. Testes: `spec/jobs/job_failure_visibility_spec.rb` e `spec/requests/api/v1/projects_spec.rb` (**P1**/**P3**). |
| OPS-081 | migrated | CreateGlobalTemplateForProjectJob | verified | `app/jobs/seed_global_templates_job.rb:21`, **enfileirado de verdade** em `app/services/project_service.rb:139` e `app/services/project_reset_service.rb:89` — era exatamente o defeito que a S13 achou (o job existia e ninguem o chamava). Teste: `spec/jobs/availability_template_jobs_spec.rb` + `spec/jobs/project_creation_jobs_spec.rb` (**P1**). |
| OPS-084 | migrated | ProjectAvailabilityTemplateRemovalJob | verified | `app/jobs/remove_project_template_job.rb:53-61` — `ProjectAvailabilityTemplate.transaction`, `destroy!` do mais fundo para a raiz (`order(level: :desc)`), nunca `destroy_all`, e `Availability::TreeService.reorder_project!` ao fim. Teste: `spec/jobs/availability_template_jobs_spec.rb` (**P1**), que cobre a reexecucao terminando sem erro. |
| OPS-085 | migrated | InsertProjectsOnDefaultUserJob | verified | `app/jobs/default_member_job.rb` (o espelho de OPS-005: usuario → todos os projetos). O `rescue` VAZIO do legado (`../sfg/lib/insert_projects_on_default_user_job.rb:11-12`) virou `after_discard` na :63, que nomeia job, usuario e causa. Testes: `spec/jobs/default_member_job_spec.rb`, `spec/jobs/job_failure_visibility_spec.rb` (**P1**) e `spec/requests/api/v1/project_scope_spec.rb`. |
| OPS-087 | migrated | Progresso de job exibido na UI | verified | Ponto unico de emissao em `app/lib/sfg/job_progress.rb:46` (`publish(project_id:, job_id:, status:, …)`) com `type: PAYLOAD_TYPE` na :56 (contrato `ai9-conventions §3.8`); canal `app/channels/project_progress_channel.rb` com `reject` no `subscribed`. Teste: `spec/channels/project_progress_channel_spec.rb` (**P2**). |
| OPS-088 | migrated | Processamento de imagem do logo do projeto | verified | Derivados declarados em `config/attachments.yml:70-72` e `:81-82` (`thumb` 80px q100, `preview` 250px q70, `large` 1500px q50) e aplicados por `Sfg::Attachments.variant_url` (`app/lib/sfg/attachments.rb:214`). `project.logo` em `config/attachments.yml:89`, `project.avatar` na :76 com teto de 5 MB (`project.rb:127` do legado). Teste: `spec/models/attachable_spec.rb` (**P2**). |
| OPS-089 | migrated | Rotinas manuais de correção de dados | verified | `lib/tasks/fix_project_data.rake:57` (`task fix_project_data`), mesmo contrato de OPS-055: `DRY_RUN` por default, `APPLY=1` grava, escrita pelo model (auditada). Alvo em `bin/rails -T` como `sfg:fix_project_data`. Teste: `spec/lib/tasks/fix_project_data_spec.rb` (**P2**). |
| OPS-120 | migrated | CreateGlobalTemplateForProjectJob — semeia o projeto novo com toda a árvore de templates globais | verified | `app/services/availability/global_seeder.rb` (130 linhas), invocado por `app/jobs/seed_global_templates_job.rb:21`, que por sua vez e enfileirado em `project_service.rb:139`. Atomico e idempotente; `is_adjusted` copiado. Teste: `spec/jobs/availability_template_jobs_spec.rb` (**P1**). |
| OPS-121 | migrated | InsertGlobalTemplateOnProjectsJob — propaga um template global recém-criado para um projeto existente | verified | `app/jobs/propagate_global_template_job.rb:139` enfileira **um `PropagateGlobalTemplateToProjectJob` por projeto** (`app/jobs/propagate_global_template_to_project_job.rb`), com um evento de progresso por projeto. Teste: `spec/jobs/propagate_global_template_coordination_spec.rb` (**P1**). |
| OPS-124 | migrated | ProjectAvailabilityTemplateRemovalJob — remove definitivamente um template do projeto, seus descendentes e todas as entradas | verified | Mesma prova de OPS-084: `app/jobs/remove_project_template_job.rb:53-61`. Fecha o D-103 — o `destroy_all` do legado contornava o proprio `restrict_with_error` e apagava lancamento financeiro sem aviso; aqui e `destroy!` registro a registro, dentro de transacao. Teste: `spec/jobs/availability_template_jobs_spec.rb` (**P1**). |
| OPS-127 | migrated | Progresso ao vivo dos jobs de disponibilidade | verified | `app/models/concerns/job_progressable.rb:30` (`ongoing_job?`), `:32` (`failed_job?`), `:44` (`live_progress_percent`) — corrige os dois defeitos do legado, escritos na :37 (sem job devolvia **100**; no fim devolvia **0**). Emissao unica por `app/lib/sfg/job_progress.rb:46`, com a chave `status:` que o front le. Testes: `spec/models/attachable_spec.rb` (**P2**) e `spec/channels/project_progress_channel_spec.rb`. |
| OPS-128 | migrated | Política de retenção/retry dos jobs falhos | verified | `config/sidekiq.yml:34-35` — `dead_max_jobs` (10.000) e `dead_timeout_in_seconds` (15.552.000 = 6 meses), com o motivo escrito nas :16-33. Retentativa e o **default do Sidekiq** (25 tentativas, ~21 dias): `app/jobs/application_job.rb` **nao** empilha `retry_on`. Fila `low_priority` declarada em `config/sidekiq.yml:11`. Testes: `spec/jobs/job_discipline_spec.rb` (**P1**) — inclusive os dois `Adding dead Sidekiq::ActiveJob::Wrapper job` observados no log da execucao, que sao o dead set funcionando. |
| OPS-129 | migrated | Rotinas manuais de conserto (executáveis só por console Rails) | verified | Viraram rake tasks nomeadas e visiveis em `bin/rails -T`: `lib/tasks/availability.rake:27` (`sfg:availability:report`, **so leitura**), `:39` (`reconcile_virtual_values`, so reporta; `APPLY=1` grava) e `:69` (`renumber`). O `destroy_existing` destrutivo do legado **nao** e portado. Nao executadas contra o banco compartilhado, de proposito. |
| OPS-150 | migrated | Importação do sistema legado (recebíveis e catálogos) | verified | **D-PAR-02 esta FECHADO.** Commit `d3379e0b` ("Carga completa fechada: DEC-132 a DEC-135"): *"A cascata do ETL fechou contra o dump de producao: 32 tabelas, 741.554 linhas, reconciliacao sem bloqueio, delta unico nos 2 `contract_deals` que a DEC-126 mandou descartar"*. Conversor: `app/lib/sfg/etl/converters/receivable_entries.rb:378` (`anomalies`). Prova de codigo re-executada agora: **P5** — `spec/lib/sfg/etl` → 301 exemplos, 2 falhas, e **as 2 falhas nao sao deste caminho** (sao o **P4-G5-01**, no conversor de indicadores). A carga em si nao foi re-executada: `sfg9_motor_load`/`sfg_legacy_dump` sao intocaveis por instrucao. |
| OPS-151 | migrated | Recalculo em massa de todos os recebíveis | verified | `app/jobs/receivables/bulk_recalculate_job.rb:33`, `queue_as :default` na :34, `perform(project_id, sync_risk: false, batch_size: 500)` na :36. Melhora medida sobre o legado (`ReceivableEntry.all.each(&:save)`, sem lote nem progresso): `find_each` em lotes, falhas visiveis (:58), **mesma** formula da tela (C2) e aliquota de IOF vigente na data do bordero (corrige D-15). `sync_risk` nasce `false` para nao desfazer o DEC-36 em silencio. Teste: `spec/jobs/receivables/bulk_recalculate_job_spec.rb` (**P1**). ⚠ **Sem rake task nem endpoint** — `grep -rn BulkRecalculateJob app lib config` so acha o proprio arquivo. E paridade porque o legado tambem era **so console**, mas ao contrario de OPS-055/089/129/311 este nao ganhou alvo nomeado; registrado para o Phase 5. |
| OPS-153 | migrated | Seeds dos catálogos | verified | `app/services/seeds/reference/{wallets,receivable_kinds,movement_kinds}.rb`, plugados em `app/services/seeds/reference/runner.rb:35` (`CATALOGS`) com o motivo escrito no bloco S6 (os tres sao FK **obrigatoria** de `receivable_entries`). ⚠ Achado que o proprio cabecalho registra e que confere no legado: la o seed existia e **nunca rodava** — as flags `should_seed_*` vinham `false` em `../sfg/db/seeds.rb`. Testes: `spec/services/seeds/reference_seeds_spec.rb` e `reference_runner_spec.rb` (**P2**). |
| OPS-154 | migrated | Textos de ajuda do formulário de recebível | verified | `app/services/receivables/help_texts.rb:25` (`CONFIG_PATH = 'config/receivables_help_inputs.yml'`), `:29` `all`, `:35` `for(field)`, `:39` `reload!`. O arquivo `config/receivables_help_inputs.yml` existe (42 linhas) e e o mesmo catalogo de `../sfg/config/receivables_help_inputs.yml`, citado na :8. |
| OPS-157 | migrated | Portabilidade da busca entre bancos | verified | `app/models/receivable_entry.rb:96` (`scope :search`) — consulta portavel, sem fragmento especifico de banco. Teste: `spec/requests/api/v1/receivables_spec.rb`. |
| OPS-158 | migrated | Datas-sentinela para filtros sem período | verified | `app/models/receivable_entry.rb:112` (`scope :between_dates`). As sentinelas viraram `Sfg::DateBounds` (`app/lib/sfg/date_bounds.rb`) em vez de monkey-patch; **sem `from`/`to` o predicado simplesmente nao e aplicado**, que e a mudanca declarada em OPS-283. Teste: `spec/requests/api/v1/receivables_spec.rb`. |
| OPS-159 | migrated | Formatação monetária (BRL hardcoded) | verified | `app/lib/sfg/money.rb:35` (`format`) e `:42` (`with_symbol(value, blank: '—')`) — e a correcao escrita na :12/:29: no legado *"nao informado" e "zero" viravam o mesmo `R$ 0,00`*; aqui nulo rende travessao e zero rende `R$ 0,00`. Lado do front: `frontend/src/lib/utils/number.ts`. Teste: `frontend/src/features/receivables/__tests__/receivables.test.tsx` — **13 testes verdes em P4**. |
| OPS-190 | migrated | Cron CRONFacade.update_renegotiations_counters — recálculo diário dos contadores | verified | **O cron diario deixa de existir, e a ausencia esta DECLARADA**: `config/schedule.yml:114-157` documenta os quatro defeitos do original (`../sfg/config/schedule.prod.rb` + `../sfg/lib/cron_facade.rb`, conferidos no legado agora) e o que o substitui: `app/services/renegotiations/aggregate_service.rb:58` (`recalculate!`, por evento, dentro da transacao, com `save!`) e `:121` (`live_overdue_for`, apuracao na consulta). Testes: `spec/jobs/no_batch_sweep_spec.rb` (o portao que reprova a volta do laco) e `spec/services/renegotiations/aggregate_service_spec.rb` (**P1**/**P2**). |
| OPS-191 | migrated | Agendamento por ambiente | verified | O legado tinha tres arquivos (`schedule.rb`, `schedule.dev.rb`, `schedule.prod.rb`, os dois ultimos com o mesmo conteudo); o ai9 tem **um** — `config/schedule.yml` — carregado por `config/initializers/sidekiq_cron.rb` no `on(:startup)`, com fuso explicito `America/Sao_Paulo` em toda entrada. Ausencia do cron de renegociacao documentada nas :114-157. Testes: `spec/config/schedule_spec.rb` (10 exemplos, incl. *"reprova uma entrada cuja classe nao existe"* e *"reprova uma expressao cron invalida"*) — **P1**. |
| OPS-192 | migrated | Armazenamento de anexos em disco local dentro de public/ | verified | Nenhum anexo do Safegold vai para `public/`: `config/attachments.yml` + `app/lib/sfg/attachments.rb:193` (`url_for`, URL **assinada** com prazo — `url_expires_in` na :115, `image_url_expires_in` na :121). ⚠ Ressalva escrita: `public/uploads/avatars/` **ainda recebe binario** por `app/controllers/api/v1/uploads.rb:111`, servido sem autenticacao por `assets_proxy_controller.rb` (`routes.rb:7`) — mas esse caminho e do **chat-builder da base ai9** (DEC-13.2), nao de anexo migrado: `frontend/src/app/pages/ProfilePage.tsx:369` e `UsersPage.tsx:403` registram a saida do avatar de la. Ver **P4-G5-04**. Teste: `spec/requests/api/v1/renegotiation_attachments_spec.rb` (**P3**). |
| OPS-193 | migrated | ImageMagick (identify) para dimensões de imagem | verified | Derivados declarados em `config/attachments.yml:70-72`/`:81-82` e resolvidos por `Sfg::Attachments.variant_url` (`app/lib/sfg/attachments.rb:214`); as dimensoes vem dos metadados do blob do ActiveStorage — **zero processo externo por render**, ao contrario do `identify` por requisicao do kt-paperclip. Teste: `spec/requests/api/v1/renegotiation_attachments_spec.rb` (**P3**). |
| OPS-194 | migrated | Detecção de spoof de media type desabilitada globalmente | verified | **Invertido de proposito**: `app/models/concerns/attachable.rb:67` (`spoofing_protection: true`) — o tipo vem dos magic bytes (Marcel), nao do `Content-Type` do cliente. O motivo esta escrito na :62 e reforcado em `carrier.rb:83` (`content_type:` sem `spoofing_protection: true` e validacao decorativa). Teste: `spec/models/attachable_spec.rb` (**P2**) e `spec/requests/api/v1/renegotiation_attachments_spec.rb` (**P3**) — `.svg`/`.html` renomeado para `.pdf` e REJEITADO. |
| OPS-195 | migrated | Escritas em massa sem callbacks (activerecord-import) | verified | `app/services/renegotiations/renumber_installments.rb:38` (`scope.update_all(...)`, com o motivo nas :6) e `renumber_payments.rb:6`. Sem callbacks, so o ordinal muda — mesmo efeito do `activerecord-import` do legado, sem a gem. Teste: `spec/requests/api/v1/renegotiation_installments_spec.rb`. |
| OPS-196 | migrated | Geração de cor por lote de parcelas | verified | `app/services/renegotiations/batch_color.rb:32` (`PALETTE`, finita), `:37` (`next_for`), `:44` (primeira cor livre) e `:49` (fallback deterministico `PALETTE[usadas.size % PALETTE.size]`) — **terminacao garantida**, ao contrario do sorteio-ate-achar do legado. Teste: `spec/requests/api/v1/renegotiation_installments_spec.rb`. |
| OPS-283 | migrated | Sentinelas de data DateTime.dinosaurs / DateTime.mars | verified | `app/lib/sfg/date_bounds.rb:1-8` substitui `../sfg/config/initializers/date_overload.rb` **sem reabrir `Date`/`DateTime`**. Efeito identico para data preenchida; a mudanca declarada e que operacao com data NULA passa a aparecer quando nao ha filtro, em vez de sumir pelo `DATE(NULL)`. Testes: `spec/lib/sfg/coercion_spec.rb` (secao de monkey patch, **P2**) e `spec/requests/api/v1/structured_operations_spec.rb`. |
| OPS-287 | migrated | Zero cobertura de testes nesta unidade | verified | `spec/services/structured/remuneration_calculator_spec.rb` existe e passa — **21 blocos `it`** contados agora (o razao dizia 25; numero corrigido aqui), verdes em **P2**. Cobre os dois lados da hierarquia polimorfica (EST e LIQ, contrato C3). Era a primeira cobertura de `StructuredOperation`/`Remuneration` — no legado a formula que multiplica o faturamento nao tinha um unico teste. |
| OPS-288 | migrated | Superfície de injeção SQL nos parâmetros de ordenação | verified | `app/controllers/api/v1/structured_operations.rb:54` — `optional :ordering_keys, type: Array[String], values: ::StructuredOperation::ORDERING.allowed.keys` (allowlist **no `params do` do Grape**, entao chave desconhecida responde **400** antes de chegar ao SQL) e `:58` idem para `ordering_style`. O `prepare_ordering` copiado em 18 models do legado deu lugar a `app/lib/sfg/sortable.rb`, que monta Hash/Arel. Teste: `spec/requests/api/v1/structured_operations_spec.rb`. |
| OPS-311 | migrated | Indicator.fix_titles — tarefa manual de reprocessamento | verified | `app/services/indicators/backfill_service.rb` + `lib/tasks/indicators.rake:15` — **dry-run por default** (:26 imprime como aplicar), `[apply]` grava. Alvo conferido em `bin/rails -T`: `indicators:backfill[mode]`. No legado era `Indicator.all.each(&:save)` sem task, sem log, sem contagem e sem previa (:6). |
| OPS-313 | migrated | Perda de dados em cascata na exclusão de indicador | verified | `app/jobs/purge_discarded_indicators_job.rb:29` (`queue_as :low_priority`), `:31` (`DEFAULT_RETENTION_DAYS = 365`), `:33` `perform`. **Agendado de verdade**: `config/schedule.yml:108-112` (`10 4 * * * America/Sao_Paulo`, fila `low_priority`, que existe em `config/sidekiq.yml:11`). Nunca apaga indicador com lancamento ou conexao. Testes: `spec/models/indicator_spec.rb` (**P2**) e `spec/config/schedule_spec.rb:42` (*"nao deixa nenhum job de expurgo fora do schedule"*, **P1**). |
| OPS-314 | migrated | Ausência total de testes automatizados | verified | Cobertura existe e passa: `spec/models/indicator_spec.rb` e `spec/services/indicators/` verdes em **P2**; `spec/requests/api/v1/` do modulo verde em **P3**. O legado tinha ZERO neste modulo. |
| OPS-331 | migrated | Dependência de ENV['alias'] para montar as URLs públicas de contrato | verified | `config/initializers/public_host.rb:35` (le `APP_HOST`) e **:39** — `raise MissingHost, 'APP_HOST nao esta definido: os links publicos de contrato ficariam quebrados.'`. Falha no **boot**, nao em silencio como o `ENV['alias']` do legado. Teste: `spec/requests/api/v1/public/contracts_spec.rb` (**P3**). |
| OPS-332 | migrated | arquivo user.html em db/seed_assets/contracts/ sem uso | verified | `app/services/seeds/reference/contracts.rb:30` — `IGNORED_SOURCES = %w[user.html].freeze`: o arquivo e **ignorado e registrado**, nao entra as cegas. Teste: `spec/requests/api/v1/contracts_spec.rb` (**P3**). |
| OPS-334 | migrated | Fluxo de aceite efetivamente desativado em produção | verified | Ciclo religado e verificado inteiro: `app/controllers/api/v1/contracts.rb:52` (`GET /pending`, alimenta o banner), `:81` (`PUT :id/accept`) e `:121` (agregado `pending` + `accepted`), com `frontend/src/components/contracts/TermsBanner.tsx`. DEC-65. Teste: `spec/requests/api/v1/contracts_spec.rb` (**P3**). |
| OPS-393 | migrated | Histórico do navegador via replaceState | verified | **Corrigido de proposito (D-92)**: `frontend/src/app/consoleNavigation.tsx:264`, `:409`, `:444`, `:479`, `:494` — `pushState` por navegacao, com o comentario nomeando o defeito legado (`replaceState` fazia o botao Voltar SAIR do console). Teste: `frontend/src/app/__tests__/routing.test.tsx:196` — **15 testes verdes em P4**, incluindo a rota curinga (*"area desconhecida mostra a tela de nao encontrado — nunca tela em branco"*). |
| OPS-394 | migrated | E-mail transacional a observers — Notification.new_observer / remove_observer / notify_observers_about_message_received | verified | Os tres viraram metodos com **template ERB**: `app/mailers/observer_mailer.rb:13` (`message_received`), `:22` (`added`), `:31` (`removed`), com as views `app/views/observer_mailer/{message_received,added,removed}.html.erb`; mais `app/mailers/message_mailer.rb:9`/`:19`. No legado o HTML era concatenacao de string marcada `html_safe` (`../sfg/.../notification.rb:44-64`) — **sem escape do que o usuario digitou**. Falha de entrega vira log por `LoggedMailDeliveryJob`, nunca 500 num write ja efetivado. |
| OPS-395 | migrated | Impersonation (gem pretender) ativa em todo o console | verified | `app/services/auth/impersonate_service.rb:52` (`Authorization::Matrix.allow?`), `:60` (`Authorization::Hierarchy.can_impersonate?` — o legado so checava o ATOR, nunca o alvo, :6), `:39` (recusa impersonar dentro de impersonacao), `:65` (`record_trail(event: 'impersonate_start', …)` no paper_trail, DEC-59). Testes: `spec/services/auth/impersonate_service_spec.rb` e `spec/services/authorization/hierarchy_spec.rb` (**P2**), mais `frontend/src/features/auth/ImpersonationBanner.tsx` coberto em **P4**. |
| OPS-397 | migrated | ENV['alias'] usado para montar os links absolutos de contrato no rodapé da sidebar | verified | `frontend/vite.config.ts:37-38` (`apiUrl: env.VITE_API_URL`, `wsUrl: env.VITE_WS_URL`) e `frontend/.env.example:2`. O que era constante Ruby (`SFG::Metadata`) virou ENV lida em build/runtime; nenhuma chave de terceiro assada no bundle (`spec/config/platform_config_spec.rb:106` varre o `.env.example` atras de segredo com valor). Lado backend: `config/initializers/public_host.rb:39` (OPS-331). |
| OPS-460 | migrated | Backend de fila: delayed_job + delayed_job_active_record (fila em tabela delayed_jobs do Postgres, não Redis) | verified | Sidekiq sobre Redis: `config/sidekiq.yml:4-11` (5 filas prefixadas por `APP_NAME`), `config/initializers/sidekiq.rb:5-8`, prefixo em `config/application.rb:42` (`config.active_job.queue_name_prefix`). Gem `delayed_job` **ausente** — `grep -c delayed_job backend/Gemfile.lock` = **0**. Regra travada por portao: `app/jobs/application_job.rb:7` — *"`rescue` em job so existe para enriquecer o log, e SEMPRE termina em `raise`"* (D-79). Teste: `spec/jobs/job_discipline_spec.rb` (**P1**). |
| OPS-461 | migrated | Daemon do worker bin/delayed_job (gem daemons) | verified | Processo **declarado e supervisionado** em vez de daemon com pidfile: `Procfile:22` (`worker: cd backend && bundle exec sidekiq -C config/sidekiq.yml`), `Procfile.dev:2` idem. Gem `daemons` ausente do `Gemfile.lock`. O `Procfile:1-20` escreve o defeito do legado que isso fecha (worker morto nao era levantado por ninguem; pidfile orfao impedia subir de novo) e a armadilha do `-C config/sidekiq.yml`. ⚠ O arquivo `backend/bin/delayed_job` ainda existe — ver **P4-G5-03** e OPS-633. |
| OPS-462 | migrated | Política de retentativa e falha Delayed::Worker | verified | Default do Sidekiq (25 tentativas, backoff exponencial, ~21 dias) **sem `retry_on` empilhado** (`app/jobs/application_job.rb`), mais destino final em `config/sidekiq.yml:34-35`. ⚠ **Correcao de fato ao razao**: o legado (`../sfg/config/initializers/delayed_job_config.rb`) tinha `Delayed::Worker.max_run_time = 7.days` e `destroy_failed_jobs = false` — ou seja, o job falho **era mantido** na tabela, nao "sumia". O que ele nao tinha era onde olhar e como reenfileirar; o dead set do Sidekiq da as duas coisas. Teste: `spec/jobs/job_discipline_spec.rb` (**P1**). |
| OPS-463 | migrated | progress_job (ProgressJob::Base) — progresso de job persistido | verified | Estado do job na **entidade**, nao numa tabela de fila: `app/models/concerns/job_progressable.rb:9` (exige `job_state` e `job_progress`), `:21` (`scope :with_ongoing_job`), `:44` (`live_progress_percent`), com a migration `db/migrate/20260825220000`. `app/lib/sfg/job_progress.rb:81` (`persist`). Corrige os dois defeitos do legado (100% sem job; 0% no fim), escritos na :37. Teste: `spec/models/attachable_spec.rb` (**P2**). ⚠ o razao citava `lib/sfg/job_progress.rb`; o caminho real e `app/lib/sfg/job_progress.rb`. |
| OPS-465 | migrated | CreateGlobalTemplateForProjectJob — gera todos os templates de disponibilidade globais dentro de um projeto novo | verified | `app/jobs/seed_global_templates_job.rb:21` **com call site provado**: `app/services/project_service.rb:139` (`SeedGlobalTemplatesJob.perform_later(project.id, actor&.id)`, com o comentario na :135 registrando que o job existia e ninguem o chamava) e `app/services/project_reset_service.rb:89`. Teste: `spec/jobs/project_creation_jobs_spec.rb` + `spec/jobs/availability_template_jobs_spec.rb` (**P1**). |
| OPS-466 | migrated | InsertGlobalTemplateOnProjectsJob — propaga um template global recém-criado para cada projeto existente | verified | Coordenador + N filhos independentes: `app/jobs/propagate_global_template_job.rb:139` (`ids.each { PropagateGlobalTemplateToProjectJob.perform_later(...) }`), com o mecanismo do D-80 escrito na :24 — *dentro de um `find_each` o `raise` **aborta o laco***, entao o primeiro projeto que falhasse deixava os seguintes sem padrao. Teste: `spec/jobs/propagate_global_template_coordination_spec.rb` (**P1**). |
| OPS-467 | migrated | InsertDefaultUserOnProjectJob — associa todos os usuários "membro padrão" a um projeto novo | verified | `app/jobs/link_default_members_job.rb:19` (`queue_as :low_priority`), `:27` `perform(project_id)`, `:63` `after_discard` publicando o evento de conclusao/falha pelo ponto unico (`Sfg::JobProgress.publish`). Teste: `spec/jobs/job_failure_visibility_spec.rb` (**P1**). |
| OPS-468 | migrated | InsertProjectsOnDefaultUserJob — associa todos os projetos a um usuário que virou "membro padrão" | verified | `app/jobs/default_member_job.rb:63` (`after_discard`) — o `rescue` **vazio** do legado (`../sfg/lib/insert_projects_on_default_user_job.rb:11-12`) virou erro consultavel: nomeia JOB, USUARIO e CAUSA no log e publica `failed` no canal do projeto. Dois portoes: `spec/jobs/job_discipline_spec.rb` (impede a volta do antipadrao por leitura de codigo) e `spec/jobs/job_failure_visibility_spec.rb` (prova que a falha APARECE) — **P1**. |
| OPS-469 | migrated | ProjectAvailabilityTemplateActivateJob — ativa um template de disponibilidade e recalcula as entradas | verified | `app/jobs/activate_project_template_job.rb` + `app/services/availability/template_lock.rb` (`unlock!` em `ensure`, fecha o D-05) + **watchdog** `app/jobs/unlock_stale_templates_job.rb`, agendado de verdade em `config/schedule.yml:171-175` (`*/10 * * * * America/Sao_Paulo`, fila `low_priority`) com o motivo nas :159-170 (o `ensure` nao cobre SIGKILL/OOM/deploy). Testes: `spec/jobs/availability_template_jobs_spec.rb` e `spec/jobs/unlock_stale_templates_job_spec.rb` (**P1**). |
| OPS-470 | migrated | ProjectAvailabilityTemplateDeactivateJob — desativa o template e recalcula entradas | verified | `app/jobs/deactivate_project_template_job.rb`, mesmo `Availability::TemplateLock` e o mesmo watchdog de OPS-469 (`config/schedule.yml:171`). Teste: `spec/jobs/availability_template_jobs_spec.rb` (**P1**). |
| OPS-471 | migrated | ProjectAvailabilityTemplateRemovalJob — remove template, templates-filhos e suas entradas | verified | `app/jobs/remove_project_template_job.rb:53` (`transaction`), `:57` (`order(level: :desc).each(&:destroy!)` — nunca `destroy_all`, com o motivo na :55), `:61` (`Availability::TreeService.reorder_project!`). Fecha o D-103. Teste: `spec/jobs/availability_template_jobs_spec.rb` (**P1**). |
| OPS-472 | migrated | whenever — arquivo base config/schedule.rb | verified | `config/schedule.yml` (175 linhas, **a unica fonte de verdade dos crons**) carregado por `config/initializers/sidekiq_cron.rb` com `load_from_hash!(…, source: "schedule")`, que tambem **remove** cron marcado `schedule` que nao esteja no arquivo. A armadilha do Redis compartilhado com o `apl9` esta escrita nas :22-28 (remocao seletiva, nunca `FLUSHDB`). O defeito do **prefixo DUPLO** (`ai9_ai9_default`, 1159 jobs presos, 56 `CleanupLoginCodesJob` que nunca rodaram) esta medido nas :34-56. Testes: `spec/config/schedule_queue_spec.rb:34/:42/:52/:64` — 4 assercoes que reprovam a volta do defeito (**P1**). |
| OPS-473 | migrated | CRONFacade.update_renegotiations_counters — diário às 00:01 | verified | Idem OPS-190: nao existe cron nem laco sobre renegociacoes, e a ausencia esta declarada em `config/schedule.yml:114-157` comparando com o legado real (conferido agora: `../sfg/config/schedule.prod.rb` `every :day, at: '00:01 am'` → `../sfg/lib/cron_facade.rb`, `Renegotiation.where.not(state: STATE__CLOSED).each { \|r\| r.update_values! }`). Substituido por `app/services/renegotiations/aggregate_service.rb:58` e `:121`. Testes: `spec/jobs/no_batch_sweep_spec.rb` e `spec/services/renegotiations/aggregate_service_spec.rb` (**P1**/**P2**). |
| OPS-474 | migrated | NotificationFacade — porta de entrada assíncrona dos 3 e-mails transacionais do app base (alias global NF) | verified | Sem fachada: os pontos de envio usam `deliver_later` direto (`app/services/auth/email_service.rb:23`, `:44`), e a durabilidade observavel vem de `app/mailers/application_mailer.rb:13` (`self.delivery_job = LoggedMailDeliveryJob`) + `app/jobs/logged_mail_delivery_job.rb:26` (`< ActionMailer::MailDeliveryJob`), `:27` (`rescue_from StandardError` que **registra e relanca**, :20-24 explicando o D-79), `:37` (`record_failure`). Teste: `spec/jobs/logged_mail_delivery_job_spec.rb` (**P1**). |
| OPS-476 | migrated | Procfile / foreman — processos declarados | verified | `Procfile:21-22` — `web` (puma) e `worker` (sidekiq **com `-C config/sidekiq.yml`**). **Nao existe processo de agendador**: o `sidekiq-cron` roda dentro do processo Sidekiq (`Procfile:12-16`, `config/initializers/sidekiq_cron.rb`). O legado declarava so `server` e `assets` (`../sfg/Procfile`, conferido agora) — o worker era subido a mao. `Procfile.dev:1-3` para desenvolvimento. |
| OPS-477 | migrated | FileToStringDecoder — leitura de arquivo (BOM/UTF-8) para string, com variantes que trocam \n por <br/> e escapam via URI::D… | verified | `app/services/seeds/reference/contracts.rb:89` (`read_source`), `:35` (`MAX_SOURCE_BYTES = 512 * 1024`) e `:97-99` (arquivo acima do teto e **ignorado com aviso**) — o leitor nasce com limite, que o legado nao tinha. As **tres variantes** do `FileToStringDecoder` (troca de `\n` por `<br/>`, escape por `URI`) **nao** sao portadas: eram elas que produziam o texto raspado no meio da frase. Teste: `spec/requests/api/v1/contracts_spec.rb` (**P3**). |
| OPS-478 | migrated | CustomFailure — Devise::FailureApp custom | verified | **Medido por ausencia**: `config/application.rb:23` (`config.api_only = true`) e `grep -rn "failure_app\|Devise::FailureApp\|flash\[" backend/app backend/config` = **zero ocorrencias**. Nao ha `store_location`, nao ha `redirect_to root`, nao ha flash — o 401 e JSON pelo `Api::Root`. Teste: `spec/requests/api/v1/attachments_spec.rb` (**P3**). |
| OPS-479 | migrated | ActiveJob sem adapter configurado → deliver_later roda in-process | verified | As duas causas de perda do legado nao existem: (a) adapter definido — Sidekiq em `config/initializers/sidekiq.rb` e filas em `config/sidekiq.yml`; (b) `raise_delivery_errors = true` nos tres ambientes (`config/environments/production.rb:75`, `development.rb:44`, `test.rb:27`, este ultimo com o motivo escrito nas :22-24). Em `test` o adapter e `:inline` de proposito (`test.rb:35`). Teste: `spec/jobs/logged_mail_delivery_job_spec.rb` (**P1**). |
| OPS-480 | migrated | ReceitaWS — consulta de CNPJ (gem receitaws ~> 0.1.2) | verified | Mecanismo entregue e provado (a **credencial** e que falta, e isso e OPS-050/DEC-130): `app/services/sfg/receita_ws/lookup_service.rb:36` (`TIMEOUT_SECONDS = 10`), `:40` (`CACHE_TTL = 365.days`), `:45` (`DEFAULT_DAILY_LIMIT = 30` — **teto por usuario/dia que o legado nao tinha**, motivo nas :16-17), `:58-63` (cache **nao** consome quota), `:71` (`remaining_quota` exposto antes de estourar), `:18` (chave no `Credential`, DEC-61, nao em ENV versionada). Retorno **nao** e persistido. Teste: `spec/requests/api/v1/cnpj_lookup_spec.rb` (**P3**) — 422 para CNPJ invalido e 503 com mensagem propria quando o servico nao responde. |
| OPS-483 | migrated | city-state (gem) — estados e cidades do Brasil | verified | Sem gem: `app/controllers/api/v1/countries.rb` e `app/controllers/api/v1/br_states.rb:27` (`CODES = Countries::BRAZILIAN_STATES.map { \|uf\| uf[:code] }`) e `:30` — **uma** lista, e a de UF sai dela (:19). Teste: `spec/requests/api/v1/countries_states_spec.rb` (**P3**). |
| OPS-484 | migrated | SMTP (Office 365) via mailer19 / ActionMailer | verified | `config/environments/production.rb:73` (`delivery_method = :smtp`) e `:80-86` — endereco, porta, dominio, usuario, senha e autenticacao **todos por ENV**, sem arquivo de engine. `openssl_verify_mode` vem de `ENV.fetch('SMTP_OPENSSL_VERIFY_MODE', 'peer')`; o `none` do legado **nao** foi trazido (ver OPS-626). Teste: `spec/config/platform_config_spec.rb:49-55` — portao que reprova `openssl_verify_mode: 'none'` nos dois ambientes (**P1**). |
| OPS-485 | migrated | DKIM — assinatura das mensagens (gem dkim) | verified | `app/lib/sfg/dkim_signer.rb:56` (`domain = ENV['DKIM_DOMAIN'].presence`), `:58` (`selector`, default `dk`), `:30` (chave: primeiro `DKIM_PRIVATE_KEY`, depois `Credential` de provedor `dkim`), `:49` (**sem `DKIM_DOMAIN` o interceptor nao faz nada**) + `app/lib/sfg/dkim_interceptor.rb`, registrados em `app/mailers/application_mailer.rb`. Tres defeitos do legado fechados: chave **versionada** (`../sfg/lib/dkim_private_key.pem`, D-85), `open()` no boot derrubando a aplicacao quando o `.pem` faltava (agora nada e lido no boot — resolucao na hora do envio, envio **degradado** e aviso uma vez por processo), dominio/seletor assados no codigo. Teste: `spec/lib/sfg/dkim_signer_spec.rb` (**P2**). |
| OPS-486 | migrated | Google Analytics (GOOGLE_ANA_APP_ID = G-7E78XXZX5X) | verified | GA4 correto e **DESLIGADO por configuracao** (DEC-87): `frontend/vite.config.ts:32` (`gaEnabled = env.VITE_GA_ENABLED === 'true'`), `:44-49` — com a flag ligada injeta `/analytics-ga4.js`; desligada injeta **um comentario no HTML dizendo que a coleta esta desligada por decisao e onde se liga**. CSP bloqueante confere: `frontend/csp.config.ts:69` so libera `googletagmanager.com` quando a flag estiver ligada. O id do legado (`G-7E78XXZX5X`) **nao** esta no codigo — `grep` por ele = zero. |
| OPS-487 | migrated | Redis | verified | `config/initializers/sidekiq.rb:5-8` (`REDIS_URL`, `timeout: 15`) e filas prefixadas por `APP_NAME` em `config/sidekiq.yml:4-11` + `config/application.rb:42`. ⚠ A armadilha real esta escrita e travada: o **namespace de cron NAO e prefixado**, e o Redis e compartilhado com o `apl9` — `config/schedule.yml:22-28` proibe `FLUSHDB`/`destroy_all` e explica por que o `load_from_hash!` so remove `source == "schedule"`. |
| OPS-488 | migrated | Action Cable | verified | `config/cable.yml:1-8` (adapter `redis` em dev/prod, `test` em teste) + `app/channels/project_progress_channel.rb` (32 linhas) com `reject` no `subscribed` e payload carimbado com `type:` (`app/lib/sfg/job_progress.rb:56`, contrato `ai9-conventions §3.8`). Teste: `spec/channels/project_progress_channel_spec.rb` (**P2**). |
| OPS-491 | migrated | kt-paperclip ~> 7.0 + ImageMagick / image_processing — motor de anexos | verified | Motor **unico**, ActiveStorage direto no model (DEC-91): `config/attachments.yml` (179 linhas, catalogo CFG-02) + `app/models/concerns/attachable.rb` (139) + `app/lib/sfg/attachments.rb` (`url_for` :193, `variant_url` :214). Limite e allowlist vem do catalogo; magic bytes por `spoofing_protection` (:67). kt-paperclip ausente do `Gemfile.lock`. Teste: `spec/models/attachable_spec.rb` (**P2**). |
| OPS-492 | migrated | ActiveStorage / config/storage.yml + ActionText | verified | `app/controllers/api/v1/attachments.rb` (107 linhas) — **autoriza primeiro, assina depois**, URL com prazo (`Sfg::Attachments.url_expires_in`). Anexo inexistente e anexo nao autorizado respondem o **mesmo 404** (fecha o D-82, que vazava existencia). `config/storage.yml` (53 linhas) parametrizado. Teste: `spec/requests/api/v1/attachments_spec.rb` (**P3**). |
| OPS-494 | migrated | Avatar de projeto — Project#avatar | verified | `app/models/project.rb:17` (`sfg_attachment :avatar`) e `:213-216` (`avatar_url` devolve a variante `preview` com prazo de imagem, e **`nil`** quando nao ha anexo). Limite de 5 MB e politica `project_member` (C1) em `config/attachments.yml:76-79`. Teste: `spec/requests/api/v1/attachments_spec.rb` (**P3**). |
| OPS-497 | migrated | Logo de fornecedor — Provider#logo | verified | `app/models/provider.rb:32` (`sfg_attachment :logo`) e `:112-115` — `logo_url` devolve `nil` na ausencia, **nunca a string `"missing.jpg"`**, que era como o Paperclip do legado representava arquivo ausente (:24-25 registra que as 4 colunas `logo_*` e o `has_logo?` nao sao recriados). Limite de 1 MB e os derivados vem de `config/attachments.yml:104`. Teste: `spec/models/attachable_spec.rb` (**P2**). |
| OPS-498 | migrated | Logo de banco/carrier — Carrier#logo | verified | `app/models/carrier.rb:90` (`sfg_attachment :logo`) e `:160-161` — o nome `logo_url` e o contrato da API nao mudam; o que muda e quem monta a URL. DEC-47 (o logo volta) + DEC-91 (anexo direto no model, **nao** pelo `Medium` removido na DEC-113, :68). Convergencia registrada na :83: a declaracao a mao da S3 tinha 2 MB (legado = 1 MB) e **sem** `spoofing_protection`. Teste: `spec/requests/api/v1/carriers_spec.rb` (**P3**). |
| OPS-500 | migrated | Configuração SMTP global do app | verified | Mesma prova de OPS-484: `config/environments/production.rb:73-86`, tudo por ENV, mais `.env.example`. Portao contra segredo com valor no exemplo: `spec/config/platform_config_spec.rb:106` (**P1**). |
| OPS-501 | migrated | Assinatura DKIM de todos os e-mails | verified | Mesma prova de OPS-485: `app/lib/sfg/dkim_signer.rb:56/:58/:30/:49` + `dkim_interceptor.rb`, registrados em `ApplicationMailer`. **A chave privada saiu do repositorio** (no legado `../sfg/lib/dkim_private_key.pem` estava versionada, D-85) e a assinatura continua possivel. Teste: `spec/lib/sfg/dkim_signer_spec.rb` (**P2**). ⚠ Pendencia de **cutover, nao de codigo**: a escolha entre assinar no app (b) e deixar o provedor assinar (a) nao foi feita; o codigo suporta as duas e **nasce inerte** sem `DKIM_DOMAIN`. Runbook secao 2, junto com a rotacao da chave antiga. |
| OPS-502 | migrated | Worker delayed_job (envio assíncrono) | verified | Envio assincrono pela fila `<APP_NAME>_mailers` (`config/sidekiq.yml:6`), consumida pelo processo `worker` do `Procfile:22`. Caminho: `deliver_later` (`app/services/auth/email_service.rb:23`) → `ApplicationMailer.delivery_job` (:13) → `app/jobs/logged_mail_delivery_job.rb:26`. Teste: `spec/jobs/logged_mail_delivery_job_spec.rb` (**P1**). |
| OPS-505 | migrated | Avatares no filesystem (paperclip) | verified | `app/models/user.rb:39` (`sfg_attachment :avatar`) e `:264-265` (`display_avatar_url`: precedencia anexo nosso > URL do OAuth, devolvendo a variante `preview` de 250 px). O comentario :27-35 registra a saida de `public/uploads/avatars/` e que as 4 colunas Paperclip **nao** sao recriadas. Consumidores migrados: `frontend/src/app/pages/ProfilePage.tsx:369` e `UsersPage.tsx:403`. Teste: `spec/requests/api/v1/attachments_spec.rb` (**P3**). ⚠ Residuo fora do escopo sfg em **P4-G5-04**. |
| OPS-506 | migrated | Detecção de spoof de mídia desabilitada globalmente | verified | Invertido nos **dois** caminhos: motor de anexos (`app/models/concerns/attachable.rb:67`) e o endpoint da base `app/controllers/api/v1/uploads.rb` — cabecalho :20-31 registra o endurecimento (a versao anterior fazia `ct.start_with?('image/')` sobre o `Content-Type` **declarado pelo cliente**; agora o tipo vem do Marcel, :100, mais teto de 3 MB na :29). Teste: `spec/models/attachable_spec.rb` (**P2**). |
| OPS-507 | migrated | Seeds obrigatórios de dados de referência | verified | `app/models/admin_message.rb:26` (`STATES`, 8), `:38` (`CONTEXTS`, 4), `:58-59` (`validates … inclusion`) + `CHECK` no banco. **Nao ha seed a rodar**: constante nao dessincroniza. Mata a armadilha do `Feedback19::State`/`Context`, que resolvia registros em **variavel de classe na carga da classe** — processo que subisse antes do seed guardava `nil` para sempre. Teste: `spec/requests/api/v1/admin_messages_spec.rb` (**P3**). |
| OPS-540 | migrated | Seed principal da app | verified | Arcabouco unico e idempotente: `app/services/seeds/reference/runner.rb:35` (`CATALOGS`), `:106`, `:124`, `:139`; `catalog.rb`, `report.rb`; `db/seeds/reference/index.rb`; alvos `reference:seed` e `reference:status` conferidos em **`bin/rails -T`**. Fatia nova pluga com um arquivo + uma linha em `CATALOGS`. Testes: `spec/services/seeds/reference_runner_spec.rb` e `reference_seeds_spec.rb` (**P2**). |
| OPS-541 | migrated | Seed da engine auth19 (papéis + habilidades) | verified | `db/seeds/reference/user_types.rb:3-6` (os 4 papeis na escala DEC-41: OG=1, Admin=2, Gerente=3, Colaborador=4; idempotente — rodar duas vezes nao duplica nem reescreve papel ja atribuido) e `db/seeds/reference/permissions.rb:5` (as **7** abilities com efeito real das 17 do legado, DEC-108 — as outras 10 tinham zero call site fora do factory e dos seeds). Teste: `spec/services/seeds/reference_seeds_spec.rb` (**P2**), incl. *"remove client/free/visitor e reatribui quem apontava para eles"*. |
| OPS-542 | migrated | Seed da engine feedback19 (estados + contextos) | verified | Idem OPS-507: `app/models/admin_message.rb:26/:38/:58-59` — enum string com constante e `CHECK` no banco, **sem tabela e sem seed**. Teste: `spec/requests/api/v1/admin_messages_spec.rb` (**P3**). |
| OPS-601 | migrated | Locale, fuso e formatação de data globais | verified | `config/application.rb:28` (`config.time_zone = 'Brasilia'`), `:29` (`config.i18n.default_locale = :'pt-BR'`), `:31` (`fallbacks = { 'pt-BR' => [:en] }`) — **um** lugar. Catalogo real em `config/locales/pt-BR.yml:15` e `:172` (`grape:`), lido de verdade: a validacao de parametro do Grape responde em pt-BR. Teste: `spec/config/platform_config_spec.rb` (**P1**). |
| OPS-603 | migrated | Carga explícita das definições de marca | verified | `frontend/src/components/brand/Logo.tsx:41` (`export const LOGO_SRC: Record<LogoTone, Record<LogoVariant, {light, dark}>>`) e `:90` — catalogo unico exportado; o JSON-LD de `SEO.tsx` importa dali em vez de repetir a string. Teste: `frontend/src/__tests__/marca-fonte-unica.test.ts` — **26 testes verdes em P4**. |
| OPS-604 | migrated | Configuração do engine de autenticação (Livetat::Auth) | verified | **Medido por ausencia**: `grep -rn "Livetat::\|AuthUx19\|Mailer19\|AuthOmni19\|UxKit19" backend/app backend/config backend/lib` devolve **3 ocorrencias, todas comentario** (`api/v1/users.rb:119`, `admin_message.rb:6`, `services/legacy/role_map.rb:28`) e **zero codigo**. Nenhuma configuracao de engine sobrevive como arquivo; o de-para dos 4 blocos `configure do c` de `../sfg/config/application.rb:61-116` esta na secao S1/F.7 do razao. Teste do que restou: `spec/lib/sfg/dkim_signer_spec.rb` (**P2**). |
| OPS-605 | migrated | Login social do Facebook — desativado | verified | `config/initializers/devise.rb:35` — de configuracao **morta** (chave presente, botao ausente) para **flag desligada**: com o default `OAUTH_FACEBOOK_ENABLED=false` a estrategia nem e registrada, e `/users/auth/facebook` responde 404 em vez de iniciar um fluxo que ninguem termina. Motivo escrito nas :26-34. Teste: `spec/config/platform_config_spec.rb:90` (**P1**). |
| OPS-606 | migrated | Configuração da UI de autenticação (Livetat::AuthUx19) | verified | `frontend/src/app/pages/LoginPage.tsx` + `frontend/src/features/auth/` (`AuthFlow`, `MagicLogin`, `CodeValidation`, `MagicLinkCallbackPage`, `OAuthCallbackPage`, `LoginNotice`, `ImpersonationBanner`, `ReasonDialog`) — as telas de sessao leem os mesmos tokens do design system; a configuracao de UI do `Livetat::AuthUx19` **nao** e portada (confirmado pela ausencia medida em OPS-604). Prova executavel nesta passada: `npx vitest run src/features/auth` → **5 arquivos, 21 testes, 0 falhas** (**P4**), incluindo o cenario de duas abas. |
| OPS-607 | migrated | Configuração do engine de e-mail + SMTP | verified | Idem OPS-484/500: `config/environments/production.rb:73-86`, SMTP inteiramente por ENV, **sem arquivo de engine** — e a ausencia do `mailer19` esta medida em OPS-604 (zero codigo `Mailer19`). |
| OPS-608 | migrated | Assinatura DKIM de todos os e-mails | verified | Idem OPS-485/501, com a metade que este ID pede nominalmente: **chave no `Credential` de provedor `dkim`** (`app/lib/sfg/dkim_signer.rb:30`, DEC-61) — encriptada, trocavel por tela, sem deploy. Sem chave a aplicacao **sobe**, o e-mail sai sem assinatura e o aviso vai ao log uma vez por processo (:49). Teste: `spec/lib/sfg/dkim_signer_spec.rb` (**P2**). |
| OPS-610 | migrated | ⚠ config/development_credentials.yml — secret_key_base commitado em texto puro | verified | Defeito do legado **corrigido**, nao funcionalidade portada: `../sfg/config/development_credentials.yml:1` versionava um `secret_key_base` de 128 hex; no ai9 ele vem de ENV e o boot falha sem ele (`config/initializers/required_env.rb`). Achado adicional **na propria base ai9** e fechado: `config/initializers/active_record_encryption.rb:4-8` — as tres chaves do Active Record Encryption vinham **commitadas como default**; em producao agora nao ha default. Teste: `spec/config/platform_config_spec.rb:19` (a lista de obrigatorias) e `:106` (varredura de segredo com valor no `.env.example`) — **P1**. |
| OPS-611 | migrated | config/credentials.yml.enc sem master.key | verified | `config/initializers/required_env.rb:3` declara o contrato — **o boot falha quando falta segredo obrigatorio**, com a lista **por ambiente** e a mensagem nomeando **todas** as ausentes (:5-8 registra a escolha oposta do legado). Teste executado: `spec/config/platform_config_spec.rb:19` (`contain_exactly('SECRET_KEY_BASE','DEVISE_JWT_SECRET_KEY','REDIS_URL','APP_NAME')`), `:35` (`raise_error(RuntimeError, /SECRET_KEY_BASE.*DEVISE_JWT_SECRET_KEY/m)`) e `:41` (string em branco conta como ausente) — **P1**. |
| OPS-613 | migrated | config/database.{arch,centos,linux,osx,win}.yml — variantes de banco por plataforma | verified | **Um** arquivo (`config/database.yml`, 52 linhas) no lugar das 5 variantes por plataforma, com `config/database.yml.example` (26) versionado e o real **ignorado** — conferido executando: `git check-ignore -v config/database.yml` → `.gitignore:108`. Bancos `sfg9_dev`/`sfg9_test`; a suite inteira desta passada rodou contra `sfg9_test` (**P1**..**P3**, **P5**). |
| OPS-614 | migrated | Gemfile.{linux,osx,prod} — três Gemfiles, Gemfile no .gitignore | verified | **Conferido executando**: `git check-ignore -v Gemfile Gemfile.lock ../frontend/pnpm-lock.yaml` → **exit=1, nenhuma saida** — os tres locks estao versionados. O legado tinha tres Gemfiles com o principal ignorado, defeito que a base ai9 tinha herdado enquanto o `pnpm-lock.yaml` do front ja era versionado. DEC-11 (Ruby 2.6.1 / Rails 6.0.3.2 como ambiente de **referencia do legado**) fica no runbook, nao reproduzido aqui — o ai9 roda **Ruby 3.4.9**, conferido na execucao. |
| OPS-616 | blocked | config/environments/production.rb | blocked | **Falta uma conferencia no servidor, nao codigo.** O codigo esta pronto e o portao provado: `config/environments/production.rb:16` (`config.active_storage.service = ENV.fetch('ACTIVE_STORAGE_SERVICE', 'disk_persistent')`) + `config/storage.yml` (com `amazon` e `s3_compatible` prontos); `spec/lib/sfg/etl/storage_gate_spec.rb` verde em **P5**. **Q-07 foi respondida (DEC-129.1): `Disk` fica**, e o portao passou a exigir afirmacao **com autor** (`ACTIVE_STORAGE_DISK_VOLUME_IS_PERSISTENT=1` **mais** `ACTIVE_STORAGE_DISK_VOLUME_CONFIRMED_BY`); afirmacao anonima nao destrava. O que este ID mede e **o anexo sobreviver ao redeploy**, e isso so se verifica no alvo. Para fechar, na ordem do runbook 1.4.1: (1) `findmnt` provando que `ACTIVE_STORAGE_DISK_ROOT` e ponto de montagem; (2) arquivo-sonda gravado, redeploy, arquivo ainda la; (3) so entao exportar as duas variaveis com o nome de quem conferiu. |
| OPS-617 | migrated | config/environments/test.rb — ambiente de teste sem suíte de testes | verified | `config/environments/test.rb` (37 linhas) com `delivery_method = :test` (:25), `raise_delivery_errors = true` (:27, motivo nas :16-24) e `active_job.queue_adapter = :inline` (:35). O legado tinha ambiente de teste e **nenhum teste** (D-114). Prova desta passada: a suite roda de verdade — **P1** (119), **P2** (717), **P3** (165), **P5** (301). |
| OPS-618 | migrated | date_overload.rb — sentinelas de data do domínio | verified | `app/lib/sfg/date_bounds.rb` (54 linhas) — `min`/`max`/`today_start`/`today_end` **sem reabrir `Date` nem `DateTime`**; o cabecalho :6-8 nomeia o `../sfg/config/initializers/date_overload.rb` que substituiu. O `today_end` de **23h59** (nao 23h59m59s) foi preservado de proposito. Prova de ausencia: `grep -rn "DateTime.class_eval\|class Date\b" backend/app backend/config` = zero. Teste: `spec/lib/sfg/coercion_spec.rb`, secao de monkey patch (**P2**). |
| OPS-619 | migrated | type_casting.rb — coerção booleana e moeda em nível de linguagem (o initializer com mais regra de negócio do app) | blocked | Metade backend **provada**: `app/lib/sfg/coercion.rb:37` (`to_bool`), `:57` (`bool_to_i`), `:61` (`numeric?`), `:66` (`to_number`), `:75` (`to_currency`), `:88` (`with_delimiter`), lendo `golden/coercion.json` (49 casos extraidos **executando** o `type_casting.rb` do legado) — `spec/lib/sfg/coercion_spec.rb` verde em **P2**. **Metade frontend sem portao vivo**: `frontend/scripts/check-coercion-golden.mjs` **nao roda** — `ERR_MODULE_NOT_FOUND: Cannot find package '@/lib'`, porque `frontend/src/lib/utils/number.ts:14` passou a importar `@/lib/config/currency` (commit `8ae521873`) e o alias do Vite nao resolve em `node`, nem com `--experimental-strip-types`, que e como o proprio cabecalho manda rodar. O script **nao esta em `package.json`** e **nenhum vitest le `golden/coercion.json`** (conferido em `src/__tests__/numero-em-pt-br.test.ts`). Falta: tornar o cross-check executavel — o caminho barato e move-lo para um teste `vitest`, que ja resolve o alias — para que "divergencia nao declarada" volte a reprovar. Ver **P4-G5-02**. |
| OPS-622 | migrated | receitaws.rb — consulta de CNPJ | verified | Initializer do legado (`../sfg/config/initializers/receitaws.rb`, com `config.token = ENV[rws_api_token]`) virou service: `app/services/sfg/receita_ws/lookup_service.rb:36` (timeout 10 s), `:40` (cache 365 dias), `:45`/`:71`/`:77` (teto diario e quota restante), erro tratado. **Token ausente responde indisponivel e o app sobe** — nada e lido no boot. Teste: `spec/requests/api/v1/cnpj_lookup_spec.rb` (**P3**). A credencial em si e OPS-050 (DEC-130). |
| OPS-623 | migrated | ⚠ paperclip.rb — detecção de spoof de media-type desativada globalmente | verified | Consumido: `app/models/concerns/attachable.rb:67` (`spoofing_protection: true`) vale para **todo** model com anexo do Safegold (`user`, `project`, `provider`, `carrier`, anexos de renegociacao). Motivo escrito na :62 e o contraste com a declaracao a mao em `carrier.rb:83`. Teste: `spec/models/attachable_spec.rb` (**P2**). |
| OPS-624 | migrated | delayed_job_config.rb — política da fila de jobs | verified | `config/sidekiq.yml:34-35` (`dead_max_jobs`, `dead_timeout_in_seconds` = 6 meses) no lugar do `../sfg/config/initializers/delayed_job_config.rb`. Job morto continua **visivel e reenfileiravel** pelo painel. ⚠ Mesma correcao de fato de OPS-462: o legado configurava `max_run_time = 7.days` e `destroy_failed_jobs = **false**` — o job falho era mantido; o que faltava era onde olhar e como reexecutar. Teste: `spec/jobs/job_discipline_spec.rb` (**P1**). |
| OPS-626 | migrated | ⚠ ssl_for_win.rb — verificação de certificado TLS desativada no Windows | verified | **Mudanca intencional de comportamento, registrada** (improvements-log PLAT-02): `openssl_verify_mode: ENV.fetch('SMTP_OPENSSL_VERIFY_MODE', 'peer')` em `config/environments/production.rb:87` e em `development.rb`. O `ssl_for_win.rb` do legado desligava `VERIFY_PEER` **globalmente** (atingindo Maps, ReceitaWS e SMTP) e **nao** e portado. Corrige tambem o C-05 da propria base ai9. Portao executado: `spec/config/platform_config_spec.rb:49-55` reprova `openssl_verify_mode: 'none'` nos dois arquivos (**P1**). |
| OPS-627 | migrated | filter_parameter_logging.rb — mascaramento de log incompleto | verified | `config/initializers/filter_parameter_logging.rb:7-8` — `cpf`, `cnpj` e `cpf_cnpj` acrescidos a mascara, com o motivo escrito (sao os identificadores que o Safegold recebe em cadastro de empresa, portador e usuario). Teste executado: `spec/config/platform_config_spec.rb:62-68` — `cpf`/`cnpj`/`cpf_cnpj` viram `[FILTERED]` e `name` e preservado (**P1**). |
| OPS-628 | migrated | Initializers neutros / defaults | verified | CSP **bloqueante desde o nascimento** (DEC-48), nao `report-only` — nao existia nem no legado nem na base: `frontend/csp.config.ts` + `frontend/vite.config.ts:41` (meta `Content-Security-Policy` injetada no HTML) + `backend/config/initializers/security_headers.rb:30` (`default-src 'none'` para a API) e `:39` (`default-src 'self'`), com excecao **escrita** para `/docs` (:19, que carrega Stoplight do unpkg) e aplicacao por `PATH_INFO` na :68. DEC-87 confere: o dominio do GA **nao** esta liberado a menos que a flag ligue (`csp.config.ts:69`). |
| OPS-629 | migrated | config/locales/ — 6 arquivos, 2 idiomas, mas UI 100% hardcoded | verified | **Um** catalogo — `ls backend/config/locales` devolve apenas `pt-BR.yml` (286 linhas) — e ele e realmente lido: `:172` (`grape:`) alimenta as mensagens de validacao do Grape, com `config/application.rb:29` apontando o default. Os 6 arquivos e 2 idiomas do legado nao sao portados porque **nunca foram chamados** (a UI legada e 100% hardcoded). Teste: `spec/config/platform_config_spec.rb` (**P1**). |
| OPS-631 | migrated | ⚠ config/puma.rb — servidor de aplicação com ambiente hardcoded como development | verified | `config/puma.rb` (38 linhas) — `RAILS_ENV` vem do ambiente, com o motivo escrito nas :5-8 (o legado fixava `development`, o que em producao significa codigo recarregavel, log verboso e o `secret_key_base` de desenvolvimento — que ainda por cima estava commitado, OPS-610). `WEB_CONCURRENCY` liga o modo cluster com `preload_app!` e reconexao do ActiveRecord no `on_worker_boot`. Consumido por `Procfile:21`. |
| OPS-632 | migrated | Procfile / config.ru / Rakefile / bin/ | verified | **Conferido executando `bin/rails -T`**: os dois namespaces nomeados aparecem — `sfg:etl:{all,extract,load,verify}` (aliases em `lib/tasks/sfg_etl.rake:290-291` sobre o `sfg_etl:` real da :43) e `demo:{seed,reset,reseed,status,ledger}` (`lib/tasks/demo.rake:22`), mais `reference:{seed,status}`, `indicators:backfill` e `sfg:availability:*`. `Procfile` de producao entregue (:21-22), `config.ru`, `Rakefile` e `bin/` da base conferidos. |
| OPS-633 | migrated | bin/delayed_job — daemon da fila | blocked | **A evidencia escrita no razao esta errada e o arquivo continua no repositorio.** `backend/bin/delayed_job` existe, esta **versionado** (`git ls-files backend/bin/delayed_job` devolve o caminho) e faz `require 'delayed/command'` + `Delayed::Command.new(ARGV).daemonize` — enquanto `grep -c delayed_job backend/Gemfile.lock` = **0**. E um daemon morto que estoura em `LoadError` se alguem o chamar, apontando para uma fila que nao existe mais. **A substituicao funcional esta feita** (`Procfile:22`, worker Sidekiq supervisionado — ver OPS-461). Falta **uma remocao**: apagar `backend/bin/delayed_job`. Nao removido aqui porque QA nao edita codigo. Ver **P4-G5-03**. |
| OPS-634 | migrated | Páginas de erro customizadas 404 / 422 / 500 | verified | `frontend/src/app/pages/ErrorPages.tsx` (as tres, na mesma moldura de `ui/States.tsx`) + `frontend/src/components/RouteErrorBoundary.tsx` (impede que excecao de tela vire tela branca). Teste: `frontend/src/app/__tests__/routing.test.tsx` — **15 testes verdes em P4**, incluindo *"area desconhecida mostra a tela de nao encontrado — nunca tela em branco"*. |
| OPS-750 | migrated | Livetat::UxKit19::Configuration — app_name e primary_color do kit de UI (#504746) | verified | Comparado contra o **registro do QA**, nao contra o legado, como o proprio ID manda: `frontend/src/styles/globals.css:16` documenta a origem (`ink (COLOR__PRIMARY) #2D2D2A → confirmado contra o logo (#292C28)`) e `:38` define `--brand-ink: 60 3% 17%`. A divergencia com o `#504746` do kit legado e **de proposito** (DEC-68, decidida por medicao do PNG do logo). Teste: `frontend/src/__tests__/marca-fonte-unica.test.ts` — 26 testes verdes em **P4**. |

---

## Resumo do lote

| veredito | quantidade |
| --- | --- |
| `verified` | **114** |
| `blocked` | **4** — OPS-050, OPS-616, OPS-619, OPS-633 |
| `dropped` | **0** |
