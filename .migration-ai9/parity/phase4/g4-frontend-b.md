# Phase 4 — lote `g4-frontend-b` (129 itens)

Cada linha e um item ABERTO do razao. Preencha `veredito` com `verified`, `blocked` ou `dropped` e escreva a EVIDENCIA — arquivo:linha do ai9, ou o comando que provou. Sem evidencia o item continua aberto.

| ID | estado atual | feature | veredito | evidencia |
| --- | --- | --- | --- | --- |
| FE-218 | migrated | PREVISÕES — remoção de parcelas em lote | verified | `frontend/src/components/renegotiations/InstallmentsTab.tsx:354-376` (ConfirmDialog do lote, `canConfirm`/`blockedReason` quando nada selecionado) + `frontend/src/components/ui/ConfirmDialog.tsx:52-103` — **sem `setTimeout`/temporizador nenhum** no arquivo (`grep -n "setTimeout\\|setInterval" ConfirmDialog.tsx` → vazio), contra os 6 s do legado. Teste: `src/app/pages/renegotiations/__tests__/renegotiations.test.tsx:88-152` ("continua aberta depois de 30 segundos"). Suíte verde (58 arq./506 testes). |
| FE-219 | migrated | PREVISÕES — bloqueio de seleção para parcela com pagamento | verified | `frontend/src/components/renegotiations/InstallmentRow.tsx:59` (`temPagamento`) e `:75-88` — com pagamento renderiza `<Tooltip content="Esta previsão tem pagamento lançado e não pode ser removida em lote.">` + ícone, **sem `<Checkbox>`**; sem pagamento renderiza o Checkbox. Mesmo critério do servidor (`InstallmentsTab.tsx:189` `semPagamento`). Teste: `src/app/pages/renegotiations/__tests__/renegotiations.test.tsx`. |
| FE-220 | migrated | Helper lateral de parcela — formulário (parcela única × múltiplas) | verified | `frontend/src/components/renegotiations/InstallmentDrawer.tsx:130-180` — 3 modos: "Parcela única" × "Múltiplas parcelas" (`ModoBotao`, ocultos em edição) e edição. Campos batem com o legado `app/views/pub/console/parts/renegotiations/detail/parts/renegotiation_installments/helper/_body.html.erb` (principal, juros, correção monetária, data, repetições, "A cada", período). `repeticoes`/`intervalo` com `min={1}` e `Math.max(1, …)` (`:154-176`). Teste: `renegotiations.test.tsx`. |
| FE-221 | migrated | Helper de parcela — cálculo em tempo real | verified | `frontend/src/hooks/useInstallmentPreview.ts:41-67` — `POST …/installments/preview` no servidor, `staleTime:0`, `enabled` só com data e principal > 0; `InstallmentDrawer.tsx:271-284` mostra `—` enquanto carrega, nunca soma local. Teste: `renegotiations.test.tsx:37-87` ("NÃO soma nada localmente" / "não consulta com rascunho inválido") + `backend/spec/requests/api/v1/renegotiation_installments_spec.rb`. |
| FE-222 | migrated | Helper de parcela — habilitação do botão Salvar | verified | `frontend/src/components/renegotiations/InstallmentDrawer.tsx:101-104` (`principalValido = mainValue > 0`, `loteValido`, `podeSalvar`) e `:124` `disabled={!podeSalvar}`; `:198-202` diz o motivo na tela. Servidor recusa pelo mesmo motivo: `backend/spec/requests/api/v1/renegotiation_installments_spec.rb`. |
| FE-223 | migrated | Helper de parcela — envio e estados | verified | `frontend/src/components/renegotiations/InstallmentsTab.tsx:96-102` — `onSuccess` fecha o painel (`setPainelParcela({aberto:false…})`); `onError` só emite toast, **não fecha**, então os valores digitados permanecem. Idem edição (`:112-118`). Teste: `renegotiations.test.tsx`. |
| FE-224 | migrated | Helper lateral de pagamento — formulário | verified | `frontend/src/components/renegotiations/PaymentDrawer.tsx:120-160` — seletor de Previsão (nº · vencimento · pendente), "Parcela + Juros + CM", "Mora e Juros por Atraso", "Pago Total" (derivado) e **Data do pagamento EDITÁVEL** (`:158` `<DatePicker …>`), contra o `readonly: 'readonly'` do legado em `app/views/pub/console/parts/renegotiations/detail/parts/renegotiation_payments/helper/_body.html.erb:49` (D-B12). Teste: `backend/spec/requests/api/v1/renegotiation_payments_spec.rb`. |
| FE-225 | migrated | Helper de pagamento — cálculo em tempo real e gate do Salvar | verified | `frontend/src/components/renegotiations/PaymentDrawer.tsx:86-99` — `pendenteDisponivel` (na edição o próprio pagamento sai da conta), `acimaDoPendente`, `podeSalvar`; `:112` `disabled={!podeSalvar}` e `:190-195` explica o bloqueio. "Pago Total" calculado a partir dos dois campos. Teste: `backend/spec/requests/api/v1/renegotiation_payments_spec.rb`. |
| FE-226 | migrated | Helper de pagamento — envio e fechamento | verified | `frontend/src/components/renegotiations/InstallmentsTab.tsx:157-164` — `onSuccess` do `salvarPagamento` faz `setPainelPagamento({aberto:false…})` + `recarregar()`. Teste: `src/app/pages/renegotiations/__tests__/renegotiations.test.tsx`. |
| FE-227 | migrated | PREVISÕES — editar/excluir pagamento inline | verified | `frontend/src/components/renegotiations/InstallmentRow.tsx:202-221` — botões "Editar pagamento"/"Excluir pagamento" na sublinha, só com `podeEscrever`; `InstallmentsTab.tsx:397-418` confirma com título **"Excluir este pagamento?"** (o legado reaproveitava "Excluir previsão"). Teste: `backend/spec/requests/api/v1/renegotiation_payments_spec.rb`. |
| FE-228 | migrated | Regras de permissão na UI (user_is_readonly) | verified | `frontend/src/hooks/usePermission.ts:16-29` — `usePermission(key)` respeita `revoked_at`; `useIsReadonly()` = `user_is_readonly`. Consumido nas telas (`grep -rn "useIsReadonly\\|usePermission(" src \| wc -l` > 0) e há teste transversal `src/__tests__/somente-leitura-esconde-escrita.test.ts`. Recusa no servidor: `backend/spec/requests/api/v1/renegotiations_authorization_spec.rb`. |
| FE-232 | migrated | Filtro "Portador" em cascata | verified | `frontend/src/features/risk/api/risk.ts:225-229` (`carriersWithActiveControl` → `GET /api/v1/risk_controls/filters`) consumido em `frontend/src/features/risk/pages/RiskConsolePage.tsx:65-68` com cascata `empresa` e reset do portador em `:105`; opção "TODOS" em `:112`. Servidor: `backend/app/controllers/api/v1/risk_controls.rb:111-123` (limites ATIVOS, `.uniq`). Teste: `backend/spec/requests/api/v1/risk_controls_spec.rb`. |
| FE-233 | migrated | Filtro de data (posição do dia) | verified | `frontend/src/features/risk/pages/RiskConsolePage.tsx:123-129` — `<DatePicker … max={new Date()} />`, data única, pt-BR pelo DS (`components/ui/DatePicker.tsx`). Exposição futura não é consultável, como no legado. |
| FE-236 | migrated | Resumo em modo "portador único" | verified | `frontend/src/features/risk/components/ExposureTables.tsx:104-149` (`ExposureSingleCarrier`: cabeçalho = portador, uma linha por tipo) e `:152-172` — quando a combinação tem mais de um limite mostra `Badge` "N limites" com Tooltip, em vez do `r[:rcs].first` silencioso do legado. Montado em `RiskConsolePage.tsx:183`. |
| FE-237 | migrated | Expandir/recolher grupos por tipo | verified | `frontend/src/features/risk/components/ExposureTables.tsx:40-93` — `<Accordion type="multiple">` do Radix (`components/ui/accordion.tsx`), **sem `defaultValue`** ⇒ grupos nascem recolhidos; o modo portador único (`:104`) não usa accordion. Reuso do DS, sem cor literal (o teste `renegotiations.test.tsx:189-233` e a suíte de risco guardam a regra de tokens). |
| FE-248 | migrated | Guardas do botão "Cadastrar" limite | verified | `frontend/src/features/risk/pages/RiskControlsPage.tsx:70-71` (`podeEscrever = papel válido && !useIsReadonly()`) e `:313-320`/`:451-456` — o "Novo limite" só existe com `podeEscrever`. As duas guardas de dado (empresa/portador do projeto) foram movidas do botão para o formulário: `frontend/src/features/risk/components/RiskControlDrawer.tsx:107-108,145-181` (`semEmpresa`/`semPortador` → `hint` + `Select` desabilitado). Gate de servidor: `backend/app/controllers/api/v1/controller_helpers.rb:267` (`READONLY_RESTRICTED`), spec `backend/spec/requests/api/v1/risk_controls_spec.rb`. ⚠ **Divergência registrada**: o mapa (`.migration-ai9/map/risk-indicators.md:273`) pedia "as mesmas mensagens"; o texto foi reescrito ("Este projeto ainda não tem empresa cadastrada. Cadastre uma em «Empresas»…") no lugar do `M.push` do legado (`app/views/pub/console/parts/risk_controls/_body.js.erb:341,345`). Mesmo efeito, redação nova, sem linha no improvements-log. |
| FE-254 | migrated | Ordenação multi-coluna | blocked | **Falta a ACUMULAÇÃO de chaves na tela de Operações de Risco.** O legado empilha: `app/views/pub/console/parts/risk_operations/_body.js.erb:42-58` faz `push`/`splice` em `ordering_keys`/`ordering_style` e envia o array inteiro (`:266-267`); o inventário (`.migration-ai9/inventory/risk.md:90`) e o mapa (`.migration-ai9/map/risk-indicators.md:318`) exigem explicitamente "acumula `ordering_keys[]`/`ordering_style[]`". No ai9 a camada de API aceita arrays (`frontend/src/features/risk/api/risk.ts:432-433,466-467`) e a `StructuredOperationsPage.tsx:111,140-144` implementa a pilha (FE-287), **mas `frontend/src/features/risk/pages/RiskOperationsPage.tsx:84,115-116` guarda um único `SortState` e envia `[sort.key]`** — o clique SUBSTITUI a ordenação em vez de empilhar. O tri-state do `DataTable.tsx:382-394` está correto; o que falta é trocar `useState<SortState\|null>` por `useState<SortState[]>` com a mesma lógica de `trocarOrdenacao` já escrita na tela de OP Estruturadas. Nenhuma DEC autoriza ordenação de chave única (`grep -n "FE-254\\|multi-coluna\\|ordering_keys" .migration-ai9/decisions.md` → vazio). |
| FE-257 | migrated | Guardas do botão "Cadastrar" operação | verified | `frontend/src/features/risk/pages/RiskOperationsPage.tsx:141-144` — as duas guardas vêm do servidor (`riskOperationsApi.availability()` → `backend/app/controllers/api/v1/risk_operations.rb`), avaliadas em `:179-181` (`semLimiteManual`) e `:333` (`semEmpresa`); o botão "Nova operação" (`:322-328`) só aparece com `podeEscrever && !semEmpresa && !semLimiteManual`, e cada bloqueio vira `EmptyState` com o caminho (`:334-347`). A mensagem preserva a do legado `app/views/pub/console/parts/risk_operations/_body.js.erb:452` ("ter um portador no projeto … e ao menos um limite … com lançamento manual"). Teste: `backend/spec/requests/api/v1/risk_operations_spec.rb`. |
| FE-258 | migrated | Formulário de operação — cascata empresa → portador → tipo | verified | `frontend/src/features/risk/components/RiskOperationDrawer.tsx:291-358` — cascata empresa → portador → tipo: trocar empresa zera portador e tipo (`:303-306`), o `Select` de portador é `disabled={!valores.company_id}` (`:336`) e o de tipo `disabled={!valores.carrier_id}` (`:352`); as consultas são `enabled` pelo degrau anterior (`:147`). Em edição os três viram texto readonly (`somenteLeitura(...)`, `:294,331,348`, com `<Lock>` e `sr-only` em `:255-259`). Teste: `src/features/risk/__tests__/riskOperations.test.tsx`. |
| FE-259 | migrated | Formulário de operação — campos e readonly | verified | `frontend/src/features/risk/components/RiskOperationDrawer.tsx` — cinco blocos: Cadastro (`:287`), Datas (`:372`), Valores (`:395`), Taxa acordada (`:415`), Outros (`:424`). "Capital da operação" readonly na edição (`:398-404`) e "Saldo inicial" editável (`:406-413`), como no legado. Ajuda por campo via `rotulo()` → `<FieldHelp scope="risk_operations">` (`:248-252`), usado em 11 rótulos; `is_on_variable` segue sem ícone porque a chave continua `TODO:` e o servidor filtra (`:243-247`) — DEC-88. Teste: `src/features/risk/__tests__/riskOperations.test.tsx`. |
| FE-260 | migrated | Formulário de operação — regras dependentes do pré-faturamento | verified | `frontend/src/features/risk/components/RiskOperationDrawer.tsx:163` (`comPre`) e `:371-379` — o bloco Datas **some inteiro** em tipo com pré-faturamento; "Encerrada" também some (`:439-450`), como no legado. Em edição as datas são `somenteLeitura` (`:386,394`) e **não são enviadas** no `update` (`:288-300` — payload sem `issue_date`/`due_date`, T-D5); a API ignora do mesmo jeito. Teste: `backend/spec/requests/api/v1/risk_operations_spec.rb`. |
| FE-261 | migrated | Formulário de operação — habilitação do salvar e validação de saída | verified | `frontend/src/features/risk/components/RiskOperationDrawer.tsx:169-184` (`faltando`, em português) — `:269-273` mostra "Falta preencher: …" e `:278` `disabled={faltando.length > 0 \|\| salvar.isPending}`. **O botão não some**, ao contrário do legado. Teste: `src/features/risk/__tests__/riskOperations.test.tsx`. |
| FE-262 | migrated | Formulário de operação — máscaras numéricas | verified | `frontend/src/features/risk/components/RiskOperationDrawer.tsx:402,411` usam `MoneyInput` e `:419` usa `PercentInput` (`@/components/ui/NumericInput`) — nenhuma máscara reimplementada na tela. Guardado por teste de fonte: `src/features/risk/__tests__/riskOperations.test.tsx:150-166` ("os campos de dinheiro usam MoneyInput, e a taxa usa PercentInput" + "nenhuma tela crava 'BRL'"). Comportamento da máscara coberto em `src/components/ui/__tests__/MoneyInput.test.tsx` e `NumericInput.test.tsx` (suíte verde). |
| FE-263 | migrated | Estados vazios do formulário de operação | verified | Dois bloqueios com link, em `frontend/src/features/risk/pages/RiskOperationsPage.tsx:333-347`: "Nenhuma empresa neste projeto" → botão para `/companies`; "Ainda não dá para abrir operação neste projeto" → botão para `/risk-controls`. O segundo também aparece como aviso no campo Portador quando a cascata volta vazia: `frontend/src/features/risk/components/RiskOperationDrawer.tsx:150,318-327` (`semPortador` → hint "Nenhum portador desta empresa tem limite ativo de tipo manual…"). Teste: `src/features/risk/__tests__/riskOperations.test.tsx`. |
| FE-271 | migrated | Helper de cadastrar/editar movimentação | verified | `frontend/src/features/risk/components/MovementDrawer.tsx:82-83` — `min`/`max` do DatePicker vêm do servidor (`opcoes.data.min_date`/`max_date`), consumidos em `:163-168` com a janela dita no hint; fim do off-by-one `issue_date+1`/`due_date+1` do legado. Toasts falam de **movimento** (`:101` "Movimento lançado."/"Movimento atualizado.", `:105`). Tipo travado em transferência (`:86`). Teste: `src/features/risk/__tests__/riskOperations.test.tsx`. |
| FE-272 | migrated | Botão "Transferir" | verified | `frontend/src/features/risk/components/MovementsTab.tsx:145-151` — o botão "Transferir" só é renderizado com `operacao.is_pre`, e abre o drawer com o tipo fixado (`MovementDrawer.tsx:86` `tipoTravado`, título "Transferir para a antecipação" em `:109-110`). Teste: `src/features/risk/__tests__/riskOperations.test.tsx`. |
| FE-273 | migrated | Excluir movimentação | verified | `frontend/src/features/risk/components/MovementsTab.tsx:242-246` — `ConfirmDialog` com título **"Excluir movimento"** (no legado, "Excluir previsão"), e `:69-74` recarrega lista, cartão da operação, último movimento, lista de operações e resumo por `invalidateQueries`. Teste: `src/features/risk/__tests__/riskOperations.test.tsx`. |
| FE-274 | migrated | Aba PRORROGAÇÕES e helper de prorrogar | verified | `frontend/src/features/risk/components/ExtensionsTab.tsx:52-59` — colunas "Prorrogado em", "Data original", "Nova data", "Por" e "Observação" (mesmo conjunto do legado), com versão mobile em `:86-91`. Drawer: `frontend/src/features/risk/components/ExtensionDrawer.tsx:57` (`min_due_date` vindo de `GET :id/extensions/new`) aplicado em `:103` (`<DatePicker … min={minima} />`). Teste: `backend/spec/services/risk/renewal_and_extension_spec.rb`. |
| FE-275 | migrated | Helper de renovar operação | verified | `frontend/src/features/risk/components/RenewalDrawer.tsx:36` — as duas datas sugeridas vêm do servidor (`GET :id/renewal`), e `:82,127` aplicam `min` no vencimento (o legado não tinha nenhum dos dois). O aviso da **DEC-35** está na tela (`:130-133`): a operação original continua aberta e consumindo limite. Teste: `src/features/risk/__tests__/riskOperations.test.tsx:244-261` ("o drawer de renovação avisa que a original continua aberta" / "nenhuma tela marca is_ended por conta própria ao renovar"). |
| FE-279 | migrated | Permissões de interface no módulo de risco | verified | Gate de servidor nos endpoints de risco: `backend/app/services/authorization/matrix.rb:36,45-48,63,87,89` (`risk`, `risk_operations`, `risk_operation_extensions`, `risk_movements`, `risk_controls`, `risk_operation_types`, `risk_movement_types`) + `authorize!(RESOURCE, …)` em `backend/app/controllers/api/v1/risk_operations.rb:91-527` e `risk_controls.rb:97,116,127`. Do lado da tela, `useIsReadonly()`/`podeEscrever` em `RiskControlsPage.tsx:70-71` e `RiskOperationsPage.tsx`, guardados pelo teste de fonte `frontend/src/__tests__/somente-leitura-esconde-escrita.test.ts`. Teste: `backend/spec/requests/api/v1/risk_types_spec.rb`. |
| FE-287 | migrated | Lista SO — ordenação por clique no cabeçalho (tri-state) | verified | `frontend/src/features/structured-operations/pages/StructuredOperationsPage.tsx:111` (`ordenacoes: SortState[]`), `:234-241` (`trocarOrdenacao` — o clique promove a chave a primária, as demais viram desempate, e o `null` do tri-state **remove** a chave da pilha) e `:140-144` (envia `orderingKeys`/`orderingStyles` paralelos); ligado ao `DataTable` em `:588-590` (`sortMode="server"`). Tri-state do DS em `frontend/src/components/ui/DataTable.tsx:382-394` (asc → desc → nenhum). Uma requisição por clique (não há o duplo `execute()` do legado — `onSortChange` é o único disparo). Chave `company` não existe entre as colunas (B-13, não há coluna "Empresa"). |
| FE-290 | migrated | Lista SO — menu de contexto da linha (⋮) | verified | `frontend/src/features/structured-operations/pages/StructuredOperationsPage.tsx:354-385` — "Ver mais" sempre; "Editar" e "Remover" só com `podeEscrever`; e as duas primeiras navegam por **rota real** (`/structured-operations/:id` e `/:id/edit`), no lugar do `{resource, topic, section}` em memória do legado (D-92). Versão mobile equivalente em `:629-655` (`MobileRowActions`). Rotas registradas em `frontend/src/app/App.tsx` (teste `src/app/__tests__/routing.test.tsx`). |
| FE-291 | migrated | Lista SO — botão "Cadastrar" com guarda de portador | verified | `frontend/src/features/structured-operations/pages/StructuredOperationsPage.tsx:210-213` (`semPortador`, com **precedência sobre** a de empresa, como no legado) e `:424-456` — "Este projeto ainda não tem portador" com o texto do legado ("É necessário ter um portador no projeto para que seja possível cadastrar uma operação estruturada") + atalho para `/project-carrier-connections`; a guarda de EMPRESA passa a ser avaliada junto (`:448-455`). O rótulo errado "Cadastrar recebível" do legado não aparece (`grep -n "Cadastrar recebível" src/features/structured-operations` → vazio). |
| FE-292 | migrated | Remoção de operação estruturada — confirmação e feedback | verified | `frontend/src/features/structured-operations/pages/StructuredOperationsPage.tsx:663-677` — `ConfirmDialog` com título **"Excluir operação estruturada"** (o legado reusava "Excluir renegociação") e a frase "A operação não pode ser desfeita. Tem certeza?". `:189-201` — a recusa cai no ramo de ERRO (`onError` → `mensagemDoServidor`), contra o `errors.any? ? :ok : :ok` do legado que respondia 200 e recarregava a lista como sucesso. |
| FE-301 | migrated | Tipo de OP Estruturada — formulário lateral (helper) | verified | `frontend/src/features/structured-operations/pages/StructuredOperationTypesPage.tsx:50` (sobre `CatalogScreen`), `:106-141` — Título (readOnly na edição, `:112-121`), Chave de integração (congelada na criação, `:129-136`) e Ativo (`CampoAtivo`, `:138`). Toast de sucesso e de erro vêm do `CatalogScreen.tsx:222,226` — o ramo de sucesso do legado era VAZIO e o de erro mostrava `<b>chave_tecnica</b>` cru. As copies erradas do legado ("Cadastrar uma taxa", "Essa construtora não pode ser alterada") não aparecem — guardado por `src/app/pages/catalogs/__tests__/CatalogScreen.test.tsx:284-302`. |
| FE-302 | migrated | Tipo de OP Estruturada — exclusão com erro silencioso | verified | `frontend/src/app/pages/catalogs/CatalogScreen.tsx:229-243` — o `onError` do `excluir` existe e emite `toast.error(mensagemDoServidor(...))`, então o 422 do servidor chega ao usuário. No legado o `$.ajax` de DELETE **não define `error`** e clicar em "Remover" simplesmente não fazia nada. Teste: `src/app/pages/catalogs/__tests__/CatalogScreen.test.tsx:199-242` ("COM vínculo: o botão some E a razão fica visível no lugar dele" / "excluir chama o servidor e recarrega a lista"). |
| FE-305 | migrated | Remuneração — máscara de percentual | verified | `frontend/src/features/structured-operations/components/RemunerationDrawer.tsx:213-221` — `<PercentInput>` **sem `min` e sem `max`**, com o comentário do T-D9 na linha `:219`. Implementação em `frontend/src/components/ui/NumericInput.tsx:144-150` (2 casas, pt-BR, exibe `12,50%` e envia `12.5`). Teste: `src/components/ui/__tests__/NumericInput.test.tsx:48-70` ("percentual e o percentual, nao a fracao"). |
| FE-308 | migrated | Console → "Tipos de Recursos" (resource_sources) — listagem e formulário | verified | `frontend/src/app/pages/catalogs/ResourceSourcesPage.tsx:45-120` sobre `CatalogScreen` — colunas Título e Chave **ordenáveis** (`:62,65-66`), drawer com Título, Chave de integração (`:109`) e Ativo (`:116`). O vazio agramatical do legado sai e distingue primeiro uso de busca sem resultado (`:55-58` + `CatalogScreen`). Teste: `src/app/pages/catalogs/__tests__/CatalogScreen.test.tsx:88-138` (os quatro estados, incluindo "o vazio de BUSCA cita o termo procurado"). |
| FE-315 | migrated | Confirmação e exclusão de indicador | verified | `frontend/src/features/indicators/components/DeleteIndicatorDialog.tsx:53-135` — consulta `GET :id/deletion_impact` **antes de qualquer escrita** e diz na tela quantos lançamentos (`:57,109-118`) e em quais projetos (`:58,120-127`); sem a resposta, a exclusão fica **bloqueada** (`:98`). No legado a tela de indicadores específicos não tinha diálogo nenhum. |
| FE-320 | migrated | Toggle conectar/desconectar indicador global ao projeto | verified | `frontend/src/features/indicators/components/ConnectionRow.tsx:123-128` — reuso do `ui/switch.tsx` (Radix) com `aria-label` que diz a direção ("Conectar"/"Desconectar" + título); o estado de envio é o `isPending` do React Query (`:18`, `:105-110`), no lugar do `preventDoubleSubmit` manual do legado. |
| FE-328 | migrated | Máscara e validação monetária do campo de valor | verified | `frontend/src/features/indicators/components/EntryCell.tsx:146-160` — a célula usa `MoneyInput` do DS (pt-BR, 2 casas, aceita negativo), não uma máscara própria. Comportamento coberto em `src/components/ui/__tests__/MoneyInput.test.tsx` (acumulador da direita para a esquerda) e `src/features/indicators/__tests__/indicators.test.ts:76-98` (`null` não vira zero; zero lançado é zero; formatação pt-BR com duas casas, negativo incluído; o valor sai como STRING). |
| FE-329 | migrated | Auto-save do lançamento (sem botão salvar) | verified | `frontend/src/features/indicators/components/EntryCell.tsx:57` (`estado: 'ocioso'\|'enviando'\|'salvo'\|'erro'`), `:65-80` (grava na saída do campo — `onBlur`, sem botão salvar), `:112-118` (`onError` com a mensagem do servidor) e `:171-181` (os três estados visíveis: spinner, ✓ e alerta com texto). O `readOnly` durante o envio (`:156`) impede o reenvio. ⚠ O timer de `:105` só apaga a marca "salvo" — **não fala com a API**, então não viola o Princípio 10 (guardado por `src/__tests__/no-api-polling.test.ts`). |
| FE-332 | migrated | Botão "ACEITAR" na toolbar da página de contrato | verified | **DEC-65** (`.migration-ai9/decisions.md:1753-1774`) decidiu a opção (b): o botão de aceitar volta como **banner persistente**, sem bloqueio de acesso. Implementado em `frontend/src/components/contracts/TermsBanner.tsx:61-112` (`acceptAllPending` + toast). No legado o botão da toolbar estava **totalmente comentado** (`app/views/pub/contracts/header/_toolbar_body.html.erb:18-25`) com handler órfão. Teste: `frontend/src/components/contracts/__tests__/termsBanner.test.tsx:63-118` (7 exemplos: aparece com pendência, aceita em bloco, some sem pendência, não consulta sem sessão, dispensar não mata para sempre, mensagem após 30 dias). ⚠ Registrado: a **barra superior** de `ContractPage.tsx:101-112` tem só Voltar + tema; o aceite mora no banner e no rodapé (FE-333), que é o desenho da DEC-65. |
| FE-333 | migrated | Botão "ACEITAR" no rodapé do card do contrato | verified | `frontend/src/app/pages/public/ContractPage.tsx:78` (sentinela `useInView` no fim do texto), `:146-162` — com sessão aberta e contrato pendente o rodapé mostra `<Progress value={chegouAoFim ? 100 : 35} />` e o botão "Aceitar {título} (v{versão})", mais o aviso do que fica registrado. No legado esse bloco estava comentado inteiro, inclusive o spinner. |
| FE-334 | migrated | Fluxo de retorno pós-aceite / pós-leitura | verified | `frontend/src/app/pages/public/ContractPage.tsx:59-66` (`return_to` vai para o SERVIDOR, que resolve pela allowlist) e `:96-97` — `destinoRecusado` quando `return_to_allowed === false`, anunciado na tela; o destino padrão é `/login` (`:96`). Fecha o D-69 (XSS refletido + open redirect no mesmo parâmetro). Teste: `backend/spec/requests/api/v1/public/contracts_spec.rb`. |
| FE-335 | migrated | Links de contrato no rodapé do menu do console | blocked | **Os dois links não existem em lugar nenhum do front.** O legado tem, no rodapé do menu (`app/views/pub/console/base/menu/_container.html.erb:38-42`), `<a target="_blank">termos de uso</a>` e `<a target="_blank">políticas de privacidade</a>`. O mapa (`.migration-ai9/map/receivables-renegotiations.md:375`) manda montá-los em `hooks/useNavItems.ts`. Medido: `grep -rn "termos-de-uso\\|politica-de-privacidade\\|/contract/" frontend/src --include=*.tsx --include=*.ts` só acha `TermsBanner.tsx:98`, `MyTermsSection.tsx:78` e a rota `App.tsx:146` — **nada em `useNavItems.ts`, `consoleNavigation.tsx` ou `Sidebar.tsx`** (o rodapé da sidebar, `Sidebar.tsx:370-491`, tem só "Modo agente" e o cartão do usuário). O que a linha do razão aponta (`backend/config/initializers/public_host.rb`) é o construtor da URL, que existe e está correto — falta **renderizar os dois links**. Falta: acrescentar os itens de rodapé (Termos de Uso / Política de Privacidade, `target="_blank"`, URL por slug) na sidebar. Nenhuma DEC os dispensa. |
| FE-336 | migrated | Checkbox de aceite em Minha Conta | verified | `frontend/src/components/contracts/MyTermsSection.tsx:34` (`concordo` nasce `false` — a caixa vem **DESMARCADA**, contra a pré-marcada do legado), `:95-104` (a caixa habilita o botão de verdade: `disabled={!concordo}`) e `:120-135` — o histórico distingue `explicit` de `implicit_legacy` (DEC-66), preservando a data original. Rota `/profile`. Suíte verde. |
| FE-337 | migrated | Checkbox de aceite no cadastro (sign-up) | verified | **DEC-65** (`.migration-ai9/decisions.md:1753-1774`) + **DEC-18.7**: o consentimento é ação **pós-entrada**, por banner, e o convite não trava por falta de contrato. Implementado em `frontend/src/components/contracts/TermsBanner.tsx:55-112`, montado globalmente em `frontend/src/components/Layout.tsx:16` (uma linha no Layout, para o banner valer em todo o console). Teste: `frontend/src/components/contracts/__tests__/termsBanner.test.tsx:83-96` ("NÃO aparece quando não há pendência" e "NÃO aparece sem sessão — e nem sequer consulta o servidor"). |
| FE-339 | migrated | Widget de contrato na lista do console | verified | `frontend/src/components/contracts/ContractCard.tsx:22-25,64-76` — "Ver" sempre; "Nova versão" só com `podePublicar`, ambos navegando por rota real (`/admin/contracts/:slug` e `/:slug/new`). A recusa correspondente é do servidor: `backend/spec/requests/api/v1/contract_versions_spec.rb` (403 provado nos dois lados). Teste de render: `frontend/src/components/contracts/__tests__/domNesting.test.tsx:52-90`. |
| FE-341 | migrated | Accordion de conteúdo por versão no detalhe | verified | `frontend/src/app/pages/admin/ContractDetailPage.tsx:88-90` — `<Accordion type="multiple">` (`components/ui/accordion.tsx`), então várias versões ficam abertas ao mesmo tempo para comparar; lista da mais recente à mais antiga (`:60-63`) com os quatro estados via `AsyncSection` (`:76-80`). |
| FE-342 | migrated | Console › Contrato › Nova versão — formulário | verified | `frontend/src/app/pages/admin/ContractVersionFormPage.tsx:118-121` — **botão "Publicar" explícito**, habilitado só com título e corpo não vazios (`:95`); `:208-240` confirma com o **número da versão** ("Publicar a versão {next_version} de {kind}?") e o aviso de que a nova passa a ser a vigente e o aceite volta a ficar pendente (`:217-228`). Aviso de alterações não gravadas em `:170`. No legado publicar era efeito colateral de digitar. |
| FE-364 | migrated | FAQ do usuário final (/u/console/faq) — menu grupo→categoria à direita + lista de itens à esquerda + busca | verified | `frontend/src/app/pages/FaqPage.tsx:38` (`useDebouncedSearch` — debounce real, contra o `keyup` sem debounce do legado), `:53-67` (busca e listagem são consultas distintas, `enabled` mutuamente exclusivo) e `:45-51` (a tela nunca abre sem categoria). Trocar de categoria **limpa** a busca, fechando o `lastQuery = " "` que sumia com itens de título curto. Erro com "tentar de novo" e vazio que aparece via `AsyncSection`. Teste: `frontend/src/app/pages/__tests__/faqPage.test.tsx` (verde na suíte). |
| FE-365 | migrated | Central de ajuda (console admin) — árvore editável Grupo → Categoria → Item, tudo inline | verified | `frontend/src/app/pages/admin/HelpCenterPage.tsx:24-36` — árvore Grupo → Categoria → Item com criação e renomeação **inline determinística**: `blur` e `Enter` gravam, `Escape` cancela, **sem temporizador** (o `focusout` de 200 ms do legado revertia a alteração e competia com o Enter). Mutations em `:60-118`, com o toast de exclusão dizendo quantas categorias/itens foram junto (`:113-114`). Teste: `frontend/src/app/pages/__tests__/helpCenterPage.test.tsx:1-` — inclui "a edição inline NÃO é revertida por temporizador: `blur` grava" (5 exemplos, verdes). |
| FE-366 | migrated | Form do item de ajuda (novo/editar, Trix) + detalhe do item | verified | `frontend/src/components/help/HelpItemEditor.tsx:112-125` — autoria **não** é reescrita (o payload manda só `title`/`description`; o legado enviava `user_id` num campo escondido e a cada edição roubava a autoria); `:119` o toast sai do modo ("Item atualizado." × "Item criado."); `:54-99` o formulário só existe depois que o corpo chega, para não gravar corpo em branco por cima. Editor de texto rico em `:5` (`RichTextEditor`, substituto do Trix). Cor de avatar determinística pelo id (`components/ui/UserAvatar.tsx`). Teste: `frontend/src/components/help/__tests__/helpItemEditor.test.tsx` (4 exemplos, verdes). |
| FE-390 | migrated | Shell do console (.console_structure) — grade de 5 áreas: topbar (console_top_holder), conteúdo (console_content_holder com… | verified | `frontend/src/components/Layout.tsx:57-107` — a casca: `Sidebar` (`:65`), `MobileTopBar` (`:62`), `MobileBottomBar`, faixas globais (`ImpersonationBanner`, `TermsBanner`) e `<main>` com `<Outlet/>` (`:99-106`). As 5 áreas do `.console_structure` viram: sidebar, área de conteúdo, faixa superior mobile, barra inferior mobile e drawer (`components/SideDrawer.tsx`). ⚠ **Defeito registrado:** `frontend/src/components/ConsoleTopbar.tsx` — que esta linha do razão cita — **não é montado em lugar nenhum** (`grep -rn "ConsoleTopbar" src` só devolve a própria definição e dois testes de fonte). A topbar foi deliberadamente eliminada, e a decisão está escrita em `frontend/src/components/SidebarSearch.tsx:10-20` e `Sidebar.tsx:291-294`; o arquivo ficou órfão. Teste da casca: `src/__tests__/empilhamento-barras.test.ts` e `src/components/__tests__/layoutRespiroDoTopo.test.ts`. |
| FE-391 | migrated | Splash / preloader — tela preta cobrindo 99vw x 98vh com o logo, some quando document.readyState == "complete"; _after.js.er… | verified | `frontend/src/app/App.tsx:2,50-66,133-175` — `lazy()` + `<Suspense fallback={<PageLoader/>}>` no lugar do splash preto `99vw × 98vh` com laço de 10 ms sobre `document.readyState`. `grep -rn "readyState" frontend/src` → vazio; nenhuma detecção de dispositivo por user-agent no servidor. O Princípio 10 é guardado por `src/__tests__/no-api-polling.test.ts`. |
| FE-392 | migrated | Topbar (nav.toolbar.context_toolbar) — ícone de menu (mobile), logo (símbolo no mobile / texto no desktop, vindos do app_the… | verified | As funções da `context_toolbar` existem, **redistribuídas**: marca em `frontend/src/components/Sidebar.tsx:244-250` (símbolo quando recolhida, logotipo completo quando aberta) e em `frontend/src/components/mobile/MobileTopBar.tsx:158,164` (`Logo variant="full"` / `variant="symbol"`); ícone de menu mobile em `Sidebar.tsx:494-505` e `MobileTopBar.tsx:90-99`; seletor de projeto e busca global em `Sidebar.tsx:295-296`. A decisão de não ter topbar está escrita em `Sidebar.tsx:291-294` e `SidebarSearch.tsx:10-20`. ⚠ Mesmo defeito da FE-390: `components/ConsoleTopbar.tsx` (o arquivo citado no razão) é **código morto** — nenhum consumidor. |
| FE-393 | migrated | Seletor de projeto na topbar — <select> com current_user.projects.order(formal: :asc), visível se show_projects_select? (pro… | verified | `frontend/src/components/ProjectSelector.tsx:39` — `if (isLoading \|\| projects.length === 0) return null`, que é exatamente o `show_projects_select?` do legado (`app/decorators/models/user_decorator.rb:159-163`: `projects.count >= 1`, com o comentário do chamado 6769 explicando por que não é `>= 2`). Lista ordenada pelo servidor: `backend/app/controllers/api/v1/current_project.rb:39` (`.order(:name)`). Painel em `FloatingPanel` (portal no `document.body`, `:57`), montado em `Sidebar.tsx:295`. Teste: `backend/spec/requests/api/v1/current_project_spec.rb`. |
| FE-394 | migrated | Busca global da topbar (#toolbar_generic_search) — input "Buscar usuários" com autocomplete em dropdown | verified | A busca global existe e está montada — em `frontend/src/components/SidebarSearch.tsx` (dropdown de sugestões em `FloatingPanel`, resultados filtrados por `useNavItems`, navegação por teclado), consumida em `frontend/src/components/Sidebar.tsx:296`. A mudança de escopo — busca de **ÁREA** sobre as mesmas entradas que o usuário enxerga, no lugar do "Buscar usuários" do legado — está justificada em `SidebarSearch.tsx:18-20`. ⚠ A implementação equivalente dentro de `components/ConsoleTopbar.tsx:72-133` é **código morto** (nenhum consumidor): duas cópias da mesma busca, uma delas sem uso. |
| FE-395 | migrated | Sidebar / menu do console — grupos acordeão com ícones zmdi, itens internos, rodapé com descrição do produto e links de "ter… | blocked | Os grupos acordeão, o item ativo por `NavLink` e o flyout em PORTAL estão prontos e conferidos: `frontend/src/components/Sidebar.tsx:302-368` (grupos), `:318-331`/`ItemLink:94-146` (`NavLink` marcando o ativo), `frontend/src/app/consoleNavigation.tsx:330-520` (os 6 grupos) e `frontend/src/hooks/useNavItems.ts`; teste `frontend/src/hooks/__tests__/useNavItems.test.ts` + `frontend/src/components/__tests__/logout.test.tsx`. **Falta o RODAPÉ do menu**: o legado tem, em `app/views/pub/console/base/menu/_container.html.erb:36-42`, (a) `<div class="metadata_wrapper">` com `SFG::Metadata::DESCRIPTION` (a descrição do produto) e (b) os dois links "termos de uso" / "políticas de privacidade" em nova aba. Nenhum dos dois existe: o rodapé do ai9 (`Sidebar.tsx:370-491`) tem só o toggle "Modo agente" e o cartão do usuário. Mesma lacuna da FE-335. Nenhuma DEC os dispensa. |
| FE-396 | migrated | Resume do usuário no topo da sidebar — avatar (ou iniciais com cor aleatória), nome encurtado e botão "Verificação: {nível}"… | blocked | Duas das três peças estão prontas: avatar/iniciais com **cor determinística** em `frontend/src/components/ui/UserAvatar.tsx:65-98` (`initialsOf` + `avatarTone` derivado do id — IMP-A23, contra o `random_color` que gerava cor nova a cada render em `observer.rb:32-36`), com teste próprio `frontend/src/components/ui/__tests__/UserAvatar.test.tsx`; e o nome encurtado em `frontend/src/components/Sidebar.tsx:433-435`. **Falta o indicador "Verificação: {nível}"** no resumo da sidebar: o legado o tem em `app/views/pub/console/base/resume/_container.html.erb:13-21` (`<a class="level_section_button">` com ícone que troca por `info.high?`), e a **DEC-74** (`.migration-ai9/decisions.md:1924-1932`) decidiu **replicá-lo como está**. No ai9 ele só existe em `/profile` (`frontend/src/app/pages/ProfilePage.tsx:622`) e em `/users/:id` (`UserDetailPage.tsx:213`) — `grep -rn "rotuloDeVerificacao\\|confiability_level" frontend/src/components/Sidebar.tsx` → vazio. Falta: acrescentar o chip de nível ao cartão do usuário (ou ao popover) da sidebar, com o ícone alternando em `high`. |
| FE-397 | migrated | Área de conteúdo (dashContainer) — o container central que recebe cada seção; identifier console_content_wrapper, containerS… | verified | `frontend/src/components/Layout.tsx:99-106` — o `<main>` é o container central que recebe cada seção pelo `<Outlet/>`, envolto em `RouteErrorBoundary` (uma exceção numa tela não desmonta a árvore inteira, que era o sintoma do legado). Espaçamento e respiro do topo/rodapé conferidos por `frontend/src/components/__tests__/layoutRespiroDoTopo.test.ts` e `src/__tests__/empilhamento-barras.test.ts`. ⚠ A linha do razão aponta `Sidebar.tsx`, que é a evidência da FE-395 copiada — o arquivo certo deste ID é o `Layout.tsx`. |
| FE-399 | migrated | Drawer direito "helper" (dashHelperHolder) — painel deslizante para formulários de criação/edição; contém o botão flutuante… | verified | `frontend/src/components/SideDrawer.tsx:13-80` — painel deslizante à direita (`fixed right-0 top-0 h-screen w-full sm:w-[500px]`), backdrop clicável (`:46`), fecha com `Escape` (`:31-37`), `data-helper` no wrapper (`:42`) e rodapé de ações opcional (`:72-75`). Reusado como está pelos formulários de `/messages` (`MessagesPage.tsx:360-372`), de risco, de renegociação e dos catálogos. |
| FE-400 | migrated | Barra de ações inferior (dashBottomHolder + ActionStack) — "Deseja salvar as alterações?" com botões Cancelar/Salvar (no mob… | verified | `frontend/src/components/ui/FormActionBar.tsx:51-140` — a barra diz o estado sujo ("Alterações não salvas", `:114-120`), lista as pendências em pt-BR e dá precedência a erro > pendências > sujo > resumo (`:41-47`). `frontend/src/hooks/useUnsavedChanges.ts:95-110` avisa antes de sair: `beforeunload` mais interceptação de clique em link interno **na fase de captura** (`useBlocker` não serve porque o app monta `<BrowserRouter>`; a limitação está escrita no cabeçalho do hook). Consumida em `frontend/src/features/receivables/pages/ReceivableFormPage.tsx:135,287-293,740-787`, onde **Descartar restaura o ponto inicial EM MEMÓRIA** — `grep -n "window.location.reload" FormActionBar.tsx useUnsavedChanges.ts ReceivableFormPage.tsx` → **vazio**, que é a correção do `cancel()` do legado. Teste: `frontend/src/components/ui/__tests__/FormActionBar.test.tsx` (11 exemplos, incluindo "com alteração, a saída é REPRESADA — e só acontece na confirmação" e "descartar limpa o estado sujo sem recarregar"). |
| FE-403 | migrated | Paginação do console (.console_navigation_wrapper) — 5 controles: primeira, anterior, input de "limite", próxima, última | verified | `frontend/src/components/ui/PaginationPill.tsx:160-220` — os **cinco** controles do legado: "Primeira página" (`:165`), "Página anterior" (`:174`), input "Itens por página" (`:193`, com rascunho que volta ao valor válido em `:100-111`), "Próxima página" (`:207`) e "Última página" (`:216`). O envelope (cabeçalho `X-Total-Count`/`X-Page`/`X-Per-Page` **ou** `meta`) é lido só por `frontend/src/lib/api/pagination.ts:54-78`. Consumido em `/messages` (`MessagesPage.tsx:294-300`). Teste: `frontend/src/components/ui/__tests__/Pagination.test.tsx` + `backend/spec/requests/api/v1/observers_spec.rb`. |
| FE-405 | migrated | Tela "Mensagens" (admin_messages) — layout flex de 2 colunas: à esquerda o card de mensagens (busca + paginação + 2 filtros… | verified | `frontend/src/app/pages/MessagesPage.tsx:205` — `grid lg:grid-cols-[minmax(0,2fr)_minmax(0,1fr)]`: à esquerda o card de mensagens (busca `:188`, dois filtros `:209-225`, paginação `:294`), à direita a coluna de observadores (`:306`). **Com item de menu**: `frontend/src/app/consoleNavigation.tsx:548` (`admin_messages` → `/messages`), no legado a tela era órfã porque o `create_console_menu` não a listava. Teste: `frontend/src/hooks/__tests__/useNavItems.test.ts:187-190`. |
| FE-406 | migrated | Widget de mensagem (.feedback_widget) — nome, e-mail, contexto (com ícone de etiqueta), situação (ícone de sino), corpo da m… | blocked | O widget existe em `frontend/src/app/pages/MessagesPage.tsx:246-274` com **nome** (`:262`), **contexto** e **situação** como `Badge` tokenizado (`:265-266` — a cor vem da chave do servidor via `VARIANTE_DE_CONTEXTO`/`VARIANTE_DE_SITUACAO`, não do hex que `context.rb:41-47` devolvia), **corpo** (`:268`), data e contagem da conversa (`:269-271`), e os campos extras **nomeados** no detalhe (`:420-435`, no lugar de `hadouken`/`shoryuken`). **Falta o E-MAIL do remetente**, que o legado mostra em `app/views/pub/console/parts/admin_messages/list/_widget.html.erb:14-18`: no ai9 `sender_email` só é usado como `colorKey`/fallback do avatar (`MessagesPage.tsx:259`; `UserAvatar.tsx:92-98` só o coloca no `alt`) — `grep -n "sender_email" src/app/pages/MessagesPage.tsx` devolve **uma única linha**. Falta: exibir o e-mail no widget (ou no cabeçalho da thread), como o legado faz. Nenhuma DEC o dispensa. |
| FE-407 | migrated | Busca + filtros de mensagens | verified | `frontend/src/app/pages/MessagesPage.tsx:126-127` — `trocarFiltro(fn)` executa a troca e chama `reset()`: **trocar filtro zera a página**. No legado o `offset` sobrevivia à troca e o usuário podia ficar numa página inexistente, com lista vazia e sem explicação. Busca com `SearchInput` em `:188` e os dois filtros (situação e contexto) em `:209-225`, com "Todos os contextos" como opção neutra. O total passa a respeitar os filtros (BE-424). |
| FE-408 | migrated | Lista de observers (.feedback_observer_widget) — nome, e-mail e menu de três pontos (Foundation Dropdown) com "Remover" e "E… | verified | `frontend/src/app/pages/MessagesPage.tsx:337-357` — lista de observadores com avatar, **nome** (`:347`), **e-mail** (`:348`) e a contagem de contextos (`:350`); clicar abre o drawer de edição (`:342`). A **remoção** existe, no rodapé do próprio drawer (`:602-603,623-626`). ⚠ Divergência de affordance registrada: no legado as duas ações ficam num menu de três pontos (Foundation Dropdown) na linha; aqui "Editar" é o clique na linha e "Remover" é um botão dentro do drawer. Mesmas ações, ponto de entrada diferente. |
| FE-409 | migrated | Drawer de criar/editar observer — formulário com "Nome do observador", "Email do observador", checkbox "Feedbacks do sistema… | verified | `frontend/src/app/pages/MessagesPage.tsx:628-670` — "Nome do observador" (`:638`), "E-mail do observador" (`:643`), checkbox "Recebe também mensagens internas" com a descrição do que muda (`:645-650`) e o `fieldset` "Contextos observados" (`:653-667`). O rótulo do bloco de contextos deixa de ser o copy-paste de "Seu nome" e a obrigatoriedade está escrita (`:657-659`). Botões Cancelar/Salvar com `disabled={!podeSalvar}` (`:629-630`). |
| FE-410 | migrated | Toasts (M.push / M.pushRaw) — sistema global de notificação usado por todo o console: M.SUCCESS, M.ERROR, M.HELP (este últim… | blocked | **Defeito D-QA4-02 continua aberto — medido agora.** O embrulho existe e está correto (`frontend/src/lib/notify.ts:33-60`: de-para de `M.SUCCESS/ERROR/WARNING/HELP` e as duas regras — `info` com `Infinity` até o usuário fechar, `error` 8 s contra 3 s do `success`). Mas: `grep -rln "@/lib/notify" frontend/src` → **1 arquivo, `src/app/pages/UiKitPage.tsx`** (a vitrine); `grep -rln "from 'sonner'" frontend/src \| wc -l` → **71 arquivos**; `grep -rno "toast\.\(success\\|error\\|info\\|warning\\|message\)(" frontend/src \| wc -l` → **273 chamadas**; e o `<Toaster>` de `frontend/src/app/App.tsx:21-38` (`BrandedToaster`) **não define `duration`** em `toastOptions`. Logo, as duas regras não valem em nenhum ponto do produto. Falta: (a) trocar `import { toast } from 'sonner'` por `@/lib/notify` nas telas, **ou** (b) fixar as durações no `BrandedToaster` e aposentar `notify.ts`; e um teste que impeça a regressão. Sem teste hoje. |
| FE-411 | migrated | Reciclável app_button (.app_button.app_inline_button) — botão base do console com .app_button_icon, .app_button_title e .app… | verified | `frontend/src/components/ui/Button.tsx:31-131` — botão base com cinco variantes fechadas (`:47`), quatro tamanhos (`:84-89`), ícone via `children` e `loading` como **estado** (`:100-127`). Adoção medida: `grep -rl "components/ui/Button'" frontend/src \| wc -l` → **109 arquivos**. Suíte verde (58 arquivos / 506 testes, 0 falhas). ⚠ Sem teste dedicado a este ID; a prova é o uso mais a suíte. |
| FE-412 | migrated | Reciclável section_button — botão dos drawers/formulários, com spinner embutido (.section_button_loader_wrapper) | verified | O `section_button` com spinner embutido é o mesmo `Button` com `loading`: `frontend/src/components/ui/Button.tsx:106-127` — fica desabilitado (`disabled={disabled \|\| loading}`), marca `aria-busy` (`:107`), troca o cursor (`:116`) e sobrepõe o `Spinner` do DS deixando o rótulo `invisible` (`:121-126`), o que preserva a largura do botão. Consumido pelos rodapés dos drawers (ex.: `InstallmentDrawer.tsx:124`, `MessagesPage.tsx:630`, `RiskOperationDrawer.tsx:278`). ⚠ **Lacuna de teste registrada**: não há exemplo que exercite `loading` no `Button` (`grep -rn "aria-busy" frontend/src/components/ui/__tests__` → vazio); o `aria-busy` testado em `mobileLibrary.test.tsx:63-67` é o do `MobileKPI`. |
| FE-413 | migrated | Reciclável app_arrow — seta CSS pura com variantes app_arrow_left / app_arrow_right (pseudo-elementos :before/:after) | verified | As setas de CSS puro do legado (`app_arrow_left`/`app_arrow_right`, com `:before`/`:after`) não foram portadas: `grep -rn "app_arrow" frontend/src` → **vazio**. O papel é feito por `ChevronLeft`/`ChevronRight` do `lucide-react`, já presente no `package.json` e usado em **23 pontos** (`grep -rn "ChevronLeft\\|ChevronRight" frontend/src --include=*.tsx \| wc -l`), entre eles `PaginationPill.tsx:165-220` e `Sidebar.tsx:258,271`. Reuso de biblioteca — um componente proprietário a menos. |
| FE-414 | migrated | Reciclável console_card — card denso de detalhe, com .card_title (+ __flat, __small), .line (+ __last, .label, .content, .co… | verified | `frontend/src/components/ui/DetailList.tsx:21-90` — o par rótulo/valor do card denso: `DetailItem { label, content }` (`:21-24`), rótulo em caixa alta com `tracking-widest` (`:76`) e valor com tratamento de **vazio** próprio (`:80-85`, `emptyValue` em vez de célula em branco), que é o que o `.line`/`.label`/`.content` do legado não distinguia. Adoção medida: **10 arquivos** importam `ui/DetailList`. Suíte verde. ⚠ Sem teste dedicado a este ID. |
| FE-415 | migrated | Reciclável app_card — card de listagem/resumo, com .app_card_title_wrapper_flex, .app_card_wrapper_flex, .app_card_info (+ .… | verified | `frontend/src/components/ui/Card.tsx:19-78` — `Card` + `CardHeader`/`CardTitle`/`CardDescription`/`CardContent`/`CardFooter`, o equivalente ao `.app_card_title_wrapper_flex` / `.app_card_wrapper_flex` / `.app_card_info` do legado; e `frontend/src/components/mobile/MobileCard.tsx` para o cartão de listagem no telefone (DEC-100 — mobile é view própria). Adoção medida: **23 arquivos** importam `ui/Card` e **18** usam `MobileCard`. Teste que exercita o cartão mobile: `frontend/src/components/mobile/__tests__/mobileLibrary.test.tsx`. ⚠ Sem teste dedicado a este ID. |
| FE-416 | migrated | Reciclável console_table — tabela flex com .console_table_header, .console_table_widget (linha), .table_column (variantes __… | verified | `frontend/src/components/ui/Table.tsx` (markup puro, consumido por `app/pages/admin/CredentialsPage.tsx`) e `frontend/src/components/ui/DataTable.tsx` — a tabela de verdade, com colunas declaradas (`Column<T>`: `header`, `align`, `width`, `sortable`, `cell`), cabeçalho, linha, primeira coluna `sticky` só quando há transbordo (`:398-401`) e view em cartões no telefone (`mobile="cards"`). Adoção: **18 arquivos**. Teste: `frontend/src/components/ui/__tests__/DataTable.test.tsx` (7 exemplos, entre eles "ordena crescente, decrescente e volta à ordem original em três cliques" e "no modo `server` delega a ordenação"), mais `dataTableArraste.test.tsx` e `dataTableTransbordo.test.tsx`. |
| FE-417 | migrated | Reciclável select (.app_button.section_input_flex + .section_select_arrow + select.section_input.section_select) — select es… | verified | `frontend/src/components/ui/Select.tsx:7` — select estilizado do DS, que aposenta o `<select>` cru com `.glass-select`. A classe **saiu do CSS**: `frontend/src/styles/globals.css:642` guarda a lápide explicando por quê (ela tematizava só a caixa fechada; a lista de `<option>` continuava desenhada pelo SO, branca no tema escuro), e `frontend/src/components/ProjectSelector.tsx:23-25` registra a troca. Adoção: **33 arquivos** importam `ui/Select`. ⚠ Sem teste dedicado a este ID. |
| FE-418 | migrated | Reciclável checkbox/radio (.control) — checkbox e radio custom com .control__indicator | verified | `frontend/src/components/ui/Checkbox.tsx` e `frontend/src/components/ui/RadioGroup.tsx` — o par `.control` / `.control__indicator` do legado vira componente do DS, com `label` e `description` como props (ver o uso em `MessagesPage.tsx:645-650`). Adoção: **6 arquivos** usam `Checkbox`, **2** usam `RadioGroup`. Lacuna real do design system, fechada (IMP-A69). ⚠ Sem teste dedicado a este ID. |
| FE-419 | migrated | Reciclável switch (.app_switch_toggle) — toggle com bolinha e gradiente, dirigido por custom properties (--offset, --gradien… | verified | `frontend/src/components/ui/switch.tsx:2,9-20` — o `.app_switch_toggle` (bolinha + gradiente dirigido por `--offset`/`--gradient`) vira o `Switch` do **Radix** (`@radix-ui/react-switch`), com estado por `data-[state=checked]` e cores de token (`bg-primary` / `bg-input`). Adoção: **14 arquivos**. Uso testado indiretamente em `frontend/src/features/risk/__tests__/riskOperations.test.tsx` e `mobileLibrary.test.tsx`. ⚠ Sem teste dedicado a este ID. |
| FE-420 | migrated | Reciclável busca (.app_search_wrapper) — barra de busca com ícone zmdi-search e input; variantes .app_standalone_search e .a… | verified | `frontend/src/components/ui/SearchInput.tsx:18-60` — ícone à esquerda (`Search`, trocado por `Spinner` enquanto carrega, `:41`), campo, botão de limpar (`:34`) e dois tamanhos. O par natural é `frontend/src/hooks/useDebouncedSearch.ts` (300 ms), citado no cabeçalho (`:14`). Adoção: **18 arquivos**. O comportamento de debounce é exercitado em `src/app/pages/__tests__/faqPage.test.tsx` e `src/app/pages/catalogs/__tests__/CatalogScreen.test.tsx:118-138` ("o vazio de BUSCA cita o termo procurado"). |
| FE-421 | migrated | Reciclável autocomplete (.app_input_complete_holder) — dropdown absoluto de sugestões, com .app_input_complete_widget por it… | verified | `frontend/src/components/ui/Autocomplete.tsx` — **uma** implementação de dropdown de sugestões, como o mapa exigia, montada sobre `ui/ResultItem.tsx` (o item) e `ui/Badge.tsx` (a variante removível). Adoção: **3 arquivos** a consomem. ⚠ Sem teste dedicado a este ID. |
| FE-422 | migrated | Reciclável generic_search_widget — item de resultado de busca genérica, com ícone, título e spinner por item | verified | `frontend/src/components/ui/ResultItem.tsx:42` — item de resultado com ícone, título e **spinner por item** (`aria-busy={loading \|\| undefined}`), que é exatamente o `generic_search_widget` do legado. Consumidor: `frontend/src/components/ui/Autocomplete.tsx` (`grep -rln "ResultItem" frontend/src --include=*.tsx` devolve só ele e a própria definição — a linha do razão contava o arquivo dele mesmo entre os "2"). ⚠ Sem teste dedicado a este ID. |
| FE-423 | migrated | Reciclável app_tabs (.app_tabs_section_wrapper + .tab_section_content) — abas em pílula com scroll horizontal | verified | `frontend/src/components/ui/tabs.tsx:62-110` — a `TabsList` é `overflow-x-auto overflow-y-hidden scroll-smooth` com a barra de rolagem escondida (`:101`), e a aba focada/ativa entra em vista sozinha (`scrollIntoView` em `:87` e `:125`) — o legado deixava as últimas abas simplesmente sumirem fora do contêiner. Adoção: **6 arquivos**. ⚠ Sem teste dedicado a este ID. |
| FE-424 | migrated | Reciclável console_section_tab — painel de conteúdo de aba: visibility: hidden; opacity: 0; height: 0 → .active revela com t… | verified | `frontend/src/components/ui/tabs.tsx:157-183` — `TabsContent` do Radix substitui o par `visibility:hidden; opacity:0; height:0` → `.active` do legado: o painel inativo simplesmente não é montado, então não há conteúdo invisível ocupando o DOM nem focável por engano. Mesmo arquivo e mesma adoção da FE-423. |
| FE-425 | migrated | Reciclável app_badge — etiqueta arredondada; variante .app_badge_transparent (contorno) e .app_badge_icon clicável (remover) | verified | `frontend/src/components/ui/Badge.tsx:22-43` — etiqueta arredondada (`rounded-full`) com as variantes de token (`default`, `secondary`, `destructive`, `outline` = o `.app_badge_transparent`, mais `success`/`warning`/`info`/`negative`), e `:52,72` a prop **`onRemove`**, que acrescenta o "x" com `removeLabel` e transforma o badge em item removível — é o `.app_badge_icon` clicável, consumido pelo `Autocomplete`. Adoção: **43 arquivos**. |
| FE-426 | migrated | Reciclável app_tooltip — tema app_theme do Tippy.js (fundo branco, sombra suave, max-width: 300px) com setas por posição (to… | verified | `frontend/src/components/ui/Tooltip.tsx:54-110` — tooltip próprio do DS, com lado preferido e **alternativo** (`:102-103`) para não sair da tela. O **Tippy.js saiu**: `grep -rn "tippy" frontend/package.json frontend/src` só acha um comentário histórico em `features/structured-operations/components/OperationForm.tsx:42` — uma dependência de terceiro a menos. O cabeçalho documenta por que o painel é portal (`.glass-panel` cria contexto de empilhamento). Adoção: **28 arquivos**. |
| FE-428 | migrated | Reciclável app_loader — spinner de 4 anéis com @keyframes app_loader; variantes .app_accent_loader e .app_thin_loader | verified | `frontend/src/components/ui/Spinner.tsx:16-45` — spinner com `animate-spin rounded-full border-current border-t-transparent` (o `@keyframes app_loader` de 4 anéis vira uma borda com topo transparente), tamanhos por prop (`xs`…) e `label` acessível opcional (`label={null}` para uso decorativo dentro de botão). Consumido por `Button.tsx:123`, `SearchInput.tsx:41`, `ResultItem.tsx` e outros — **5 arquivos** o importam diretamente. Lacuna real do DS, fechada (IMP-A71). |
| FE-430 | migrated | time_ago — tempo relativo em pt-BR | verified | `frontend/src/lib/utils/date.ts:161-165` — `timeAgo` único, sobre `formatDistanceToNow` do `date-fns` com `locale: ptBR` e `fallback` em vez de "Invalid Date". Teste: `frontend/src/lib/utils/__tests__/transversais.test.ts:67-82` — "é UM utilitário e produz texto relativo em pt-BR" e "data inválida devolve o fallback". O legado tinha dois (app + engine `ux_kit19`) e esta base tinha mais dois inline; os consumidores passaram a chamar este. |
| FE-431 | migrated | format_money — moeda brasileira | verified | `frontend/src/lib/utils/number.ts:30-33` (`formatMoney`, com `fallback` para nulo/NaN) mais `formatAmount` (`:36`) e `formatMoneyCompact` (`:60`) para célula densa. Reuso: já existia, não foi reimplementado, e o parse que desfaz a formatação é o `parseNumberPtBr` do mesmo módulo. Teste: `frontend/src/lib/utils/__tests__/transversais.test.ts:139-147` ("moeda é `formatMoney` de utils/number" — o teste reprova uma segunda implementação) e `src/__tests__/numero-em-pt-br.test.ts`. |
| FE-432 | migrated | gender_prefix — concordância de gênero em textos | verified | `backend/config/locales/pt-BR.yml:247-273` — `audit_trail.artigos` e `audit_trail.entidades`, cada entidade com `{ rotulo:, genero: }` (ex.: `:254` `User: { rotulo: "conta de usuário", genero: f }`). O `gender_prefix` deixa de ser função e vira o par rótulo/gênero no catálogo, consumido por `backend/app/lib/sfg/audit_summary.rb`. No legado a função lia `user.info.gender` (1/2) e **não tinha consumidor**. Teste: `backend/spec/lib/sfg/audit_summary_spec.rb`. |
| FE-433 | migrated | initials — iniciais para avatar | verified | `frontend/src/components/ui/UserAvatar.tsx:65-72` (`initialsOf`) — reuso: o design system já produzia as iniciais, com fallback para o e-mail quando não há nome; não foi reimplementado (o próprio legado marcava `#FIXME: ta no ux_kit19`). Teste: `frontend/src/lib/utils/__tests__/transversais.test.ts:139-152` (bloco "reuso — não há segunda implementação") + `frontend/src/components/ui/__tests__/UserAvatar.test.tsx`. |
| FE-434 | migrated | chop_middle_words — nome curto | verified | `frontend/src/lib/utils/text.ts:29` (`chopMiddleWords`) — primeiro + último nome. Teste: `frontend/src/lib/utils/__tests__/transversais.test.ts:22-46` (5 exemplos): "devolve primeiro e último nome", "nome único passa inteiro", "entrada vazia devolve vazio, como no legado", **"nulo não levanta"** (no legado dava `NoMethodError`) e "espaços repetidos não viram nomes vazios". |
| FE-435 | migrated | random_color — cor aleatória de identificação (gem color-generator) | verified | `frontend/src/components/ui/UserAvatar.tsx:58` (`avatarTone`) — cor **determinística** (djb2 sobre a chave), em tokens semânticos; a gem `color-generator` do legado sai e não é substituída. Teste: `frontend/src/lib/utils/__tests__/transversais.test.ts:148-152` ("a cor de identificação é determinística") + `frontend/src/components/ui/__tests__/UserAvatar.test.tsx`. |
| FE-436 | migrated | month_array — 12 meses localizados | verified | `frontend/src/lib/utils/date.ts:189` (`monthOptions`) — 12 meses do catálogo pt-BR do `date-fns`, com o valor **1..12** que o servidor espera. Teste: `frontend/src/lib/utils/__tests__/transversais.test.ts:103-111` ("doze meses em pt-BR, com o valor 1..12 que o servidor espera"). |
| FE-437 | migrated | ten_years_array — janela de anos | verified | `frontend/src/lib/utils/date.ts:202` (`yearWindow`) — faixa preservada `ano-5 .. ano+5`. Teste: `frontend/src/lib/utils/__tests__/transversais.test.ts:112-121` ("preserva a faixa do legado: ano-5 até ano+5"). O nome do legado mentia: são **onze** anos, não dez. |
| FE-438 | migrated | pluralize_for — plural simplificado | verified | `backend/config/locales/pt-BR.yml:274-276` (`audit_trail.plurais`, só o irregular ou acentuado) + `backend/app/lib/sfg/inflection.rb:12-45` — o dado vence a regra, e o que não estiver no catálogo cai em **três** regras do português regular. O `+ "s"` do legado produzia "permissãos" e "papels"; e lá a função não tinha consumidor. Teste: `backend/spec/lib/sfg/audit_summary_spec.rb`. |
| FE-439 | migrated | slice_in — distribuição em N colunas | verified | `frontend/src/lib/utils/text.ts:53` (`sliceIn`) — UMA cópia (o legado tinha três, duas dentro de controllers). Teste: `frontend/src/lib/utils/__tests__/transversais.test.ts:48-66` (4 exemplos), incluindo **"zero colunas NÃO descarta a lista"** (o `sliceIn(lista, 0)` do legado descartava tudo em silêncio) e "coluna sem item fica vazia, não some". |
| FE-440 | migrated | days_js_array — datas para o datepicker JS | verified | `frontend/src/lib/utils/date.ts:95,176` (`toIsoDate`, `toIsoDateTime`) — a fronteira passa a ser **ISO-8601**, com params `DateTime` no Grape do lado do servidor. O `days_js_array` montava `"%m/%d/%Y"`, formato ambíguo num sistema de vencimentos. Teste: `frontend/src/lib/utils/__tests__/transversais.test.ts:83-102` (4 exemplos), incluindo "nunca devolve formato brasileiro para a API" e "entrada inválida devolve null, não uma data inventada". |
| FE-441 | migrated | create_console_menu — o menu lateral inteiro do console, montado por papel e permissão (a maior regra de negócio escondida e… | verified | `frontend/src/app/consoleNavigation.tsx:330-560` — o `create_console_menu` (D-118) vira **configuração declarativa**: 6 grupos, cada item com `roles`, `resource` e `requiresProject`; o filtro está em `frontend/src/hooks/useNavItems.ts:46-131` (`papelPassa` + `projetoPassa` + modo). Teste: `frontend/src/hooks/__tests__/useNavItems.test.ts` — cobre os **dois lados** por papel (`:37-72`), o gate de participação (`:74-103`), e ainda "papel desconhecido cai no conjunto MAIS RESTRITO" e "todo item visível tem página montada — menu nunca leva a 404" (`:141-148`). |
| FE-442 | migrated | week_days — nome do dia da semana em pt-BR | verified | `frontend/src/lib/utils/date.ts:216` (`nomeDoDiaDaSemanaPorIndice`) — nome completo do catálogo pt-BR. Teste: `frontend/src/lib/utils/__tests__/transversais.test.ts:122-138` (3 exemplos): "domingo e sábado NÃO ganham '-feira'", "os dias úteis ganham" e "índice fora da faixa dá a volta, como o `% 7` do legado". O legado escrevia a gramática como condicional (`unless nday==0\|\|nday==6`). |
| FE-443 | migrated | tracking_color / tracking_icon — aparência do item da trilha | verified | `frontend/src/components/audit/auditAppearance.ts:28-50` — mapa `event → { icon, tone, label }` como **DADO**, com `PADRAO` para evento desconhecido; as cores são tokens semânticos (`bg-success/15`, `bg-info/15`, `bg-negative/15`, `bg-warning/20`), nunca literais. Registro honesto no próprio arquivo (`:10-16`): no legado `tracking_color`/`tracking_icon` (`application_helper.rb:182-190`) **ignoram o argumento** e devolvem constante — não havia mapa a portar, isto é comportamento novo (improvements-log). Teste: `frontend/src/components/audit/__tests__/AuditTrailTimeline.test.tsx:100-126` (4 exemplos, incluindo "a cor é token semântico, nunca literal"). |
| FE-445 | migrated | Widget de item da trilha (timeline de atividade) | verified | `frontend/src/components/audit/AuditTrailTimeline.tsx` + `AuditTrailItem.tsx`, montados em `frontend/src/app/pages/admin/AuditTrailPage.tsx` (rota e item de menu em `consoleNavigation.tsx`, conferidos por `useNavItems.test.ts:169`). Teste: `frontend/src/components/audit/__tests__/AuditTrailTimeline.test.tsx:25-98` (10 exemplos) — mostra o **autor real**, na impersonação **as duas pessoas**, autor ausente vira "sistema", o instante absoluto fica no `datetime` ISO enquanto o relativo é o que se lê, e os **quatro estados** (incluindo erro distinguível de vazio). |
| FE-446 | migrated | Payload JSON da trilha | verified | `backend/app/controllers/api/entities/audit_version.rb:18-64` — `entity_label` do catálogo pt-BR (`:23`), `event`, **`summary` DERIVADO** por `Sfg::AuditSummary` (`:31`, não coluna — então o defeito do legado, em que `resume > 300` fazia o `save` devolver `false` e o evento sumir, não tem onde acontecer), `author`, `impersonated`, `reason`, `ip_address`, `occurred_at` em ISO-8601 UTC e `changes` campo a campo, com `snapshot` só no detalhe. Teste: `backend/spec/requests/api/v1/audit_trail_spec.rb`. |
| FE-481 | migrated | Monitor do usuário no widget de navegação — GET /users/<id>.json a cada 1000 ms | verified | O monitor de 1 s **não foi reintroduzido** — e o legado já o tinha desligado (`.start()` comentado desde a "Build #5695", com `success` vazio). O estado da sessão chega por **evento**: `backend/app/channels/permissions_channel.rb:26-31` (fluxo sempre do usuário da conexão; pedir o de outro é `reject` — fecha a flag de upstream U2), consumido em `frontend/src/app/pages/users/PermissionsPage.tsx:80-84` via `useChannel`; e por **rotação de `jti`** no bloqueio da conta (`backend/app/services/users_service.rb:235-241` + `auth/token_service.rb:49,66-72`), que derruba a sessão aberta na hora (DEC-39). Teste do portão: `frontend/src/__tests__/no-api-polling.test.ts` (nenhum `setInterval` fala com a API e `useAutoRefresh` segue sem consumidor). |
| FE-482 | migrated | Visibilidade do progresso de job para o usuário (o "progresso" do progress_job) | verified | `frontend/src/hooks/useJobProgress.ts:55-108` — o progresso chega por **Action Cable** (`useChannel('ProjectProgressChannel', …)`, `:102`), com `status`/`percent` normalizados (`:76-85`, incluindo a proteção contra percentuais alternados em `:66`) e `reset` explícito. Nenhum `setInterval` (guardado por `frontend/src/__tests__/no-api-polling.test.ts`). O contrato de payload corrigido é o do OPS-127. |
| FE-484 | migrated | Upload de anexos da renegociação (limites na UI) | verified | `frontend/src/features/attachments/validation.ts:14-65` — os textos do legado palavra por palavra (`:14` "O máximo de arquivos permitido para envio é de N arquivos"; `:18` "O tamanho máximo de cada arquivo permitido para envio é de N MB"), com os **números vindos do servidor** e o comentário dizendo que isto é conveniência, não segurança (`:29`). Teste: `frontend/src/features/attachments/__tests__/validation.test.ts` (7 exemplos), incluindo "recusa arquivo grande com o texto do legado", "conta o que já está anexado, não só o lote", "deixa passar quando o limite ainda não chegou do servidor" e "usa os números do servidor, nunca constantes escritas na tela". |
| FE-501 | migrated | Layout real das telas de auth (sobrescrita do app) | verified | `frontend/src/app/pages/LoginPage.tsx:36` usa o componente `Logo` da marca (`components/brand/Logo.tsx`), montado sobre os **tokens do app** — não há `app_theme` por usuário (DEC-55/DEC-56). Teste: `frontend/src/__tests__/marca-fonte-unica.test.ts:140-216` — "nenhuma rota, tela ou chamada de API de CRUD de tema", "todo caminho de logo citado no componente aponta para arquivo existente" e "nenhuma tela referencia o arquivo do logo direto — só o componente `Logo`". |
| FE-507 | migrated | Tela "nova senha" do SFG (a que está no ar) | verified | A tela "nova senha" do SFG não tem equivalente por senha (DEC-14: o Safegold não tem senha); o caminho correspondente é `/magic-login`, e o **estado de erro é alcançável**: `frontend/src/features/auth/MagicLinkCallbackPage.tsx:15,25,34,44-48` — "Link inválido ou expirado.", "Resposta inválida do servidor." e "Link expirado ou já utilizado. Solicite um novo acesso." Teste: `frontend/src/features/auth/__tests__/MagicLinkCallback.test.tsx` (2 exemplos, verdes) + `MagicLogin.test.tsx` (3). |
| FE-513 | migrated | Painel de permissões do usuário (show/_abilities) | verified | `frontend/src/app/pages/users/UserDetailPage.tsx:148-177` (aba "Permissões" dentro das `Tabs`) e `:414-418` — **a tela DIZ o que sumiu**: "Das 17 permissões do sistema antigo sobraram **sete** — as que de fato…", exatamente para quem vem do legado procurar as outras dezesseis. A aba e o conteúdo passam a ser gateados no mesmo lugar (`podeEscrever`, `:174`), fechando o FE-022. O comentário `:315-330` registra por que este painel **não** assina o `PermissionsChannel` (o canal só entrega o fluxo da própria conexão). |
| FE-515 | migrated | Widget de usuário (card) | verified | `frontend/src/app/pages/UsersPage.tsx:507-566` — o card do usuário com opacidade reduzida quando bloqueado (`:507`) e o **selo** de conta bloqueada (`:524-526`, DEC-39), além de desabilitar personificar (`:561`). Teste: `frontend/src/app/pages/__tests__/UsersPage.dados-reais.test.tsx:102-116` ("conta bloqueada aparece com selo (DEC-39)"), no mesmo arquivo que prova que a lista vem da API e não de mock. |
| FE-516 | migrated | Payloads JSON/XML de usuário (contrato canônico) | verified | `backend/app/controllers/api/entities/user.rb:6-120` — o contrato canônico, com **todos** os campos que o mapa (`.migration-ai9/map/auth-admin.md:528`) exige: `id`, `email`, `phone`, `name`, `avatar_url` (`:14`), endereço (`:18-24`), `user_type_id`/`is_og`/`user_type`/`user_type_slug` (`:25-33`), `last_login_at`/`login_count` (`:35-36`), `identifier`/`username` (`:42-43`), bloqueio (`:47-49`), `confiability_level` (`:54`), `biography_html`/`biography_text` (`:80-96`), `custom_variables` (`:99`) e `permissions` (`:111`). ⚠ **A prova citada no razão não prova nada**: `frontend/src/lib/api/__tests__/auth.test.ts` são 2 exemplos de esqueleto (`expect(1+1).toBe(2)` e um objeto mock). A cobertura real é `frontend/src/app/pages/__tests__/UsersPage.dados-reais.test.tsx` (11 exemplos sobre o payload de verdade) + `backend/spec/requests/api/v1/users_spec.rb`. |
| FE-518 | migrated | Partial de metadados/SEO das telas de auth | verified | `frontend/index.html:18` (`<title>Safegold — gestão de risco, recebíveis e operações estruturadas</title>`), `:31-32,46,49` (favicon `.ico` + PNG 32, apple-touch 180, `apple-mobile-web-app-title`) e `:58` (`og:title`); o componente por tela é `frontend/src/components/seo/SEO.tsx:25-59`, que compõe `{title} \| Safegold` e emite `description`, JSON-LD e Open Graph. Teste: `frontend/src/__tests__/marca-fonte-unica.test.ts:163-216`. |
| FE-523 | migrated | Formulário de mensagem (messages/_form) e o formulário equivalente do SFG | verified | `frontend/src/app/pages/MessagesPage.tsx:512-558` — o drawer "Nova mensagem" com os campos do `messages/_form` do legado (Contexto em `:532-540`, Mensagem em `:542-556`), `maxLength={500}` (`:548`, o mesmo limite no banco, no servidor e na tela — no legado a coluna era `string(255)` e a validação aceitava 500) e contador de caracteres restantes (`:555`, o `countChar` do legado). Campos extras **nomeados** (`extra1_label`/`extra2_label`, `:420-435`) no lugar de `hadouken`/`shoryuken`. Contrato do lado do servidor em `backend/app/controllers/api/entities/admin_message.rb:14-42`. |
| FE-526 | migrated | Payloads JSON de mensagem | verified | `backend/app/controllers/api/entities/admin_message.rb:14-42` — o payload que a tela consome em `frontend/src/app/pages/MessagesPage.tsx:246-274`: `sender_name`, `sender_email`, `context` + `context_label`, `state` + `state_label`, `message`, `created_at`, `notes_count`, `is_read`, `is_favorite` e os pares `extra1_*`/`extra2_*`. **A cor não viaja**: o servidor manda a chave e a tela escolhe a variante do `Badge` (`:265-266`, `VARIANTE_DE_CONTEXTO`/`VARIANTE_DE_SITUACAO`), contra o hex que `context.rb:41-47` devolvia. Teste: `backend/spec/requests/api/v1/admin_messages_spec.rb`. |
| FE-527 | migrated | Telas do SFG que enviam mensagem | verified | `frontend/src/app/pages/MessagesPage.tsx:196-201` (botão "Nova mensagem") + `:361-365` (`NovaMensagemDrawer`) — o ponto de envio aponta para o endpoint **autenticado** (DEC-40) e nome/e-mail vêm da sessão, não de campos livres como no formulário público do legado. |
| FE-528 | migrated | Console de mensagens administrativas e de observadores (SFG) | verified | `frontend/src/app/pages/MessagesPage.tsx` inteiro é o console de mensagens **e** de observadores, em duas colunas (`:205`), e **tem item de menu**: `frontend/src/app/consoleNavigation.tsx:548` (`admin_messages`, papéis `og`/`admin`). No legado a tela era órfã — o `create_console_menu` não a listava. Teste: `frontend/src/hooks/__tests__/useNavItems.test.ts:186-192` ("DS2-1: a tela de mensagens NÃO é órfã — tem item de menu"). |
| FE-530 | migrated | Template de e-mail confirm_feedback_to (HTML + texto) | blocked | **Falta a versão TEXTO.** O legado tem o par completo: `engines/mailer19/app/views/livetat/mailer19/mailing/confirm_feedback_to.html.erb` **e** `confirm_feedback_to.text.erb`. No ai9 o equivalente é `backend/app/views/message_mailer/received.html.erb` (marca Safegold correta, protocolo, contexto — o `GOAT BY POLEMK` da base saiu, U13, e o layout `backend/app/views/layouts/mailer.html.erb:24,30` usa `APP_NAME`), mas `ls backend/app/views/message_mailer/` devolve **só `.html.erb`** (`note_added` e `received`) — o mesmo vale para `observer_mailer/` (`added`, `message_received`, `removed`). O layout texto existe (`backend/app/views/layouts/mailer.text.erb`), então falta só o corpo. Consequência: cliente que recusa HTML recebe mensagem vazia. Falta também o teste: a regra "tem versão .text.erb ao lado de cada .html.erb" existe em `backend/spec/services/auth/email_service_spec.rb:39-43` mas **só varre `auth_mailer`**; não há spec de `MessageMailer`/`ObserverMailer` (`grep -rln "MessageMailer\\|ObserverMailer" backend/spec` → vazio). |
| FE-533 | migrated | Template de e-mail generic_message | verified | `backend/app/views/auth_mailer/{invite,magic_link,magic_login_code}.html.erb` sobre `backend/app/views/layouts/mailer.html.erb` — o `generic_message` do `mailer19` vira template ERB por caso, com a marca vindo de `ENV.fetch('APP_NAME','Safegold')` (`layouts/mailer.html.erb:24,30`). Teste: `backend/spec/services/auth/email_service_spec.rb:33-37` ("não monta HTML dentro do Ruby") + `backend/spec/jobs/logged_mail_delivery_job_spec.rb`. ⚠ Nota: cor literal (`#2D2D2A`, `#63635C`) nos templates é correta e deliberada — cliente de e-mail não lê custom property; o critério de tokens vale para as telas, não para o e-mail. |
| FE-534 | migrated | Template de e-mail generic_message_with_link | verified | `backend/app/views/auth_mailer/*.text.erb` — **versão texto ao lado de cada HTML**: `invite.text.erb`, `magic_link.text.erb`, `magic_login_code.text.erb`, sobre `backend/app/views/layouts/mailer.text.erb`. Teste: `backend/spec/services/auth/email_service_spec.rb:39-43` ("tem versão .text.erb ao lado de cada .html.erb", varrendo os templates de `auth_mailer`). |
| FE-535 | migrated | Template de e-mail do SFG send_welcome_email_to_new_generic_user (credenciais de acesso) | verified | `backend/app/views/auth_mailer/invite.html.erb:11-27` (+ `invite.text.erb`) — o convite leva **magic link de primeiro acesso** (`@magic_url`, com o link também em texto puro para quem não clica) e **nenhuma senha**: `:22-27` diz explicitamente "Sem senha: o Safegold entra por código enviado ao seu e-mail ou WhatsApp" (D-38 / DEC-14). Teste: `backend/spec/services/auth/email_service_spec.rb:49-61` ("envia o convite com o magic link" / "recusa convidar quem não tem e-mail"). |
| FE-536 | migrated | Template de e-mail do SFG send_email_to_recovery_password_user (instruções de recuperação) | verified | `backend/app/views/auth_mailer/magic_login_code.html.erb:5-14` e `magic_login_code.text.erb:1-13` — código de acesso, prazo de 5 minutos, aviso de não compartilhar e "se você não pediu, ignore". O `<title>` correto vem do layout (`backend/app/views/layouts/mailer.html.erb:30`, `<title><%= app_name %></title>`). Teste: `backend/spec/services/auth/email_service_spec.rb` + `backend/spec/jobs/logged_mail_delivery_job_spec.rb`. |
| FE-538 | migrated | Biblioteca de componentes JS/CSS do kit (insumo direto para a lib de componentes do ai9) | verified | O kit do `ux_kit19` virou a biblioteca do ai9: **43** componentes em `frontend/src/components/ui/*.tsx` e **4** módulos em `frontend/src/lib/utils/*.ts`. **Nada foi para `window`**: `grep -rn "^\s*window\.[A-Za-z_]* =\\|globalThis\.[A-Za-z_]* =" frontend/src` (excluindo `location`/`scroll`) → **vazio**, contra os globais que `app/frontend/js/index.js.erb:31-38` publicava. Prova de conjunto: `npx vitest run` — **58 arquivos / 506 testes, 0 falhas** (executado em 27/08/2026). |
| FE-740 | migrated | ContextToolbar / Toolbar / SearchBar — a barra de contexto do console (topo): título, botão voltar, loader, mensagens, busca… | verified | `frontend/src/components/PageHeader.tsx:23-108` — título (`:91`), subtítulo (`:92`), `rightSlot` para ações (`:97`), `searchSlot` para a busca da tela (`:100`), estado de carregamento (`:104`) e `sticky` opcional (`:75`). As 751 linhas de `toolbars.js` viram este componente mais o `SearchInput` (IMP-A72). Adoção medida: **38 arquivos** importam `components/PageHeader`. ⚠ **Sem teste próprio** — a prova é o uso e a suíte verde. |
| FE-741 | migrated | SimpleMenu — widget de menu com item selecionado derivado da URL corrente | verified | `frontend/src/components/Sidebar.tsx:94-146` (`ItemLink`) e `:318-331` — o item selecionado vem do `NavLink` do react-router (`isActive`), não de uma derivação da URL feita à mão como no `simple_menu.js` do legado. Teste: `frontend/src/components/__tests__/logout.test.tsx` (renderiza a sidebar) + `frontend/src/hooks/__tests__/useNavItems.test.ts:141-148` ("todo item visível tem página montada — menu nunca leva a 404"). |
| FE-742 | migrated | Helpers JS globais do app — scriptLoader, toastAlert, countChar, getExtendedMonth, getExtendedWeekday, extendedDate, brazili… | verified | De-para conferido função a função contra `app/frontend/js/helpers.js` do legado: `scriptLoader` (`:1`) → `lazy()`/import do Vite (`frontend/src/app/App.tsx:50-66`), sem carregador dinâmico próprio; `getExtendedMonth` (`:62`) → `nomeDoMes` (`frontend/src/lib/utils/date.ts:79`); `getExtendedWeekday` (`:71`) → `nomeDoDiaDaSemana` (`date.ts:87`); `extendedDate` (`:83`) → `formatDateLong` (`date.ts:71`); `brazilianDate` (`:101`) → `formatDate` + `FORMATO_DATA` (`date.ts:17,59`); `countChar` (`:53`) → contador na tela que precisa (`MessagesPage.tsx:555`); `toastAlert` (`:43`) → `frontend/src/lib/notify.ts`. Endereço em `lib/utils/address.ts:23,44`. Teste: `frontend/src/lib/utils/__tests__/transversais.test.ts` (22 exemplos) + `frontend/src/components/__tests__/ProtectedRoute.test.tsx`. ⚠ O defeito de consumo do `notify` está registrado na **FE-410** (blocked) — não é duplicado aqui. |
| FE-743 | migrated | Componente proprietário Dialog — modal de confirmação/erro/aviso com título, mensagem, tipo e botões | verified | `frontend/src/components/ui/dialog.tsx:1-8` — o `vendor/dialog` proprietário do legado não veio (DEC-10): o modal é o `Dialog` do **Radix** (`@radix-ui/react-dialog`), com título, descrição, rodapé e fechamento por `Escape`/overlay de graça. O caso de confirmação/erro/aviso com tipo e botões é o `frontend/src/components/ui/ConfirmDialog.tsx` (`tone: 'destructive' \| 'primary'`, `canConfirm`, `blockedReason`), montado sobre ele. Teste: `frontend/src/app/pages/renegotiations/__tests__/renegotiations.test.tsx:88-152` (3 exemplos sobre o `ConfirmDialog`). |
| FE-744 | migrated | Componente proprietário Doughnut — gráfico de rosca com séries, legenda, cores default, valor central clicável e eventos de… | verified | `frontend/src/components/charts/RechartsPie.tsx:4-112` — rosca (`variant: 'donut'` abre o miolo e mostra o total no centro, `:37-39`), legenda (`:112`), séries com `Cell` e cores de `vibrantPalette()` (`frontend/src/components/charts/theme.ts:33`), que é **token** — muda sozinha entre claro e escuro (`RechartsPie.tsx:27`). ⚠ **Prova que precisa sobreviver, e confirmada aqui**: o legado **não instanciava** o `Doughnut` em view nenhuma — isto é o componente ficando disponível, não uma tela portada (OPS-746 / NEW-001). Sem teste próprio; a suíte do front está verde. |
| FE-745 | migrated | Locale pt-BR do datepicker (air-datepicker): nomes de dias/meses, "Hoje"/"Limpar", dateFormat dd/mm/yyyy, timeFormat hh:ii,… | blocked | Quatro dos cinco itens do locale estão prontos: **dd/mm/aaaa** (`frontend/src/components/ui/DatePicker.tsx:52` placeholder + `frontend/src/lib/utils/date.ts:17` `FORMATO_DATA = 'dd/MM/yyyy'`), **nomes de meses/dias em pt-BR** (`frontend/src/components/ui/Calendar.tsx:16,106,168` com `locale: ptBR` do `date-fns`), **semana começando no domingo** (`Calendar.tsx:21,61` — `DIAS = ['D','S','T','Q','Q','S','S']`) e **"Limpar"** (`DatePicker.tsx:122`, `aria-label="Limpar data"`). **Falta o atalho "Hoje"**, que o mapa nomeia explicitamente (`.migration-ai9/map/auth-admin.md:403`: *"O locale pt-BR do air-datepicker (dd/mm/yyyy, semana no domingo, «Hoje»/«Limpar») vira configuração do componente novo"*) e que o legado tem em `app/frontend/vendor/js/datepicker_overrides.js`: `grep -rn "Hoje" frontend/src/components/ui/` → **vazio**, e `Calendar.tsx` não tem rodapé de atalhos (`:196-199` fecha a grade e o componente). Falta: um botão "Hoje" no rodapé do `Calendar`, respeitando `min`/`max` quando o campo os declara. |
