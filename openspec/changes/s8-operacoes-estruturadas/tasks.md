# Tasks: S8 — Operações estruturadas e remuneração

Fila resumível do Phase 3. **Uma tarefa = um comportamento verificável.** Cada tarefa cita
os IDs que fecha, para o `parity-ledger.md`. Ordem: **dados → backend → frontend → testes →
paridade**, com a trava do `design.md` §7: **o golden nasce junto com a fórmula**, e
`E1`…`E5` estão verdes antes de qualquer tela exibir prévia de recibo **e** antes de
`DB-296` mexer na precisão das colunas.

**Pré-requisitos duros:** S5 (o `RiskOperationType` é a classe **LIQ**), S7 (`RiskOperation`
como `operation_class`, `available_for_receipt`) e o handshake com `charges`/`receipts` — a
tabela `receipts` e o `Charge#calc!` são do bloco vizinho.

Verificação: `cd backend && bundle exec rspec` (comparar com a **lista** de falhas do
baseline) · `cd frontend && node node_modules/typescript/bin/tsc --noEmit` (baseline
**0 erro**) · `vitest` não roda neste ambiente.

Contratos: **C1** (escopo no endpoint), **C2** (serviço único), **C3** (hierarquia
invertida, testar **os dois lados**). Convenções: `ai9-conventions.md` §3.6, §4, §6.

> ## Estado final: **112 de 112 marcadas** (26/08/2026)
>
> A última era a **13.5**, aberta por ser **impossível como escrita**: os goldens `E1`…`E7`
> não tinham contra o que ser conferidos, porque as migrations desta família **nunca subiram
> em produção** (DEC-103b). A **DEC-115** a **reescreveu** — conferência contra a **FONTE de
> 2022** — depois de o usuário confirmar que não existe outra base. Era o **mesmo** problema
> da **S5/12.4** e da **S7/11.4**, e as três se resolveram com a mesma resposta, no mesmo dia.
>
> A auditoria de QA que fechou a 13.5 achou um buraco real: os arquivos desta fatia diziam
> `⚠ FONTE, não ORÁCULO`, mas **nenhum** carregava a marca literal `NUNCA EXECUTADO EM
> PRODUÇÃO` que a DEC-103b tornou padrão, e os dois goldens `E6` e o `E7` da factory não
> tinham marca nenhuma. Corrigido nos seis arquivos.
>
> **O razão está fechado:** 78 IDs `migrated` (74 `build` + 4 `adapt`), cada um com alvo,
> teste e nota; 20 `dropped`, cada um com evidência escrita. **Zero `pending`, zero
> `blocked`, zero `build?` sem resolução.** Nenhum ID desta fatia está `verified`, e isso é
> deliberado: pela régua desta migração `verified` exige comparação com **dado de produção**,
> e para esta família ele não existe.
>
> **Esta fatia desbloqueou 3 tarefas da S6** — `2.35`, `2.36` e `4.27` (`BE-188` e `BE-189`,
> que estavam `blocked`). Fechar o golden do lote revelou um **500 real** no "desmarcar
> recibo" que vivia escondido desde a S6 porque o caminho era inalcançável sem
> `Remuneration`.

---

## Bloco 0 — Decisões que destravam trabalho (não produzem código)

- [x] 0.1 **T-D6 — Q-R3: o `balance` da operação estruturada deveria evoluir?** Levar ao usuário com a evidência: a cópia roda em **toda** gravação (até editando só a observação) e **nada no legado inteiro** dá baixa nesse saldo — não existe movimento, liquidação nem baixa de operação estruturada. Ou falta uma feature inteira, ou a coluna é decorativa. Default: **replicar o reset e documentar a coluna como decorativa** — fecha a dúvida de `BE-292` — **decidido: replicar, e documentar a coluna como decorativa.** A evidência foi levada e é a que a tarefa previa: o reset roda em `before_validation` **sem `on:`**, logo em toda gravação (golden `E6` prova com "editar só a observação"), e a varredura não achou nenhum caminho de movimento, liquidação ou baixa de operação estruturada — diferente de `RiskOperation`, que tem `RiskMovement`. `BE-292` fechado. Há exemplo de spec afirmando a **ausência** (`spec/models/structured_operation_spec.rb`, "nada dá baixa nesse saldo"), para que criar o caminho quebre o teste e traga a T-D6 de volta à mesa em vez de ser contornada.
- [x] 0.2 **T-D7 — Q-R5: `resource_kinds` é portado ou descartado?** Pedir `SELECT COUNT(*) FROM receivable_entries WHERE resource_kind_id IS NOT NULL` no dump. **Zero → `BE-307`, `BE-720`…`BE-724`, `FE-307` e `DB-289` viram `dropped` com evidência** e a tabela é removida numa tarefa explícita; maior que zero → a superfície é construída como o mapa descreve. **Não descartar sem o número** — fecha 8 dúvidas de uma vez — **portão ABERTO com o número, e o número foi zero.** `resource_kinds` tem **0 linhas** e **0 de 28.131** `receivable_entries` têm `resource_kind_id` (`analise-dump-producao.md` §2, consulta 2). Os 10 IDs estão `dropped` **com a evidência** no `parity-ledger.md`. A tabela nunca foi criada no ai9 (a S6 já a deixara fora), então 13.4 se cumpre por não-criação, registrada — não por omissão.
- [x] 0.3 **T-D8 — Q-R17: a remuneração é mesmo percentual flat sobre o capital, sem prazo?** É **dinheiro cobrado do cliente** e o modelo guarda `issue_date`, `due_date` e `agreed_rate`, o que **sugere** um pro-rata que não existe. Default: replicar a fórmula exatamente, com golden — **nada muda no valor cobrado** — confirma `BE-305` — **confirmado: percentual FLAT, sem prazo e sem pro-rata.** `BE-305` fechado com golden. A suspeita da tarefa era razoável (o modelo guarda `issue_date`, `due_date` e `agreed_rate`) e é **falsa**: há exemplo provando que operação de 30 e de 360 dias com o mesmo capital dão o **mesmo** valor, e outro provando que mudar `agreed_rate` de 0,01 para 99,99 **não move** o resultado. Nada muda no valor cobrado.
- [x] 0.4 **T-D5 (compartilhada com S7) — Q-R10: as datas de operação existente são editáveis?** Default: **regra de servidor** nos dois módulos irmãos — fecha a dúvida de `FE-297` e alinha com `FE-260` — **decidido: regra de SERVIDOR nos dois módulos irmãos.** Em `structured_operations`, `issue_date` e `due_date` **não são declaradas** no `params do` do `PUT` — o servidor não as aceita, e há spec provando que enviar datas de 2020 devolve 200 com as datas originais intactas. Alinha com `FE-260` (S7). `FE-297` fica para o agente de frontend, com a regra já valendo por baixo.
- [x] 0.5 **T-D9 — Q-R16: a taxa de remuneração aceita valor fora de 0–100?** Hoje `250%` passa na UI e a API aceita ainda mais casas. É a taxa que multiplica **todo** o faturamento. Default: **replicar** (validar a faixa recusaria registro que o sistema aceita hoje) — **decidido: replicar, sem validar a faixa.** `250%` passa, `-5%` passa, e há spec afirmando isso nos dois níveis (model e endpoint). Validar recusaria registro que o sistema aceita hoje, e a faixa é decisão de negócio (DEC-37), não da migração. ⚠ **P-026 perdeu o objeto**: a tabela `remunerations` não existe em produção, então não há dado contra o qual medir "existe taxa fora de 0–100".
- [x] 0.6 Registrar em `.migration-ai9/improvements-log.md` as mudanças visíveis **IMP-R2** (as listas passam a paginar de verdade; a tela deixa de trazer tudo de uma vez) e **IMP-R4** (operação com data nula passa a aparecer quando não há filtro de período — hoje o `DATE(NULL)` a exclui em silêncio) — **registrado** em `.migration-ai9/improvements-log.md`: **IMP-R2** como `EST-S8-05` (as três listas da unidade passam a paginar de verdade, cada uma quebrada de um jeito diferente) e **IMP-R4** como `EST-S8-06` (operação com data nula passa a aparecer sem filtro de período; a sentinela `DateTime.dinosaurs`/`mars` morre). Mais 12 entradas que a fatia produziu, entre elas **`EST-S8-01`**, o 500 no "desmarcar recibo".
- [x] 0.7 Transcrever para `.migration-ai9/upstream-flags.md`, se ainda não estiverem lá: `pg_search` sem uso; `paper_trail` no Gemfile sem uso — **conferido: os dois JÁ ESTAVAM lá** (linha 4 da tabela do topo e seção `#S7-1`). O que a S8 acrescentou é a **correção de metade do registro**, em `#S8-1`: `pg_search` continua com **0** usos e fica como opção de plataforma, mas **`paper_trail` não está mais "sem uso"** — a DEC-59/DEC-78 e a S19 montaram `Sfg::AuditTrail` entre o desenho e a execução. Seguir o `design.md` §6 ao pé da letra hoje deixaria a operação estruturada fora da trilha financeira que `RiskOperation`, `Receipt` e `Charge` já têm.

## Bloco 1 — Dados: as 5 tabelas da unidade nascem com integridade

- [x] 1.1 Migration `structured_operations` com índices em `project_id`, `company_id`, `carrier_id`, `operation_type_id`, `receipt_id` e composto (`project_id`, `issue_date`, `due_date`) — o `search` sempre faz 3 INNER JOINs + range de datas; `observation` varchar(255) → `text`; `agreed_rate` float → `decimal(9,4)`; `is_on_variable`/`is_ended` → boolean; **`original_balance` migra com o sinal negativo preservado** — fecha `DB-280` — **feito** em `20260826230000_operacoes_estruturadas_e_remuneracao.rb`. Divergência deliberada: `agreed_rate` é `decimal(7,4)`, não `decimal(9,4)` — vale a 1.10, e `risk_operations` (a tabela irmã na mesma fórmula de recibo) já está nessa escala.
- [x] 1.2 `receipt_id` com índice **obrigatório** (é o campo de `available_for_receipt`, consultado em todo cálculo de candidatos) e decisão conjunta com o bloco `charges` sobre a relação circular `structured_operations.receipt_id` × `receipts.operation_id` — fecha `DB-281` — **feito**: `add_index :structured_operations, :receipt_id` + FK `on_delete: :restrict`. A relação circular fica com **os dois lados** (a coluna é o predicado de `available_for_receipt`), como o `design.md` §4 previa. Fecha também a **1.15 da S6**.
- [x] 1.3 Migration `structured_operation_types` com índice único em `title` e em `integration_key` (hoje **nenhum**, apesar do nome), os 4 `is_*` → boolean e o `where('is_active = 1 ')` literal trocado por bind — fecha `DB-283` — **feito**: únicos em `title` e `integration_key`; os 4 `is_*` boolean; o `where('is_active = 1 ')` literal some no `scope :active` do `GlobalCatalog` (bind + boolean).
- [x] 1.4 Migration `remunerations` com índice em `project_id` e **único composto** (`project_id`, `operation_type_type`, `operation_type_id`) — hoje a unicidade só existe em `validates_uniqueness_of`, sujeita a corrida — e FK/`dependent:` para `receipts` — fecha `DB-284` — **feito**: `index_remunerations_on_project_and_type` único em (`project_id`, `operation_type_type`, `operation_type_id`), mais `add_foreign_key :receipts, :remunerations` com `on_delete: :restrict` e `has_many :receipts, dependent: :restrict_with_error` no model.
- [x] 1.5 `remunerations.title` **continua desnormalizado**, reescrito a cada save, porque é a coluna que a busca textual usa (decisão **B-06**) — fecha `DB-285` — **feito**: a coluna existe, é `null: false` e é reescrita por `before_validation :copy_title_from_operation_type` em todo save.
- [x] 1.6 Migration `resource_sources` com índice único em `title` e `legacy_id` com índice único, mapeando `durecliq.url_id` do banco Django anterior. **As fontes já são dados migrados de um sistema anterior**: preservar `legacy_id` é obrigatório — fecha `DB-287` e `DB-288` — **feito na S6** e conferido aqui: `20260826220000_catalogos_de_recebivel.rb:124-136` cria `resource_sources` com único em `title`, em `integration_key` e em `legacy_id`. A tabela é da S8 e nasceu lá por dependência de FK — **não recriar**.
- [x] ~~1.7 Migration `resource_kinds` (tabela e colunas), **mesmo com a superfície bloqueada** por T-D7: preservar dado é barato, perder é irreversível — fecha `DB-286`~~ — **NÃO EXECUTADA, por decisão medida.** O portão **T-D7** abriu com o número: no dump de produção `resource_kinds` tem **0 linhas** e **0 de 28.131** `receivable_entries` têm `resource_kind_id` (`analise-dump-producao.md` §2, consulta 2). O argumento do `design.md` §5 ("dado é irreversível") **perde o objeto quando não há dado**: criar a tabela preservaria zero linha. Os 10 IDs já estão `dropped` **com a evidência** no `parity-ledger.md` (`BE-307`, `BE-720`…`BE-724`, `FE-307`, `DB-286`, `DB-289`, `DB-294`). A S6 já a deixara fora de `20260826220000_catalogos_de_recebivel.rb`: **não há tabela a remover** — ela nunca foi criada no ai9 (é a tarefa 13.4 cumprida por não-criação, registrada aqui para que ninguém a crie por engano depois).
- [x] 1.8 FKs reais com `ON DELETE RESTRICT` nas 5 migrations da unidade — **nenhuma das 5 legadas declara `foreign_key: true`**. Somem dois estados ilegíveis: recibo órfão e operação com carrier apagado sumindo pelo INNER JOIN — fecha `DB-282` — **feito**: as 3 tabelas novas nascem com FK real `ON DELETE RESTRICT` (projects, companies, carriers, structured_operation_types, users×2, receipts) + `receipts → remunerations`. `remunerations.operation_type_id` é polimórfico e por isso tem `check_constraint` no lugar de FK.
- [x] 1.9 Os 9 flags integer → boolean com mapeamento **`≠ 0 → true`** e um dry-run que **lista as ocorrências de valores `2+` antes de converter** — fecha `DB-295` — **feito, e o dry-run tem resposta MEDIDA: não há uma linha para converter.** Os 9 flags (`is_active`, `is_default`, `allow_manual_operations`, `allow_receivable_entries`, `has_pre_faturamento` em `structured_operation_types`; `is_on_variable`, `is_ended` em `structured_operations`; e os de `resource_kinds`) nascem `boolean` com o mapeamento `≠ 0 → true` embutido no default de cada coluna. **A varredura de valores `2+` devolveu zero ocorrências porque as três tabelas NÃO EXISTEM no dump de produção** (DEC-103b — as migrations nunca subiram) e `resource_kinds` foi descartada pelo portão T-D7. Não é conversão pulada: é conversão sem objeto, e o motivo fica escrito para o ETL da S14 não procurar dado que não há.
- [x] 1.10 Padronizar precisão: valores em `decimal(14,2)` e taxas em `decimal(7,4)`, **mantendo a sequência de operações de `BE-305` idêntica**. Só depois dos goldens `E1`…`E5` verdes. `Charge#calc!` soma esses valores — mudança de precisão afeta cobrança — fecha `DB-296` — **feito, e nesta ordem**: os goldens `E1`…`E5` estão verdes (`spec/services/structured/remuneration_calculator_spec.rb`, 25 exemplos, 0 falhas) e só então a precisão foi fixada. Valores em `decimal(14,2)`, taxas em `decimal(7,4)`, a mesma escala de `risk_operations` e de `receipts.fee`. A sequência de `BE-305` é idêntica: `operation_value × (fee.to_f / 100.0)`, `decimal × Float`, com o artefato do float do `E5` preservado.
- [x] 1.11 `created_by_id`/`updated_by_id` com FK real, preenchidos **no servidor**. Hoje só `StructuredOperation` declara `belongs_to :user`, e nos outros três models `user_id` vem de `hidden_field` e **nunca é verificado** — é forjável. Em `StructuredOperation` o legado força `current_user.id` no create **e no update** (o "autor" vira o último editor): o ai9 **separa os dois**, preservando o último editor e acrescentando o autor — fecha `DB-297` — **feito nas duas metades.** Esquema: `structured_operations.user_id` (AUTOR) e `updated_by_id` (ÚLTIMO EDITOR) com FK real `ON DELETE RESTRICT` para `users`. Servidor: `Structured::OperationService` carimba o autor no create (via `ProjectScopedService`) e o editor em **todo** save, os dois vindos da SESSÃO — nenhum é declarado no `params do`. Spec prova que outro usuário editando muda `updated_by_id` e **não** muda `user_id`, que é a informação que o legado perdia no primeiro save de outra pessoa.
- [x] 1.12 Contrato de dados com `charges`/`receipts`: índice único obrigatório em (`operation_id`, `project_id`, `operation_type`) — hoje a unicidade é só de aplicação; `fee` float → decimal; registros anteriores podem ter `date`/`operation_title` NULL. **A entidade é do bloco vizinho; a fórmula e a política de arredondamento são desta fatia** — fecha `DB-290` — **conferido e fechado**. O índice único (`operation_id`, `project_id`, `operation_type`) e o `fee decimal(7,4)` já vieram da S6 (`index_receipts_on_operation_unique`); `date`/`operation_title` são anuláveis por desenho (B-08). **A metade desta fatia — a fórmula e a política de arredondamento — está entregue**: `ROUND_HALF_UP`, 2 casas, em `Charges::ReceiptGenerator::ROUNDING_SCALE/ROUNDING_MODE`, com golden dos dois lados.

## Bloco 2 — Dados: seeds

- [x] 2.1 Seed `structured_operation_types` — Fomento, Comissária, Intercompany, Auto Liquidável, **todos `is_default`** (logo nenhum removível pela UI), `has_pre_faturamento = false` e `allow_receivable_entries = false` em todos. **As 4 `integration_key` são contrato** (integrações externas podem depender delas). Some o `user_id: 11` hardcoded inconsistente — fecha `DB-292` — **feito e EXECUTADO**: `Seeds::Reference::StructuredOperationTypes`, plugado no `Runner`. Rodado duas vezes: `4 criados` → `4 inalterados`, `total=4`. Os 4 são `is_default` (logo nenhum removível), `has_pre_faturamento=false`, `allow_receivable_entries=false`, `allow_manual_operations=true` (o default da migration de 2022, que a chamada do seed legado não sobrescreve). As 4 chaves conferidas: `fomento`, `comissaria`, `intercompany`, `auto_liquidavel`. **O `user_id: 11` do quarto tipo sumiu** — autor nulo descreve a verdade: ninguém criou estas linhas.
- [x] 2.2 Seed `resource_sources` — Caixa, Comissária, Defasagem, Fomento, Garantia, Recompra, Retenção, com chaves derivadas e **idempotente por `legacy_id`/`integration_key`**: em ambiente real esses registros **coexistem com os importados do legado** e o mesmo título pode colidir com a unicidade — fecha `DB-293` — **feito na S6 e confirmado aqui, com a divergência registrada**: `Seeds::Reference::ResourceSources` semeia as **6 linhas de PRODUÇÃO** medidas no dump (Caixa, Garantia, Comissaria, Fomento, Recompra, 13º salário), **não as 7 do mapa** (não há "Defasagem" nem "Retenção"). Por **DEC-30** o dado de produção vence a lista do mapa. Idempotente por `legacy_id`, que é a chave natural — é o que faz a coexistência com os registros importados do legado não furar a unicidade de título. A chave `13?_salario` é preservada como está: `I18n.transliterate("13º salário")` produz `13? salario` e é essa a chave que está em produção; normalizá-la para `13_salario` seria trocar a forma de uma chave de integração em silêncio.
- [x] ~~2.3 Seed `resource_kinds` — as 5 linhas, incluindo "Antecipacao de Recebíveis" (`is_active: false`, `is_conta_corrente: true`, `is_unique: true`) e as outras 4 com os dois flags falsos. **É o único lugar do sistema onde esses flags recebem valores diferentes**; como nenhum código os lê, a semântica está perdida e vem do negócio. Corrigir o título sem acento **só se** T-D7 mantiver a entidade — fecha `DB-294`~~ — **NÃO SEMEADO**, pelo mesmo portão T-D7 da 1.7: sem tabela não há seed. `DB-294` já está `dropped` com a evidência no `parity-ledger.md`. A semântica dos dois flags de "Antecipacao de Recebíveis" continua perdida e agora não tem mais objeto.

## Bloco 3 — Backend: a fórmula de remuneração (E2) · **zona DEC-02**

> Cada tarefa deste bloco entrega **fórmula + golden na mesma tarefa**.

- [x] 3.1 `Structured::RemunerationCalculator.calculate(operation)` replicando a sequência exata de `receipt.rb:41-66`: recusa por `receipt_id` preenchido → copia projeto/título/data → busca a remuneração por (projeto, tipo, classe) → recusa se não houver → **congela** `kind`/`title`/`fee`/`operation_value` → **`value = operation_value × (fee.to_f / 100.0)`** → `temp_id`. **Goldens `E1`…`E5`**: 5.100,00 · 31,48 · 1.533,95 · **255,01** (fronteira) · **7.770,00** (artefato de float na 10ª casa) — fecha `BE-305` — **feito e VERDE**: `Structured::RemunerationCalculator.calculate`, fachada sem aritmética própria sobre `Charges::ReceiptGenerator` (contrato **C2**). Os 5 goldens conferidos **executando** a mesma aritmética Ruby do legado: `5100.0` · `31.48128` · `1533.9506` · `255.0051` · `7769.9992229999990000001` — o artefato do float do `E5` aparece na **décima** casa, como o `design.md` previa.
- [x] 3.2 Política de arredondamento **explícita e documentada** (`ROUND_HALF_UP`, 2 casas no valor final) que **reproduz** o resultado atual do cast. Os mesmos goldens rodam antes e depois — fecha parte de `BE-305` — **feito**: `ROUNDING_SCALE = 2` e `ROUNDING_MODE = BigDecimal::ROUND_HALF_UP`, declarados no dono da aritmética. **Reproduz** o cast: os mesmos 5 goldens passam antes e depois. Há exemplo travando `HALF_UP` contra `HALF_EVEN` (255,005 → 255,01, não 255,00), que é a diferença que 4 dos 5 goldens não veriam. Muda a **prévia**, que antes mostrava `31,48128` e gravava `31,48`.
- [x] 3.3 Os dois caminhos de erro deixam de ser exceção não tratada: operação já faturada e projeto sem remuneração para o tipo passam a **409/422 com mensagem de negócio**, não 500 — fecha `BE-294` — **feito**: operação já faturada → **409** (`ALREADY_BILLED`, conflito de estado); projeto sem remuneração para o tipo → **422** (`NO_REMUNERATION`). Há exemplo travando a **ordem** do legado: com os dois problemas ao mesmo tempo, quem recusa é o passo 1 (recibo), não o passo 4 (remuneração).
- [x] 3.4 `#candidates(project)`: para cada remuneração, `operation_class.where(project_id:, operation_type_id:, receipt_id: nil)`, **cada candidato já calculado**. Paginado (hoje percorre todas as remunerações do projeto sem paginação); `operation_class` nil deixa de dar 500. **Golden `E7`**: operação **encerrada continua candidata** (Q-R18) — fecha `BE-306` — **feito**: `candidates(project, page:, per_page:)`. Pagina com `Kaminari.paginate_array` (os candidatos vêm de **duas** tabelas — não há `ORDER BY` que atravesse as duas); `page: nil` devolve tudo, que é o que a gravação em lote precisa. `operation_class` nil pula a linha em vez de dar 500 — e o `check_constraint` de `DB-284` torna o cenário inalcançável pelo banco. **Golden `E7` verde**: operação encerrada continua candidata.
- [x] 3.5 `spec/services/structured/remuneration_calculator_spec.rb` reunindo `E1`…`E5` e `E7`. **Hoje há zero cobertura** para `StructuredOperation`, `StructuredOperationType`, `Remuneration`, `ResourceKind`, `ResourceSource` e `Receipt` — este é o primeiro teste da unidade — fecha `OPS-287` — **feito**: `spec/services/structured/remuneration_calculator_spec.rb`, **25 exemplos, 0 falhas**. Cobre `E1`…`E5`, `E7`, a política de arredondamento, os dois erros de negócio, o `temp_id` dos **dois** lados (EST e LIQ — contrato **C3**) e a ausência de pro-rata (T-D8). Era a primeira cobertura da unidade inteira.

## Bloco 4 — Backend: remunerações (E2)

- [x] 4.1 `Remuneration` `before_validation`: `title = operation_type.title` em todo save; `operation_class` → `RiskOperation`/`StructuredOperation`; `beauty_type` → `"LIQ"`/`"EST"`. Tipo inválido devolve **422**, não 500 (hoje `operation_type.title` em `nil`). A sigla é **persistida no recibo** e usada por `Charge#calc!` para separar totais — fecha `BE-304` — **feito**: `before_validation :copy_title_from_operation_type` em todo save; `operation_class`/`beauty_type` como tabela, sem o ramo `else`; tipo inválido → **422** por `inclusion` + `check_constraint`. ⚠ **Achado executando** (`EST-S8-03`): ler `operation_type` com classe desconhecida levantava `NameError` **dentro do callback**, antes de a validação falar — 500 pela porta dos fundos. Corrigido com `rescue NameError`.
- [x] 4.2 `Structured::RemunerationService#create`: unicidade `operation_type_id` no escopo (`operation_type_type`, `project_id`) — é o que garante que `Receipt#fetch` ache **uma** taxa; `project_id` **forçado ao projeto corrente** (hoje vem de campo hidden — falha de segurança); validação de **inclusão** de `operation_type_type` (hoje valor arbitrário é aceito e depois quebra em `operation_class` nil e `beauty_type` `"???"`). Faixa de `value` **não** validada (T-D9) — fecha `BE-301` — **feito**: `Structured::RemunerationService < ProjectScopedService`. `project_id` **forçado** ao projeto corrente e nem declarado no `params do`; `operation_type_type` validado por `values:` no Grape + `inclusion` no model + `check_constraint` no banco; unicidade (projeto, classe, tipo) nos **dois** níveis, com spec provando que o índice recusa mesmo contornando a validação. Faixa de `value` **não** validada (T-D9).
- [x] 4.3 `#update`: busca **com** escopo de projeto; `title` recalculado a partir do tipo em todo save; trocar o tipo **não** recalcula recibos já emitidos (o recibo congela `fee`/`title`/`kind`) — replicar, o histórico fica correto. A imutabilidade que o formulário promete passa a valer no servidor — fecha `BE-302` — **feito**: busca **com** escopo (id alheio → 404); `title` recalculado do tipo em todo save. Spec prova que **trocar o tipo não recalcula recibo já emitido** — o recibo congela `fee`/`title`/`kind`, e o histórico fica correto sozinho.
- [x] 4.4 `#destroy` com `restrict_with_error`: hoje `has_many :receipts` **sem `dependent:`** deixa **recibos órfãos**, e como `Receipt belongs_to :remuneration` é obrigatório, qualquer save posterior desses recibos falha. Passa a 422 — fecha `BE-303` — **feito**: `has_many :receipts, dependent: :restrict_with_error` + FK `receipts → remunerations` `ON DELETE RESTRICT`. **422** com o vínculo nomeado, provado no model e no endpoint.
- [x] 4.5 `#index`: paginação, ordenação e total (hoje **não há nenhum dos três** — a lista volta inteira sem `ORDER BY`), mais o filtro por `operation_type_type` (hoje LIQ e EST vêm juntos, sem filtro) e busca textual em `title` — fecha `BE-300` — **feito**: paginação (Kaminari + `X-Total-Count` real), ordenação com allowlist (chave desconhecida → **400**), filtro por `operation_type_type` (LIQ/EST vinham juntas, sem filtro) e busca textual em `title`. O legado não tinha **nenhum** dos três primeiros.

## Bloco 5 — Backend: operações estruturadas (E1)

- [x] 5.1 `StructuredOperation` `before_validation on: :create`: `title` em branco recebe `carrier.title`; `carrier_id` inválido devolve **422** (hoje `NoMethodError` antes da validação de presença, e o erro amigável nunca aparece) — fecha `BE-290` — **feito**: `title` em branco recebe `carrier.title` **só no create** (spec prova que renomear o portador depois não mexe); portador ausente devolve **erro de validação**, não `NoMethodError`.
- [x] 5.2 `project_id` **sempre** derivado da empresa, em todo save, sobrescrevendo o form — **replicar o mecanismo**. Mas trocar a empresa **move a operação de projeto em silêncio** e pode invalidar remuneração/recibo emitidos: passa a exigir **confirmação explícita** (operação nomeada, não efeito colateral) — fecha `BE-291` — **feito**: `project_id` derivado da empresa em todo save (mecanismo replicado), **mais** a confirmação explícita: empresa de outro projeto exige `confirm_project_change: true` e devolve 422 com `code: PROJECT_CHANGE_REQUIRES_CONFIRMATION`. Deixa de ser efeito colateral de trocar um select.
- [x] 5.3 `before_validation`: `original_balance = -(|valor|)` e, em seguida, `balance = original_balance` **sem `on:` restrito** — qualquer update de qualquer campo reseta o saldo corrente. **Golden `E6`**: editar só a observação faz o `balance` voltar a −50.000,00. **Replicar** (T-D6) — fecha `BE-292` — **feito e travado pelo golden `E6`**, nos dois níveis: model (`spec/models/structured_operation_spec.rb`) e endpoint (editar só a observação faz o `balance` voltar a −50.000,00).
- [x] 5.4 `validates`: obrigatórios company, project, carrier, operation_type, user, issue_date, operation_value, due_date. **Replicar as ausências** — sem `due_date >= issue_date`, sem `operation_value > 0`, sem faixa de `agreed_rate`, sem unicidade de `contract_number` (Q-R7) — fecha `BE-293` — **feito**, e as **ausências** têm spec próprio: sem `due_date >= issue_date`, sem `operation_value > 0`, sem faixa de `agreed_rate`, sem unicidade de `contract_number`. ⚠ Achado no caminho: `validates :operation_value, presence: true` é **dead code** — a coluna nasce `default: 0` na migration de 2022, então nunca dispara. Replicados os dois lados, com o exemplo que registra a inutilidade (`EST-S8-13`).
- [x] 5.5 `scope :available_for_receipt` (`receipt_id: nil`), escrito pelo `after_create` de `Receipt` e limpo no `after_destroy` — fecha parte de `BE-294` — **feito e provado ida e volta**: entrar no lote grava `receipt_id`, sair do lote **limpa**. ⚠ **Foi aqui que apareceu o 500** (`EST-S8-01`): `BulkReceiptsService` destruía o recibo antes de soltar a operação, e a FK real recusava. O lado LIQ estava quebrado desde a S6 e ninguém podia saber — sem `Remuneration`, o caminho era inalcançável. Ordem invertida, na mesma transação.
- [x] 5.6 `is_ended`, `is_on_variable` e `agreed_rate` persistidos e exibidos, **sem inventar consumidor** (varredura confirma que nenhum cálculo, relatório, filtro ou job os lê; em especial **`agreed_rate` NÃO é a taxa que remunera**). Teste que documenta a ausência de consumidor (Q-R14) — fecha `BE-295` — **feito**, com o teste que documenta a **ausência de consumidor**: nenhuma das três aparece no SQL de `available_for_receipt`, `search` ou `in_period`, e o calculador prova que `agreed_rate` de 0,01 a 99,99 **não move** o valor faturado. Q-R14 fechada por medição.
- [x] 5.7 `Structured::OperationService#index` + `EP/structured_operations.rb`: escopo por `current_project!`; os três joins continuam **INNER** (com as FKs de `DB-282`, "carrier apagado" deixa de ser possível). **Corrige a IDOR**: `structured_operation_id` filtra **dentro** do escopo, nunca substitui a relation — fecha `BE-280` — **feito**: `Structured::OperationService#index` com `current_project!`, os três joins **INNER** (seguros agora que as FKs de `DB-282` impedem carrier apagado) e `includes` para matar o N+1 da serialização. **IDOR corrigida**: `structured_operation_id` filtra **dentro** do escopo — spec prova que id de outro projeto devolve lista vazia.
- [x] 5.8 Busca `q` em `carriers.title` **ou** `structured_operations.title`, sempre "contém". **Replicar o alcance**: não busca por `contract_number` nem por empresa, embora ambos apareçam na tabela (Q-R12) — fecha `BE-281` — **feito**, com o alcance **e a limitação** replicados (Q-R12): busca em `carriers.title` OU `structured_operations.title`, e há spec provando que **não** busca por `contract_number` nem por empresa. O termo entra por bind e escapado — `100%` é texto literal, não curinga (OPS-056).
- [x] 5.9 `#in_period`: `DATE(due_date) >= DATE(from) AND DATE(issue_date) <= DATE(to)` — a operação aparece se o período **intersecta** o intervalo dela (é a mesma regra de `BE-242`, e a coincidência é a evidência de que não é typo). Sem `from`/`to`, o predicado **não é aplicado**; data malformada → **400** — fecha `BE-282` — **feito**: `DATE(due_date) >= DATE(from) AND DATE(issue_date) <= DATE(to)`, com spec provando a **intersecção**. Sem `from`/`to` o predicado **não é aplicado**; data malformada → **400** (o `rescue_from Grape::Exceptions::ValidationErrors` da base).
- [x] 5.10 Allowlist de ordenação (title, operation_type, carrier, contract_number, issue_date, operation_value, balance, due_date, agreed_rate) validada no `params do … end` do Grape, com **400** para chave desconhecida. A chave `company` **sai** (decisão **B-13**: não há coluna "Empresa" na tela). Ordenação e paginação passam a ser **sempre** aplicadas — fecha `BE-283` e `OPS-288` — **feito**: allowlist validada no `params do` do Grape com `values: ORDERING.allowed.keys`, chave desconhecida → **400**. A chave `company` **saiu** (B-13) e há spec provando que ela agora responde 400. Ordenação e paginação sempre aplicadas.
- [x] 5.11 `X-Total-Count` com o total **real** (hoje é calculado **depois** do `limit!/offset!` e devolve no máximo o tamanho da página) e teto de `per_page` (hoje `l=999999` é aceito). Defaults `l=1000`, `o=0` — fecha `BE-284` — **feito por REUSO**, não por código novo: o `paginate` do `ControllerHelpers` já emite `X-Total-Count` do `total_count` **sem limite**, e `normalize_per_page` já tem teto. Spec prova os dois: 5 registros com `per_page=2` devolvem 2 linhas e `X-Total-Count: 5`, e `per_page=999999` volta limitado.
- [x] 5.12 `#create`: `user_id` sobrescrito por `current_user`, resposta JSON, e **`:id` fora do permit** (permitir `id` em create é a mesma família de mass assignment do D-60/D-68) — fecha `BE-285` — **feito**: `user_id` vem da sessão e **`:id` está fora do permit**. Spec envia `id` e `user_id` forjados no corpo e prova que os dois são ignorados.
- [x] 5.13 `#update`: um save (hoje são três, reexecutando os callbacks 3×) e busca **com escopo de projeto** — hoje dá para editar operação de outro projeto sabendo o id — fecha `BE-286` — **feito**: **um** save (o legado fazia três seguidos, reexecutando os callbacks 3×) e busca **com** escopo — spec prova que `PUT` em operação de outro projeto responde 404 e não altera o registro alheio.
- [x] 5.14 `#destroy` com `has_one :receipt, dependent: :restrict_with_error` e **422 de verdade**: hoje o ternário degenerado faz a exclusão bloqueada responder **200**, o front recarrega a lista e a operação continua lá — fecha `BE-287` — **feito**: 422 **de verdade** (o ternário `errors.any? ? :ok : :ok` respondia 200), com spec provando que a operação **continua no banco** depois da recusa.
- [x] 5.15 Predicado de período sem sentinela: fim do monkey-patch `DateTime.dinosaurs`/`mars` (±2000 anos) enviado como bound. Efeito idêntico para dados com data preenchida; **operação com data nula passa a aparecer** quando não há filtro — **IMP-R4** — fecha `OPS-283` — **feito**: `in_period` sem sentinela. `IMP-R4` registrado, com spec dos dois casos — operação de data nula **aparece** sem filtro e **continua fora** com filtro.
- [x] 5.16 Textos de ajuda carregados **uma vez** (config), não `YAML.load_file` a cada requisição — hoje sumir do deploy dá 500 no formulário. As 13 chaves contêm **todas o mesmo placeholder**; conteúdo real é Q-R9 — fecha `OPS-284` — **feito**: `Structured::HelpTexts` sobre `Rails.cache`, reusando o mecanismo que a S6 entregou em `Receivables::HelpTexts` (não inventei um segundo). Arquivo ausente devolve `{}`; spec prova. As **13** chaves do legado foram lidas uma a uma e têm **todas** o mesmo placeholder — conteúdo nasce vazio (Q-R9), e campo sem chave não exibe indicador de ajuda.

## Bloco 6 — Backend: tipos de operação estruturada (E1)

- [x] 6.1 `Structured::OperationTypeService#index`: `where('is_active = 1')` literal vira where com bind; `.limit().offset()` sem `!` **eram descartados** e passam a paginar; **o filtro de ativo passa a ser opcional** — hoje tipo desativado desaparece da tela e **não pode ser reativado pela UI**. Chave de ordenação inválida → 400 — fecha `BE-296` — **feito**: `where('is_active = 1 ')` literal vira o `scope :active` do `GlobalCatalog` (bind + boolean); paginação real; **filtro de ativo OPCIONAL** — spec prova que tipo desativado agora **aparece** na administração e pode ser reativado, o que no legado só dava por SQL. Chave de ordenação inválida → 400.
- [x] 6.2 `#create`: `title` obrigatório e único; `integration_key` derivada do título (`I18n.transliterate(title).downcase.gsub(" ","_")`) **só no create**, com **unicidade** (hoje dois títulos diferentes podem colidir na chave). Sai o `destroy` em objeto não persistido. Os 4 flags aceitos pelo permit sem formulário e sem consumidor são migrados como colunas, sem ganhar consumidor (Q-R15) — fecha `BE-297` — **feito**: `title` obrigatório e único; `integration_key` derivada **só no create** e **única no banco** — spec prova que "Auto Liquidável" e "Auto  Liquidavel", que derivam a mesma chave, colidem com 422 em vez de em silêncio. O `destroy` em objeto não persistido some com o molde do `CatalogService`. Os 3 flags sem consumidor viram coluna e **não** ganham leitor (Q-R15).
- [x] 6.3 `#update`: a imutabilidade que a UI promete (`title` readonly) passa a valer **no servidor**, junto com `is_default` e `integration_key` — alterar a chave a qualquer momento **quebra integrações** — fecha `BE-298` — **feito, e com mensagem**: `title` e `integration_key` são **declarados** no `params do` de propósito, para serem recusados com **422 e o motivo**. Deixá-los fora faria o Grape descartá-los em silêncio — 200 sem nada ter mudado, que parece ter funcionado e é pior.
- [x] 6.4 `#destroy` com **uma** guarda (hoje há duas redundantes: controller e `before_destroy`): `is_default` + `restrict_with_error`, com 422 e mensagem legível. **Todos os 4 tipos semeados são `is_default`**, logo nenhum é removível — replicar, mas dizendo por quê — fecha `BE-299` — **feito com UMA guarda**: `is_default` no `before_destroy` do model e uso via `blocking_dependents`. Spec prova 422 nos dois casos, com a mensagem dizendo por quê, e que os **4 tipos semeados** são todos `is_default` — logo nenhum é removível.

## Bloco 7 — Backend: fontes e tipos de recurso (E3)

- [x] 7.1 `Structured::ResourceSourceService#index`: `ResourceSource.all` + ordenação por título/chave. **É a entidade realmente usada** (`ReceivableEntry` exige `resource_source_id`) e é **apenas classificatória** — não entra em nenhuma fórmula de tarifa, IOF, custo efetivo ou remuneração. `is_active` **continua não filtrando nada** (Q-R19) — fecha `BE-308` — **feito por EXTENSÃO do que a S6 deixou**, não por serviço novo: `ResourceSourceService` já herdava de `CatalogService`. Ordenação por título/chave, e `is_active` **continua não filtrando** o select do borderô (Q-R19) — o motivo está escrito no endpoint: filtrar tornaria irreeditável todo borderô histórico que usa uma fonte desativada.
- [x] 7.2 `EP/resource_sources.rb` com drawer (`new`) e `show` com **404 estruturado**, mais autorização de servidor conforme a matriz DEC-18 — fecha `BE-725` e `BE-726` — **feito**: `show` com **404 estruturado** (id inexistente e id malformado), drawer servido pelo mesmo `GET /:id`, e autorização de servidor pela linha `resource_sources` da matriz DEC-18 na escrita (a leitura segue o gate de `receivables`, que é quem consome).
- [x] 7.3 `#create`: `title` obrigatório e único, `integration_key` derivada **no create**; sai o `destroy` em objeto não persistido — fecha `BE-727` — **feito**: `title` obrigatório e único, `integration_key` derivada no create; título duplicado → 422, não 500.
- [x] 7.4 `#update`: um save, e `integration_key` **explicitamente imutável** — renomear o título não mexe na chave, porque **é ela que os relatórios usam**. Hoje isso é acidental; passa a ser documentado — fecha `BE-728` — **feito**: um save, e `integration_key` **explicitamente imutável** — 422 com o motivo. Spec prova que renomear o título **não** toca a chave. No legado a imutabilidade era acidental (o formulário simplesmente não mandava o campo).
- [x] 7.5 `#destroy` com `restrict_with_error` de recebíveis — **aqui a guarda realmente dispara**, porque a coluna é preenchida — devolvendo **422 com a dependência**. Hoje os dois ramos devolvem `:ok` e o usuário vê a lista recarregar como se tivesse excluído — fecha `BE-729` — **feito**: 422 com a dependência. Spec cria um borderô apontando para a fonte e prova que a exclusão é recusada e a fonte **continua lá** — aqui a guarda realmente dispara, porque a coluna está preenchida em 28.131 de 28.131 linhas.
- [x] ~~7.6 **Atrás do portão T-D7** — superfície de `resource_kinds`: `#index` (`ResourceKind.all`, busca por título, ordem por título asc), drawer, `#show` 404, `#create`/`#update` com `integration_key` derivada só no create, e `#destroy` com 422 real (ainda que a guarda nunca dispare hoje). **Não construir antes do número** — fecha `BE-307`, `BE-720`, `BE-721`, `BE-722`, `BE-723` e `BE-724`, **ou** os converte em `dropped` com evidência~~ — **superfície NÃO construída** — o portão T-D7 abriu com **zero** e os 6 IDs (`BE-307`, `BE-720`…`BE-724`) já estão `dropped` com evidência no `parity-ledger.md`. Não construir era o resultado previsto pela própria tarefa ("**ou** os converte em `dropped` com evidência").

## Bloco 8 — Backend: autorização (transversal da unidade)

- [x] 8.1 Policy do **AUTH.S3** aplicada a `EP/structured_operations.rb`, `EP/structured_operation_types.rb`, `EP/remunerations.rb`, `EP/resource_sources.rb` e `EP/resource_kinds.rb`, conforme as linhas correspondentes de `authorization-matrix.md`. **Corrige risco de segurança confirmado**: os controllers dedicados desta unidade herdam `requires_current_user? == false` — `search`/`create`/`update`/`destroy` **não exigem login pelo `before_action`**. No ai9 todo endpoint exige JWT válido + policy. Cada teste verifica **os dois lados** da hierarquia (**C3**) — fecha `FE-309` — **feito** nos três endpoints que a S8 montou (`structured_operations`, `structured_operation_types`, `remunerations`) e no de `resource_sources`, que ganhou a escrita. `authenticate_user!` + `authorize!` da `Authorization::Matrix` em toda ação. Spec prova **401 sem JWT** em todos os verbos e os **dois lados** de C3: o Colaborador **escreve** operação estruturada (é o que a matriz diz) e **não escreve** no catálogo. ⚠ Escrevi o primeiro exemplo esperando 403 para o Colaborador e recebi 201 — a expectativa é que estava errada. `resource_kinds` não tem endpoint (T-D7).
- [x] 8.2 `user_is_readonly` deixa de ser puramente visual: escrita bloqueada **no servidor** em todos os endpoints da unidade, com teste por endpoint — **feito por REUSO** do gate global `require_not_readonly!`, que já roda em todo `/api/v1/*`. O que faltava era a **prova**: spec concede `user_is_readonly` e verifica `POST`, `PUT` e `DELETE` → **403 `READONLY_RESTRICTED`**, com o `GET` passando.

## Bloco 9 — Frontend: operações estruturadas (E1)

> **Blocos 9, 10 e 11 conferidos contra os endpoints REAIS** (26/08/2026). Eles foram construídos
> contra o contrato do `design.md` enquanto os controllers não existiam, e depois verificados
> rodando contra `Api::V1::StructuredOperations`, `StructuredOperationTypes`, `Remunerations` e
> `ResourceSources` — parâmetros, `ordering_keys[]`/`ordering_style[]`, envelope de paginação e
> códigos de erro batem. O único ajuste que o contrato real exigiu foi o
> `confirm_project_change` (**BE-291**), que a tela passou a pedir ao usuário em vez de engolir.


- [x] 9.1 `FE/structured-operations/pages/StructuredOperationsPage.tsx` — colunas menu, Portador, Tipo, Título, Contrato, Emissão, Capital, Saldo, Vencimento, Tx acordada; grupo "Gestão". Corrige dois copy-paste: o título da aba é **"Safegold - Garantias do Projeto"** e o rótulo do deep-link é "Recebívels" — fecha `FE-280`
- [x] 9.2 Estado de carregamento: o modo `silent` (que congela a lista com dados velhos em quase toda navegação) vira `keepPreviousData` do React Query — mesma sensação, sem tela mentindo — fecha `FE-281`
- [x] 9.3 `DS/EmptyState.tsx` com **dois** estados distintos: "primeiro uso" e "busca sem resultado" — hoje são indistinguíveis — fecha `FE-282`
- [x] 9.4 `DS/ErrorState.tsx` com React Query `isError` — hoje o callback `failure` do proxy é **vazio**: 500 no `search` deixa a tela em loading eterno ou com dado velho, **sem mensagem nenhuma**. Não há referência visual no legado; o estado é novo por necessidade — fecha `FE-283`
- [x] 9.5 `SearchBox.tsx` — **um** elemento (o legado tem dois com a mesma classe), debounce 300 ms, busca só de espaços abortada — fecha `FE-284`
- [x] 9.6 `DS/DateRangePicker.tsx` em modo range: só a inicial → período de 1 dia; corrige o bug do ano (o ano final era lido de `from`) — fecha `FE-285`
- [x] 9.7 Filtros de empresa/portador do projeto corrente e de tipo com `.all` (inclui inativos), enquanto o cadastro usa `.active`. **Replicar a divergência** (decisão **B-04**) — fecha `FE-286`
- [x] 9.8 `DS/DataTable.tsx` tri-state acumulando `ordering_keys[]`; corrige o **duplo `execute()` por clique** (duas requisições idênticas ao servidor); a chave `company` não aparece porque saiu da allowlist (**B-13**) — fecha `FE-287`
- [x] 9.9 `DS/Pagination.tsx` com `limit` 50 e `offset` 0, funcionando porque o total agora é real — hoje **todas** as habilitações dependem de um `totalCount` truncado — fecha `FE-288`
- [x] 9.10 `OperationRow.tsx` — BRL, `contract_number` com fallback "-" (hoje fica em branco), datas `%d/%m/%Y`, `agreed_rate` com **2 casas na lista e no detalhe** (hoje sai cru na lista e com `sprintf` no detalhe). A regra `has_pre_faturamento → datas viram "-"` é replicada, mas data nula deixa de quebrar a renderização — fecha `FE-289`
- [x] 9.11 Menu de ações Ver mais / Editar / Remover (os dois últimos só para não-readonly), com **rotas reais** em vez de `resource/topic/section` em memória (D-92) — fecha `FE-290`
- [x] 9.12 Guarda de portador no projeto com a mesma mensagem; corrige o rótulo "Cadastrar recebível"; a guarda de **empresa** (hoje só dentro do form) passa a ser avaliada junto — fecha `FE-291`
- [x] 9.13 Confirmação "A operação não pode ser desfeita. Tem certeza?"; corrige o título "Excluir renegociação"; a exclusão bloqueada por recibo passa a cair no ramo de erro **com mensagem de negócio**, não a recarregar a lista como sucesso — fecha `FE-292`

## Bloco 10 — Frontend: formulário e detalhe (E1)

- [x] 10.1 `OperationForm.tsx` — duas colunas (Cadastro + Datas à esquerda; Valores + Taxa Acordada + Outros à direita), selects pré-selecionando a primeira empresa/portador/tipo ativo. Sai a gambiarra do `hidden_field :id` renomeado para `is_pub_domain` e o `data-movements` que carrega JSON de `MovementKind` e **nunca é lido**. As 13 tooltips existem como mecanismo, com conteúdo em branco (Q-R9) — fecha `FE-293`
- [x] 10.2 Máscaras via `DS/MoneyInput`/`DS/PercentInput`; o aviso de separador continua sendo toast e continua **não impedindo** a digitação — replicar — fecha `FE-294`
- [x] 10.3 Barra de ação explícita substituindo o registro por `change`. **Corrige o pior comportamento do formulário**: hoje, se **qualquer** obrigatório fica vazio, a ação é **removida da barra sem mensagem** e o usuário perde a possibilidade de salvar sem saber por quê. Passa a dizer o que falta. Corrige também a divergência `due_date` (não obrigatória no front, obrigatória no backend) — fecha `FE-295`
- [x] 10.4 Datas no cadastro com amarração cruzada (emissão define `minDate` do vencimento e vice-versa); **na edição, readonly na tela e imutáveis no servidor** (T-D5). Sai o seletor `.closing_date_hidden`, herdado de outra tela; data nula deixa de quebrar a view — fecha `FE-297`
- [x] 10.5 Bloqueios: sem portador → mensagem bloqueante com precedência sobre empresa; sem empresa → link "clique aqui" que abre criação inline e recarrega. Corrige a concordância "operação de estruturada" — fecha `FE-298`
- [x] 10.6 `OperationDetailPage.tsx` — card CADASTRO com Operação, Tipo, Portador, Contrato, Projeto, datas, **Saldo Inicial com sinal**, Capital, **Saldo com sinal**, Taxa (2 casas), Observação; não mostra empresa, flags nem recibo. **DEC-01 — replicar os saldos negativos exibidos** (Q-R20). Id inexistente → **404**, não 500 — fecha `FE-299`

## Bloco 11 — Frontend: tipos, remunerações e recursos (E1 + E2 + E3)

- [x] 11.1 `OperationTypesPage.tsx` — colunas Título/Chave ordenáveis, grupo "Cadastro", só admin/og/manager. "Excluir" não aparece para `is_default` — e como **todos os tipos semeados são default**, na prática nenhum é removível: replicar, **com a mensagem dizendo por quê** — fecha `FE-300`
- [x] 11.2 `OperationTypeDrawer.tsx` — Título (readonly na edição), Chave de integração, Ativo. Corrige as copies erradas ("Cadastrar uma taxa"/"Editar uma taxa", do helper de remunerações; "Essa construtora não pode ser alterada", de outro domínio); sucesso passa a emitir toast (hoje o ramo de sucesso é vazio); erro deixa de mostrar `<b>chave_tecnica</b>` cru — fecha `FE-301`
- [x] 11.3 Exclusão de tipo com confirmação e tratamento de erro. **Hoje o `$.ajax` de DELETE não define `error`** e o backend responde 422 quando bloqueado: o script de resposta **nunca executa** e, para o usuário, clicar em "Remover" simplesmente **não faz nada** — fecha `FE-302`
- [x] 11.4 `RemunerationsPage.tsx` — colunas Classe (LIQ/EST), Operação, Taxa (%), menu; grupo "Projeto"; "Cadastrar" exige não-readonly **e** admin/og/manager. Ganha ordenação e paginação; o `%` passa a aparecer na célula, não só no cabeçalho — fecha `FE-303`
- [x] 11.5 `RemunerationDrawer.tsx` — "Remuneração para" (Operações liquidáveis = `RiskOperationType`, ou estruturadas = `StructuredOperationType`), "Tipo de Operação" conforme a classe, "Valor da taxa (%)". **Um** select controlado por estado no lugar dos dois sobrepostos com troca de `name`/`disabled_operation_type_id`. Na edição, o select **consegue exibir** o tipo selecionado mesmo desativado, sem oferecê-lo para novos (Q-R21), e campos disabled deixam de sumir do submit — fecha `FE-304`
- [x] 11.6 `DS/PercentInput` na taxa, 2 casas, **sem limite superior nem inferior** (T-D9) — fecha `FE-305`
- [x] 11.7 Deep-links `/remunerations/add` e `/remunerations/:id/edit` como rotas reais. Corrige dois defeitos: `openEdit(id)` grava `taxId` enquanto o proxy lê `remuneration_id` (pelo deep-link a URL vira `/remunerations/undefined/edit.js`), e o ERB escapado põe a string literal `<%= pub_console_path %>` no `history.replaceState` — fecha `FE-306`
- [x] 11.8 `ResourceSourcesPage.tsx` — colunas Título/Chave ordenáveis, drawer com Título, Chave, Ativo. Corrige o vazio agramatical "Não existem utilização de recurso cadastrado". Se `resource_kinds` sobreviver a T-D7, os dois rótulos de menu **precisam ficar distintos** (Q-R22) — fecha `FE-308`
- [~] 11.9 **`dropped` pela DEC-110** — ~~Atrás do portão T-D7~~ — `ResourceKindsPage.tsx`, com entrada de menu (hoje a tela existe **só por URL direta**), botão "Cadastrar" respeitando `user_is_readonly` (hoje **não respeita**, divergindo de todas as outras telas da unidade) e estado vazio correto — fecha `FE-307` **ou** o converte em `dropped`
  - ⛔ **DROPPED — DEC-110 (26/08/2026).** O portão T-D7 foi respondido pelo dump de produção:
    `resource_kinds` tem **0 linhas** e **0 de 28.131** `receivable_entries` a referenciam. A DEC-82
    já tinha a condição aprovada; faltava o número. **Não foi implementado**: nem
    `ResourceKindsPage.tsx`, nem entrada de menu, nem cliente de API. `FE-307` entra no razão como
    `dropped` com evidência. Consequência neste bloco: a **Q-R22** (os dois rótulos de menu que
    precisariam ficar distintos) **desapareceu** — sobra só `resource_sources`, e a 11.8 fica com o
    rótulo do legado sem ambiguidade.
- [x] 11.10 `useNavItems.ts` com os 4 itens: "Operações Estruturadas" (Gestão), "Remunerações" (Projeto), "Tipos de Recursos" = `resource_sources` (Cadastro), "Tipos de OP Estruturada" (Cadastro). Filtro por papel vindo da matriz DEC-18; títulos de aba corrigidos — fecha `OPS-286`
- [x] 11.11 Formatação monetária e percentual sai do backend (monkey-patch em `Integer`/`Float`/`BigDecimal`, com `rescue` que engolia falha e devolvia o número cru) e passa a `Intl.NumberFormat('pt-BR')` + `date-fns` no front, sobre `lib/format/money.ts` — fecha `OPS-289`

## Bloco 12 — Testes

> Os goldens estão no Bloco 3. Este bloco cobre o que não é golden.

- [x] 12.1 `spec/models/structured_operation_spec.rb` — derivações do `before_validation`, o reset do `balance` (golden `E6`), as validações ausentes que continuam ausentes — **feito**: `spec/models/structured_operation_spec.rb`, **17 exemplos**. As três derivações, o golden `E6`, as quatro validações ausentes que continuam ausentes, o dead code de `operation_value`, as três colunas sem consumidor e a FK real de `receipt_id`.
- [x] 12.2 `spec/models/remuneration_spec.rb` — unicidade (projeto, classe, tipo) na aplicação **e** no índice; `beauty_type` `"LIQ"`/`"EST"`; tipo inválido → 422 — **feito**: `spec/models/remuneration_spec.rb`, **10 exemplos**. Unicidade nos **dois** níveis (aplicação e índice), `beauty_type` LIQ/EST, tipo inválido, o `title` desnormalizado reescrito em todo save, o bloqueio de exclusão e a faixa não validada.
- [x] 12.3 `spec/requests/api/v1/structured_operations_spec.rb` — CRUD, 422 na exclusão bloqueada por recibo, 400 em chave de ordenação inválida, `X-Total-Count` real — **feito**: `spec/requests/api/v1/structured_operations_spec.rb`, **36 exemplos**. CRUD, 422 na exclusão bloqueada por recibo, 400 em chave de ordenação inválida, `X-Total-Count` real, o ciclo ida-e-volta do recibo e os textos de ajuda.
- [x] 12.4 `spec/requests/api/v1/remunerations_spec.rb` — `project_id` forçado, `operation_type_type` validado por inclusão, 422 na exclusão com recibos — **feito**: `spec/requests/api/v1/remunerations_spec.rb`, **21 exemplos**. `project_id` forçado, `operation_type_type` validado por inclusão, 422 na exclusão com recibos, e o select do painel lateral com o tipo desativado da edição (Q-R21).
- [x] 12.5 Teste de escopo (**C1**) por endpoint: um usuário de `P1` não alcança operação nem remuneração de `P2` **nem passando o id por parâmetro** — **feito**: escopo por endpoint nos dois recursos escopados. Usuário de P1 não alcança operação nem remuneração de P2 **nem passando o id por parâmetro** — lista vazia no `index`, **404** no `show`/`PUT`/`DELETE`.
- [x] 12.6 Teste de autenticação: **nenhum** endpoint da unidade responde sem JWT válido (é a correção de segurança de `FE-309`) — **feito**: bloco "autenticação (FE-309)" em `structured_operations_spec.rb`, `remunerations_spec.rb` e `structured_catalogs_spec.rb`. **Nenhum** verbo responde sem JWT.
- [x] 12.7 Teste de autorização (**C3**) por endpoint, verificando **os dois lados** da hierarquia — **feito**: os dois lados em cada recurso. Onde a matriz separa (os catálogos, `CRUD CRUD CRUD R`) o Colaborador lê e não escreve; onde não separa (`structured_operations`, `remunerations`) o outro lado é o gate de `user_is_readonly`, testado por verbo.
- [x] 12.8 Seeds: rodar duas vezes não duplica; as 4 `integration_key` de tipo estruturado e as 7 de fonte de recurso existem; a coexistência com dados importados não quebra a unicidade — **feito**: `spec/requests/api/v1/structured_catalogs_spec.rb`, bloco "seeds de referência". Rodar duas vezes não duplica; as **4** chaves de tipo estruturado e as **6** de fonte de recurso existem; a idempotência por `legacy_id` sobrevive à coexistência com dados importados, e `13?_salario` é preservada.
- [x] 12.9 Front: type-check em **0 erro**; revisão visual da lista, do formulário e do detalhe em **light e dark**
  - ✅ **Verificado EXECUTANDO** (26/08/2026), não deduzido. `tsc --noEmit` em **0 erro**, e as telas
    foram dirigidas por Playwright contra o backend real (`vite` na :5173 + Puma na :3000), com login
    de verdade pelo código do `magic_login`. O que apareceu **rodando** e que o portão verde não pega:
    1. a tabela de operações **estourava o container** — "Portador" saía cortado e a coluna de AÇÕES
       ficava fora da tela. A `width` da coluna é só sugestão na `Table` da base
       (`table-layout: auto`), então o limite passou a ser `max-w` no **conteúdo**, que é o que a
       truncagem precisa para valer;
    2. "Saldo" **quebrava o sinal negativo em duas linhas** (o `-` virava um traço solto acima do
       valor) — resolvido com `whitespace-nowrap` nas células monetárias;
    3. a taxa do candidato a recibo saía como `2.55%` — ponto decimal de JavaScript no meio de uma
       coluna em reais. Passou a `Intl` pt-BR com **4 casas** (a escala de `remunerations.value`).
  - Exercitado ponta a ponta: remuneração EST/Fomento 2,55% → operação de R$ 200.000,20 →
    candidato a recibo com **R$ 5.100,01 (2,5500%)**, que é o arredondamento `ROUND_HALF_UP` do
    golden **E4** na fronteira (`5100,0051`); lote salvo com 9 operações (R$ 8.134,86); exclusão da
    operação faturada recusada com **422 real** e mensagem de negócio (`FE-292`); cobrança `done`
    em somente leitura. Dado de teste **removido do banco** ao fim (a cobrança voltou a R$ 3.034,85
    com 8 recibos).
  - Light **e** dark conferidos em 1440x900, e **mobile em 390x844** (DEC-100): a lista vira
    `MobileCard`, as ações da linha vão para a folha do rodapé e o destino é alcançável pela
    `MobileBottomBar`.

## Bloco 13 — Paridade e fechamento

- [x] 13.1 Marcar no `parity-ledger.md` os **74 `build`** e os **4 `adapt`** desta fatia, um a um — **feito, e conferido linha a linha por varredura do próprio razão**: os **78** IDs (74 `build` + os 4 `adapt` `FE-309`, `DB-290`, `OPS-285`, `OPS-286`) estão `migrated`, cada um com **alvo, teste e nota** na sua linha da tabela do inventário. Duas correções que a conferência achou e que a marcação "em bloco" teria escondido:
      **(a) `OPS-285` ainda estava `pending`** — era o único dos 78 fora do lugar, e é justamente um `adapt` (o ETL: código descartado por D-105/DEC-12, colunas `legacy_*` preservadas). Ganhou alvo (`load_order.yml:59-62`, o conversor `ResourceSources` com `owner_slice: S8`), teste (a idempotência por `legacy_id`) e nota.
      **(b) 9 linhas `DB` estavam com a coluna de TESTE em `—`** (`DB-280`…`DB-285`, `DB-287`, `DB-288`, `DB-293`) — tinham alvo e nota, mas nenhuma prova citada, e a tarefa pede os três. Preenchidas com o spec que de fato trava cada esquema (`structured_operation_spec.rb`, `remuneration_spec.rb`, `structured_catalogs_spec.rb`, e o novo `bulk_receipts_service_spec.rb` para as duas FKs de `receipt_id`).
      Há **seção própria da S8 no topo do razão**, com o placar (78 `migrated` / 20 `dropped`), a régua que impede promover a `verified` e o que a fatia desbloqueou na S6.
- [x] 13.2 Registrar `dropped` **com a evidência da varredura** para os 9 `drop`: `BE-288`, `BE-289`, `BE-309`, `DB-289`, `DB-291`, `FE-296`, `OPS-280`, `OPS-281`, `OPS-282` (motivos em `proposal.md` §"`drop` — motivo registrado") — **feito**: os 9 estão `dropped` no `parity-ledger.md`, **cada um com a evidência da varredura**, não por omissão. Três famílias: código morto que dava 500 em 100% das chamadas (`BE-288`, `BE-289`, `OPS-281`); feature que nunca existiu — 8 rotas geradas e **zero** controller/model/tabela (`BE-309`, `DB-291`, `OPS-280`); e mecanismo trocado com comportamento preservado (`OPS-282`, o `Dev.ilike`). `FE-296` é o caso que **guarda um sinal**: o no-op do legado revela a intenção `balance = capital`, que conflita com a regra do model — decisão **B-11**, segue o model, e o golden `E6` a trava. `DB-289` já estava `dropped` pelo portão T-D7.
- [x] 13.3 Fechar ou reencaminhar os **11 `build?`** — **os 11 estão fechados, com a resolução escrita na própria linha do razão. Nenhum ficou por omissão.**
      **`BE-292`** (T-D6) → `migrated`. Replicar o reset e documentar a coluna como decorativa; golden `E6` nos dois níveis, e há exemplo de spec afirmando a **ausência** de baixa, para que criar o caminho quebre o teste em vez de contornar a decisão em silêncio.
      **`FE-297`** (T-D5) → `migrated`. Regra de **servidor**, a mesma da `FE-260` (S7): as datas não são declaradas no `params do` do `PUT`.
      **Os 9 de `resource_kinds`** (T-D7 / **DEC-110**) → `dropped` **com o número medido**: 0 linhas na tabela e **0 de 28.131** `receivable_entries` com `resource_kind_id`. Com `DB-561`, são **10** IDs desta família descartados.
      ⚠ **A conferência achou o último resíduo vivo de `resource_kinds` e ele foi removido aqui**: `backend/app/services/authorization/matrix.rb` ainda declarava `'resource_kinds' => %w[CRUD CRUD CRUD R]` — a tela de PERMISSÕES publicava permissão para algo **sem tabela, sem model, sem endpoint e sem item de menu**. Saiu, com o motivo escrito no lugar da linha. `spec/services/authorization` + `permissions_spec.rb`: **32 exemplos, 0 falhas** depois da remoção. O outro resíduo que a nota do `DB-286` apontava (o conversor no `load_order.yml`) já estava resolvido: hoje está em `do_not_migrate`, ao lado de `geolocations`.
- [x] 13.4 Se T-D7 vier **zero**: converter os 7 IDs de superfície e o `DB-289` em `dropped` com evidência e **remover a tabela `resource_kinds` numa tarefa explícita** — nunca por omissão — **feito, e por não-criação REGISTRADA, nunca por omissão.** T-D7 veio **zero**. Os 7 IDs de superfície (`BE-307`, `BE-720`…`BE-724`, `FE-307`) e o `DB-289` estão `dropped` com a evidência, mais `DB-286`, `DB-294` e `DB-561`. **Não há tabela a remover**: a S6 já a deixara fora de `20260826220000_catalogos_de_recebivel.rb` e a S8 não a criou. Fica escrito em quatro lugares — tarefas 1.7, 2.3 e F.1, e uma nota no `Seeds::Reference::Runner` — para que ninguém a crie por engano depois.
- [x] 13.5 **REESCRITA pela DEC-115 (26/08/2026), e cumprida na forma reescrita.**
      ~~Conferir contra o legado, com o dump carregado, os goldens `E1`…`E7`~~
      → **Conferir contra a FONTE de 2022**, registrando em cada golden a marca de que ele
      tem **fonte, não oráculo**.
      **Por que mudou:** `create_structured_operations`, `create_structured_operation_types`,
      `create_remunerations` e `create_receipts` estão entre as **24 que nunca subiram**. O
      dump não tem uma única operação estruturada, uma única remuneração nem um único recibo.
      O usuário confirmou que não existe outra base: *"nao tem, a tabela de excel que tinha
      foi perdida"*.
      **O que a auditoria de QA achou em 26/08/2026, e corrigiu.** Os arquivos desta fatia
      diziam a coisa certa com as palavras erradas: traziam `⚠ FONTE, não ORÁCULO — DEC-103b`,
      que explica o fato, mas **nenhum deles continha a marca literal** que a DEC-103b tornou
      padrão, e dois goldens (`E6` no model e no request, `E7` na factory) não tinham marca
      nenhuma. Uma marca que só existe em paráfrase não é encontrável por quem procura por ela
      três anos depois — que é o único momento em que ela serve para alguma coisa. Agora, com
      `⚠ NUNCA EXECUTADO EM PRODUÇÃO — DEC-103b / DEC-115` e `arquivo:linha` do legado:
      `remuneration_calculator_spec.rb` (`E1`…`E5` em `:43`, `E7` em `:204`),
      `receipt_generator_spec.rb`, `bulk_receipts_service_spec.rb`,
      `structured_operation_spec.rb` (`E6`), `structured_operations_spec.rb` (`E6`) e
      `factories/structured.rb` (`E7`, que também ganhou a citação do legado que não tinha).
      Os três arquivos que ainda diziam *"a tarefa 13.5 continua aberta para reconferir contra
      o dump"* foram corrigidos: a tarefa não está aberta, foi **reescrita**.
      **O que sustenta os números, e que continua não sendo oráculo:** cada valor cita
      `../sfg/app/models/receipt.rb:41-66`; `E1`…`E5` e `E7` verdes com `E1` =
      200.000,00 × 2,55% = **5.100,00**; os mesmos cinco gravados e relidos de
      `decimal(15,2)`; e a única conferência contra sistema externo que era possível — o
      `ROUND_HALF_UP` comparado com o `CAST(... AS numeric(15,2))` do próprio Postgres,
      **5 de 5 iguais**. Isso prova que a política declarada reproduz o truncamento que o
      legado obtinha por efeito colateral do adapter, **não** que o número esteja certo.
      Reconferido executando em 26/08/2026: **148 exemplos, 0 falhas**.
      **NÃO promove nada a `verified`.** Os 78 IDs param em `migrated`, deliberadamente.
- [x] 13.6 Handshake com **`charges`/`receipts`**: a fórmula é consumida por `Receipt#fetch`, o índice único de `DB-290` existe e `Charge#calc!` separa LIQ de EST pela sigla persistida. **E2 não fecha sem isto** — **feito e conferido EXECUTANDO** contra o servidor de dev (`http://127.0.0.1:3000`): `GET /charges/:id/receipts` responde **200** com o candidato já calculado (capital 200.000,00 × 2,55% → **5.100,00**, golden `E1`, `kind: EST`, `temp_id` estável); `PUT` com o `temp_id` responde **200** e a operação sai de `available_for_receipt`; `PUT` com lista **vazia** responde **200** e o candidato volta. O índice único de `DB-290` existe (`index_receipts_on_operation_unique`, `[operation_id, project_id, operation_type]`, `unique=true`) e `Charge#calc!` separa pela sigla persistida (`Receipt.risk`/`Receipt.structured` sobre `LIQ`/`EST`). ⚠ **Foi este handshake que revelou o 500 do "desmarcar"** — ver `EST-S8-01`.
- [x] 13.7 Handshake com **`receivables`**: `resource_sources` disponível e obrigatório em `ReceivableEntry`, com `legacy_id` preservado — **feito e conferido EXECUTANDO**: `ResourceSource` disponível pelo endpoint (leitura da S6 + escrita da S8), obrigatório em `ReceivableEntry`, e **6 de 6** linhas com `legacy_id` preservado — o de-para com `durecliq.url_id` do Django anterior, que é a única prova de proveniência dessas linhas.
- [x] 13.8 Handshake com **S7**: `RiskOperation` responde como `operation_class` da classe LIQ e `available_for_receipt` funciona nos dois agregados — **feito e conferido EXECUTANDO**: `Remuneration::OPERATION_CLASSES` mapeia `RiskOperationType → RiskOperation` e `StructuredOperationType → StructuredOperation`; `available_for_receipt` existe e funciona **nos dois agregados** (`risk_operations.receipt_id IS NULL` e `structured_operations.receipt_id IS NULL`); e o spec do calculador exercita a classe **LIQ** ponta a ponta, com `temp_id` `RCP-{projeto}-LIQ-…` e valor 5.100,00.


## Fechamento de órfãos do Phase 2 — esquema de estruturadas e remuneração

Cinco IDs que não tinham dono e passam a ser desta fatia. Ver a seção correspondente do
`proposal.md`.

- [x] ~~F.1 `resource_kinds` + seed, plugado no arcabouço de seed de referência de S3.~~ **NÃO EXECUTADA** — o portão T-D7 abriu com zero (0 linhas na tabela, 0 de 28.131 `receivable_entries` com `resource_kind_id`). `DB-561` passa a `dropped` com a mesma evidência de `DB-286`/`DB-294`. Há uma nota explícita no `Seeds::Reference::Runner` dizendo para não criar a linha nem a tabela.
      **Fecha: DB-561.**
- [x] F.2 `resource_sources` + seed de 7 linhas. **Fecha: DB-562.** — **feito na S6, conferido aqui**: tabela em `20260826220000_catalogos_de_recebivel.rb` e `Seeds::Reference::ResourceSources`. São **6** linhas, não 7: as de PRODUÇÃO, medidas no dump (DEC-30 — o dado real vence a lista do mapa). `DB-562` fechado.
- [x] F.3 `structured_operation_types` + seed de 4 linhas. **Fecha: DB-580.** — **feito**: ver 2.1. `structured_operation_types` (migration 20260826230000) + `Seeds::Reference::StructuredOperationTypes` com as 4 linhas, idempotente por `integration_key`.
- [x] F.4 Conferir `structured_operations` contra a descrição de `data-schema`. **Fecha: — **conferido**: as 20 colunas de `structured_operations` batem com a descrição de `data-schema`, com as conversões de DB-295/DB-296 registradas coluna a coluna no comentário SQL.
      DB-581.**
- [x] F.5 `remunerations` com `decimal(15,2)` — **preservar a precisão do legado**, não — **feito com divergência registrada**, e a divergência é o ponto: a premissa da tarefa está errada nas duas pontas. O legado **não** é `decimal(15,2)` — é `t.float :value` (`20220629123512_create_remunerations.rb:6`); e `receipts.fee`, que **copia** esta coluna, já nasceu `decimal(7,4)` na S6. Guardar a taxa com 2 casas e copiá-la para uma de 4 perderia a 3ª e a 4ª **na origem** (1,7550% → 1,75%) e mudaria o valor faturado. `decimal(7,4)` preserva **mais** que `decimal(15,2)` para uma taxa.
      "melhorar" para outra escala. Verificável: a coluna é `decimal(15,2)`. **Fecha:
      DB-582.**
- [x] F.6 Golden test da fórmula de remuneração alimentado com valores extraídos do legado — **feito**: `spec/services/structured/remuneration_calculator_spec.rb`, contrato **C2**. O caso de arredondamento onde `float` e `decimal` divergem é o `E5` — produto exato `7769.9992229999990000001`, artefato na **décima** casa —, e há exemplo dedicado que quebra se alguém trocar `fee.to_f / 100.0` por aritmética `BigDecimal`: o valor final continuaria 7.770,00 e os outros quatro goldens continuariam verdes, ou seja, **a sequência do DEC-02 mudaria sem nenhum teste reclamar**.
      (contrato **C2**), cobrindo o arredondamento onde `float` e `decimal` divergem.
      **Fecha: DB-582 (parte).**
