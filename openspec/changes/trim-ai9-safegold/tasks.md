# Tasks: trim da base ai9 para o Safegold

Fila de trabalho do Phase 1b. Uma tarefa só é marcada quando a feature **sumiu de fato**
(front + back + dados), a verificação passou e o ledger virou `to-remove` → `removed`.

Verificação (o `pnpm` não existe neste shell — usar os binários locais):
- `cd frontend && node node_modules/typescript/bin/tsc --noEmit` — **baseline: 305 erros, todos em `src/components/3d/`**
- `cd backend && bundle exec rspec` — **baseline medido: 1362 examples, 6 failures, 2 pending**.
  Ja estava vermelho antes do trim. Criterio dos blocos de backend: **no maximo 6 falhas, e as
  mesmas 6**. Falha nova = introduzida pelo bloco.
- **`vitest` NAO roda neste ambiente**: falta `@rollup/rollup-win32-x64-msvc` (o `node_modules`
  foi instalado no Linux e o Node roda no Windows). Limitacao **anterior** ao trim — a suite do
  front nao pode ser usada como portao ate ser resolvida. O portao real e o type-check.

## Bloco 1 — Folhas visuais + landing (risco baixo)
- [x] 1.1 `AI9-022` cenas 3D/WebGL — `frontend/src/components/3d/**` (11 cenas + `chat/`). **Critério: o type-check deve ir de 305 erros para 0**
- [x] 1.2 `AI9-023` UI de terminal/typewriter — `components/terminal/*`, `stores/useTerminalStore.ts`
- [x] 1.3 `AI9-024` easter egg sazonal — `components/seasonal/*`, `styles/easter*`
- [x] 1.4 `AI9-031` audio visualizer — `app/experiments/audio-visualizer/*`, `components/chat/AudioVisualizerPlayer.tsx`
- [x] 1.5 `AI9-028` design demo — `app/pages/DesignDemoPage.tsx` + rota `/design-demo`
- [x] 1.6 `AI9-029` guia de rastreamento — `app/pages/TrackingGuidePage.tsx`, `backend/docs/{TRACKING_GUIDE,UTM_STANDARDS,GTM_SETUP}.md`, rota `/guia-rastreamento`
- [x] 1.7 `AI9-027` preview de site — `app/pages/{SitePreviewPage,BuildPage}.tsx`, ~~`components/preview/*`~~, item `/preview-site` da Sidebar
  - ⚠️ **`components/preview/*` NÃO era do AI9-027** — é o modo de *preview de plano* (`usePlanPreviewStore`, AI9-002), importado por 10 páginas, entre elas `DashboardPage` (AI9-033, mantida) e `MediaPage` (AI9-016, mantida). **Movido para o Bloco 4.**
- [x] 1.8 `AI9-026` NavKit — `frontend/src/NavKit/*`, `app/NavKitDemo.tsx`, `app/pages/{NavKitHome,NavKitOverview}.tsx`
- [x] 1.9 `AI9-025` Brazilian Software — `app/pages/BrazilianSoftware/*` + endpoint público
- [x] 1.10 `AI9-032` MCP n8n — `tools/n8n-mcp-server/**` (nenhum código da app depende)
- [x] 1.11 `AI9-021` landing campfire — `components/campfire/**` (16 componentes + `sections/`) **e, na mesma tarefa, apontar `/` para a tela de login (DEC-13.3)**
- [x] 1.12 Verificar bloco: type-check em 0 erro · nenhuma rota órfã · commit único reversível · ledger `removed` para 021–032
  - [x] type-check em **0 erro** (baseline 305) · eslint limpo · nenhuma rota órfã · ledger `removed` · `.migration-ai9/removed-features.md` escrito
  - [x] commit único reversível — feito pelo orquestrador; type-check reconferido de forma independente (0 erro)

## Bloco 2 — Analytics
- [x] 2.1 `AI9-011` Painel TV — `features/metrics/PainelTV.tsx`, `MosaicoParede`, `LiveFeed`, `ScoreRing` (+ os 2 testes e os re-exports do barril)
- [x] 2.2 `AI9-012` logger ao vivo + heatmap — `app/pages/admin/{EventLoggerPage,HeatmapPage}.tsx`, `services/analytics/get_heatmap_data.rb`, `hooks/useEventStream.ts`, `lib/analytics/useHeatmapTracker.ts`, `channels/event_logger_channel.rb`, rotas `/admin/logger` e `/admin/heatmap`, itens de `Sidebar`/`useNavItems`
- [x] 2.3 `AI9-013` espelhamento hub "brsw" — os 3 jobs + `lib/tasks/hub_backfill.rake` + `spec/models/lead_hub_push_spec.rb`, o cron `hub_metricas` do sidekiq, as ENVs `HUB_URL`/`HUB_SOURCE_TOKEN`/`HUB_INSTALACAO` do `.env.example` e a ponte `push_to_hub` do `Lead`
- [x] 2.4 `AI9-010` analytics próprio — tracking, funil, KPIs, viewers, GA4 + Meta CAPI. Hub `TrackedEvent` (model, `Viewer`, 7 migrations, schema, 7 endpoints, 11 services, 3 jobs, 26 arquivos de front)
  - ⚠️ **`Purchase` NÃO saiu.** A `tasks.md` mandava tirar junto, mas 2 dos 9 consumidores são do **AI9-030 (login, MANTIDO)**: `auth/checkout_session_service.rb` (23 usos) e `Api::Entities::User#purchased_plan_ids`. Saíram só as amarras com o funil. **Decisão do model inteiro movida para o Bloco 4** — evidência completa em `removed-features.md`.
  - ⚠️ **`DashboardChannel`, `PublicEventsChannel`, `analytics_service.rb`, `dashboard_kpis_broadcast_job.rb`, `KpiCard`, `AnomalyAlerts`, `charts/Recharts*`, `MobileMiniKpi` e `invalidateAnalytics.ts` ficaram** — catalogados como AI9-010, mas os consumidores reais são features mantidas (AI9-033, AI9-007) ou dos Blocos 4/6. Evidência por consumidor em `removed-features.md`.
  - ⚠️ **Dívida:** `DashboardPage.tsx` (AI9-033, mantida) perde a fonte de dados — consumia `GET /api/v1/analytics/dashboard`. Type-check e specs seguem verdes; a degradação é de runtime e foi registrada.
- [x] 2.5 Verificar bloco: type-check **0 erro** (= baseline) · eslint limpo · `rspec` **1298 examples, 5 failures, 2 pending** — subconjunto exato das 6 do baseline (a 6ª era `lead_hub_push_spec.rb`, apagado com o AI9-013); **nenhuma falha nova** · 7 migrations exclusivas apagadas + `tracked_events`/`viewers` fora do `schema.rb` · ledger `removed` para 010–013
  - A lista das 6 falhas do baseline não existia em lugar nenhum (só o número). Foi medida para este bloco rodando `rspec` em HEAD e está registrada em `removed-features.md`.

## Bloco 3 — Conteúdo
- [x] 3.1 `AI9-019` transcrição por IA — `api/v1/transcriptions.rb`, `post_transcription_service.rb`, `post_transcription_job.rb`, `process_audio_transcription_job.rb`, `TranscriptPage.tsx`, o gate `users.can_transcribe` (migration + schema + CRUD + checkbox) e a rota `/transcript`
  - ⚠️ **`ai/audio_transcription_service.rb` NÃO saiu** — `process_meta_webhook_job.rb:482` (AI9-009) transcreve áudio do WABA com ele. **Movido para o Bloco 5.**
  - ⚠️ **`audio_converter_service.rb` NÃO saiu, e não era do AI9-019** — o único chamador do repo é `api/v1/operations.rb:250` (AI9-014). **Movido para o Bloco 7.**
- [x] 3.2 `AI9-020` agenda/Calendar/Drive — `services/agenda/**`, `google_drive_connection.rb`, `ai/tools/calendar_guard.rb`, `AgendaMemory` + migration + tabela, os 3 jobs, os 2 crons do sidekiq, `lib/tasks/agenda.rake` e o seed `assistente_gui_agent.rb`
  - As tools do agente saíram **junto** (antecipando parte da 8.2, como a tarefa previa): `tool_executor` (calendar/plane/memory + `execute_calendar`), `tool_registry` (constantes + `CAPABILITY_TOOLS` + `calendar_access:`), `agent_service` (passo 5.1 e as 3 instruções de tool) e o painel "Integrações" do `AIAgentConfigPanel.tsx`. **Tinha de ser agora**: sem isso, agente com capability `calendar` dava `NameError`.
  - `Agenda::PhoneNormalizer` ≠ `PhoneNormalizer` global — só o do namespace `Agenda` saiu. O global (`app/services/phone_normalizer.rb`) **ficou**, mas perdeu todos os consumidores quando a pilha de superadmin do blog saiu na 3.5; registrado como residual, não como remoção.
- [x] 3.3 `AI9-015` showrooms — `db/seeds/brsw_showrooms.rb`, `brsw_showrooms_logos.rb` e `brsw_showrooms_media.rb` **apagados inteiros**; com eles morreu a dívida do Bloco 1 (as 3 `description:` citando "NavKit"). Nenhum era carregado pelo `db/seeds.rb`.
- [x] 3.3b `AI9-015` showrooms — endpoints (privado + público), entity, model, service, 6 migrations, a tabela, `ShowroomsPage.tsx`, `ShowroomPostWidget.tsx` (zero importadores) e a fronteira com o `Medium`: `belongs_to :showroom`, `media.showroom_id` + índice + FK, params, `expose`, `showroomsApi`/`showroomsPublicApi` e a interface `Showroom`. **`Medium` ficou intacto** (AI9-016). Saíram também as 3 chaves i18n `showroom_*`, órfãs desde o Bloco 1.
- [x] 3.4 `AI9-017` pedidos/entregas/milestones — **colisão de `Project` RESOLVIDA**: 6 endpoints, 8 entities, 10 models, 7 services, 14 migrations, 10 tabelas e 33 specs. A feature **não tinha frontend**.
  - Saíram junto, com evidência de que o catálogo errou o dono: `Pricing` + `PricingCalculator` (catalogados AI9-001 — só o `Order` os usa), `ScopeCalculator` (catalogado AI9-002 — só `Order`/`specifications_service`) e **`AccessCode`** (catalogado AI9-030/login — mas seus únicos `as: :resource` eram `Delivery` e `Project`, e ele não aparece em nenhum arquivo de auth).
  - ⚠️ **`SupportPlan` NÃO saiu** — é AI9-002. Só a FK `orders → support_plans`.
- [x] 3.5 `AI9-004` blog — 10 endpoints, 8 entities, 9 models, 11 services (`blog/**` e `whatsapp/admin_auth_resolver.rb` inteiros), 4 jobs, 2 seeds, 16 migrations, 9 tabelas, 21 specs e 10 telas. Resolveu as pendências deixadas por 3.1/3.2 (`PostTranscriptionService`, `Blog::*`, `GoogleDriveConnection`).
  - ⚠️ **`app/models/user.rb` teve de ser editado** (infra mantida): o `after_commit :invalidate_admin_auth_cache` chamava `Whatsapp::AdminAuthResolver` — sem a remoção, todo save que trocasse telefone daria `NameError`.
  - O modo **"blog"** do `SidebarModeToggle` saiu inteiro, com guarda no `sidebarModeStore` para quem já o tinha salvo em `localStorage`.
  - O bloco de cron do Sidekiq ficou vazio: os 3 jobs que restavam eram todos do blog.
- [x] 3.6 `AI9-005` WhatsApp/Evolution — **REMOÇÃO PARCIAL (DEC-14)**. Saíram: `api/whats/v1/{chats,groups,messages}.rb`, o recurso `messages-upsert` de `webhooks.rb`, `polemk_{chat,group}_service.rb`, `whats_message_service.rb`, `whatsapp_notification_service.rb`, `PolemkChatMessage`, `PolemkInstanceGroup` (+2 migrations e 2 tabelas), 13 specs, `chatsApi` e o card "Mensagens" da `WhatsappPage`. Podados de `evolution_connection.rb`: `create_group`, `check_number` e `get_media_base64`. Os **7** `%w[email whatsapp]` dos endpoints de auth **não foram tocados**.
  - ⚠️ **RECUSA 1 — `whats_app_webhook_service.rb` FICOU.** Apesar do nome, é o ciclo de vida da conexão (`process_connection_update`/`process_logout_instance`/`process_qrcode_updated`): grava o QR na `PolemkInstance`, faz o broadcast que a `WhatsappPage` escuta e é o **único** enfileirador do `EvolutionReconnectJob` (linha 193). Removê-lo mataria o login por WhatsApp.
  - ⚠️ **RECUSA 2 — `polemk_webhook_service.rb` + `PolemkWebhook` + tabela + entity + recurso `config` FICARAM.** É o que registra na Evolution para onde mandar `QRCODE_UPDATED`/`CONNECTION_UPDATE`; `WhatsappPage.tsx:302` chama `webhooksApi.config` e lê `instance.polemk_webhooks`.
  - Editados fora do escopo por necessidade: `asaas_{charge,payment}_webhook_service.rb` (AI9-001) perderam as chamadas a `WhatsappNotificationService`, com os 3 specs de Asaas ajustados.
- [x] 3.7 Verificar bloco: **login por e-mail E por WhatsApp conferido por leitura da cadeia inteira** (tabela em `removed-features.md`) · type-check **0 erro** (= baseline) · eslint limpo · `rspec` **987 examples, 5 failures, 1 pending** — as 5 são exatamente as 5 do baseline, **nenhuma nova** · 40 migrations apagadas e 23 tabelas fora do `schema.rb`, sem nenhuma migration de `drop` · ledger `removed` para 004/015/017/019/020 e `removed (parcial)` para 005
  - Duas falhas novas apareceram na primeira rodada (`spec/models/polemk_instance_spec.rb:8,9`, `has_many` para models apagados) e foram corrigidas **antes** do commit.

## Bloco 4 — Comercial
- [x] 4.1 `AI9-003` cupons/afiliados — 6 endpoints/entities, `Coupon`, `CouponService`, 6 migrations, 2 tabelas (`coupons`, `coupons_plans`), 6 specs, 6 telas (admin + `partner/*` + `CouponRedirectPage`), `couponStore`, `PartnerMenu` e a **rota catch-all `/:code`** do `App.tsx`
  - Saiu junto, com evidência: o **papel `partner` do `UserType`** (`self.partner`, `partner?`, o seed `'Parceiro - Acesso ao programa de afiliados'`) e os campos de cupom da `UsersPage`/`users.rb`/`users_service.rb`. O papel existia **só** para o programa de afiliados; sem cupons não sobra nada que ele autorize.
- [x] 4.2 `AI9-001` pagamentos Asaas — `api/asaas/v1/**` (6 arquivos), os 5 services Asaas, `SalesService`, `Payments::CreateCharge`, `PaymentsChannel`, entities `Purchase`/`Sale`/`Subscription`, `PaymentsPage`, `CreditCardForm`, `paymentsApi`/`salesApi`/`chargesApi`, as 18 ENVs `ASAAS_*` e o `ClientApplication` "ASAAS" do seed
  - **`AnalyticsService` + `DashboardKpisBroadcastJob` saíram aqui** (dívida do Bloco 2): os **únicos** enfileiradores do job eram `asaas_payment_webhook_service.rb` (3×) e `asaas_charge_webhook_service.rb` (1×). Com o AI9-001 fora, os dois ficaram sem consumidor. **`DashboardChannel` ficou** — `LeadMessage` ainda transmite por ele (AI9-006, Bloco 6).
  - Saíram também as **13 colunas de cartão de `users`** (`credit_card_*`, `cardholder_*`) + as 2 migrations: só o checkout Asaas as escrevia, e eram dado de cartão em coluna aberta.
- [x] 4.3 `AI9-018` onboarding guiado — `api/v1/{setup,onboarding_templates}.rb`, entity, `OnboardingTemplate`, `OnboardingTemplatesService`, 4 migrations, a tabela `onboarding_templates`, 3 specs, `SetupPage`, `AdminOnboardingTemplatesPage`, `onboardingTemplatesAdminApi` e as rotas `/setup` e `/onboarding-templates`
- [x] 4.4 **`Sidebar`/`useNavItems` refatorados** — `useNavItems.ts` virou a **fonte única e estática** do menu (`CONSOLE_NAV_ITEMS`, 9 itens = as rotas que sobraram em `App.tsx`), sem tocar em plano/feature/compra. A `Sidebar` deixou de ter lógica própria: perdeu seu `MENU_KEY_MAP` duplicado, o `PlanSelector`, o `usePlanPreviewStore` e os blocos `isOG`/`isPartner`; agora só renderiza `useNavItems()`. `MobileBottomBar` já lia do mesmo hook. O filtro por **modo** (AI9-033) continua. O ponto de extensão por **papel** (Phase 3, DEC-18) está documentado no hook, com o campo a acrescentar e o único lugar a filtrar.
  - `PermissionsSyncService` **removido**: os 7 pontos de entrada eram `Purchase`, `Subscription`, `CheckoutSessionService`, `PlansService`, `AsaasPaymentWebhookService` e `POST /permissions/sync` — e o único mecanismo era `plan.plan_features → PlanFeaturePermission → Permission`. Sem planos ele não tem de onde tirar permissão. **`Permission`/`UserPermission` FICARAM** (infra: `api/v1/downloads.rb` do AI9-016 e `GET /permissions/me` usam), assim como `PermissionAuditLog`/`PermissionConflict`. Saiu só o join `plan_feature_permissions` e a coluna `permission_audit_logs.plan_id`.
  - `ClientRoute` **removido**: era gate por plano ("Seu plano atual não permite…" → `/plans-redirect`). Sem planos ele não tem para onde redirecionar (`/dashboard` estava dentro dele = loop). `OgRoute`/`VisitorRoute` (papel) e `ProtectedRoute` (auth) ficaram.
  - `Topbar.tsx` **removido**: terceira cópia órfã da nav (zero importadores desde o Bloco 1), inteira sobre `/payments`, `/plans`, `/coupons` e `/partner/*`.
- [x] 4.5 `AI9-002` planos/checkout/feature-gating — 7 endpoints, 6 entities, 8 models, 7 services, 1 job, o concern `ActsAsLimited`, 23 migrations, 8 tabelas, 20 specs, 8 telas, `PlanSelector`/`UpgradeModal`/`UpgradeRequiredModal`, `usePlanFeatures`/`usePlanPreview` e 10 blocos de `endpoints.ts`
  - `db/seeds/niche_plans.rb`, `db/seeds/plans_and_features.rb` e `script/update_feature_descriptions.rb` apagados **inteiros** (com eles morreu a dívida do Bloco 1: `find_or_create_feature("NavKit Spatial UX")` e `campfire:`). Junto: `lib/tasks/plans.rake`, `script/{cleanup,upgrade}_plans.rb`, `plan_agent_redirect.rb`, `scratch_plans.rb`, `plans_dump.txt`, `test_features.rb` e o bloco de planos/support-plans do `db/seeds.rb`.
  - `SupportPlan` + CRUD **saíram** (herdado do Bloco 3, que recusou por ser AI9-002).
  - `Purchase` **saiu** (herdado do Bloco 2): os 2 consumidores que o seguravam eram `auth/checkout_session_service.rb` e `Api::Entities::User#purchased_plan_ids` — os dois são AI9-002 e morreram aqui.
  - `components/preview/*` + `usePlanPreviewStore` **saíram** (herdado do Bloco 1): eram 7 importadores, não 10. `PaymentsPage`/`SetupPage` morreram neste bloco; as 5 restantes (`DashboardPage`, `MediaPage`, `UsersPage`, `OperationsPage`, `LeadsChatPage`) foram desacopladas — os 3 componentes só renderizavam quando `isPreviewMode`, então bastou tirar o badge, o card e o CTA.
  - Saiu também o `GET /api/v1/flows/by_plan` + `chat_flows.target_plan_identifier` + scope `for_plan` (AI9-007 é mantido, mas essa rota escolhia agente **por plano**; o único consumidor era o `PlanSelector`), a tool `send_checkout_link` de `tool_executor`/`tool_registry` (criava `Purchase` — daria `NameError`) e o seed `goat_briefing_agent.rb`, que lia `Plan.where(is_active: true)` na carga.
- [x] 4.6 Verificar bloco: **console navega sem `plan_features` — verificado NO BROWSER**, com login real (backend + vite): o `user` do store não tem mais `features`, `plan_id`, `purchased_plan_ids` nem `has_onboarding`, e o menu renderiza os 9 itens; `/users` abre e carrega dados · `tsc --noEmit` **0 erro** (= baseline) · eslint limpo · `bin/rails zeitwerk:check` **"All is good!"** · `rspec` **763 examples, 3 failures** — subconjunto exato das 5 do baseline · `vitest` **passou a rodar dentro do WSL**: 5 falhas, todas de auth e pré-existentes · 36 migrations apagadas, 12 tabelas fora do `schema.rb`, nenhuma migration de `drop` · ledger `removed` para 001/002/003/018
  - **2 das 5 falhas do baseline ficaram VERDES** (`spec/requests/api/v1/users_spec.rb:111` e `spec/services/users_service_spec.rb:85`): as duas quebravam em `user.destroy!` por causa de `has_many :user_feature_usages` (AI9-002) apontando para a tabela **fantasma** `user_feature_usages` — sem migration e fora do `schema.rb`. A associação saiu com o AI9-002 e o destroy voltou a funcionar.
  - ⚠️ **Defeito pré-existente descoberto ao logar de verdade:** `POST /auth/v1/magic_login/request_code` responde **500** — `undefined local variable or method 'current_ip'`. `current_ip` não existe em lugar nenhum do app (`magic_login.rb:33,40,79,85` e `oauth.rb:119` o chamam) e os specs o **estubam** (`allow_any_instance_of(Grape::Endpoint).to receive(:current_ip)`), então a suíte nunca acusou. Já era assim em `e06be801`; **fora do escopo do Bloco 4** — registrado em `upstream-flags.md`.

## Bloco 5 — Meta
- [x] 5.1 `AI9-009` Meta/Instagram — 3 endpoints (`integrations.rb`, `webhooks/meta.rb`, `comment_keywords.rb`), 3 models, `services/meta/**` (4 arquivos), os 2 jobs (`process_meta_webhook` 555 LOC + `meta_token_health`), o initializer `meta_token_health.rb`, 5 migrations, 3 tabelas, 9 arquivos de spec e 5 telas/componentes do front. **A feature tinha 2 telas órfãs** (`InstagramSettingsPage`, `CommentKeywordsPanel`: zero importadores) e **1 endpoint morto** (`comment_keywords.rb` não estava montado em `base.rb`)
  - Saiu junto, com evidência de dono errado no catálogo: `api/v1/comment_keywords.rb` e `features/chat-builder/components/CommentKeywordsPanel.tsx` estavam catalogados em **AI9-007** (chatbot, mantido) só por morarem em pasta de chat — operam 100% sobre `instagram_comment_keywords`.
  - **Herdado do Bloco 3 resolvido:** `ai/audio_transcription_service.rb` apagado — o consumidor (`process_meta_webhook_job.rb:482`) morreu nesta tarefa.
  - **Pernas Meta amputadas em arquivos do AI9-006** (que só sai no Bloco 6, mas o bloco tem de ficar verde sozinho): `POST /leads/:id/private_reply`, os canais `instagram`/`messenger`/`waba` de `omnichannel/dispatch_service.rb` (−179 LOC) e o ramo "bot da integração"/"apenas humanos" de `omnichannel/inbound_processor_service.rb` (−66 LOC). O dono do canal Meta é o AI9-009, não o AI9-006.
  - Front: rotas `/admin/integrations` e `/admin/omnichannel`, o item **"Canais"** do menu (9 → 8 itens), o `integrationsApi` inteiro, 9 tipos e o **modo Private Reply** do `LeadsChatPage`.
  - Fora do escopo, corrigido de passagem: `lib/tasks/export_startpoint.rake` referenciava 4 models apagados no **Bloco 4** (`SupportPlan`, `Plan`, `PlanFeature`, `PlanFeatureAssignment`) — o `next unless defined?` não protegia, porque o `NameError` é ao montar o array. E `docs/META_CAPI_SETUP.md`, órfão do AI9-010 desde o Bloco 2, foi apagado.
- [x] 5.2 Limpar em `chat_flow`: as 3 `has_many` removidas
  - ⚠️ **A tasks.md estava incompleta:** `app/models/operation.rb` também tinha `has_many :instagram_comment_reply_sents, dependent: :nullify`. Sem removê-la, todo `operation.destroy` quebrava — **2 falhas novas** (`operations_spec.rb:55`, `operation_service_spec.rb:82`) apareceram e foram corrigidas antes do commit.
- [x] 5.3 Verificar bloco · ledger `removed` — `tsc --noEmit` **0 erro** · eslint limpo · `bin/rails zeitwerk:check` **"All is good!"** · `rspec` **667 examples, 2 failures** — a falha `webhooks/meta_spec.rb:87` **desapareceu junto com o arquivo**, e as 2 restantes são exatamente as de AI9-006 e AI9-014 · `vitest` 5 falhas, as mesmas de auth · rotas conferidas **executando** (`Api::Root.routes`: nenhuma rota Meta sobreviveu) · 5 migrations apagadas, 3 tabelas fora do `schema.rb`, nenhuma migration de `drop`
  - **Defeito pré-existente novo, registrado (flag 9):** `LoginCode.mask_destination` não existe; ao bater o teto de 5 pedidos/15 min, `POST /auth/v1/magic_login/request_code` responde **500** em vez de 429. Fora do escopo (auth é mantida e intocável), mas contamina a medição do `rspec` — limpar o contador do Redis antes de medir.

## Bloco 6 — Leads
- [x] 6.1 `AI9-006` leads e omnichannel — 4 endpoints, 3 entities, 5 models (`lead`, `lead_message`, `canal` + os concerns `filtravel_por_origem` e `identificavel`), 5 services, `services/omnichannel/**`, 2 jobs, `lead_chat_channel`, 21 migrations, 3 tabelas (`leads`, `lead_messages`, `canais`), 18 arquivos de spec e 14 telas/componentes do front (`features/leads/**`, `components/leads/**`, `LeadsChatPage`, `CanaisPage`)
  - **Saiu junto o chat PÚBLICO de captação**, que o catálogo não separava do chatbot: `public_chat_service.rb`, `api/v1/public/chat_callback.rb`, `PublicChatChannel` e os 3 endpoints `session`/`messages`/`message` de `api/v1/public/chat.rb`. Escreviam `Lead` e o funil (`current_stage`, `discovery_level`…). Ficaram `resolve_assets` (AI9-014) e `routing` (AI9-007).
  - **Achado não catalogado:** `app/controllers/public/v1/chat.rb` — um **segundo namespace público** montado em `root.rb:117`, 3 endpoints inteiros sobre `PublicChatService`. Não aparece em nenhum documento do trim.
  - **Editado em feature de AUTH (mantida), com evidência:** `create_or_update_lead` de `visitor_auth_service.rb` e `visitor_signup_with_link_service.rb` — criavam um `Lead` com `current_stage: 'discovery'` a cada login de visitante. É CRM dentro do login; **nada da cadeia de autenticação foi tocado** (conferido executando: `request_code` → 422).
  - **Herdado do Bloco 4 (1/2):** a `DashboardPage` foi reescrita para estado vazio. Era 100% `GET /api/v1/analytics/dashboard` (morto desde o Bloco 2) com KPIs de vendas/assinaturas/leads e um **mock** de números para visitante. Saíram junto `analyticsApi`, `AnomalyAlerts`, `invalidateAnalytics.ts`, `MobileMiniKpi` e o `DashboardChannel` (back+front+spec), todos sem consumidor. **Não construí indicador novo** — DEC-09.
  - **Herdado do Bloco 4 (2/2):** as 7 colunas `leads.checkout_*_at` saíram com a tabela.
  - 3 migrations foram **editadas** em vez de apagadas (`create_chat_sessions`, `create_agent_runs`, `add_flow_intelligence_columns`) porque criam objetos de features mantidas.
- [x] 6.2 Verificar bloco · ledger `removed` — `tsc --noEmit` **0 erro** · eslint limpo · `zeitwerk:check` **"All is good!"** · `rspec` **500 examples, 1 failure** — a falha `lead_cross_channel_service_spec.rb:35` **desapareceu junto com o arquivo**; sobrou só a de AI9-014 · `vitest` 5 falhas, as mesmas de auth · 21 migrations apagadas, 3 tabelas + 2 colunas fora do `schema.rb`, nenhuma migration de `drop`
  - **Conferido EXECUTANDO** (servidor de pé, `curl` real): `GET /chat/session` **200** com o fluxo respondendo e `POST /chat/input` **201** — o chatbot roda sem `Lead`; `request_code` **422**; `/api/v1/operations` **401**; `/api/v1/leads`, `/api/v1/canais` e `/api/v1/public/chat/session` **404**. `Api::Root.routes`: 145 → 113.
  - 6 falhas novas apareceram na primeira rodada (2 de `shoulda-matchers` para associações removidas, 4 por um stub de `Omnichannel::DispatchService`) e foram corrigidas antes do commit.
  - ⚠️ **HERANÇA EXPLÍCITA PARA O BLOCO 8, assumida:** `Lead`/`LeadMessage` eram também a **persistência de conversa do chatbot**. Removi o `Lead` de verdade (manter significaria importar um model de CRM de 587 LOC com o funil inteiro para o Safegold) e **não construí** armazém substituto — o agente ficou **sem memória de conversa**, a sessão não retoma e o `AIChatWidget` aponta para 3 endpoints que agora dão 404. Onde mexer está mapeado linha a linha em `removed-features.md`. **Já fiz, de quebra: 8.1 inteira, a parte de `Lead` da 8.2, `chat_session.lead_id` e `agent_run.lead_id` da 8.3 e quase toda a 8.4.**

## Bloco 7 — Operations
- [x] 7.1 `AI9-014` Operations + base de conhecimento/embeddings — **colisão de nome RESOLVIDA**: 2 endpoints, 2 entities, 3 models (`operation`, `operation_asset`, `operation_knowledge`), `services/operations/**` (detector de intenção por embedding + gerador de embedding), `operation_service.rb`, `generate_embedding_job.rb`, 9 migrations, 3 tabelas, a **extensão `vector`** do `schema.rb`, 11 arquivos de spec e 7 do front. O nome `Operation` fica livre para "operação de crédito" do Safegold
  - ⚠️ **Correção da premissa da tasks.md:** "todos os FKs para `Operation` já saíram nos blocos 5 e 6" **não era verdade**. Sobravam `chat_flows.operation_id` e `agent_runs.operation_id` (AI9-007, mantido) — e com eles a capability `assets` do agente, a injeção de `OperationKnowledge` no system prompt (o RAG) e 2 das 3 etapas do `Ai::FlowMatcher`. Tudo isso saiu aqui.
  - **Herdado do Bloco 3 resolvido:** `audio_converter_service.rb` apagado — o único chamador era `api/v1/operations.rb:250`.
  - **8.3 da tasks.md cumprida por inteiro:** `chat_flow.operation_id` e `agent_run.operation_id` saíram aqui; `chat_session.lead_id` e `agent_run.lead_id` saíram no Bloco 6.
  - ⚠️ **RECUSA com evidência — `ToolExecutor` e `ToolRegistry` FICAM, vazios.** Sem `lead_capture` (Bloco 6) e sem `assets` (Bloco 7) o agente não tem ferramenta nenhuma, mas o que mora nesses arquivos é o **motor multi-provider de tool calling** (`format_specs` traduz uma spec para `input_schema` do Anthropic, `type: function` da OpenAI e `functionDeclarations` do Google) — é o núcleo do AI9-007 que o DEC-13.2 manda manter, e o ponto de extensão do Bloco 8. Os 2 specs foram **reescritos para afirmar o contrato novo**, não afrouxados: provam que `CAPABILITY_TOOLS == {}` e que as 4 tools removidas devolvem `success: false`.
  - ⚠️ **RECUSA — `GET /api/v1/public/chat/routing` fica.** Mora no arquivo que perdeu `resolve_assets`, mas lê `ChatFlow.mapped_routes`: é roteamento de agente por rota, AI9-007 puro. Conferido executando: **200**.
  - ⚠️ **RECUSA — as gems `pgvector` e `neighbor` ficam no `Gemfile`.** A extensão `vector` saiu do `schema.rb` (é o que importa para carregar o banco), mas mexer no `Gemfile.lock` obriga a um `bundle install` que pode travar o resto do trim, e gem sem uso não é referência pendurada em código. Registrado como residual para o Phase 2.
- [x] 7.2 Verificar bloco · ledger `removed` — `tsc --noEmit` **0 erro** · eslint limpo · `zeitwerk:check` **"All is good!"** · `rspec` **417 examples, 0 failures** — 🎯 **o baseline zerou** · `vitest` 5 falhas, as mesmas de auth · **`vite build` ✓** · 9 migrations apagadas, 3 tabelas + 2 colunas + a extensão `vector` fora do `schema.rb`, nenhuma migration de `drop`
  - **As 3 falhas do baseline sumiram uma a uma, cada uma no seu bloco:** `webhooks/meta_spec.rb:87` (AI9-009) no Bloco 5, `lead_cross_channel_service_spec.rb:35` (AI9-006) no Bloco 6 e `operations/embeddings/generate_service_spec.rb:9` (AI9-014) no Bloco 7. Era exatamente o critério de aceite.
  - 36 falhas novas apareceram na primeira rodada (todas dos specs do chatbot que exercitavam `Operation`) e foram corrigidas antes do commit.
  - **Conferido EXECUTANDO:** `GET /chat/session` **200** com o fluxo respondendo e `POST /chat/input` **201** — o chatbot roda sem `Lead` **e** sem `Operation`; `request_code` **422**; `/api/v1/operations` e `/public/chat/resolve_assets` **404**; `/public/chat/routing` **200**; `/api/v1/media` e `/api/v1/flows` **401**. `Api::Root.routes`: 145 → 113 → 94.
  - **Menu do console: 9 → 5 itens** ao longo dos três blocos (`/dashboard`, `/media`, `/admin/chat/flows`, `/users`, `/admin/credentials`), cada saída com a rota morta junto.

## Bloco 8 — Desacoplar o chatbot (AI9-007 fica, adaptado — DEC-13.2)
Uso definido: **assistente interno no console**. Não captura lead.
- [x] 8.1 Remover o nó `save_to_lead` — **já entregue pelo Bloco 6** (nó, catálogo e flow builder). Conferido antes de refazer: `services/ai/nodes/save_to_lead.rb` não existe e não há `save_to_lead` em lugar nenhum do repo
- [x] 8.2 Remover as ferramentas do agente que operam sobre lead — **já entregue** (parte `Lead` no Bloco 6, parte `assets` no Bloco 7). `ToolExecutor`/`ToolRegistry` ficam vazios de propósito (recusa registrada do Bloco 7 — são o motor multi-provider de tool calling). Conferido: `CAPABILITY_TOOLS == {}`
- [x] 8.3 Remover associações e colunas — **as 4 colunas já tinham saído** (Blocos 6 e 7). **Mas a 8.3 estava incompleta e ninguém tinha visto:** `AgentRun` ainda declarava `belongs_to :lead, optional: true`, apontando para um model que não existe e uma coluna fora do `schema.rb`. Não quebrava no boot (a associação só resolve a constante quando acessada) e por isso atravessou `zeitwerk:check` e `rspec` inteiros. Removido aqui
- [x] 8.4 Limpar as referências restantes a `Lead` em `services/ai/**` — sobrava a prosa de cabeçalho dos 3 providers ("Supports tool/function calling for **AI lead generation**"). Corrigida. O que resta com a palavra "lead" é registro histórico deliberado dos Blocos 6/7, em comentário
- [x] 8.5 **Confirmar que o essencial continua vivo** — conferido EXECUTANDO (servidor de pé + navegador, não leitura): motor multi-provider, flow builder, execução de fluxo, sessões, telemetria e widget. O widget foi **religado**: os 3 endpoints 404 saíram dele
- [x] 8.6 Verificar — `rspec` **440 exemplos, 0 falhas** · `tsc --noEmit` **0 erros** · `eslint` limpo · `zeitwerk:check` **"All is good!"** · `vitest` **5 falhas, as mesmas de auth** · ledger `AI9-007 = kept (adaptado)`

### 8.7 — Histórico de conversa (DEC-20) — **o coração do bloco**
- [x] `Ai::ConversationMemory` — histórico em `Rails.cache` (o `RedisCacheStore` que o `Rack::Attack` já usa). **Sem migration, sem model, sem tabela**, como o DEC-20 mandou
- [x] TTL **2 horas**, deslizante, com o porquê comentado no arquivo (piso = duração de um atendimento real; teto = "vive na sessão e expira", senão o Redis vira a tabela que o DEC-20 recusou)
- [x] Teto de 40 mensagens, cortando pelo fim — janela de contexto, não retenção
- [x] Fail-soft: Redis fora degrada para "agente sem memória", nunca derruba a resposta
- [x] `agent_service.rb` — `history = []` fixo virou `ConversationMemory.history_for(session)` + o turno corrente; a resposta é gravada **depois** do `extract_options`, para o marcador `[opcoes: ...]` nunca voltar ao modelo
- [x] **NÃO construído, de propósito (DEC-20):** retomada de sessão depois de fechar a aba e trilha auditável. Foram abertos mão conscientemente e estão registrados

### 8.8 — Telemetria: `channel` (DEC-20)
- [x] `channel` vinha de `lead.source_type` e ficou nulo no Bloco 6. Passa a ser a constante `Ai::AgentService::CHANNEL = 'console'` — o DEC-13.2 define um uso só, logo um canal só

### 8.9 — Religar o widget (era o resto da 8.5)
- [x] `publicChat.ts` perdeu 4 dos 5 métodos: `sendMessage`, `getMessages` e `getSession` (os 3 **404** desde o Bloco 6) e `resolveAssets` (404 desde o Bloco 7, e sem nenhum consumidor). Sobrou `getRouting`, que é AI9-007 e continua 200
- [x] `AIChatWidget.tsx` — o `mode="support"` inteiro saiu: era um `useQuery` batendo em endpoint 404 a cada abertura (e cujo resultado ninguém lia), um `useChannel("PublicChatChannel")` para um canal que não existe em `app/channels/`, e um `sendMutation` morto. Os **4** call sites do widget passam `mode="flow"`
- [x] `chatFlow.ts` — `getPublic`/`postPublic`/`postPublicForm` (que mandam `X-Skip-Auth: 1`) viraram `get`/`post`. Sem isso o widget não anexaria token e não faria refresh no 401
- [x] `useChatFlow.ts` — recuperação de 404: sessão guardada no `sessionStorage` que pertença a outro usuário é descartada e uma nova é aberta, em vez de travar o widget para sempre

### 8.10 — **Fora do escopo original: o assistente não tinha DONO de conversa**
Levantado pelo coordenador, confirmado no código e EXECUTANDO. Três defeitos que se somavam num vazamento real:
- [x] **`/chat/*` era PÚBLICO** — `root.rb` tinha `^/api/v1/chat(/.*)?$` e `^/chat(/.*)?$` na allowlist. Herança do chat de captação (AI9-006). As duas entradas saíram; o assistente exige token. Conferido: **401** sem token nos 3 endpoints
- [x] **`ChatSession` ficou sem dono** — o dono era o `lead`, removido no Bloco 6, e nada ocupou o lugar. Migration `20260824120000_add_user_to_chat_sessions` (`user_id uuid`, índice e FK). **Não contraria o DEC-20:** o que ele descartou foi a tabela de MENSAGENS
- [x] **IDOR em `api/v1/chat.rb`** — `ChatSession.find(params[:session_id])` com id inteiro sequencial. Virou `sessao_do_dono!` → `current_user.chat_sessions.find_by(id:)` → **404**
- [x] A chave do Redis inclui o usuário (`ai9:chat:history:u<user_id>:s<session_id>`), senão o vazamento se propagaria para a memória da conversa
- [x] Em `/chat/upload` a checagem de dono foi movida para **antes** das validações de arquivo: na ordem anterior um `session_id` alheio com arquivo inválido respondia 422, e 422 confirma que a sessão existe
- [x] Recusa registrada: `Ai::Nodes::Redirect#auto_auth` (auto-login de visitante a partir do chat) **ficou órfão** — ver removed-features.md

### 8.11 — Seeds de agente (a prosa comercial)
- [x] Os 3 seeds de AGENTE não eram só prosa velha: traziam `extract_lead: true` (capability removida no Bloco 6) e `tools_enabled: true` (removida no Bloco 7), e o `triagem_polemk` mandava chamar `calendar_list_events`/`calendar_create_event` (removidas no Bloco 3). Era **configuração morta**
- [x] Removidos os 6 arquivos (`laura_agent.rb`, `laura_flow.json`, `maya_flow.json`, `goat_agent.rb`, `data_agent_flow.json`, `triagem_polemk_agent.rb`) e os 3 blocos correspondentes de `db/seeds.rb`
- [x] Criado `db/seeds/assistente_console_agent.rb` — **um** agente, `is_default: true`, com o prompt do DEC-13.2: ajuda com o console, não vende, não pede cadastro, não agenda

### 8.12 — **Achado fora do escopo: o catálogo de modelos estava MORTO**
- [x] Conferido EXECUTANDO contra `GET https://api.anthropic.com/v1/models` com a chave do `.env`: `claude-3-5-sonnet-20241022` (o default do `anthropic_provider.rb`, o de todos os seeds e a opção do seletor do flow builder) e `claude-3-haiku-20240307` **não existem mais** — a API devolve `404 - model: ...`. O assistente respondia "ocorreu um erro" em **todo** turno
- [x] **Nenhum portão pegava**: o `rspec` estuba o provider com WebMock. Foi encontrado porque o aceite exigia conversar de verdade — é a lição "verifique EXECUTANDO" acontecendo pela terceira vez nesta migração
- [x] `anthropic_provider.rb`: `DEFAULT_MODEL = 'claude-opus-5'`, lista `MODELOS_APOSENTADOS` reapontada para o default, e `SEM_AMOSTRAGEM` — a família 5 / 4.7+ rejeita `temperature`/`top_p` com **400**, e o provider mandava `temperature: 0.7` sempre
- [x] Seletor do flow builder e o seed atualizados. Listas de OpenAI/Google **não** tocadas: não verifiquei aquelas APIs, e afirmar sem medir é o erro que este bloco existe para não repetir

## Fechamento do Phase 1b
- [x] 9.1 `.migration-ai9/removed-features.md` completo — o que saiu, por quê, os nós removidos, o commit de cada bloco
  - [x] **O que faltava era o commit.** O documento registrava o *baseline* de cada bloco (o HEAD contra o qual as verificações foram medidas) e não o commit **do bloco**, então o `git revert` prometido em cada seção "Reversão" não tinha alvo escrito. Tabela dos 10 commits (8 blocos + follow-up do 1 + fechamento) na seção "Fechamento do Phase 1b"
- [x] 9.2 QA verifica **pelo avesso**: as 27 sumiram (sem rota, sem código morto, sem tabela órfã, sem nav quebrada) **e** as 8 mantidas continuam funcionando — incluindo **login por e-mail E por WhatsApp** (DEC-14) e o chatbot desacoplado (DEC-13.2)
  - [x] **1.122 arquivos varridos**, 29 famílias de termo (uma por feature removida), em `backend/{app,config,db,lib,spec,docs}`, `frontend/{src,public}`, `tools/`, `scripts/`, `config/`, `specs/` e a raiz. O que foi **procurado** está tabelado no `removed-features.md` — não só o que foi achado
  - [x] Portões: `tsc --noEmit` **0 erros** (baseline do trim 305) · `eslint src` **0 findings** · `rspec` **1887 examples / 2 failures** (baseline do trim 1362/6; as 2 são de fatia — S11/S4 e o portão de schema da S14)
  - [x] **Limpo, com a prova:** nenhuma tabela de feature removida sobrevive no `schema.rb`; nenhuma tradução órfã (14 chaves cruzadas contra todo o `src/`); nenhum job agendado órfão (6 entradas de `schedule.yml` × `app/jobs/`); nenhuma entrada órfã em `attachments.yml`; nenhuma rota de front nem item de menu órfão — `App.tsx` e a sidebar leem o **mesmo** registro (`consoleNavigation.tsx`), e `useNavItems.ts:115` filtra `element !== null`
  - [x] **As 30 tabelas órfãs do `schema.rb` (`fly_*`, `work_*`, `budget*`, `achievements`, `user_achievements`, `drops`, `point_events`) são pré-existentes da base ai9, não do trim** — já catalogadas em ETL-S14-03 e na allowlist `Sfg::Etl::TargetBaseline::INHERITED_ORPHANS`. Reconferido: são exatamente 30 e nenhuma pertence às 27
  - [x] **19 resíduos NOVOS**, `R-01` a `R-19`, cada um com arquivo e linha. **Três são da natureza do `public_brand`** (regra que diz "sim" sem dono): `api/root.rb:23,25,26` — 3 entradas de **bypass de autenticação** para rotas que saíram com AI9-005/006; `app/channels/public_events_channel.rb` — canal órfão sem autorização, com `ApplicationCable::Connection` que **não** rejeita conexão anônima; `frontend/src/components/VisitorRoute.tsx:31-36` — guard **VIVO** nas 3 rotas `/admin/chat/*` decidindo por `visitor`/`client`/`cliente`, tipos que o **DEC-41 removeu** (o backend do DEC-41 foi limpo, o front não)
  - [x] **Nenhum resíduo foi corrigido nesta tarefa, e é de propósito**: R-01 a R-03 mudam comportamento de autenticação e autorização; R-17/R-18 mexem na matriz do DEC-18, que é contrato aprovado pelo usuário. QA registra com evidência; quem corrige é a fatia dona, com teste
  - [x] **As 8 mantidas conferidas:** os 7 `%w[email whatsapp]` dos endpoints de auth intactos e as 6 rotas de `magic_login`/`code_validation` na allowlist (DEC-14); o assistente responde autenticado em `/api/v1/chat/*` e `/api/v1/public/chat` está reduzido ao `routing` (DEC-13.2/DEC-20)
- [x] 9.3 Ledger sem nenhum `to-remove` restante — é o que destrava o Phase 2
  - [x] Zero linhas em `to-remove`. Os 35 IDs `AI9-*`: **28 `removed`** (AI9-005 é `removed (parcial)`, DEC-14) e **7 `kept`** (AI9-007 `kept (adaptado)` + 008, 016, 030, 033, 034, 035)
- [x] 9.4 Atualizar `.migration-ai9/checkpoint.md` e regravar o grafo do ai9 (o hook faz no commit)
  - [x] `checkpoint.md`: seção "Phase 1b — FECHAMENTO", os checkboxes dos Blocos 2–8 (estavam `[ ]` com os blocos entregues), o commit faltante do Bloco 2 (`dbcca3c9`), o número do razão na fila de despacho (750 → **752** `migrated`) e a numeração real das DECs (vão até **DEC-105**; o **104 foi pulado de propósito**, e a nota está no fim de `decisions.md`)
  - [x] **Grafo NÃO regravado nesta passagem, e está declarado**: quem regrava é o hook do `graphify` no commit, e este agente não commita. `graphify-out/` está em 26/08/2026 e já indexa o `analise-dump-producao.md`
